server:
  port: 8080

spring:
  config:
    import:
      - classpath:model.yml
  threads:
    virtual:
      enabled: true

logging:
  level:
    com.aigateway: DEBUG
  pattern: "[Gateway %d{HH:mm:ss}] %msg%n"

management:
  endpoints:
    web:
      exposure:
        include: health, prometheus
