# 整体审查报告（2026-08-10）

> 审查范围：`docs/implementation/`（版本1~版本5 详细实施计划）+ `docs/plan/`（00 概念 / 01 总计划 / 版本1~6 概要）+ `docs/AI Gateway & Inference Scheduler - 完整项目设计文档 v3.0.md` + `ai-gateway/src/main/java` 现有代码（V1+V2 已落地，V3/V4 未合并）。
> 审查方法：7 个并行审查代理 × 交叉维度（V5↔V4、V5↔V1/V2/V3、V5↔现有代码、V5↔总体规划+内部自审、V1/V2 自身+演进+代码符合度、V3/V4 自身+演进+基线假设、总体规划层连续性），全部发现经原文/代码双端核对。
> 统计：共 80+ 项发现（高 15 / 中 34 / 低 30+）。本文档按"待修复优先级"组织；每项含位置、问题、依据、修复建议。

---

## 一、按文档统计

| 文档 | 高 | 中 | 低 | 合计 |
|---|---|---|---|---|
| 版本5-详细实施计划.md（新写） | 11 | 17 | 12 | ~40 |
| 版本4-详细实施计划.md | 0 | 9 | 7 | ~16 |
| 版本3-详细实施计划.md | 1 | 3 | 4 | ~8 |
| 版本2-详细实施计划.md | 0 | 4 | 3 | ~7 |
| 版本1-详细实施计划.md | 0 | 5 | 4 | ~9 |
| 规划层（docs/plan/ + 设计文档 v3.0） | 2 | 4 | 6 | ~12 |

---

## 二、严重问题（高，必须修复）

### V5 计划（11 项）

**V5-1. 组件改造清单遗漏 3 个注入 `GovernanceProperties` 的 V4 组件**
- 位置：V5 §2.2 组件表、§10.3
- 问题：`TokenMeter(GovernanceProperties)`、`BudgetFilter(TokenUsageStore, TokenManager, GovernanceProperties, GatewayMetrics)`、`UsageLedger(UsageDao, BudgetManager, GovernanceProperties, ScheduledExecutorService)` 三者的构造器都注入 GovernanceProperties，V5 只列了 GovernanceService/GovernanceFilter。T1 去注解后三者启动直接 `NoSuchBeanDefinitionException`。
- 修复：§2.2/§10.3 补列三组件（estimation 系数、usageFlush 参数改从快照读）。

**V5-2. 13.2 端点清单静默丢失 V4 的 budgets 两端点，与 §11/§21 自相矛盾**
- 位置：V5 §13.2 vs §11（"V4 的 Admin API 建预算端点保留"）vs §21（"保留的端点路径含 /v1/admin/budgets"）
- 修复：13.2 补回 GET/POST `/v1/admin/budgets`（V4 保留，DB 驱动 upsert + 审计）。

**V5-3. BudgetManager 启动路径不恢复 `used_value`，破坏 V4 演示 D"重启不丢账"**
- 位置：V5 §11 rebuild 代码（只从配置 defs 构建，used=0）
- 依据：V4 §24"budget.used_value 快照是 V5 管理面的地基"；V4 演示 D/验收 1。
- 修复：§11 明确启动时从 `budget` 表 `used_value` 恢复；§17.2 回归清单补演示 D。

**V5-4. execution 段 15 条校验规则全部漏收**
- 位置：V5 §8.2 ConfigValidator（对 execution 段仅白名单放行，无任何校验）vs §2.2"V3 的 ExecutionPolicyManager 校验全部收敛"
- 依据：V3 ExecutionPolicyManager.validate 的 15 条 require（totalMs>0、defaultTimeout 四层>0、retry 参数域、circuitBreaker 参数域、cooldown 参数域等）。
- 修复：§8.1 规则表补 execution 行 + §8.2 补校验代码（或明确列为 H1 手敲 TODO）。

**V5-5. Budget id 规则错误引用 V4 + 字段名与 V4 实体不符**
- 位置：V5 §11（"id = scope:scopeValue:limitType:period，同 V4 的 upsert id 规则"；字段 used/windowId、limit 可变）
- 依据：V4 实际 `BudgetKey(scope, scopeValue)` 二维键；实体字段 `usedMicro`(AtomicLong)/`currentPeriodId`(volatile)/`limit`(final)。
- 修复：删除"同 V4"错误引用、明确 V5 id 规则与 V4 键的映射；字段名改 usedMicro/currentPeriodId；显式声明"Budget.limit 去掉 final"实体改造。

**V5-6. §9.1"热更新五步"顺序与 §9.2 代码、§16 时序矛盾**
- 位置：V5 §9.1（"…预检 → 原子替换全局引用 → 通知相关模块"）vs §9.2（通知全部成功后才 `current = next`）vs §16 时序
- 依据：学习版 4.2 同错。若按 9.1 先替换再通知，"通知失败回滚"无从谈起，破坏 COW 招牌语义。
- 修复：9.1 与学习版 4.2 改为"…预检 → 通知组件重建（任一步失败整体回滚）→ 原子替换全局引用"。

**V5-7. ConfigError 双重 `gateway.` 前缀**
- 位置：V5 §6.2 toString 加 "gateway." 前缀 + §8.2/§9.2 调用处又拼 "gateway." 前缀 → 输出 `gateway.gateway.channels[0]…`
- 修复：按 §6.2 Javadoc 约定（at() 的 path 不带前缀），全文档统一调用处去前缀；核对 §8.3/§18.3 断言与演示。

**V5-8. ModelRegistry 手敲代码自创 `channel(String)`、删掉真实 `findChannel(String)`（Optional），调用方编译失败**
- 位置：V5 §10.2 vs 真实代码 OpenAIConnector:178,195 / HealthChecker:77
- 修复：新代码保留 `findChannel(String)` 走 state 读；`channel()` 只能作为附加便捷方法；§10.4 用例改用 findChannel；手敲要求第 3 条"channelIds() 等"改为真实方法名（findByAlias/findAll/findChannel/aliases/strategyOf）。

**V5-9. jackson-dataformat-yaml 不在传递依赖里，"无新增依赖"不成立**
- 位置：V5 §4 + §7 ConfigLoader + §13.1 AdminService（`new ObjectMapper(new YAMLFactory())` 编译不过）
- 依据：spring-boot-starter-json 3.2.0 只含 databind/jdk8/jsr310/parameter-names；依赖树无 jackson-dataformat-yaml。
- 修复：二选一——pom 显式加 jackson-dataformat-yaml（Boot 管理版本 2.15.x），或序列化改用 SnakeYAML `Yaml.dump`。

**V5-10. 改造清单漏 PluginEngine，插件热更新链路断裂**
- 位置：V5 §10.3/§19 T5 vs 真实代码 PluginEngine 构造器把 `registry.all()` 快照进 final List、运行期不再读注册表
- 修复：PluginEngine 加入改造（实现 ConfigAware 或 `runPhase` 动态取 `registry.all()`）。

**V5-11. 插件"实例不可变"与 Spring 单例 + configure 改状态 + 失败回滚三者冲突**
- 位置：V5 §10.3/§9.2 回滚逻辑 vs 真实代码（CanaryGroupPlugin 的 stableInstances 等可变字段被 configure 修改）
- 修复：§10.1 ConfigAware 契约补充"configure 幂等可重入"约定——rebuild 用新 config 重新 validate+configure，回滚时用旧 config 重新 configure 恢复。

### 规划层（2 项）

**P-1. V4 依赖声明错误（总计划 + V4 概要）**
- 位置：01-实现总计划 第 1 节依赖表（V4 挂在 V3 分支）、版本4-治理与计量.md §2（"版本 1 与版本 3 已完成"）
- 依据：V4 详细计划明确预算降级复用 **V2** 的信号机制（"插件写信号 → 过滤器消费 → Scorer 动态调权"），不依赖 V3 执行层；总计划/V4 概要与实现路径矛盾。
- 修复：依赖表 V4 行改"版本 1、2、3"；V4 概要 §2 与 §4.4/4.6 的"版本 3 降级链消费"统一为"决策链淘汰"。

**P-2. V3 概要内部自相矛盾（V2 依赖表述）**
- 位置：版本3-容错与流式治理.md §2（"不需要版本 2 的插件框架，并行开发"）vs §4.3/4.4/4.7（引用 V2 的 RoutingDecision/ModelState/打分器）vs §6 验收（"版本 1（或版本 2）功能回归"）
- 修复：§2 改为"与版本 2 并行开发（执行层不依赖插件框架，但降级链来源与 ModelState 在 V2 合并后自动生效）"，§4.x 加注"V2 合并后"。

---

## 三、中等问题（修复建议按版本顺序）

### V5 计划（17 项）

1. **tokens 段种子导入无归属**（§1/§9 vs V4"创建即入库"语义）：明确执行者（TokenManager 启动时从快照 tokens 段导入 DB）+ agw_demo 自动创建逻辑是否保留。
2. **exceedAction 职责归属**（§11 塞进 BudgetManager vs V4 由 GovernanceService 按 props 判断）：保持 V4 归属或明示迁移。
3. **rollback/revoke/quota/status 写端点审计落点不明**（§13.2 vs §13.1 vs 演示 C 期望 CONFIG_ROLLBACK 条目）：明确各写端点审计记录位置（AdminService 统一或控制器直调 AuditLedger）。
4. **quota 端点依赖 V4 不存在的能力**：TokenManager 无额度更新、ApiToken 是 record 不可变、TokenDao 无 UPDATE SQL——需声明新增能力。
5. **AvailabilityFilter 表述前后不一 + 改造无任务承接**（§2.2"读配置 enabled" vs §10.3"（或读 store）" vs §13.2）：统一为"继续读 ChannelStore.isEnabled（store 由配置 rebuild）"，并补进 §10.3 与任务表。
6. **渠道删除语义变化未标注 + `deletedIds()` 是 V4 没有的 DAO 方法** + `channel_exists`(409) 错误码存废未交代（§13.2/§15.2）：标注"总是软删"、补 deletedIds、说明 V4 两错误码处置。
7. **演示 C detail 断言矛盾**（§13.1 detail 是 diff 行，不含"rollback"字样，演示 C 断言含）：修正演示预期（rollback 信息在 opType 与 note）。
8. **UsageDao.summarize 新签名未给出**（V4 是 `(tokenId, model, days)`）：补扩展签名与返回列契约。
9. **GatewayProperties 双绑定**：V4 §2 曾写"GatewayProperties 新增 governance 段"——V5 §7 需显式声明 GatewayProperties **移除** governance 嵌套字段，只由 GovernanceProperties 绑定。
10. **getDefaultOutputTokens() 跨界读取迁移**（GovernanceProperties 读 decision 段）：V5 后改读 config.props().getDecision()。
11. **channels[].timeout 缺失**（V5 §5.1 声称"V3 超时覆盖"但示例与速查表都没有）：补示例与速查表行。
12. **CONDITIONAL 规则结构校验漏收**（when.signal 非空、op∈{EQ,NE,IN}、select 非空、value 类型约束 IN=List/EQ·NE=标量）：补进 §8.2（H1 范围）。
13. **qualityScore∈[0,1] 与 latencyProfileMs>0 校验漏收**（§10.2 声称已上收但 §8.2 未实现）：补上。
14. **插件同名去重 + `plugin.validate(config)` 自检归属未定**：补去重；明确 validate 由 ConfigValidator 调用（构造器已注入 List<GatewayPlugin>）。
15. **权重四因子齐全校验**：V2 PolicyManager.validateWeights 检查 latency/cost/quality/health 四因子齐全，V5 §8.2 只查权重和——口径变松，需补齐（与 §8.1"各值 > 0"承诺一致）。
16. **15.1 指标计数位置在手敲代码段中不存在**（config_applies/config_version 声称在 H2、admin_rejections 声称在 H4，但 §9.2/§12.2 代码无指标调用）：补进手敲代码或改归属为 T10。
17. **删除引用保护表述**（§13.2 说"预检引用保护"，实际靠校验器 invalid_config 兜底）：二选一对齐。
18. **学习版 4.1 env 口径矛盾**（"只查变量名存在" vs 详细计划 §8.1"不查变量存在性"）：修改学习版口径为"渠道凭证只校验前缀；管理密钥 env 缺失 fail-closed"。
19. **5 个既有测试文件迁移方案缺失**（ModelRegistryTest/PolicyManagerTest/ScorerTest/ModelStateStoreTest/PluginRegistryTest 用旧构造器 new 组件会编译失败，校验用例语义失效）：§17.1 补迁移说明（校验用例并入 ConfigValidatorTest，组件测试改 mock ConfigService）。
20. **ModelStateStore 构造器需保留 ModelRegistry 依赖**（initialState 取值来源）：§10.3 注明构造器为 (ConfigService, ModelRegistry)。
21. **GatewayProperties 需同时移除 `@Configuration` 注解**（否则仍是 Bean，强提示机制失效，静默注入全默认值 props）：§2.2/§20/T1 明确，T1 验收加"先跑一遍确认报错组件清单"。
22. **status 端点标注"新增"应为"改造"**（V4 已有同路径端点）。
23. **低级别杂项**：连接池"按渠道 ID 复用"改为"按上游地址复用"；findAll() 保留 instanceId 去重保序；时序图补 AdminService 节点；指标简称/全名统一；modifyConfig 写全 4 参；测试总表补 GovernanceAdminControllerTest；Set import 提示修正；new ConfigError 与 at() 统一；错误体 detail/errors 两套结构统一；审计模块归属（observability vs governance/audit）口径统一；18.5 证据链补 9.3 启动失败与 10.4 旧引用用例；8.1 tokens 段字段校验补一行；clear-candidates config.enabled 迁移说明；forRequest 渠道 timeout 来源说明；DecisionLogStore 保留环形缓冲偏离 V2 预想的说明。

### V4 计划（9 项中）

1. §14.1 `GovernanceFilter` 使用未注入的 `metrics`（doFilter 调 rateLimited）。
2. §12.3 `GovernanceBootstrapPlugin` 覆写不存在的 `phase()` 方法（GatewayPlugin SPI 无该方法，编译不过）。
3. §11.4 测试 `chunk()` 用 5 参构造 ChatChunk——V3 合并基线是 7 字段（+usage/+error）。
4. §8.3 吊销测试与实现/mock 行为矛盾（revoke 先抛 404 到不了断言；validate 走 SQL 过滤得 UNKNOWN 而非 DISABLED；重启前后语义漂移）：统一"吊销 = 内存删除 + DB 标记 + warmup 过滤 revoked"语义。
5. governance 配置绑定归属冲突（§2 表/§3 说 GatewayProperties 加 governance 段，实际全部代码注入独立 GovernanceProperties）：明确 GatewayProperties 只加 tokens 段。
6. §0 H 表 S 编号 off-by-one（H 表写 S6~S11，正文标题是 S5~S10）。
7. §5.1 标注"V4 完整结构"但示例以 V2 为基线（无 execution 段/channels[].timeout），与头部"V3 已合并"矛盾：补 execution 段或改标注。
8. §10.3 precheck 断言值错误（limit=100、used=0 时 precheck(…,1,0) 为 true，断言 isFalse 必失败，应传 101）。
9. §23 风险 10 条件写反（"V3 已合并按 14.4 备路径"——14.4 是未合并备路径）。

### V3 计划（3 项中 + 1 项高）

1. **[高]** §15.1 GatewayMetrics 缺 `cooldownSkipped`（§12.2/§13.2 两处调用）。
2. §18.2 集成场景 B 与 §19.4 自相矛盾（单渠道故障时请求经降级链返回 200，不会出现 503 circuit_open——需把 mock-b 也打满故障）。
3. §13.4 Controller `metadata()` 丢掉 V2 的 tenant/canary_group（金丝雀插件读 canary_group，回归风险）。
4. §8 行/§2 现状声明过期（"V2 仅有计划文档"——现已合并）；§2"findByInstanceId"无使用点；§3 测试清单缺 FailureClassifierTest；研读资料（LiteLLM/New API/Portkey）零落地引用。

### V2 计划（4 项中）

1. token 估算口径错误（"汉字约 3 字/token" vs 实际"CJK 1 字/token + 其他 4 字符/token"，fc710d5 已改代码未同步计划）；§15.3 示例 cost 0.00513 → 0.00514。
2. §5.1/§21.2 声称"主 model.yml 仍保持 V1 结构、需手动扩展"——实际已提交 V2 完整结构。
3. §8 行衔接声明虚构"版本 1 中已删除的旧决策引擎类（DecisionService、SimpleScorer 等）"——git 历史无此两类。
4. §20.2/§21.5 场景 C 配置加载方式（计划写"追加挂载"，实际是独立 model-failclosed.yml + spring.config.import 组合）。
5. 低：§14.2 流式降级成功分支缺 metrics.success（b4dec68 已修代码）；§20.1 测试现状"未提交"已过时。

### V1 计划（5 项中）

1. §6 S3 附近"Spring Boot 默认开启 SNAKE_CASE 宽松匹配"错误论断（与同节"必须显式 @JsonProperty"自相矛盾）。
2. §19 风险 4"record 默认忽略未知字段，无需 @JsonIgnoreProperties"错误——实际 5 个 DTO 都显式加注解。
3. §7.3 S5b 代码段缺"重复 alias 应拒绝"实现（H5 注释要求了，实际代码有 seenAliases）。
4. §14.1 mock 流式分流方式（计划写 `params = "stream=true"`，实际是读 body stream 字段统一入口）。
5. §2 现状盘点虚构（`ModelConfig`、"@PatchMapping"均不存在于 git 历史）。
6. 低：S10 兜底 Throwable→Exception+404；HealthController 实际 5 字段；结构图缺 ModelsResponse；H2 编号无定义；缺 §0 手敲清单节；概要 4.4"503"→实际 502。

---

## 四、已核对无问题的重要项（审查深度证据，非问题）

- **V5**：V4 §24 四条预留路径保持 ✓；execution 段 31 个字段与 V3 定义逐一一致 ✓；health/decision 段与 V1/V2 一致 ✓；TokenManager/BudgetManager.upsert/ChannelStore/GovernanceService 签名与 V4 一致 ✓；预算枚举三组与 V4 一致 ✓；审计表与 V4 四表无冲突 ✓；ChatGatewayService/MeteringCallback 接口"不动"成立 ✓；热更新 COW 链路自洽 ✓；错误码六类与端点/演示一致 ✓；H1~H5 与正文对应、T1~T12 任务闭环 ✓；占位符仅刻意 TODO H1 ✓。
- **V1/V2**：全部声称类与代码签名逐一对上（决策引擎五 Filter order、三策略、插件框架五钩子、示例插件、mock 三 Controller）；model.yml 与 V2 计划逐字段一致；缺省值全部一致；指标名/端点/测试类全部存在；92 个测试全绿。
- **V3/V4**：H 编号与正文对应；容错组件/治理组件签名跨节一致；四张表与 DAO 契约一致；错误码/演示 curl 闭环；V4 引用的 V3 组件（MeteringCallback/FallbackChainExecutor/StreamProxy）与 V3 定义一致；V3 未合并时 V4 备路径真实可行。
- **规划层**：总计划版本划分与六概要无范围漂移；术语一致；渠道管理 V4→V5 边界清晰；设计文档 P0/P1/P2 路线图与版本划分对齐。

---

## 五、修复优先级建议

| 优先级 | 范围 | 处理方式 |
|---|---|---|
| P0 | V5 计划 11 项高 + 17 项中 | 由 Claude 直接修复（V5 为本轮新写文档） |
| P1 | V4 计划（16 项） | 需你确认后修（含 1 项会影响 V5 对接的：§5.1 基线标注、governance 绑定归属） |
| P2 | V3/V2/V1 计划（约 24 项） | 多为"计划未同步已落地代码"类（口径/示例/现状描述过期），建议合并 V3/V4 前修 |
| P3 | 规划层（12 项） | 依赖声明（P-1/P-2 高）建议先改；其余随版本演进修订 |

> 备注：本次审查未修改任何文档（除 V5 是本轮新写的）；以上修复均需逐项确认后执行。

---

## 六、修复记录（2026-08-10，全部完成）

| 文档 | 修复项数 | 状态 | 主要修复 |
|---|---|---|---|
| 版本5-详细实施计划.md | ~40 | ✅ 全部 | 组件清单补 3 个治理组件 + PluginEngine；findChannel 保留；jackson-dataformat-yaml 依赖；execution 15 条校验上收；CONDITIONAL 结构/qualityScore/四因子/插件去重校验；ConfigError 前缀统一；五步顺序（9.1/1 节）；Budget 字段名/id 规则/used_value 恢复/exceedAction 归属；budgets 端点补回；rollback/quota 审计落点；summarize 新签名；tokens 种子归属；指标计数归属（T10）；时序图补 AdminService；错误体统一；既有测试迁移方案；channels[].timeout；17.2 补演示 D；18.5 证据链；@Configuration 注解；插件 configure 幂等约定；连接池措辞；低级别杂项 |
| 版本4-详细实施计划.md | 16 | ✅ 全部 | GovernanceFilter 补 metrics 注入；删除 phase() 覆写；ChatChunk 补 2 参；吊销测试语义统一（UNKNOWN/401 + findAll 过滤 revoked）；governance 绑定归属；H 表 S 编号 -1；5.1 基线标注；precheck 断言 101；风险 10 条件对调；24 节 V5 表述修正等 |
| 版本3-详细实施计划.md | 8 | ✅ 全部 | cooldownSkipped 补定义；场景 B 两阶段；metadata() 保留 tenant/canary_group；现状声明更新；findByInstanceId 删除；FailureClassifierTest 补清单；研读资料落地引用（LiteLLM/New API/Portkey）；AvailabilityFilter 澄清 |
| 版本2-详细实施计划.md | 6 | ✅ 全部 | token 估算改 CJK 口径（含示例 0.00513→0.00514）；model.yml 现状声明；旧决策类虚构修正；场景 C 独立文件加载；metrics.success 补上；测试现状更新 |
| 版本1-详细实施计划.md | 9 | ✅ 全部 | SNAKE_CASE 错误论断删除；record 忽略字段修正 + 注解补上；S5b 补 seenAliases；mock body 分流；现状盘点改写；S10 兜底 Exception+404；HealthController 5 字段；结构图补 ModelsResponse；H2 编号说明 |
| 01-实现总计划.md | 4 | ✅ 全部 | V4 依赖补 V2（依赖图 + 说明）；V6 能力清单补管理后台/精确 Token 计数 + 研读总览补 tiktoken/jtokkit；observability 审计归属注；技术基线声明 |
| 版本3-容错与流式治理.md | 4 | ✅ 全部 | §2 V2 依赖表述统一；4.3/4.4/4.5 加"V2 合并后"注与成本降级落地说明 |
| 版本4-治理与计量.md | 5 | ✅ 全部 | §2 依赖声明；4.4 决策链闭环表述；4.2 DB 覆盖机制说明；§8 多租户承诺取消；5.3 shadow 默认不做 |
| 版本5-配置热更新与管理面.md | 3 | ✅ 全部 | 4.4 承接说明；4.2 五步顺序修正；4.1 env 口径修正 |
| 版本6-进阶能力（可选）.md | 1 | ✅ 全部 | §9 多租户/兑换码/充值说明 |
| 版本1-统一接入与基础网关.md | 2 | ✅ 全部 | 4.4 503→502；4.6 流式 usage 表述 |
| 版本2-插件流水线与决策引擎.md | 1 | ✅ 全部 | 4.5 策略类型映射说明（FALLBACK_CHAIN/CANARY） |

> 修复方式：V5 由 Claude 直接修复（本轮新写，上下文最熟）；V4/V3/V2/V1 由并行子代理按报告逐项修复（手术式，每项 grep 定位 + 验证）；规划层由 Claude 修复。所有修改均为文档编辑，未删除任何文件。
