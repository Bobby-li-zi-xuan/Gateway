# 版本 3：详细实施计划（含关键代码段）

> 配套文档：《版本3-容错与流式治理（学习版）》讲“是什么、为什么”，本文档讲“怎么做”。
> 技术基线：Java 21、Spring Boot 3.2、Spring MVC + Java 21 虚拟线程（阻塞式）、Maven 多模块（ai-gateway / mock-model-server）。
> 前置依赖：版本 1 已完成并跑通（见《版本1-详细实施计划.md》）；版本 3 与版本 2 **并行开发**，本文档以“版本 2 尚未合并”为默认基线，并在第 2 节给出合并后的对接方式。
> 本文档的关键代码段是**实现参考**，允许按你的编码风格调整；但接口契约（配置结构、端点、错误体、指标名）请保持一致。
>
> ⚠️ 2026-08-06：当前工作区版本 1 已完整落地（Spring MVC + 虚拟线程、SSE 透传、健康检查、失败切换），版本 2 仅有计划文档、代码未合并。版本 3 的新增容错代码放在 `execution/` 包，状态闭环放在 `decision/state/` 包（与版本 2 计划中的同路径兼容）；`ChatGatewayService` 只做“接线”改造，不把容错逻辑堆进主流程类。

---

## 0. 手敲清单与实施分工（先读）

版本 3 的代码分两类：**必须你手敲默写的核心（H1~H7）** 与 **Codex 已实现的脚手架**。本版本的核心是“失败分类 → 状态机 → 执行编排”这条容错链路，七个手敲点对应学习版文档 4.1~4.7 的七个功能模块。

> ⚠️ **编号独立声明**：V3 的 H1~H7 与 V2 计划的 H1~H6 **无对应关系**（V2 的 H1~H6 = 插件引擎 / FilterChain / Scorer / Scheduler / ModelStateStore / DecisionEngine）。两个版本并行开发，各自维护独立的手敲编号；代码注释中的 `TODO Hx` 以文件所在模块区分，不要跨版本对照。

### 必须手敲默写（H1~H7）

| 编号 | 手敲内容 | 位置 | 为什么必须手敲 |
|---|---|---|---|
| H1 | 重试执行器 `RetryExecutor`（触发条件 / 退避抖动 / Retry-After / 安全边界） | 第 8 节 S6 | “什么失败能重试、什么只能放弃”是版本 3 的核心心法 |
| H2 | 分层超时 `TimeoutGuard`（总时长 / 首字节 / 空闲定时器） | 第 9 节 S7 | 精确回答“卡在哪一步”，流式取消的关键基建 |
| H3 | 熔断状态机 `CircuitBreaker`（滑动窗口 / 半开 / 慢调用） | 第 10 节 S8 | 关闭 → 打开 → 半开的转换是版本 3 招牌算法 |
| H4 | 渠道冷却 `CooldownManager`（连续失败 / 冷却 / 恢复探测 / 翻倍） | 第 11 节 S9 | 与熔断分工配合的轻量状态机 |
| H5 | 降级链执行 `FallbackChainExecutor`（按序执行 / 每候选限制 / 总时长） | 第 12 节 S10 | “主选失败 → 换下一个 → 全部失败 503”的执行编排 |
| H6 | 流式治理 `StreamProxy`（SSE 规范化 / 错误事件 / 取消上游 / 计量回调） | 第 13 节 S11 | 流式安全边界与资源释放，本版本最难也最值钱的部分 |
| H7 | 状态事件管道 `StateEventPipeline` + `ModelStateStore` 扩展 | 第 14 节 S12 | 单写者事件管道，把执行结果变成决策情报 |

手敲方法：先合上文档自己写 → 写不出来再看代码段 → 看完再自己重写一遍，直到能默写。

手敲类路径：
- H1 `ai-gateway/src/main/java/com/aigateway/execution/retry/RetryExecutor.java`
- H2 `ai-gateway/src/main/java/com/aigateway/execution/timeout/TimeoutGuard.java`
- H3 `ai-gateway/src/main/java/com/aigateway/execution/circuit/CircuitBreaker.java`
- H4 `ai-gateway/src/main/java/com/aigateway/execution/cooldown/CooldownManager.java`
- H5 `ai-gateway/src/main/java/com/aigateway/execution/fallback/FallbackChainExecutor.java`
- H6 `ai-gateway/src/main/java/com/aigateway/execution/stream/StreamProxy.java`
- H7 `ai-gateway/src/main/java/com/aigateway/decision/state/StateEventPipeline.java` + `ai-gateway/src/main/java/com/aigateway/decision/state/ModelStateStore.java`

### Codex 已实现（脚手架，不需要手敲）

配置绑定（`ExecutionPolicies` / `TimeoutPolicy` / `RetryPolicy` / `CircuitBreakerConfig` / `CooldownConfig`）、`ExecutionPolicyManager` 启动校验、失败分类（`Failure` / `FailureType` / `UpstreamCallException` / `FailureClassifier`）、`ExecutionChainResolver`（候选链来源）、`OpenAIConnector` 超时与失败分类改造、`StreamSession` 会话句柄、`ChatCompletionController` 断连处理与 metadata 提取、`GatewayMetrics` 扩展、mock 故障注入扩展、单测骨架。

代码中手敲部分留 `TODO H1~H7` 标记与说明注释，提示你该写哪一段；未完成前调用对应功能会直接报错。

---

## 1. 目标与范围

版本 3 完成后，系统支持：

- **重试**：可配置触发条件（连接失败、超时、429、5xx），指数退避 + 随机抖动；429 优先遵循上游 `Retry-After`；**首字节发出前才允许重试**，流式一旦开始输出绝不重放。
- **分层超时**：连接、完整响应、首字节、流式空闲、整条降级链总时长五层独立配置；渠道级默认 + 请求级覆盖（header）。
- **降级链**：按序执行候选（V1 模式为加权随机排列，V2 合并后为 `RoutingDecision.fallbackChain`）；每个候选尝试次数有限，整链受总时长约束；预留成本降级接口（V4 接入）。
- **熔断**：按“渠道 + 模型”维度独立状态机（关闭 → 打开 → 半开），滑动窗口统计失败率与慢调用率，状态写入 `ModelState` 供决策层消费。
- **渠道冷却**：连续失败达阈值进入冷却，冷却期从候选剔除；到期自动恢复，恢复后第一个请求视为探测，失败则时长翻倍（有上限）。
- **流式治理**：SSE 逐 chunk 规范化转发、补齐缺失字段、上游错误事件转统一错误体；客户端断开 → 取消上游调用并释放连接；空闲超时；每个 chunk 触发计量回调（V4 接入 Token 累加）。
- **状态闭环**：执行结果、超时、熔断转换、冷却进出统一写入事件管道（单写者），聚合为 `ModelState` 快照供决策层读取。

**非目标**（版本 3 不做什么）：

- 不实现令牌、配额、限流（版本 4；本版本只处理上游 429 触发的重试）。
- 不实现成本计量与预算降级（版本 4；本版本只预留计量回调与“成本降级”扩展点）。
- 不实现管理面与配置热更新（版本 5；所有容错参数启动期固定）。
- 不做分布式状态（远期；熔断/冷却均为单实例内存态，可从日志重放）。
- 不实现会话亲和（设计文档已说明，默认关闭）。

---

## 2. 与现有代码的关系（先盘点，再动手）

| 现有文件 | 现状 | 处理方式 |
|---|---|---|
| `ChatGatewayService` | V1 主流程：健康过滤 + 加权随机 + 失败切换（maxAttempts=2 写死） | 改造为“解析候选链 → 交给 `FallbackChainExecutor` / `StreamProxy`”；失败切换语义被 V3 的降级链完整替代，不再手写循环 |
| `OpenAIConnector` | 非流式 / 流式 + SSE 解析；超时写死 30s | 超时参数化（`TimeoutPolicy`）；异常统一分类为 `UpstreamCallException`；流式返回 `StreamSession` 支持取消；SSE 行解析逻辑保留复用 |
| `GatewayProperties` | channels + models + health | 新增 `execution` 配置段与 `channels[].timeout` 覆盖 |
| `ModelRegistry` | 别名 → 候选 + 渠道表 + 启动校验 | 保留不动；新增 `findByInstanceId`（链解析用）与 `findByChannelId`（冷却事件广播用，见 14.3）辅助 |
| `ModelInstance` | record：实例/别名/渠道/模型/权重/能力 | 保留不动（V2 若合并则带价格/质量/延迟画像，V3 不依赖） |
| `HealthChecker` | 渠道级健康快照 | 保留不动；候选链解析复用 `isHealthy` |
| `GatewayMetrics` | success / failure / streamChunk | 增加 retry / fallback / timeout / circuit / cooldown / streamCancelled 指标 |
| `ChatCompletionController` | 非流式 / 流式 + SseEmitter | 增加“客户端断开 → 显式取消上游”与 metadata 提取（`X-Gateway-Timeout-Ms` 等，签名与 V2 对齐） |
| `ChatChunk` | OpenAI 流式分片 DTO | 增加可选 `error` 字段（统一错误事件），正常 chunk 不输出该字段 |
| `ChatRequest` | V1 record | 不变（V2 的 token 估算在 CapabilityFilter 内私有实现，V3 不依赖） |
| `GatewayException` | 统一业务异常 | 不变；V3 新增错误码见 15.2 |
| mock-model-server | 延迟 / 故障 / 健康开关已支持 | 扩展故障注入：可配置错误状态码（429/500/503）、`Retry-After`、流式中途失败 / 停顿 / 断开，客户端断连日志 |
| 测试 | V1 单测 + 集成测试 | 更新 `ChatGatewayService` 构造器与签名；新增 7 个模块的单测与 3 个集成场景 |

> 路径前缀：`ai-gateway/src/main/java/com/aigateway/`。容错执行代码放 `execution/`（retry / timeout / circuit / cooldown / fallback / stream 子包），状态闭环放 `decision/state/`，与设计文档的模块边界（execution、decision、observability）对齐。

### 2.1 与版本 2 的并行关系（重要）

版本 2 与版本 3 都会改动 `ChatGatewayService` 与 `OpenAIConnector`，为了避免并行开发互相踩踏，约定如下：

1. **职责边界**：V2 只负责“决策”（候选收窄 + 打分 + 排序，输出 `RoutingDecision`）；V3 只负责“执行”（重试 / 超时 / 降级 / 熔断 / 冷却 / 流式）。V3 的执行入口统一接收 `List<ModelInstance>` 候选链，**不自己决策**。
2. **默认基线（V2 未合并）**：候选链由 `ExecutionChainResolver` 生成——健康过滤 → 冷却/熔断过滤 → 按“加权随机不重复抽样”排列（等价于 V1 每次失败后重新加权随机的语义，保证加权分布不回退）。
3. **V2 合并后**：`ExecutionChainResolver` 检测到 `DecisionEngine` bean 存在，则改用 `RoutingDecision.primary() + fallbackChain()` 作为候选链，其余执行逻辑零改动。
4. **状态快照互通**：V3 把熔断状态、冷却状态、慢调用计数写进 `ModelState`（`decision/model/ModelState.java`，与 V2 计划同路径同字段风格），V2 打分器直接读取，无需适配层。
5. **合并顺序建议**：先合并 V3（执行层独立可测），再合并 V2（决策层只替换候选链来源）；任一顺序下，第 12 节的 `execute(chain, ...)` 契约不变。

---

## 3. 工程结构与新增文件

```
ai-gateway/src/main/java/com/aigateway/
├── GatewayApplication.java                    （保留）
├── api/
│   ├── controller/ChatCompletionController.java（改造：metadata 提取 + 断连取消）
│   ├── dto/ChatChunk.java                      （扩展：可选 error 字段）
│   └── error/GlobalExceptionHandler.java       （保留，错误表增加 V3 行）
├── core/
│   ├── domain/model/                           （保留；V2 合并后才有扩展字段）
│   ├── exception/GatewayException.java         （保留）
│   └── service/ChatGatewayService.java         （接线：链解析 + 执行器 + 流式代理）
├── decision/
│   ├── model/ModelState.java                   （新增：V3 扩展字段，V2 同路径兼容）
│   └── state/
│       ├── ModelStateStore.java                （新增：EWMA + 事件应用）
│       └── StateEventPipeline.java             （🖊 H7：单写者事件管道）
├── execution/
│   ├── connector/OpenAIConnector.java          （改造：超时参数化 + 失败分类 + StreamSession）
│   ├── model/
│   │   ├── FailureType.java                    （新增：失败分类枚举）
│   │   ├── Failure.java                        （新增：失败样本）
│   │   ├── UpstreamCallException.java          （新增：内部执行异常）
│   │   ├── ExecutionPolicies.java              （新增：运行期容错策略快照）
│   │   ├── TimeoutPolicy.java                  （新增：分层超时配置）
│   │   ├── RetryPolicy.java                    （新增：重试配置）
│   │   ├── CircuitBreakerConfig.java           （新增：熔断配置）
│   │   ├── CooldownConfig.java                 （新增：冷却配置）
│   │   ├── CircuitState.java                   （新增：CLOSED / OPEN / HALF_OPEN）
│   │   └── StreamSession.java                  （新增：流式会话句柄）
│   ├── policy/ExecutionPolicyManager.java      （新增：启动校验 + 请求级覆盖）
│   ├── classify/FailureClassifier.java         （新增：异常/状态码 → Failure）
│   ├── retry/RetryExecutor.java                （🖊 H1）
│   ├── timeout/TimeoutGuard.java               （🖊 H2）
│   ├── circuit/CircuitBreaker.java             （🖊 H3）
│   ├── cooldown/CooldownManager.java           （🖊 H4）
│   ├── fallback/
│   │   ├── ExecutionChainResolver.java         （新增：候选链来源）
│   │   └── FallbackChainExecutor.java          （🖊 H5）
│   └── stream/StreamProxy.java                 （🖊 H6）
├── observability/GatewayMetrics.java           （扩展）
└── state/                                      （health / registry 保留）
```

mock-model-server 新增/修改：

```
mock-model-server/src/main/java/com/aigateway/mock/
├── model/ModelProfile.java                     （扩展：errorStatus / retryAfterSeconds / 流式故障参数）
├── model/ModelRegistry.java                    （扩展：setter 支持新参数）
├── controller/BehaviorController.java          （扩展：接收新注入参数）
└── controller/ChatController.java              （扩展：流式中途故障 / 停顿 / 断开 + 断连日志）
```

测试新增（`ai-gateway/src/test/java/com/aigateway/`）：

```
execution/retry/RetryExecutorTest.java
execution/timeout/TimeoutGuardTest.java
execution/circuit/CircuitBreakerTest.java
execution/cooldown/CooldownManagerTest.java
execution/fallback/FallbackChainExecutorTest.java
execution/stream/StreamProxyTest.java
decision/state/StateEventPipelineTest.java
decision/state/ModelStateStoreTest.java
```

---

## 4. 依赖（pom.xml）

**本轮不新增任何依赖**。理由（面试可讲）：

- 重试 / 熔断 / 冷却不需要引入 resilience4j：状态机 + 环形数组用 JDK `ConcurrentHashMap` / `synchronized` 就能实现，自研才能讲清“失败计数 → 冷却 → 恢复”的机制；
- 超时与取消基于 JDK `HttpClient` + `ScheduledExecutorService`，无需额外库；
- 唯一新增的 Bean 是一个全局 `ScheduledExecutorService`（超时定时 + 状态管道聚合），在 `infra/config` 下新建配置类即可（Codex 脚手架，见第 9 节）。


---

## 5. 配置设计（关键代码段 S2）

### 5.1 `model.yml`（V3 完整结构）

> 路径：`ai-gateway/src/main/resources/model.yml`

在版本 1 的 `gateway.health / channels / models` 基础上新增 `gateway.execution`（容错治理参数）与 `channels[].timeout`（渠道级超时覆盖）。版本 2 合并后新增的 `decision / policies / plugins` 与本版本互不干扰，可共存。

```yaml
gateway:
  health:                       # V1 保留
    intervalSeconds: 5
    timeoutMs: 2000
    consecutiveFailures: 3

  execution:                    # V3 新增：容错治理参数（全部有缺省，旧配置可直接启动）
    totalMs: 60000              # 整条降级链的总时长上限（所有候选/尝试共享）
    defaultTimeout:             # 渠道级缺省分层超时
      connectMs: 500            # 建立 TCP 连接
      requestMs: 30000          # 非流式：发请求到收完整响应
      firstByteMs: 10000        # 流式：发请求到收到首个数据 chunk
      idleMs: 30000             # 流式：相邻 chunk 的最大间隔（空闲超时）
    retry:
      maxAttemptsPerCandidate: 1        # 每个候选默认尝试次数（默认 1 = 不重试同实例）
      sameInstanceOnConnectionFailure: true  # 连接级失败允许同实例重试一次（学习版 4.1）
      respectRetryAfter: true           # 429 时优先遵循上游 Retry-After
      maxRetryAfterMs: 5000             # Retry-After 等待上限，防止被上游拖死
      backoffBaseMs: 100                # 指数退避基数
      backoffMaxMs: 2000                # 退避上限
      jitterRatio: 0.2                  # 随机抖动比例（防惊群）
      retryableStatuses: [429, 500, 502, 503, 504]
    circuitBreaker:
      windowSize: 100            # 环形窗口：统计最近 N 次调用
      minimumRequests: 5         # 样本不足时不触发熔断
      failureRateThreshold: 0.5  # 失败率阈值（0~1）
      slowCallThresholdMs: 10000 # 超过该延迟判定为慢调用
      slowCallRateThreshold: 0.5 # 慢调用率阈值
      openDurationMs: 30000      # 打开状态持续时间
      halfOpenMaxRequests: 1     # 半开态放行的探测请求数
    cooldown:
      consecutiveFailures: 5     # 连续失败多少次进入冷却
      cooldownMs: 30000          # 冷却时长
      maxCooldownMs: 300000      # 恢复探测失败翻倍的上限
      recoveryProbe: true        # 恢复后第一个请求视为探测，失败则时长翻倍
    state:
      flushIntervalMs: 500       # 状态事件管道聚合周期（毫秒）

  channels:                     # V1 保留 + V3 超时覆盖
    - id: mock-a
      provider: openai-compatible
      baseUrl: http://localhost:8001
      credentialsRef: ""
      weight: 10
      timeout:                   # V3 可选：渠道级覆盖；不配则用 defaultTimeout
        connectMs: 500
        requestMs: 30000
        firstByteMs: 8000
        idleMs: 20000
    - id: mock-b
      provider: openai-compatible
      baseUrl: http://localhost:8002
      credentialsRef: ""
      weight: 5
      # timeout 不配 → 用 defaultTimeout

  models:                       # V1 保留（V2 合并后增加 strategy / priceIn 等，与本版本无关）
    - alias: qwen
      candidates:
        - channelId: mock-a
          model: qwen-large
          weight: 8
          capability:
            contextLength: 128000
            multimodal: false
            toolCallSupport: true
            codingAbility: MEDIUM
            reasoningLevel: HIGH
            qualityLevel: QUALITY_HIGH
        - channelId: mock-b
          model: qwen-small
          weight: 2
          capability:
            contextLength: 32000
            multimodal: false
            toolCallSupport: false
            codingAbility: LOW
            reasoningLevel: LOW
            qualityLevel: FAST

  # ── V2 合并后追加（示例，V3 不消费）──
  # decision: { logSize: 100, ewmaAlpha: 0.3, errorRateAlpha: 0.1 }
  # policies: [ ... ]
  # plugins: [ ... ]
```

### 5.2 新配置字段速查

| 配置 | 说明 | 缺省 |
|---|---|---|
| `execution.totalMs` | 整条降级链总时长上限 | `60000` |
| `execution.defaultTimeout.connectMs` | 连接超时 | `500` |
| `execution.defaultTimeout.requestMs` | 非流式完整响应超时 | `30000` |
| `execution.defaultTimeout.firstByteMs` | 流式首字节超时 | `10000` |
| `execution.defaultTimeout.idleMs` | 流式空闲超时 | `30000` |
| `channels[].timeout.*` | 渠道级覆盖上述四层（可选） | 不配 = defaultTimeout |
| `execution.retry.maxAttemptsPerCandidate` | 每个候选默认尝试次数 | `1` |
| `execution.retry.sameInstanceOnConnectionFailure` | 连接级失败允许同实例重试一次 | `true` |
| `execution.retry.respectRetryAfter` | 429 优先遵循 `Retry-After` | `true` |
| `execution.retry.maxRetryAfterMs` | `Retry-After` 等待上限 | `5000` |
| `execution.retry.backoffBaseMs / backoffMaxMs / jitterRatio` | 指数退避 + 抖动 | `100 / 2000 / 0.2` |
| `execution.retry.retryableStatuses` | 触发降级/重试的上游状态码 | `429,500,502,503,504` |
| `execution.circuitBreaker.windowSize` | 环形窗口大小 | `100` |
| `execution.circuitBreaker.minimumRequests` | 最少样本数 | `5` |
| `execution.circuitBreaker.failureRateThreshold` | 失败率阈值 | `0.5` |
| `execution.circuitBreaker.slowCallThresholdMs` | 慢调用判定阈值 | `10000` |
| `execution.circuitBreaker.slowCallRateThreshold` | 慢调用率阈值 | `0.5` |
| `execution.circuitBreaker.openDurationMs` | 打开持续时长 | `30000` |
| `execution.circuitBreaker.halfOpenMaxRequests` | 半开探测数 | `1` |
| `execution.cooldown.consecutiveFailures` | 连续失败进入冷却的阈值 | `5` |
| `execution.cooldown.cooldownMs` | 冷却时长 | `30000` |
| `execution.cooldown.maxCooldownMs` | 冷却翻倍上限 | `300000` |
| `execution.cooldown.recoveryProbe` | 恢复探测开关 | `true` |
| `execution.state.flushIntervalMs` | 状态管道聚合周期 | `500` |

兼容性约定：版本 1 的旧 `model.yml`（没有 `execution` 段、渠道没有 `timeout`）**必须能直接启动**——全部走缺省值，行为比 V1 更强（多了熔断/冷却/超时），但路由与转发语义不回退。

### 5.3 请求级覆盖（header）

学习版文档 4.2 要求“渠道级默认 + 请求级覆盖”。本版本提供两个可选的请求级覆盖（通过 header 传递，由 Controller 提取进 metadata）：

| Header | 覆盖内容 | 示例 |
|---|---|---|
| `X-Gateway-Timeout-Ms` | 覆盖整条链总时长 `totalMs` | `X-Gateway-Timeout-Ms: 15000` |
| `X-Gateway-Idle-Timeout-Ms` | 覆盖流式空闲超时 `idleMs` | `X-Gateway-Idle-Timeout-Ms: 5000` |

解析失败（非正整数）→ `400 invalid_request`，避免请求静默使用错误超时。其余层级的请求级覆盖留到版本 5 管理面统一实现，本版本不扩。

---

## 6. 领域模型（关键代码段 S3）

> 路径：`ai-gateway/src/main/java/com/aigateway/execution/model/`

### 6.1 失败分类（`FailureType` / `Failure` / `UpstreamCallException`）

“失败不可怕，可怕的是不知道什么失败能重试、什么失败只能放弃。”所以第一步是把所有失败归成**类型**，而不是只存一条异常消息。

```java
// 路径：execution/model/FailureType.java
public enum FailureType {
    CONNECTION,              // 连接失败：DNS/拒绝/重置/半关闭（唯一允许同实例重试的类型）
    TIMEOUT_CONNECT,         // 连接超时
    TIMEOUT_REQUEST,         // 非流式完整响应超时
    TIMEOUT_FIRST_BYTE,      // 流式首字节超时
    TIMEOUT_IDLE,            // 流式空闲超时
    TIMEOUT_TOTAL,           // 整条降级链总时长超时
    HTTP_429,                // 上游限流（可遵循 Retry-After）
    HTTP_5XX,                // 上游 5xx（可换候选）
    HTTP_OTHER,              // 其它非 2xx（一般不可重试）
    BAD_SSE,                 // SSE 解析失败
    STREAM_INTERRUPTED,      // 流式首字节后中断
    CANCELLED,               // 客户端断开 / 主动取消（不是失败，不计数）
    UNKNOWN
}
```

```java
// 路径：execution/model/Failure.java
public record Failure(
        FailureType type,      // 失败类型（决策的唯一依据）
        String reason,         // 人可读描述（日志/指标）
        int upstreamStatus,    // 上游 HTTP 状态码；无则 -1
        boolean retryable,     // 是否允许触发重试/降级（由分类器 + 策略共同决定）
        long retryAfterMs      // 上游 Retry-After（毫秒）；无则 -1
) {
    public boolean isTimeout() {
        return switch (type) {
            case TIMEOUT_CONNECT, TIMEOUT_REQUEST, TIMEOUT_FIRST_BYTE,
                 TIMEOUT_IDLE, TIMEOUT_TOTAL -> true;
            default -> false;
        };
    }
}
```

```java
// 路径：execution/model/UpstreamCallException.java
// 内部执行异常：连接器与执行器之间传递“结构化失败”，不再用裸 Exception 猜类型。
public class UpstreamCallException extends RuntimeException {

    private final Failure failure;

    public UpstreamCallException(Failure failure) {
        super(failure.reason());
        this.failure = failure;
    }

    public UpstreamCallException(Failure failure, Throwable cause) {
        super(failure.reason(), cause);
        this.failure = failure;
    }

    public Failure failure() { return failure; }
}
```

`FailureClassifier`（Codex 脚手架）负责把“异常 + HTTP 状态码”映射成 `Failure`：网络异常 → `CONNECTION`；`HttpTimeoutException` → `TIMEOUT_CONNECT/TIMEOUT_REQUEST`；非 2xx → 按状态码映射；SSE 解析异常 → `BAD_SSE`。判定 `retryable` 时读 `RetryPolicy.retryableStatuses` 与失败类型（连接/超时/429/5xx 可重试，4xx 业务错误不可重试）。

### 6.2 策略快照（`TimeoutPolicy` / `RetryPolicy` / `CircuitBreakerConfig` / `CooldownConfig` / `ExecutionPolicies`）

```java
// 路径：execution/model/TimeoutPolicy.java
public record TimeoutPolicy(
        long connectMs,     // 建立 TCP 连接
        long requestMs,     // 非流式完整响应
        long firstByteMs,   // 流式首字节
        long idleMs         // 流式空闲（相邻 chunk 最大间隔）
) {
    public static TimeoutPolicy defaults() {
        return new TimeoutPolicy(500, 30_000, 10_000, 30_000);
    }
}

// 路径：execution/model/RetryPolicy.java
public record RetryPolicy(
        int maxAttemptsPerCandidate,      // 默认 1：不重试同实例
        boolean sameInstanceOnConnectionFailure, // 连接级失败允许同实例重试一次
        boolean respectRetryAfter,        // 429 优先遵循 Retry-After
        long maxRetryAfterMs,             // Retry-After 等待上限
        long backoffBaseMs,               // 退避基数
        long backoffMaxMs,                // 退避上限
        double jitterRatio,               // 抖动比例 0~0.5
        Set<Integer> retryableStatuses    // 可重试/可降级的状态码
) {}

// 路径：execution/model/CircuitBreakerConfig.java
public record CircuitBreakerConfig(
        int windowSize,               // 环形窗口大小
        int minimumRequests,          // 最少样本数
        double failureRateThreshold,  // 失败率阈值
        long slowCallThresholdMs,     // 慢调用判定阈值
        double slowCallRateThreshold, // 慢调用率阈值
        long openDurationMs,          // 打开持续时长
        int halfOpenMaxRequests       // 半开探测数
) {}

// 路径：execution/model/CooldownConfig.java
public record CooldownConfig(
        int consecutiveFailures,  // 连续失败阈值
        long cooldownMs,          // 冷却时长
        long maxCooldownMs,       // 翻倍上限
        boolean recoveryProbe     // 恢复探测开关
) {}

// 路径：execution/model/ExecutionPolicies.java
// 运行期不可变快照：一次请求从“缺省 → 渠道覆盖 → 请求级覆盖”合并后的最终策略。
public record ExecutionPolicies(
        long totalMs,               // 整条降级链总时长
        TimeoutPolicy timeout,
        RetryPolicy retry,
        CircuitBreakerConfig circuitBreaker,
        CooldownConfig cooldown
) {}
```

### 6.3 熔断状态（`CircuitState`）

```java
// 路径：execution/model/CircuitState.java
public enum CircuitState { CLOSED, OPEN, HALF_OPEN }
```

状态机的完整逻辑在第 10 节手敲（H3）；这里只定义状态枚举，`ModelState` 快照里也用它，保证“运维状态”与“决策可见状态”是同一套语义。

### 6.4 流式会话（`StreamSession`）

```java
// 路径：execution/model/StreamSession.java
// 连接器返回的流式会话句柄：执行器/代理可以通过 cancel() 主动取消上游连接。
// JDK HttpClient 语义：响应体 InputStream 未消费完时 close() 会取消底层交换。
public interface StreamSession extends AutoCloseable {

    /** 关闭上游连接（幂等：多次调用只生效一次） */
    void cancel();

    /** 是否已被取消（区分“正常结束”与“被取消”，避免把取消误报为失败） */
    boolean isCancelled();

    @Override
    default void close() { cancel(); }
}
```

### 6.5 实时状态（`ModelState`，V3 扩展）

> 路径：`ai-gateway/src/main/java/com/aigateway/decision/model/ModelState.java`（与版本 2 计划同路径，合并时字段直接对齐）

```java
// V2 已有字段（EWMA 延迟/错误率/inFlight） + V3 新增字段
public record ModelState(
        String instanceId,
        double ewmaLatencyMs,     // 指数移动平均延迟（Scorer 的 latency 因子）
        double errorRate,         // 0~1，EWMA 错误率
        int inFlight,             // 当前并发数
        long lastUpdatedAt,       // 最近一次更新时间戳
        CircuitState circuitState, // V3：熔断状态（决策层可读）
        long slowCalls,           // V3：慢调用累计计数（决策输入）
        long totalCalls,          // V3：总调用累计计数（决策输入）
        boolean cooling           // V3：渠道冷却（渠道级，按实例展示）
) {}
```

---

## 7. 配置绑定与启动校验（关键代码段 S4、S5）

### 7.1 `GatewayProperties` 扩展（代码段 S4，Codex）

```java
// 路径：ai-gateway/src/main/java/com/aigateway/infra/config/GatewayProperties.java
// 在 V1/V2 字段基础上追加 execution 段与渠道级 timeout 覆盖。
@Configuration
@ConfigurationProperties(prefix = "gateway")
@Data
public class GatewayProperties {

    private Health health = new Health();
    private Execution execution = new Execution();   // V3 新增
    private List<ChannelDef> channels = new ArrayList<>();
    private List<ModelDef> models = new ArrayList<>();
    // ... V2 的 decision / policies / plugins 按需保留

    /** V3：容错治理参数（嵌套类字段名与 model.yml 一一对应） */
    @Data
    public static class Execution {
        private long totalMs = 60_000;
        private TimeoutDef defaultTimeout = new TimeoutDef();
        private RetryDef retry = new RetryDef();
        private CircuitBreakerDef circuitBreaker = new CircuitBreakerDef();
        private CooldownDef cooldown = new CooldownDef();
        private StateDef state = new StateDef();
    }

    @Data
    public static class TimeoutDef {
        private long connectMs = 500;
        private long requestMs = 30_000;
        private long firstByteMs = 10_000;
        private long idleMs = 30_000;
    }

    @Data
    public static class RetryDef {
        private int maxAttemptsPerCandidate = 1;
        private boolean sameInstanceOnConnectionFailure = true;
        private boolean respectRetryAfter = true;
        private long maxRetryAfterMs = 5_000;
        private long backoffBaseMs = 100;
        private long backoffMaxMs = 2_000;
        private double jitterRatio = 0.2;
        private List<Integer> retryableStatuses = List.of(429, 500, 502, 503, 504);
    }

    @Data
    public static class CircuitBreakerDef {
        private int windowSize = 100;
        private int minimumRequests = 5;
        private double failureRateThreshold = 0.5;
        private long slowCallThresholdMs = 10_000;
        private double slowCallRateThreshold = 0.5;
        private long openDurationMs = 30_000;
        private int halfOpenMaxRequests = 1;
    }

    @Data
    public static class CooldownDef {
        private int consecutiveFailures = 5;
        private long cooldownMs = 30_000;
        private long maxCooldownMs = 300_000;
        private boolean recoveryProbe = true;
    }

    @Data
    public static class StateDef {
        private long flushIntervalMs = 500;
    }

    /** V3：渠道级超时覆盖（可选） */
    @Data
    public static class ChannelDef {
        private String id;
        private String provider;
        private String baseUrl;
        private String credentialsRef = "";
        private int weight = 1;
        private TimeoutDef timeout;   // null = 用 execution.defaultTimeout
    }
}
```

### 7.2 `ExecutionPolicyManager` 启动校验（代码段 S5a，Codex）

```java
// 路径：ai-gateway/src/main/java/com/aigateway/execution/policy/ExecutionPolicyManager.java
@Component
public class ExecutionPolicyManager {

    private final GatewayProperties props;
    private final ExecutionPolicies defaults;

    public ExecutionPolicyManager(GatewayProperties props) {
        this.props = props;
        this.defaults = buildDefaults(props.getExecution());
        validate(defaults);   // 启动期尽早失败：容错参数非法比线上 5xx 好一万倍
    }

    /** 请求级策略：缺省 → 渠道覆盖 → header 覆盖 */
    public ExecutionPolicies forRequest(String channelId, Map<String, String> metadata) {
        GatewayProperties.TimeoutDef channelTimeout = props.getChannels().stream()
                .filter(c -> c.getId().equals(channelId))
                .map(GatewayProperties.ChannelDef::getTimeout)
                .filter(Objects::nonNull)
                .findFirst().orElse(null);

        long totalMs = parseHeader(metadata, "x-gateway-timeout-ms", defaults.totalMs());
        long idleMs = parseHeader(metadata, "x-gateway-idle-timeout-ms",
                channelTimeout != null ? channelTimeout.getIdleMs() : defaults.timeout().idleMs());

        TimeoutPolicy timeout = new TimeoutPolicy(
                channelTimeout != null ? channelTimeout.getConnectMs() : defaults.timeout().connectMs(),
                channelTimeout != null ? channelTimeout.getRequestMs() : defaults.timeout().requestMs(),
                channelTimeout != null ? channelTimeout.getFirstByteMs() : defaults.timeout().firstByteMs(),
                idleMs);
        return new ExecutionPolicies(totalMs, timeout,
                defaults.retry(), defaults.circuitBreaker(), defaults.cooldown());
    }

    private static long parseHeader(Map<String, String> metadata, String key, long fallback) {
        String raw = metadata == null ? null : metadata.get(key);
        if (raw == null || raw.isBlank()) return fallback;
        try {
            long value = Long.parseLong(raw.trim());
            if (value <= 0) throw new NumberFormatException();
            return value;
        } catch (NumberFormatException e) {
            throw new GatewayException(400, "invalid_request",
                    "请求级超时 header 必须是正整数: " + key + "=" + raw);
        }
    }

    /** 启动校验规则：所有容错参数必须在合理范围内，否则启动失败 */
    private void validate(ExecutionPolicies p) {
        require(p.totalMs() > 0, "execution.totalMs 必须 > 0");
        require(p.timeout().connectMs() > 0 && p.timeout().requestMs() > 0
                && p.timeout().firstByteMs() > 0 && p.timeout().idleMs() > 0,
                "execution.defaultTimeout 各层必须 > 0");
        require(p.retry().maxAttemptsPerCandidate() >= 1, "maxAttemptsPerCandidate 必须 >= 1");
        require(p.retry().jitterRatio() >= 0 && p.retry().jitterRatio() <= 0.5,
                "jitterRatio 必须在 [0, 0.5]");
        require(p.retry().backoffBaseMs() <= p.retry().backoffMaxMs(),
                "backoffBaseMs 不能大于 backoffMaxMs");
        require(!p.retry().retryableStatuses().isEmpty(), "retryableStatuses 不能为空");
        require(p.circuitBreaker().windowSize() >= 10, "windowSize 至少 10");
        require(p.circuitBreaker().minimumRequests() >= 1
                        && p.circuitBreaker().minimumRequests() <= p.circuitBreaker().windowSize(),
                "minimumRequests 必须在 [1, windowSize]");
        require(p.circuitBreaker().failureRateThreshold() > 0
                        && p.circuitBreaker().failureRateThreshold() <= 1,
                "failureRateThreshold 必须在 (0, 1]");
        require(p.circuitBreaker().slowCallRateThreshold() > 0
                        && p.circuitBreaker().slowCallRateThreshold() <= 1,
                "slowCallRateThreshold 必须在 (0, 1]");
        require(p.circuitBreaker().openDurationMs() > 0, "openDurationMs 必须 > 0");
        require(p.circuitBreaker().halfOpenMaxRequests() >= 1, "halfOpenMaxRequests 必须 >= 1");
        require(p.cooldown().consecutiveFailures() >= 1, "consecutiveFailures 必须 >= 1");
        require(p.cooldown().cooldownMs() > 0, "cooldownMs 必须 > 0");
        require(p.cooldown().maxCooldownMs() >= p.cooldown().cooldownMs(),
                "maxCooldownMs 不能小于 cooldownMs");
    }

    private static void require(boolean ok, String message) {
        if (!ok) throw new GatewayException(500, "invalid_config", message);
    }

    /** 缺省策略快照（熔断 / 冷却等单例 Bean 装配时取用，见 10.2 / 11.2 装配说明） */
    public ExecutionPolicies defaults() { return defaults; }

    private static ExecutionPolicies buildDefaults(GatewayProperties.Execution e) {
        return new ExecutionPolicies(
                e.getTotalMs(),
                new TimeoutPolicy(e.getDefaultTimeout().getConnectMs(),
                        e.getDefaultTimeout().getRequestMs(),
                        e.getDefaultTimeout().getFirstByteMs(),
                        e.getDefaultTimeout().getIdleMs()),
                new RetryPolicy(e.getRetry().getMaxAttemptsPerCandidate(),
                        e.getRetry().isSameInstanceOnConnectionFailure(),
                        e.getRetry().isRespectRetryAfter(),
                        e.getRetry().getMaxRetryAfterMs(),
                        e.getRetry().getBackoffBaseMs(),
                        e.getRetry().getBackoffMaxMs(),
                        e.getRetry().getJitterRatio(),
                        Set.copyOf(e.getRetry().getRetryableStatuses())),
                new CircuitBreakerConfig(e.getCircuitBreaker().getWindowSize(),
                        e.getCircuitBreaker().getMinimumRequests(),
                        e.getCircuitBreaker().getFailureRateThreshold(),
                        e.getCircuitBreaker().getSlowCallThresholdMs(),
                        e.getCircuitBreaker().getSlowCallRateThreshold(),
                        e.getCircuitBreaker().getOpenDurationMs(),
                        e.getCircuitBreaker().getHalfOpenMaxRequests()),
                new CooldownConfig(e.getCooldown().getConsecutiveFailures(),
                        e.getCooldown().getCooldownMs(),
                        e.getCooldown().getMaxCooldownMs(),
                        e.getCooldown().isRecoveryProbe()));
    }
}
```

设计要点：

- **启动期校验，而不是运行期报错**：阈值写成 `0.5` 之外的非法值（如 `1.5`）会让熔断永远不触发或永远触发，必须在启动时拦住；
- **header 解析失败即 400**：请求级覆盖是显式契约，解析失败不能静默回退；
- 本类只做“合并与校验”，不持有任何可变状态，天然线程安全。

---

## 8. 重试执行器（关键代码段 S6，🖊 H1）

> 路径：`ai-gateway/src/main/java/com/aigateway/execution/retry/RetryExecutor.java`

> 🖊 **手敲 H1**：本节全部手敲；`Failure` / `FailureType` / `RetryPolicy` 为第 6 节脚手架。

### 8.1 重试决策表（先背下来）

“什么失败能重试”由**失败类型 + 策略**共同决定，不按异常消息猜：

| 失败类型 | 同实例重试？ | 换候选（降级）？ | 说明 |
|---|---|---|---|
| `CONNECTION` | ✅ 一次（可配） | ✅ | 连接没建立成功，重打无副作用 |
| `TIMEOUT_CONNECT` | ❌ | ✅ | 超时通常意味着对端忙，换一个更合理 |
| `TIMEOUT_REQUEST` | ❌ | ✅ | 同上 |
| `TIMEOUT_FIRST_BYTE` | ❌ | ✅（首字节前） | 流式没开始输出，换候选安全 |
| `TIMEOUT_IDLE` | ❌ | ❌（首字节后） | 已经出过数据，只能中断 |
| `HTTP_429` | ✅ 一次（遵循 Retry-After，可配） | ✅ | 上游限流，等一会儿或换人 |
| `HTTP_5XX` | ❌ | ✅ | 服务端故障，换部署实例 |
| `HTTP_OTHER`（4xx 等） | ❌ | ❌ | 业务错误，重试也没用 |
| `BAD_SSE` | ❌ | 首字节前 ✅ / 首字节后 ❌ | 解析失败不是瞬时故障 |
| `CANCELLED` | ❌ | ❌ | 客户端断开，直接结束 |

### 8.2 主逻辑（代码段 S6，🖊 H1 全部手敲）

```java
// 路径：ai-gateway/src/main/java/com/aigateway/execution/retry/RetryExecutor.java
package com.aigateway.execution.retry;

/**
 * 重试执行器：只处理“同实例重试”的决策，不负责换候选。
 *
 * 设计语义（对照学习版文档 4.1）：
 * 1. 连接级失败（CONNECTION）且配置允许 → 同实例重试一次（指数退避 + 抖动）；
 * 2. HTTP 429 且上游给了 Retry-After 且配置允许 → 等待后同实例重试一次；
 * 3. 其它可重试失败（超时 / 5xx）→ 原样抛出，由降级链执行器换下一个候选；
 * 4. 不可重试失败（4xx / BAD_SSE / CANCELLED / 首字节后中断）→ 原样抛出；
 * 5. 所有等待都受 TimeoutGuard 总时长预算约束：预算不够就不再等，直接放弃。
 *
 * 安全边界（本项目关键设计）：本类不知道“流式首字节”的存在——首字节语义由
 * StreamProxy（H6）在更外层把关；这里只保证“等待不超总时长、同实例不无限重试”。
 */
@Component
public class RetryExecutor {

    private static final Logger log = LoggerFactory.getLogger(RetryExecutor.class);
    private final GatewayMetrics metrics;

    public RetryExecutor(GatewayMetrics metrics) {
        this.metrics = metrics;
    }

    /** 一次“候选调用”的抽象：返回结果或抛结构化失败 */
    public interface Attempt<T> {
        T run() throws UpstreamCallException;
    }

    /**
     * 执行一次候选调用，按策略决定是否同实例重试。
     *
     * @param instanceId 候选实例（指标打点用）
     * @return 成功结果
     * @throws UpstreamCallException 重试耗尽（或不可重试）时抛出原始失败
     */
    public <T> T execute(RetryPolicy policy, TimeoutGuard guard,
                         String instanceId, Attempt<T> attempt) {
        int attemptNo = 0;   // 0 = 首次调用；1 = 第一次同实例重试（最多一次）
        while (true) {
            try {
                return attempt.run();
            } catch (UpstreamCallException e) {
                Failure f = e.failure();

                // 不可重试 / 不该同实例重试 → 直接上抛，交给降级链换候选
                if (!shouldRetrySameInstance(policy, f, attemptNo)) {
                    throw e;
                }

                long waitMs = waitMillis(policy, attemptNo, f);
                // 预算检查：等待会超过整条链的总时长 → 放弃（不再死等）
                if (!guard.sleepWithinBudget(waitMs)) {
                    throw new UpstreamCallException(
                            new Failure(FailureType.TIMEOUT_TOTAL,
                                    "同实例重试等待超出总时长预算: " + instanceId, -1, true, -1),
                            e);
                }
                log(instanceId, "同实例重试", "attempt=" + attemptNo + " reason=" + f.type()
                        + " wait=" + waitMs + "ms");
                metrics.retry(instanceId, f.type().name());
                attemptNo++;
            }
        }
    }

    /**
     * 同实例重试的两种情形（学习版 4.1）：
     * - 连接级失败：连接都没建立，重打一次是安全的（默认允许）；
     * - 429 + Retry-After：上游明确告诉你“等多久再试”（默认允许）。
     * 其余一律不重试同实例——重试 = 换候选，由外层完成。
     */
    private boolean shouldRetrySameInstance(RetryPolicy policy, Failure f, int attemptNo) {
        if (attemptNo >= 1) return false;                     // 同实例最多重试一次
        if (!f.retryable()) return false;
        if (f.type() == FailureType.CONNECTION) {
            return policy.sameInstanceOnConnectionFailure();
        }
        return f.type() == FailureType.HTTP_429
                && policy.respectRetryAfter()
                && f.retryAfterMs() > 0;
    }

    /**
     * 等待时长：
     * - 429：优先听上游 Retry-After（受 maxRetryAfterMs 上限约束）；
     * - 连接失败：指数退避 base × 2^n（受 backoffMaxMs 上限）+ 随机抖动。
     */
    private long waitMillis(RetryPolicy policy, int attemptNo, Failure f) {
        if (f.type() == FailureType.HTTP_429) {
            return Math.min(f.retryAfterMs(), policy.maxRetryAfterMs());
        }
        long base = Math.min(policy.backoffBaseMs() << attemptNo, policy.backoffMaxMs());
        long jitter = (long) (base * policy.jitterRatio() * ThreadLocalRandom.current().nextDouble());
        return base + jitter;
    }

    private static void log(String instanceId, String event, String detail) {
        // 结构化日志（SLF4J）：可按级别过滤、可携带 MDC 上下文（requestId）
        log.info("[{}] event={} detail={}", instanceId, event, detail);
    }
}
```

设计要点：

- **重试次数上限是“隐式”的**：同实例最多一次（`attemptNo >= 1` 即停），不会无限重试；换候选的次数由降级链长度控制；
- **退避公式可讲**：`min(base × 2^n, max) + jitter`，n 是重试序号；抖动用 `[0, base×jitterRatio)` 的随机量，防止多个请求同时重打（惊群）；
- **429 的 `Retry-After` 优先级最高**：上游比网关更清楚限流窗口；但没有该 header 时 429 也直接换候选（本类不等待）；
- `TimeoutGuard` 在下一节手敲，`sleepWithinBudget` 是“总时长预算”的唯一闸门。

---

## 9. 分层超时（关键代码段 S7，🖊 H2）

> 路径：`ai-gateway/src/main/java/com/aigateway/execution/timeout/TimeoutGuard.java`

> 🖊 **手敲 H2**：`TimeoutGuard` 全部手敲；调度器 Bean、Connector 接入为 Codex 脚手架。

### 9.1 分层超时语义（先背下来）

| 层 | 配置 | 谁在计时 | 超时后的动作 |
|---|---|---|---|
| 连接 | `connectMs` | HttpClient（按渠道缓存客户端） | `TIMEOUT_CONNECT` → 换候选 |
| 完整响应 | `requestMs` | HttpClient `HttpRequest.timeout` | `TIMEOUT_REQUEST` → 换候选 |
| 首字节 | `firstByteMs` | TimeoutGuard 定时器（关闭 body 流） | `TIMEOUT_FIRST_BYTE` → 首字节前可换候选 |
| 空闲 | `idleMs` | TimeoutGuard 定时器（每 chunk 重置） | `TIMEOUT_IDLE` → 首字节后只能中断 |
| 整链总时长 | `totalMs` | TimeoutGuard deadline | `TIMEOUT_TOTAL` → 立即放弃整条链 |

### 9.2 调度器 Bean（代码段 S7a，Codex）

```java
// 路径：ai-gateway/src/main/java/com/aigateway/infra/config/GatewaySchedulers.java
@Configuration
public class GatewaySchedulers {

    /** 全局定时器线程池：超时关闭流、状态管道聚合都用它；任务必须轻量（关闭流/读队列） */
    @Bean(destroyMethod = "shutdownNow")
    public ScheduledExecutorService gatewayScheduler() {
        return Executors.newScheduledThreadPool(2, r -> {
            Thread t = new Thread(r, "gw-timer");
            t.setDaemon(true);
            return t;
        });
    }
}
```

### 9.3 `TimeoutGuard` 主逻辑（代码段 S7，🖊 H2 全部手敲）

```java
// 路径：ai-gateway/src/main/java/com/aigateway/execution/timeout/TimeoutGuard.java
package com.aigateway.execution.timeout;

/**
 * 超时守卫：管两件事——
 * 1. 整条降级链的总时长 deadline（所有尝试共享，重试等待也受它约束）；
 * 2. 流式的“定时关闭”定时器（首字节 / 空闲）：到点后由回调关闭上游连接。
 *
 * 学习要点：
 * - 用 System.nanoTime() 算剩余时间，不受系统时钟被修改影响；
 * - 定时器回调只做“关闭连接 / 置标志”这类轻量动作，绝不在定时器线程里做阻塞 IO；
 * - close() 幂等：正常结束、取消、超时都会调，不会重复执行清理。
 */
public final class TimeoutGuard implements AutoCloseable {

    private final long totalDeadlineNanos;          // 创建时刻 + totalMs
    private final ScheduledExecutorService scheduler;
    private final List<ScheduledFuture<?>> timers = new CopyOnWriteArrayList<>();
    private final AtomicBoolean closed = new AtomicBoolean();

    public TimeoutGuard(long totalMs, ScheduledExecutorService scheduler) {
        this.totalDeadlineNanos = System.nanoTime() + TimeUnit.MILLISECONDS.toNanos(totalMs);
        this.scheduler = scheduler;
    }

    /** 整条链是否已超时（每次尝试前先查一次，超时就别再打） */
    public boolean expired() {
        return System.nanoTime() >= totalDeadlineNanos;
    }

    /** 剩余预算（毫秒）；为负按 0 处理 */
    public long remainingMs() {
        long nanos = totalDeadlineNanos - System.nanoTime();
        return Math.max(0, TimeUnit.NANOSECONDS.toMillis(nanos));
    }

    /** 注册一个一次性定时器（首字节 / 空闲都用它）；guard 已关闭则忽略 */
    public void scheduleOnce(long delayMs, Runnable action) {
        if (closed.get()) return;
        timers.add(scheduler.schedule(() -> {
            if (!closed.get()) action.run();
        }, delayMs, TimeUnit.MILLISECONDS));
    }

    /** 取消全部定时器（收到首字节 / 正常结束时调用） */
    public void cancelTimers() {
        timers.forEach(f -> f.cancel(false));
        timers.clear();
    }

    /**
     * 在总时长预算内睡眠（重试退避 / Retry-After 等待的唯一入口）。
     * 预算不够或线程被中断 → false（调用方应放弃，而不是继续等）。
     */
    public boolean sleepWithinBudget(long ms) {
        if (ms <= 0) return true;
        if (remainingMs() < ms) return false;
        try {
            Thread.sleep(ms);
            return true;
        } catch (InterruptedException e) {
            Thread.currentThread().interrupt();
            return false;
        }
    }

    @Override
    public void close() {
        closed.set(true);
        cancelTimers();
    }
}
```

### 9.4 `OpenAIConnector` 超时接入（代码段 S7b，Codex）

连接器改造原则：**连接器只做“超时到点就抛结构化失败”，不决定重试/降级**。

- 非流式：`builder.timeout(Duration.ofMillis(policies.timeout().requestMs()))`；捕获 `HttpTimeoutException` 后由 `FailureClassifier` 判定为 `TIMEOUT_REQUEST`，分类处记录 `metrics.timeout(instanceId, "request")`（连接超时同理记 `"connect"`）；
- 连接超时：`HttpClientFactory` 增加按 `connectMs` 缓存的客户端（`Map<Long, HttpClient>`），避免每次请求新建；
- 流式：`stream(...)` 签名改为返回 `StreamSession`（见 6.4），内部用 `TimeoutGuard` 注册首字节定时器（收到首个 chunk 后 `cancelTimers()`）与空闲定时器（每 chunk `scheduleOnce(idleMs, ...)` 重置）；定时器到点后执行 `body.close()`——**JDK HttpClient 语义：未消费完的响应体关闭 = 取消上游交换**，阻塞中的 `readLine()` 会抛 `IOException`，由 `FailureClassifier` 结合“定时器是否触发”的标记判定为 `TIMEOUT_FIRST_BYTE / TIMEOUT_IDLE`，分类处记录 `metrics.timeout(instanceId, "first_byte" / "idle")`；
- 客户端断开（consumer 抛 `ClientDisconnectedException`）：连接器先 `session.cancel()`，再**原样放行该异常**——读取循环只捕获 `IOException`，不得捕获或包装回调抛出的 `ClientDisconnectedException`，由 `StreamProxy`（13.2）捕获走“取消”路径；读循环自身捕获的 IOException 仍按 `cancelled` 标记分类为 `CANCELLED`。

> 关键坑：**区分“被取消”和“上游失败”**。连接器内部用一个 `AtomicBoolean cancelled`，`cancel()` 置位；读取循环捕获 IOException 时先查这个标记——已取消 → `CANCELLED`，未取消 → 按真实失败分类。否则客户端断开会被误记为上游失败，污染熔断统计。

---

## 10. 熔断状态机（关键代码段 S8，🖊 H3）

> 路径：`ai-gateway/src/main/java/com/aigateway/execution/circuit/CircuitBreaker.java`

> 🖊 **手敲 H3**：`CircuitBreaker` 全部手敲；`CircuitBreakerRegistry`（实例注册表 + 指标 + 事件发布）为 Codex 脚手架。

### 10.1 状态机（先背下来）

```
       失败率/慢调用率超阈值
   CLOSED ──────────────────► OPEN
      ▲                        │
      │ 半开探测成功            │ 打开时长到期
      │                        ▼
      └─────────────────── HALF_OPEN
          半开探测失败 → 回到 OPEN 重新计时
```

- **CLOSED（关闭）**：正常放行，滑动窗口记录最近 N 次结果；失败率或慢调用率超阈值 → OPEN。
- **OPEN（打开）**：请求直接快速失败（不再打上游），走降级链；持续 `openDurationMs` 后 → HALF_OPEN。
- **HALF_OPEN（半开）**：只放行 `halfOpenMaxRequests` 个探测请求；成功 → CLOSED 并清空窗口；失败 → OPEN 重新计时。
- 粒度：按“渠道 + 模型”（instanceId）独立实例，互不干扰。

### 10.2 主逻辑（代码段 S8，🖊 H3 全部手敲）

```java
// 路径：ai-gateway/src/main/java/com/aigateway/execution/circuit/CircuitBreaker.java
package com.aigateway.execution.circuit;

/**
 * 单实例熔断器：滑动窗口（环形数组）统计最近 N 次结果。
 *
 * 学习要点：
 * - 环形数组：写入 O(1)，旧样本自动被新样本挤出——这就是“滑动窗口”；
 * - 所有状态转换在 synchronized(lock) 内完成：熔断是全局性决策，不允许并发竞态
 *   把“打开”和“关闭”同时写进去；
 * - OPEN 期的拒绝请求不进入窗口（它们没打上游，不是有效样本）。
 */
public final class CircuitBreaker {

    private final String instanceId;
    private final CircuitBreakerConfig config;
    private final Object lock = new Object();

    private CircuitState state = CircuitState.CLOSED;
    private final boolean[] failures;   // 环形窗口：true = 失败
    private final boolean[] slows;      // 环形窗口：true = 慢调用
    private int index;                  // 下一个写入位
    private int filled;                 // 已填充样本数
    private int failureCount;
    private int slowCount;
    private long openUntilMs;
    private int halfOpenPermits;

    public CircuitBreaker(String instanceId, CircuitBreakerConfig config) {
        this.instanceId = instanceId;
        this.config = config;
        this.failures = new boolean[config.windowSize()];
        this.slows = new boolean[config.windowSize()];
    }

    /** 是否放行本次请求（执行前调用） */
    public boolean allowRequest() {
        synchronized (lock) {
            return switch (state) {
                case CLOSED -> true;
                case OPEN -> {
                    // 打开期到期 → 转半开并发放探测配额
                    if (System.currentTimeMillis() >= openUntilMs) {
                        state = CircuitState.HALF_OPEN;
                        halfOpenPermits = config.halfOpenMaxRequests();
                        yield true;
                    }
                    yield false;
                }
                case HALF_OPEN -> {
                    if (halfOpenPermits > 0) {
                        halfOpenPermits--;
                        yield true;
                    }
                    yield false;
                }
            };
        }
    }

    /**
     * 记录一次真实调用结果（只有被放行的请求才会调用这里）。
     *
     * @param success 是否成功
     * @param latencyMs 耗时
     * @param slowThresholdMs 慢调用阈值（null 表示不判定慢调用）
     */
    public void record(boolean success, long latencyMs, Long slowThresholdMs) {
        boolean slow = slowThresholdMs != null && latencyMs >= slowThresholdMs;
        synchronized (lock) {
            // 半开态：探测结果直接决定去向，不进窗口
            if (state == CircuitState.HALF_OPEN) {
                resetWindow();
                if (success) {
                    state = CircuitState.CLOSED;
                } else {
                    open();
                }
                return;
            }
            push(success, slow);
            // 样本足够且失败率 / 慢调用率任一超阈值 → 打开
            if (filled >= config.minimumRequests()
                    && (failureRate() >= config.failureRateThreshold()
                        || slowRate() >= config.slowCallRateThreshold())) {
                open();
            }
        }
    }

    /** 环形数组写入：先减去旧样本贡献，再加新样本 */
    private void push(boolean success, boolean slow) {
        if (filled < config.windowSize()) filled++;
        if (failures[index]) failureCount--;
        if (slows[index]) slowCount--;

        failures[index] = !success;
        slows[index] = slow;
        if (!success) failureCount++;
        if (slow) slowCount++;
        index = (index + 1) % config.windowSize();
    }

    private void open() {
        state = CircuitState.OPEN;
        openUntilMs = System.currentTimeMillis() + config.openDurationMs();
        halfOpenPermits = 0;
    }

    private void resetWindow() {
        Arrays.fill(failures, false);
        Arrays.fill(slows, false);
        index = 0;
        filled = 0;
        failureCount = 0;
        slowCount = 0;
    }

    private double failureRate() {
        return filled == 0 ? 0 : (double) failureCount / filled;
    }

    private double slowRate() {
        return filled == 0 ? 0 : (double) slowCount / filled;
    }

    public CircuitState state() {
        synchronized (lock) { return state; }
    }

    public String instanceId() { return instanceId; }
}
```

设计要点：

- **“样本不足不熔断”**：`minimumRequests` 防止刚启动时一两次失败就误杀；
- **失败率和慢调用率是“或”关系**：上游即使没报错，但一直慢到拖垮吞吐，同样应该熔断；
- **半开探测失败回到 OPEN**：重新计时而不是立即关闭，给上游留恢复窗口；
- `CircuitBreakerRegistry`（Codex）按 instanceId 懒加载实例，`record(...)` 后把状态转换发布为 `StateEvent.CircuitTransition` 到状态管道（第 14 节），并记录 `gateway.circuit.breaker.events` 指标。

---

## 11. 渠道冷却（关键代码段 S9，🖊 H4）

> 路径：`ai-gateway/src/main/java/com/aigateway/execution/cooldown/CooldownManager.java`

> 🖊 **手敲 H4**：本节全部手敲；`CooldownConfig` 为第 6 节脚手架。

### 11.1 与熔断的分工（先背下来）

| | 熔断 | 冷却 |
|---|---|---|
| 粒度 | 渠道 + 模型（instanceId） | 渠道（channelId） |
| 依据 | 滑动窗口失败率 / 慢调用率 | 连续失败次数 |
| 触发 | 阈值 + 最少样本 | 连续 N 次失败（轻量） |
| 恢复 | 半开探测 | 到期自动恢复 + 探测翻倍 |
| 定位 | 持续故障 | 短时抽风 |

两者独立触发，**任一命中即从候选剔除**；日志必须写明是哪个原因跳过（风险第 5 条）。

### 11.2 主逻辑（代码段 S9，🖊 H4 全部手敲）

```java
// 路径：ai-gateway/src/main/java/com/aigateway/execution/cooldown/CooldownManager.java
package com.aigateway.execution.cooldown;

/**
 * 渠道冷却：连续失败达阈值 → 冷却；到期后第一个请求视为“恢复探测”。
 *
 * 状态（每个渠道一份）：
 * - consecutiveFails：连续失败计数（成功即清零）；
 * - coolingUntilMs：冷却到期时间（0 = 不在冷却）；
 * - currentCooldownMs：当前冷却时长（探测失败翻倍用）；
 * - pendingProbe：冷却到期后进入探测期，下一个失败直接翻倍重新冷却。
 *
 * 学习要点：
 * - ConcurrentHashMap.compute 原子更新，并发记录不丢计数；
 * - compute 回调内不做任何阻塞 IO、不触碰同一 map 的其它键；
 * - isCooling() 顺带做“到期 → 进入探测期”的惰性状态迁移；迁移必须走 compute
 *   原子完成，**不能**在 get() 返回的裸对象上写字段——否则与 recordFailure 的
 *   compute 竞态（TOCTOU），一次本应记录的失败会被静默丢弃；
 * - 冷却进出通过 Listener 回调通知接线层（发布事件与指标），回调必须轻量且不抛异常。
 */
public final class CooldownManager {

    /** 冷却状态变化回调：@param cooling true=进入冷却；false=冷却结束/恢复（含转入探测期） */
    @FunctionalInterface
    public interface Listener {
        void onCooldownChange(String channelId, boolean cooling, long cooldownMs);
    }

    private final CooldownConfig config;
    private final Listener listener;
    private final Map<String, ChannelCooldown> states = new ConcurrentHashMap<>();

    private static final class ChannelCooldown {
        int consecutiveFails;
        long coolingUntilMs;
        long currentCooldownMs;
        boolean pendingProbe;
    }

    public CooldownManager(CooldownConfig config) {
        this(config, (channelId, cooling, cooldownMs) -> {});
    }

    public CooldownManager(CooldownConfig config, Listener listener) {
        this.config = config;
        this.listener = listener;
    }

    /** 渠道是否在冷却期（执行前调用）；到期会自动转入探测期 */
    public boolean isCooling(String channelId) {
        // 惰性迁移必须用 compute：裸写 get() 返回的对象会与 recordFailure 的 compute 竞态
        ChannelCooldown s = states.compute(channelId, (id, old) -> {
            if (old == null || old.coolingUntilMs == 0) return old;
            if (System.currentTimeMillis() >= old.coolingUntilMs) {
                old.coolingUntilMs = 0;                     // 冷却结束
                old.pendingProbe = config.recoveryProbe();  // 进入探测期
                listener.onCooldownChange(id, false, 0);
            }
            return old;
        });
        return s != null && s.coolingUntilMs > 0;
    }

    /** 记录一次失败：连续失败达标 → 冷却；探测期失败 → 翻倍冷却 */
    public void recordFailure(String channelId) {
        states.compute(channelId, (id, old) -> {
            ChannelCooldown s = old != null ? old : new ChannelCooldown();
            if (s.coolingUntilMs != 0) return s;  // 已在冷却期：不再累计

            if (s.pendingProbe) {
                // 恢复探测失败：重新冷却，时长翻倍（有上限）
                long next = Math.min(Math.max(s.currentCooldownMs * 2, config.cooldownMs()),
                        config.maxCooldownMs());
                enterCooldown(s, next);
                s.consecutiveFails = 1;
                s.pendingProbe = false;
                listener.onCooldownChange(channelId, true, next);
                return s;
            }

            s.consecutiveFails++;
            if (s.consecutiveFails >= config.consecutiveFailures()) {
                enterCooldown(s, config.cooldownMs());
                s.consecutiveFails = 0;
                listener.onCooldownChange(channelId, true, config.cooldownMs());
            }
            return s;
        });
    }

    /** 记录一次成功：清零计数、退出探测期、恢复基准冷却时长 */
    public void recordSuccess(String channelId) {
        states.compute(channelId, (id, old) -> {
            if (old == null) return null;
            boolean wasCooling = old.coolingUntilMs != 0;
            old.consecutiveFails = 0;
            old.pendingProbe = false;
            old.coolingUntilMs = 0;
            old.currentCooldownMs = config.cooldownMs();
            if (wasCooling) listener.onCooldownChange(id, false, 0);
            return old;
        });
    }

    private void enterCooldown(ChannelCooldown s, long durationMs) {
        s.currentCooldownMs = durationMs;
        s.coolingUntilMs = System.currentTimeMillis() + durationMs;
    }
}
```

设计要点：

- **冷却只按渠道**：同一渠道下多个模型共享冷却状态——渠道“整体抽风”时全部候选被剔除；
- **恢复探测的“翻倍”来自 `currentCooldownMs`**：第一次冷却 30s，探测失败 → 60s，再失败 → 120s……直到 `maxCooldownMs`（默认 5 分钟）；
- 成功会重置 `currentCooldownMs` 回基准值，避免一次故障让渠道“永远越冷却越长”；
- **指标与事件（接线层装配）**：`CooldownManager` 本身零依赖（不含事件/指标代码），冷却进出通过 `Listener` 回调通知接线层。由 Codex 在配置类中装配 Bean：

  ```java
  // 路径：ai-gateway/src/main/java/com/aigateway/infra/config/GatewayBeans.java（Codex 接线）
  @Bean
  public CooldownManager cooldownManager(ExecutionPolicyManager policyManager,
                                         StateEventPipeline stateEvents, GatewayMetrics metrics) {
      CooldownManager.Listener listener = (channelId, cooling, cooldownMs) -> {
          stateEvents.offer(new StateEvent.CooldownChange(channelId, cooling, cooldownMs));
          metrics.cooldownEvent(channelId, cooling ? "enter" : "exit");
      };
      return new CooldownManager(policyManager.defaults().cooldown(), listener);
  }
  ```

  回调只做“入队 + 原子计数”两类轻量动作，**不得阻塞、不得抛异常**（compute 回调内调用）。`CooldownConfig` 取 `ExecutionPolicyManager.defaults().cooldown()`（7.2 暴露的缺省快照）。

---

## 12. 降级链执行（关键代码段 S10，🖊 H5）

> 路径：`ai-gateway/src/main/java/com/aigateway/execution/fallback/`

> 🖊 **手敲 H5**：`FallbackChainExecutor` 全部手敲；`ExecutionChainResolver` 与 `ChatGatewayService` 接线为 Codex 脚手架。

### 12.1 候选链来源（代码段 S10a，Codex）

```java
// 路径：ai-gateway/src/main/java/com/aigateway/execution/fallback/ExecutionChainResolver.java
@Component
public class ExecutionChainResolver {

    private final ModelRegistry registry;
    private final HealthChecker healthChecker;
    private final ObjectProvider<DecisionEngine> decisionEngine; // V2 合并后自动生效

    public ExecutionChainResolver(ModelRegistry registry, HealthChecker healthChecker,
                                  ObjectProvider<DecisionEngine> decisionEngine) {
        this.registry = registry;
        this.healthChecker = healthChecker;
        this.decisionEngine = decisionEngine;
    }

    /**
     * 生成本次请求的执行链。
     * - V2 合并后：DecisionEngine 出主选 + 降级链（决策与执行分离）；
     * - V2 未合并（默认基线）：健康过滤 + “加权随机不重复抽样”排列，
     *   等价于 V1 每次失败后重新加权随机的语义，保证加权分布不回退。
     * 熔断 / 冷却不在链生成时剔除，而在执行时逐候选检查（状态可能在链生成后变化）。
     */
    public List<ModelInstance> resolve(ChatRequest request, String requestId,
                                       Map<String, String> metadata) {
        DecisionEngine engine = decisionEngine.getIfAvailable();
        if (engine != null) {
            // V2 合并后：决策输出 RoutingDecision，V3 只消费
            // DecisionOutcome outcome = engine.decide(request, requestId, metadata);
            // RoutingDecision d = outcome.decision();
            // return concat(d.primary(), d.fallbackChain());
        }

        List<ModelInstance> healthy = registry.findByAlias(request.model()).stream()
                .filter(inst -> healthChecker.isHealthy(inst.channelId()))
                .toList();
        return weightedShuffle(healthy);
    }

    /** 加权随机不重复抽样：每轮用剩余候选重新按权重挑一个（等价 V1 的“失败后重新抽”）。 */
    private List<ModelInstance> weightedShuffle(List<ModelInstance> candidates) {
        List<ModelInstance> remaining = new ArrayList<>(candidates);
        List<ModelInstance> chain = new ArrayList<>();
        while (!remaining.isEmpty()) {
            ModelInstance pick = new WeightedRandomPicker(remaining).pick()
                    .orElseThrow(() -> new GatewayException(503, "no_available_model", "候选为空"));
            chain.add(pick);
            remaining.remove(pick);
        }
        return List.copyOf(chain);
    }
}
```

### 12.2 `FallbackChainExecutor` 主逻辑（代码段 S10，🖊 H5 全部手敲）

```java
// 路径：ai-gateway/src/main/java/com/aigateway/execution/fallback/FallbackChainExecutor.java
package com.aigateway.execution.fallback;

/**
 * 降级链执行器（非流式）：按序尝试候选，每个候选内部由 RetryExecutor 处理同实例重试。
 *
 * 执行语义（对照学习版 4.3）：
 * 1. 每个候选尝试前：查总时长 → 查熔断 → 查冷却，任一命中就跳过（记录原因）；
 * 2. 候选调用失败：可重试的交给 RetryExecutor（连接重试 / 429 Retry-After），
 *    其余直接换下一个候选；
 * 3. 全部候选失败：区分“尝试过但失败”（502 / 504）与“全被熔断/冷却跳过”（503）；
 * 4. 整条链受 TimeoutGuard 总时长约束：预算用完立即放弃，不再发起新尝试。
 */
@Component
public class FallbackChainExecutor {

    private static final Logger log = LoggerFactory.getLogger(FallbackChainExecutor.class);

    private final OpenAIConnector connector;
    private final CircuitBreakerRegistry circuitBreakers;
    private final CooldownManager cooldowns;
    private final RetryExecutor retryExecutor;
    private final ExecutionPolicyManager policyManager;
    private final ModelStateStore stateStore;
    private final StateEventPipeline stateEvents;
    private final GatewayMetrics metrics;
    private final ScheduledExecutorService scheduler;

    public FallbackChainExecutor(OpenAIConnector connector,
                                 CircuitBreakerRegistry circuitBreakers,
                                 CooldownManager cooldowns,
                                 RetryExecutor retryExecutor,
                                 ExecutionPolicyManager policyManager,
                                 ModelStateStore stateStore,
                                 StateEventPipeline stateEvents,
                                 GatewayMetrics metrics,
                                 ScheduledExecutorService scheduler) {
        this.connector = connector;
        this.circuitBreakers = circuitBreakers;
        this.cooldowns = cooldowns;
        this.retryExecutor = retryExecutor;
        this.policyManager = policyManager;
        this.stateStore = stateStore;
        this.stateEvents = stateEvents;
        this.metrics = metrics;
        this.scheduler = scheduler;
    }

    /** 执行完整降级链；成功返回，失败抛 GatewayException */
    public ChatCompletion execute(List<ModelInstance> chain, ChatRequest request,
                                  String requestId, Map<String, String> metadata) {
        if (chain.isEmpty()) {
            throw new GatewayException(503, "no_available_model",
                    "模型[" + request.model() + "] 当前没有可用实例");
        }
        // 总时长预算取首候选的请求级策略（实现简化；各候选渠道超时仍各自解析）
        ExecutionPolicies firstPolicies = policyManager.forRequest(chain.get(0).channelId(), metadata);
        TimeoutGuard guard = new TimeoutGuard(firstPolicies.totalMs(), scheduler);

        Failure lastFailure = null;
        boolean attempted = false;
        boolean skippedByCircuit = false;
        boolean skippedByCooldown = false;

        for (ModelInstance candidate : chain) {
            if (guard.expired()) {
                metrics.timeout(candidate.instanceId(), "total");
                throw mapFinalFailure(lastFailure, guard);
            }
            ExecutionPolicies policies = policyManager.forRequest(candidate.channelId(), metadata);

            // 熔断 / 冷却：任一命中即剔除（学习版 4.4 / 4.5）
            if (!circuitBreakers.allowRequest(candidate.instanceId())) {
                skippedByCircuit = true;
                metrics.circuitRejected(candidate.instanceId());
                log(requestId, "候选被熔断跳过", candidate.describe());
                continue;
            }
            if (cooldowns.isCooling(candidate.channelId())) {
                skippedByCooldown = true;
                metrics.cooldownSkipped(candidate.channelId());
                log(requestId, "候选被冷却跳过", candidate.describe());
                continue;
            }

            attempted = true;
            long start = System.nanoTime();
            stateStore.beginRequest(candidate.instanceId());
            try {
                // RetryExecutor 只处理“同实例重试”；失败会以 UpstreamCallException 抛出
                ChatCompletion result = retryExecutor.execute(policies.retry(), guard,
                        candidate.instanceId(),
                        () -> connector.complete(candidate, request, policies));
                recordOutcome(candidate, true, elapsedMs(start), null, policies);
                metrics.success(request.model(), candidate.instanceId());
                log(requestId, "执行成功", candidate.describe());
                return result;
            } catch (UpstreamCallException e) {
                long elapsed = elapsedMs(start);
                lastFailure = e.failure();
                recordOutcome(candidate, false, elapsed, lastFailure, policies);
                metrics.failure(request.model(), candidate.instanceId());
                metrics.fallback(candidate.instanceId(), lastFailure.type().name());
                log(requestId, "候选失败，降级",
                        candidate.describe() + " reason=" + lastFailure.type());
            } finally {
                stateStore.endRequest(candidate.instanceId());
            }
        }

        if (!attempted) {
            // 全被跳过 ≠ 全失败：503 语义不同，错误体要能说清原因
            String type = skippedByCircuit ? "circuit_open" : "cooldown_active";
            throw new GatewayException(503, type,
                    "所有候选均被" + (skippedByCircuit ? "熔断" : "冷却") + "剔除，未发起上游调用");
        }
        throw mapFinalFailure(lastFailure, guard);
    }

    /** 一次真实调用结束后：熔断窗口、状态管道、冷却计数全部在这里收口（单点记账） */
    private void recordOutcome(ModelInstance candidate, boolean success, long latencyMs,
                               Failure failure, ExecutionPolicies policies) {
        long slowThreshold = policies.circuitBreaker().slowCallThresholdMs();
        boolean slow = latencyMs >= slowThreshold;
        circuitBreakers.recordResult(candidate.instanceId(), success, latencyMs, slowThreshold);

        if (success) {
            stateEvents.offer(new StateEvent.Success(candidate.instanceId(), latencyMs));
            if (slow) {
                stateEvents.offer(new StateEvent.SlowCall(candidate.instanceId(), latencyMs));
            }
            cooldowns.recordSuccess(candidate.channelId());
        } else {
            stateEvents.offer(new StateEvent.Failure(candidate.instanceId(), failure.type()));
            if (slow) {
                stateEvents.offer(new StateEvent.SlowCall(candidate.instanceId(), latencyMs));
            }
            cooldowns.recordFailure(candidate.channelId());
        }
    }

    /** 最终失败 → 客户端错误体：超时映射 504，其余 502 */
    private GatewayException mapFinalFailure(Failure failure, TimeoutGuard guard) {
        if (guard.expired()) {
            return new GatewayException(504, "timeout_total", "整条降级链总时长超时");
        }
        if (failure == null) {
            return new GatewayException(502, "upstream_failed", "所有候选均失败");
        }
        return switch (failure.type()) {
            case TIMEOUT_CONNECT -> new GatewayException(504, "timeout_connect", failure.reason());
            case TIMEOUT_REQUEST -> new GatewayException(504, "timeout_request", failure.reason());
            case TIMEOUT_FIRST_BYTE -> new GatewayException(504, "timeout_first_byte", failure.reason());
            case TIMEOUT_IDLE -> new GatewayException(504, "timeout_idle", failure.reason());
            case TIMEOUT_TOTAL -> new GatewayException(504, "timeout_total", failure.reason());
            default -> new GatewayException(502, "upstream_failed",
                    "所有候选均失败: " + failure.type() + " - " + failure.reason());
        };
    }

    private static long elapsedMs(long startNanos) {
        return TimeUnit.NANOSECONDS.toMillis(System.nanoTime() - startNanos);
    }

    private static void log(String requestId, String event, String detail) {
        // 结构化日志（SLF4J）：可按级别过滤、可携带 MDC 上下文（requestId）
        log.info("[requestId={}] event={} detail={}", requestId, event, detail);
    }
}
```

设计要点：

- **记账收口**：熔断窗口、冷却计数、状态管道事件在 `recordOutcome` 一处完成，避免多个调用点漏记/重记；
- **503 与 502 语义分离**：`circuit_open` / `cooldown_active` 是“没打上游就失败”，`upstream_failed` 是“打了都失败”，可观测性上必须能区分；
- **总时长是硬边界**：每次循环先查 `guard.expired()`；`RetryExecutor` 的等待也受同一 guard 约束，不会出现“重试把总时长拖爆”；
- 预留成本降级接口：V4 预算插件可通过 metadata / 信号跳过超预算候选，执行器只认“候选列表”，不感知预算逻辑。

### 12.3 `ChatGatewayService` 接线（代码段 S10b，Codex）

```java
// 路径：ai-gateway/src/main/java/com/aigateway/core/service/ChatGatewayService.java
// V3：主流程只做“解析候选链 + 交给执行器”，容错细节全部下沉。
@Service
public class ChatGatewayService {

    private final ExecutionChainResolver chainResolver;
    private final FallbackChainExecutor fallbackExecutor;
    private final StreamProxy streamProxy;

    public ChatGatewayService(ExecutionChainResolver chainResolver,
                              FallbackChainExecutor fallbackExecutor,
                              StreamProxy streamProxy) {
        this.chainResolver = chainResolver;
        this.fallbackExecutor = fallbackExecutor;
        this.streamProxy = streamProxy;
    }

    public ChatCompletion complete(ChatRequest request, String requestId,
                                   Map<String, String> metadata) {
        return fallbackExecutor.execute(
                chainResolver.resolve(request, requestId, metadata),
                request, requestId, metadata);
    }

    public void stream(ChatRequest request, String requestId, Map<String, String> metadata,
                       Consumer<ChatChunk> consumer) {
        streamProxy.streamChain(
                chainResolver.resolve(request, requestId, metadata),
                request, requestId, metadata, consumer);
    }
}
```

---

## 13. 流式治理（关键代码段 S11，🖊 H6）

> 路径：`ai-gateway/src/main/java/com/aigateway/execution/stream/StreamProxy.java`

> 🖊 **手敲 H6**：`StreamProxy` 全部手敲；`ClientDisconnectedException`、`StreamState`、`MeteringCallback`、Connector 流式会话、Controller 断连处理为 Codex 脚手架。

### 13.1 流式 DTO 扩展（代码段 S11a，Codex）

`ChatChunk` 增加两个可选字段：`usage`（部分上游在结束 chunk 携带 token 用量，V4 计量用）与 `error`（统一错误事件）。类级 `@JsonInclude(NON_NULL)` 保证正常 chunk 序列化不出现 `"error":null`。

```java
// 路径：ai-gateway/src/main/java/com/aigateway/api/dto/ChatChunk.java
@JsonIgnoreProperties(ignoreUnknown = true)
@JsonInclude(JsonInclude.Include.NON_NULL)
public record ChatChunk(
        String id,
        String object,
        long created,
        String model,
        List<ChunkChoice> choices,
        Usage usage,          // V3：可选，结束 chunk 的 token 用量（V4 计量）
        StreamError error     // V3：可选，统一错误事件（正常 chunk 为 null）
) {
    // ChunkChoice / Delta 保持 V1 不变

    /** 统一流式错误体：序列化为 {"error":{"type":...,"message":...}} */
    @JsonIgnoreProperties(ignoreUnknown = true)
    public record StreamError(String type, String message) {}
}
```

`Usage` 复用 `ChatCompletion.Usage` 的 JSON 映射（snake_case 字段）。

### 13.2 `StreamProxy` 主逻辑（代码段 S11，🖊 H6 全部手敲）

```java
// 路径：ai-gateway/src/main/java/com/aigateway/execution/stream/StreamProxy.java
package com.aigateway.execution.stream;

/**
 * 流式代理：在连接器之上做“治理”，连接器只负责低层 SSE 读取与超时关闭。
 *
 * 治理职责：
 * 1. 逐 chunk 规范化（补齐 id/created/model，错误事件转统一错误体）；
 * 2. 首字节标记：首字节前失败可降级，首字节后失败只能中断（安全边界）；
 * 3. 客户端断开 → 由连接器取消上游连接，本层直接返回（不换候选、不发错误）；
 * 4. 每 chunk 触发计量回调（V4 接入 Token 累加，失败不影响转发）；
 * 5. 流结束输出结束事件；usage 缺失时用近似值（V4 细化）。
 */
@Component
public class StreamProxy {

    private static final Logger log = LoggerFactory.getLogger(StreamProxy.class);

    /** 一次流式候选尝试的结果 */
    private enum OutcomeKind { SUCCESS, CANCELLED, FAILED }
    private record StreamOutcome(OutcomeKind kind, Failure failure) {
        static StreamOutcome success() { return new StreamOutcome(OutcomeKind.SUCCESS, null); }
        static StreamOutcome cancelled() { return new StreamOutcome(OutcomeKind.CANCELLED, null); }
        static StreamOutcome failed(Failure f) { return new StreamOutcome(OutcomeKind.FAILED, f); }
    }

    private final OpenAIConnector connector;
    private final CircuitBreakerRegistry circuitBreakers;
    private final CooldownManager cooldowns;
    private final ExecutionPolicyManager policyManager;
    private final ModelStateStore stateStore;
    private final StateEventPipeline stateEvents;
    private final GatewayMetrics metrics;
    private final MeteringCallback metering;
    private final ScheduledExecutorService scheduler;

    public StreamProxy(OpenAIConnector connector,
                       CircuitBreakerRegistry circuitBreakers,
                       CooldownManager cooldowns,
                       ExecutionPolicyManager policyManager,
                       ModelStateStore stateStore,
                       StateEventPipeline stateEvents,
                       GatewayMetrics metrics,
                       MeteringCallback metering,
                       ScheduledExecutorService scheduler) {
        this.connector = connector;
        this.circuitBreakers = circuitBreakers;
        this.cooldowns = cooldowns;
        this.policyManager = policyManager;
        this.stateStore = stateStore;
        this.stateEvents = stateEvents;
        this.metrics = metrics;
        this.metering = metering;
        this.scheduler = scheduler;
    }

    /** 流式降级链：语义与非流式一致，多一条“首字节后不降级”的硬边界 */
    public void streamChain(List<ModelInstance> chain, ChatRequest request, String requestId,
                            Map<String, String> metadata, Consumer<ChatChunk> consumer) {
        if (chain.isEmpty()) {
            throw new GatewayException(503, "no_available_model",
                    "模型[" + request.model() + "] 当前没有可用实例");
        }
        ExecutionPolicies firstPolicies = policyManager.forRequest(chain.get(0).channelId(), metadata);
        TimeoutGuard guard = new TimeoutGuard(firstPolicies.totalMs(), scheduler);

        Failure lastFailure = null;
        boolean attempted = false;
        boolean skippedByCircuit = false;
        boolean skippedByCooldown = false;

        for (ModelInstance candidate : chain) {
            if (guard.expired()) {
                metrics.timeout(candidate.instanceId(), "total");
                throw mapFinalFailure(lastFailure, guard);
            }
            ExecutionPolicies policies = policyManager.forRequest(candidate.channelId(), metadata);

            if (!circuitBreakers.allowRequest(candidate.instanceId())) {
                skippedByCircuit = true;
                metrics.circuitRejected(candidate.instanceId());
                log(requestId, "流式候选被熔断跳过", candidate.describe());
                continue;
            }
            if (cooldowns.isCooling(candidate.channelId())) {
                skippedByCooldown = true;
                metrics.cooldownSkipped(candidate.channelId());
                log(requestId, "流式候选被冷却跳过", candidate.describe());
                continue;
            }

            attempted = true;
            long start = System.nanoTime();
            boolean[] started = {false};   // 首字节标记：lambda 内修改外部变量用数组（学习点）
            stateStore.beginRequest(candidate.instanceId());
            try {
                StreamOutcome outcome = streamOnce(candidate, request, policies,
                        requestId, started, consumer);
                if (outcome.kind() == OutcomeKind.CANCELLED) {
                    // 客户端已断开：取消已完成，直接结束本次请求（不换候选、不发错误）
                    metrics.streamCancelled(request.model(), candidate.instanceId());
                    log(requestId, "客户端断开，已取消上游", candidate.describe());
                    return;
                }
                if (outcome.kind() == OutcomeKind.SUCCESS) {
                    recordOutcome(candidate, true, elapsedMs(start), null, policies);
                    metrics.success(request.model(), candidate.instanceId());
                    metering.onFinish(usageOrApproximate(candidate, request));
                    return;
                }

                // 失败：
                lastFailure = outcome.failure();
                recordOutcome(candidate, false, elapsedMs(start), lastFailure, policies);
                metrics.failure(request.model(), candidate.instanceId());
                if (started[0]) {
                    // ⚠️ 安全边界：首字节后绝不重试/降级（客户端已收到部分内容）
                    throw new GatewayException(502, "stream_interrupted",
                            "流式输出中断: " + lastFailure.reason());
                }
                metrics.fallback(candidate.instanceId(), lastFailure.type().name());
                log(requestId, "流式候选失败(首字节前)，降级",
                        candidate.describe() + " reason=" + lastFailure.type());
            } finally {
                stateStore.endRequest(candidate.instanceId());
            }
        }

        if (!attempted) {
            String type = skippedByCircuit ? "circuit_open" : "cooldown_active";
            throw new GatewayException(503, type,
                    "所有候选均被" + (skippedByCircuit ? "熔断" : "冷却") + "剔除，未发起上游调用");
        }
        throw mapFinalFailure(lastFailure, guard);
    }

    /** 单个候选的流式调用：连接器负责超时/取消，本层负责规范化与断连识别 */
    private StreamOutcome streamOnce(ModelInstance candidate, ChatRequest request,
                                     ExecutionPolicies policies, String requestId,
                                     boolean[] started, Consumer<ChatChunk> consumer) {
        StreamState state = new StreamState(candidate.model());
        try (StreamSession session = connector.stream(candidate, request, policies, chunk -> {
            ChatChunk normalized = normalize(chunk, state);
            if (!started[0]) started[0] = true;
            try {
                metering.onChunk(normalized);      // 计量回调：失败不影响转发
            } catch (Exception e) {
                // 计量异常必须吞掉：计量是附加能力，不能反过来打断转发
                log.warn("[requestId={}] 计量回调异常（忽略）: {}", requestId, e.toString());
            }
            consumer.accept(normalized);           // 客户端断开时抛 ClientDisconnectedException
        })) {
            return StreamOutcome.success();
        } catch (ClientDisconnectedException e) {
            return StreamOutcome.cancelled();
        } catch (UpstreamCallException e) {
            return StreamOutcome.failed(e.failure());
        }
    }

    /**
     * SSE 规范化：补齐上游缺失的 id / created / model，错误事件透传为统一错误体。
     * 规则：首个非空值记入 StreamState，后续 chunk 复用同一个 id（整个流共享响应 ID）。
     */
    private ChatChunk normalize(ChatChunk raw, StreamState state) {
        if (raw.id() != null) state.id(raw.id());
        if (raw.created() != 0) state.created(raw.created());
        if (raw.model() != null) state.model(raw.model());
        if (raw.usage() != null) state.usage(raw.usage());
        return new ChatChunk(
                raw.id() != null ? raw.id() : state.id(),
                "chat.completion.chunk",                    // object 固定规范化
                raw.created() != 0 ? raw.created() : state.created(),
                raw.model() != null ? raw.model() : state.model(),
                raw.choices(),
                raw.usage() != null ? raw.usage() : null,
                raw.error());
    }

    private void recordOutcome(ModelInstance candidate, boolean success, long latencyMs,
                               Failure failure, ExecutionPolicies policies) {
        long slowThreshold = policies.circuitBreaker().slowCallThresholdMs();
        boolean slow = latencyMs >= slowThreshold;
        circuitBreakers.recordResult(candidate.instanceId(), success, latencyMs, slowThreshold);
        if (success) {
            stateEvents.offer(new StateEvent.Success(candidate.instanceId(), latencyMs));
            cooldowns.recordSuccess(candidate.channelId());
        } else {
            stateEvents.offer(new StateEvent.Failure(candidate.instanceId(), failure.type()));
            cooldowns.recordFailure(candidate.channelId());
        }
        if (slow) {
            stateEvents.offer(new StateEvent.SlowCall(candidate.instanceId(), latencyMs));
        }
    }

    /** usage 缺失时的近似值（V4 细化真实计量） */
    private ChatCompletion.Usage usageOrApproximate(ModelInstance candidate, ChatRequest request) {
        return new ChatCompletion.Usage(0, 0, 0); // V4 用估算公式替换
    }

    private GatewayException mapFinalFailure(Failure failure, TimeoutGuard guard) {
        if (guard.expired()) {
            return new GatewayException(504, "timeout_total", "整条降级链总时长超时");
        }
        if (failure == null) {
            return new GatewayException(502, "upstream_failed", "所有候选均失败");
        }
        // 与非流式（12.2）同一映射，保证 15.2 错误码表在流式场景下同样可达
        // （TIMEOUT_IDLE 发生在首字节后、已走 stream_interrupted，此分支实际不可达，保留对称）
        return switch (failure.type()) {
            case TIMEOUT_CONNECT -> new GatewayException(504, "timeout_connect", failure.reason());
            case TIMEOUT_REQUEST -> new GatewayException(504, "timeout_request", failure.reason());
            case TIMEOUT_FIRST_BYTE -> new GatewayException(504, "timeout_first_byte", failure.reason());
            case TIMEOUT_IDLE -> new GatewayException(504, "timeout_idle", failure.reason());
            case TIMEOUT_TOTAL -> new GatewayException(504, "timeout_total", failure.reason());
            default -> new GatewayException(502, "upstream_failed",
                    "所有候选均失败: " + failure.type() + " - " + failure.reason());
        };
    }

    private static long elapsedMs(long startNanos) {
        return TimeUnit.NANOSECONDS.toMillis(System.nanoTime() - startNanos);
    }

    private static void log(String requestId, String event, String detail) {
        // 结构化日志（SLF4J）：可按级别过滤、可携带 MDC 上下文（requestId）
        log.info("[requestId={}] event={} detail={}", requestId, event, detail);
    }
}
```

### 13.3 客户端断开与计量回调（代码段 S11b，Codex）

```java
// 路径：ai-gateway/src/main/java/com/aigateway/execution/stream/ClientDisconnectedException.java
// Controller 写 SseEmitter 失败（客户端断开）时抛出，StreamProxy 据此走“取消”路径。
public class ClientDisconnectedException extends RuntimeException {
    public ClientDisconnectedException(Throwable cause) {
        super("客户端断开连接", cause);
    }
}

// 路径：ai-gateway/src/main/java/com/aigateway/execution/stream/MeteringCallback.java
// V4 计量接入点：本版本默认实现只打日志，V4 替换为 Token 累加实现。
public interface MeteringCallback {
    default void onChunk(ChatChunk chunk) {}
    default void onFinish(ChatCompletion.Usage usage) {}
}
```

> ⚠️ **必须注册默认 Bean**：`StreamProxy` 构造器注入 `MeteringCallback`（接口），若容器中只有接口没有实现 Bean，启动会因 `NoSuchBeanDefinitionException` 失败。在 `infra/config` 配置类中补一个缺省实现（Codex 接线，与 11.2 的 `GatewayBeans` 同文件）：

```java
// 路径：ai-gateway/src/main/java/com/aigateway/infra/config/GatewayBeans.java（Codex 接线）
/** 缺省计量回调：空实现。V4 接入 Token 累加时，只需替换该 Bean 定义 */
@Bean
public MeteringCallback meteringCallback() {
    return new MeteringCallback() {};   // 使用接口默认方法（空实现）
}
```

### 13.4 `ChatCompletionController` 改造（代码段 S11c，Codex）

```java
// 路径：ai-gateway/src/main/java/com/aigateway/api/controller/ChatCompletionController.java
private SseEmitter streamResponse(ChatRequest request, String requestId,
                                  HttpServletRequest httpRequest) {
    SseEmitter emitter = new SseEmitter(0L);
    Thread.ofVirtual().name("sse-" + requestId).start(() -> {
        try {
            gatewayService.stream(request, requestId, metadata(httpRequest), chunk -> {
                try {
                    emitter.send(chunk);
                } catch (IOException e) {
                    // 客户端断开：抛给 StreamProxy 走“取消上游”路径
                    throw new ClientDisconnectedException(e);
                }
            });
            try {
                emitter.complete();
            } catch (Exception ignore) {
                // 连接已断开，complete 可能抛 IllegalStateException——忽略即可
            }
        } catch (Exception e) {
            emitter.completeWithError(e);
        }
    });
    return emitter;
}

/** 提取请求级容错 header（V2 合并后追加 tenant / canary_group 等） */
private Map<String, String> metadata(HttpServletRequest req) {
    Map<String, String> result = new HashMap<>();
    header(req, "X-Gateway-Timeout-Ms")
            .ifPresent(v -> result.put("x-gateway-timeout-ms", v));
    header(req, "X-Gateway-Idle-Timeout-Ms")
            .ifPresent(v -> result.put("x-gateway-idle-timeout-ms", v));
    return Map.copyOf(result);
}

private Optional<String> header(HttpServletRequest req, String name) {
    String value = req.getHeader(name);
    return value == null || value.isBlank() ? Optional.empty() : Optional.of(value.trim());
}
```

完整时序见第 17 节；流式安全边界的测试见第 18 节。

---

## 14. 状态系统增强（关键代码段 S12，🖊 H7）

> 路径：`ai-gateway/src/main/java/com/aigateway/decision/state/`

> 🖊 **手敲 H7**：`StateEventPipeline` 与 `ModelStateStore.apply` 全部手敲；事件模型（`StateEvent`）为 Codex 脚手架。

### 14.1 事件模型（代码段 S12a，Codex）

```java
// 路径：ai-gateway/src/main/java/com/aigateway/decision/state/StateEvent.java
// 所有会影响 ModelState 的“事实”都建模成事件：执行结果、慢调用、熔断转换、冷却进出。
public sealed interface StateEvent permits
        StateEvent.Success,
        StateEvent.Failure,
        StateEvent.SlowCall,
        StateEvent.CircuitTransition,
        StateEvent.CooldownChange {

    /** 成功样本：更新 EWMA 延迟、错误率衰减 */
    record Success(String instanceId, long latencyMs) implements StateEvent {}

    /** 失败样本：更新错误率 */
    record Failure(String instanceId, FailureType type) implements StateEvent {}

    /** 慢调用样本：累计慢调用计数（熔断的慢调用率输入） */
    record SlowCall(String instanceId, long latencyMs) implements StateEvent {}

    /** 熔断状态转换：CLOSED → OPEN → HALF_OPEN → CLOSED */
    record CircuitTransition(String instanceId, CircuitState from, CircuitState to) implements StateEvent {}

    /** 冷却进出：true = 进入冷却；false = 冷却结束/恢复 */
    record CooldownChange(String channelId, boolean cooling, long cooldownMs) implements StateEvent {}
}
```

### 14.2 `StateEventPipeline` 主逻辑（代码段 S12，🖊 H7 全部手敲）

```java
// 路径：ai-gateway/src/main/java/com/aigateway/decision/state/StateEventPipeline.java
package com.aigateway.decision.state;

/**
 * 状态事件管道：单写者聚合。
 *
 * 为什么需要它（学习版 4.7）：
 * - 请求线程只负责“发事件”（无锁队列 offer，O(1)，不阻塞转发）；
 * - 唯一的写线程按周期把事件批量应用到 ModelStateStore，状态更新与决策读取解耦；
 * - 决策层读的是“最近快照”，不会因为每毫秒高频写而锁竞争。
 *
 * 学习要点：
 * - ConcurrentLinkedQueue.offer 无界但线程安全；事件量级 = 请求量级，内存可控；
 * - flush() 是唯一调用 ModelStateStore.apply 的地方（测试里可手动调用）；
 * - 复用全局 gatewayScheduler（9.2，daemon 线程）：不再自建线程池，避免与超时
 *   定时器各跑一个池（FlushIntervalMs 即聚合周期，与全局调度器共享线程）。
 */
@Component
public class StateEventPipeline {

    private final Queue<StateEvent> queue = new ConcurrentLinkedQueue<>();
    private final ModelStateStore stateStore;
    private final long flushIntervalMs;
    private final ScheduledExecutorService writer;

    public StateEventPipeline(ModelStateStore stateStore, GatewayProperties props,
                              ScheduledExecutorService scheduler) {
        this.stateStore = stateStore;
        this.flushIntervalMs = props.getExecution().getState().getFlushIntervalMs();
        this.writer = scheduler;   // 注入全局 gatewayScheduler（9.2），不自行创建
    }

    @PostConstruct
    void start() {
        writer.scheduleAtFixedRate(this::flush,
                flushIntervalMs, flushIntervalMs, TimeUnit.MILLISECONDS);
    }

    /** 请求线程入口：只入队，不阻塞 */
    public void offer(StateEvent event) {
        queue.offer(event);
    }

    /** 单写者：排空队列并逐个应用（测试可直接调用做确定性断言） */
    public void flush() {
        StateEvent event;
        while ((event = queue.poll()) != null) {
            stateStore.apply(event);
        }
    }
}
```

### 14.3 `ModelStateStore` 扩展（代码段 S12b，🖊 H7 与 14.2 一起手敲）

```java
// 路径：ai-gateway/src/main/java/com/aigateway/decision/state/ModelStateStore.java
package com.aigateway.decision.state;

/**
 * 状态存储：EWMA 延迟 / 错误率（V2 已有算法）+ 熔断 / 冷却 / 慢调用快照（V3）。
 * 只被 StateEventPipeline 的单写者线程更新；请求线程只读 stateOf()。
 */
@Component
public class ModelStateStore {

    private final Map<String, ModelState> states = new ConcurrentHashMap<>();
    private final Map<String, AtomicInteger> inFlight = new ConcurrentHashMap<>();
    private final ModelRegistry registry;   // 冷却事件是渠道级，需查渠道下全部实例
    private final double latencyAlpha;  // EWMA 延迟系数（V2 decision.ewmaAlpha，缺省 0.3）
    private final double errorAlpha;    // 错误率系数（缺省 0.1）

    public ModelStateStore(GatewayProperties props, ModelRegistry registry) {
        // V2 合并后从 props.getDecision() 读取；未合并时用缺省值
        this.latencyAlpha = 0.3;
        this.errorAlpha = 0.1;
        this.registry = registry;
    }

    /** 读最近快照（决策层用，零阻塞） */
    public ModelState stateOf(String instanceId) {
        ModelState base = states.computeIfAbsent(instanceId, this::initialState);
        return new ModelState(base.instanceId(), base.ewmaLatencyMs(), base.errorRate(),
                inFlightOf(instanceId), base.lastUpdatedAt(),
                base.circuitState(), base.slowCalls(), base.totalCalls(), base.cooling());
    }

    private ModelState initialState(String instanceId) {
        return new ModelState(instanceId, 1000, 0.0, 0,
                System.currentTimeMillis(), CircuitState.CLOSED, 0, 0, false);
    }

    /** 请求开始：并发计数 +1（执行器调用，不是事件——高频且只对执行有意义） */
    public void beginRequest(String instanceId) {
        inFlight.computeIfAbsent(instanceId, k -> new AtomicInteger()).incrementAndGet();
    }

    /** 请求结束：并发计数 -1 */
    public void endRequest(String instanceId) {
        AtomicInteger counter = inFlight.get(instanceId);
        if (counter != null) counter.decrementAndGet();
    }

    /** 单写者入口：把事件应用到状态（ConcurrentHashMap.compute 保证原子性） */
    public void apply(StateEvent event) {
        switch (event) {
            case StateEvent.Success s -> states.compute(s.instanceId(), (id, old) -> {
                ModelState st = old != null ? old : initialState(id);
                double ewma = latencyAlpha * s.latencyMs() + (1 - latencyAlpha) * st.ewmaLatencyMs();
                double err = (1 - errorAlpha) * st.errorRate();
                return withBase(st, ewma, err, st.circuitState(), st.slowCalls(),
                        st.totalCalls() + 1, st.cooling());
            });
            case StateEvent.Failure f -> states.compute(f.instanceId(), (id, old) -> {
                ModelState st = old != null ? old : initialState(id);
                double err = errorAlpha * 1.0 + (1 - errorAlpha) * st.errorRate();
                return withBase(st, st.ewmaLatencyMs(), err, st.circuitState(),
                        st.slowCalls(), st.totalCalls() + 1, st.cooling());
            });
            case StateEvent.SlowCall sc -> states.compute(sc.instanceId(), (id, old) -> {
                ModelState st = old != null ? old : initialState(id);
                return withBase(st, st.ewmaLatencyMs(), st.errorRate(), st.circuitState(),
                        st.slowCalls() + 1, st.totalCalls(), st.cooling());
            });
            case StateEvent.CircuitTransition ct -> states.compute(ct.instanceId(), (id, old) -> {
                ModelState st = old != null ? old : initialState(id);
                return withBase(st, st.ewmaLatencyMs(), st.errorRate(), ct.to(),
                        st.slowCalls(), st.totalCalls(), st.cooling());
            });
            case StateEvent.CooldownChange cc -> {
                // 冷却按渠道，状态按实例展示：广播到该渠道下所有实例。
                // 边界处理：先在 compute 外取实例列表（compute 回调内禁止触碰 map 其它键）；
                // 注册表中暂无该渠道 → 空列表，循环不执行（下游决策读到旧冷却态，可接受）。
                List<ModelInstance> instances = registry.findByChannelId(cc.channelId());
                for (ModelInstance inst : instances) {
                    states.compute(inst.instanceId(), (id, old) -> {
                        ModelState st = old != null ? old : initialState(id);
                        return withBase(st, st.ewmaLatencyMs(), st.errorRate(),
                                st.circuitState(), st.slowCalls(), st.totalCalls(), cc.cooling());
                    });
                }
            }
        }
    }

    private ModelState withBase(ModelState st, double ewma, double err, CircuitState circuit,
                                long slowCalls, long totalCalls, boolean cooling) {
        return new ModelState(st.instanceId(), ewma, err, st.inFlight(),
                System.currentTimeMillis(), circuit, slowCalls, totalCalls, cooling);
    }

    private int inFlightOf(String instanceId) {
        AtomicInteger counter = inFlight.get(instanceId);
        return counter == null ? 0 : counter.get();
    }
}
```

设计要点：

- **EWMA 算法不变**（V2 的 `new = α × sample + (1 - α) × old`），V3 只是把入口从直接调用改成事件管道，算法可复用、可面试；
- **CooldownChange 是渠道级事件**：应用时按渠道广播到旗下实例（通过 `ModelRegistry` 查 instanceId 列表），避免决策层读不到冷却状态；
- **快照与写入解耦**：写入只发生在聚合线程，读取是 CHM 原子快照，无锁竞争——这就是“状态更新频率与决策频率解耦”；
- ⚠️ 与 V2 相同的最容易踩的坑：`compute` 回调内**禁止**对同一 map 做任何其他更新（含 `computeIfAbsent`），初始状态必须从 map 外部构造。

---

## 15. 指标与错误码（关键代码段 S13，Codex）

### 15.1 `GatewayMetrics` 扩展

```java
// 路径：ai-gateway/src/main/java/com/aigateway/observability/GatewayMetrics.java
// V1 的 success / failure / streamChunk 保留，以下为 V3 增量：

/** 重试事件：gateway_retry_total{instance,reason}（设计文档 7.2 的 retry_total{from,to,reason}） */
public void retry(String instanceId, String reason) {
    Counter.builder("gateway.retry.total")
            .tag("instance", instanceId)
            .tag("reason", reason)
            .register(meterRegistry).increment();
}

/** 降级事件：gateway_fallback_events{from,reason}（换候选时记录） */
public void fallback(String fromInstance, String reason) {
    Counter.builder("gateway.fallback.events")
            .tag("from", fromInstance)
            .tag("reason", reason)
            .register(meterRegistry).increment();
}

/** 超时事件：gateway_timeout_total{instance,stage}（stage=connect/request/first_byte/idle/total） */
public void timeout(String instanceId, String stage) {
    Counter.builder("gateway.timeout.total")
            .tag("instance", instanceId)
            .tag("stage", stage)
            .register(meterRegistry).increment();
}

/** 熔断转换：gateway_circuit_breaker_events{instance,to}（to=open/half_open/close） */
public void circuitEvent(String instanceId, String to) {
    Counter.builder("gateway.circuit.breaker.events")
            .tag("instance", instanceId)
            .tag("to", to)
            .register(meterRegistry).increment();
}

/** 熔断拒绝：gateway_circuit_breaker_rejections{instance}（快速失败次数） */
public void circuitRejected(String instanceId) {
    Counter.builder("gateway.circuit.breaker.rejections")
            .tag("instance", instanceId)
            .register(meterRegistry).increment();
}

/** 冷却事件：gateway_cooldown_events{channel,to}（to=enter/exit） */
public void cooldownEvent(String channelId, String to) {
    Counter.builder("gateway.cooldown.events")
            .tag("channel", channelId)
            .tag("to", to)
            .register(meterRegistry).increment();
}

/** 流式取消：gateway_stream_cancelled{model,instance}（客户端断开，不算失败） */
public void streamCancelled(String alias, String instanceId) {
    Counter.builder("gateway.stream.cancelled")
            .tag("model", alias)
            .tag("instance", instanceId)
            .register(meterRegistry).increment();
}
```

### 15.2 错误码约定（V3 增量）

| type | HTTP | 触发场景 | 归属 |
|---|---|---|---|
| `timeout_total` | 504 | 整条降级链总时长超时 | V3 |
| `timeout_connect` | 504 | 建立连接超时 | V3 |
| `timeout_request` | 504 | 非流式完整响应超时 | V3 |
| `timeout_first_byte` | 504 | 流式首字节超时 | V3 |
| `timeout_idle` | 504 | 流式空闲超时 | V3 |
| `circuit_open` | 503 | 所有候选被熔断跳过 | V3 |
| `cooldown_active` | 503 | 所有候选被冷却跳过 | V3 |
| `stream_interrupted` | 502 | 流式首字节后中断（不降级） | V3 |
| `invalid_request` | 400 | 请求级超时 header 非法 | V3 |
| `upstream_failed` | 502 | 所有候选均失败 | V1 保留 |
| `upstream_error` | 502 | 上游非 2xx | V1 保留 |
| `bad_upstream_sse` | 502 | SSE 解析失败 | V1 保留 |
| `no_available_model` | 503 | 无可用候选 | V1 保留 |
| `invalid_config` | 500 | 配置校验失败 | V1 保留 |

---

## 16. mock 服务端修改（关键代码段 S14，Codex）

mock 是容错演示的“故障工厂”：V1 已支持 errorRate / latencyMs / health，V3 增加**错误状态码、Retry-After、流式中途故障 / 停顿 / 错误事件、断连日志**。

### 16.1 `ModelProfile` 扩展

```java
// 路径：mock-model-server/src/main/java/com/aigateway/mock/model/ModelProfile.java
public record ModelProfile(
        String name,
        String version,
        long baseLatencyMs,
        double errorRate,
        double streamingDelayMs,
        int activeRequests,
        int queueSize,
        double gpuUsage,
        double memoryUsage,
        String responseStyle,
        Map<String, Object> metadata,
        // ── V3 新增 ──
        int errorStatus,              // 故障注入的 HTTP 状态码（默认 500）
        int retryAfterSeconds,        // 429 时返回的 Retry-After 秒数
        int streamFailAfterChunks,    // 流式发送 N 个 chunk 后异常断开；-1 = 关闭
        int streamStallAfterChunks,   // 流式发送 N 个 chunk 后停顿；-1 = 关闭
        long streamStallMs,           // 停顿时长（毫秒）
        int streamErrorAfterChunks    // 流式发送 N 个 chunk 后发错误事件；-1 = 关闭
) {}
```

`ModelRegistry`（mock 侧）为每个新字段增加 `setXxx` setter（照旧“基于旧画像生成新画像再替换引用”），并在 `init()` 的三种模型画像里填默认值：`errorStatus=500, retryAfterSeconds=0, streamFailAfterChunks=-1, streamStallAfterChunks=-1, streamStallMs=0, streamErrorAfterChunks=-1`。

### 16.2 `BehaviorController` 扩展

```java
// 新增可注入参数（与现有 errorRate / latencyMs / health 同风格）：
//   errorStatus: 500 / 503 / 429
//   retryAfterSeconds: 429 时返回 Retry-After 头（如 2）
//   streamFailAfterChunks: 流式中途断开（如 3）
//   streamStallAfterChunks + streamStallMs: 流式中途停顿（如 2 + 30000）
//   streamErrorAfterChunks: 流式中途发错误事件（如 2）
// GET /mock/behavior 返回值同步增加这些字段。
```

### 16.3 `ChatController` 流式故障注入与断连日志

```java
// 非流式失败：按 errorStatus 返回对应错误（429 带 Retry-After 头），其余抛异常
if (gen.shouldFail(profile)) {
    sleep(latencyMs);
    registry.requestFinished();
    if (profile.errorStatus() == 429 && profile.retryAfterSeconds() > 0) {
        return ResponseEntity.status(429)
                .header("Retry-After", String.valueOf(profile.retryAfterSeconds()))
                .body(Map.of("error", Map.of(
                        "type", "rate_limit_error",
                        "message", "mock rate limited")));
    }
    throw new RuntimeException("Mock model internal error (status=" + profile.errorStatus() + ")");
}

// 流式：逐 token 发送时按注入参数制造“中途故障”
int sent = 0;
for (String token : tokens) {
    emitter.send(buildChunk(requestId, profile.name(), token, false));
    sent++;
    if (profile.streamFailAfterChunks() >= 0 && sent == profile.streamFailAfterChunks()) {
        throw new RuntimeException("mock stream broken after " + sent + " chunks");
    }
    if (profile.streamStallAfterChunks() >= 0 && sent == profile.streamStallAfterChunks()) {
        Thread.sleep(profile.streamStallMs());   // 模拟“卡住”，网关空闲超时会被触发
    }
    if (profile.streamErrorAfterChunks() >= 0 && sent == profile.streamErrorAfterChunks()) {
        emitter.send(Map.of("error", Map.of(
                "type", "upstream_error",
                "message", "mock stream error event")));
        emitter.complete();
        return;
    }
    Thread.sleep((long) profile.streamingDelayMs());
}
```

```java
// 客户端断开日志：emitter.send 抛 IOException 时记录（演示 C 的可观测性证据）
try {
    for (String token : tokens) {
        emitter.send(buildChunk(requestId, profile.name(), token, false));
        Thread.sleep((long) profile.streamingDelayMs());
    }
    emitter.send(buildChunk(requestId, profile.name(), "", true));
    emitter.complete();
} catch (IOException e) {
    System.out.printf("[Mock %s] 客户端断开，取消生成 requestId=%s%n",
            profile.name(), requestId);
} catch (Exception e) {
    emitter.completeWithError(e);
} finally {
    registry.requestFinished();
}
```

> 演示时把 mock-a 配置成“流式发 3 个 chunk 后断开”，可以同时验证网关的 `stream_interrupted`（首字节后失败不降级）与 mock 侧的断连日志。

---

## 17. 一次请求的完整时序

### 17.1 非流式（含失败降级）

```
Client
  │ POST /v1/chat/completions {model:"qwen", messages:[...], headers:{X-Gateway-Timeout-Ms:15000}}
  ▼
RequestIdFilter（生成 requestId）
  ▼
ChatCompletionController（提取 metadata：x-gateway-timeout-ms）
  ▼
ChatGatewayService.complete()                          ← 接线（12.3）
  └─ ExecutionChainResolver.resolve()                  ← 12.1
       └─ [健康过滤] → [加权随机不重复抽样]（V2 合并后 = RoutingDecision 主选+降级链）
  └─ FallbackChainExecutor.execute(chain, ...)         ← 🖊 H5
       ├─ TimeoutGuard（totalMs 预算）                   ← 🖊 H2
       ├─ 候选 1（mock-a:qwen-large）
       │    ├─ CircuitBreaker.allowRequest? → 熔断则跳过 ← 🖊 H3
       │    ├─ CooldownManager.isCooling? → 冷却则跳过   ← 🖊 H4
       │    ├─ RetryExecutor（连接失败重试一次 / 429 Retry-After）← 🖊 H1
       │    │    └─ OpenAIConnector.complete（requestMs 超时 + 失败分类）
       │    ├─ 成功 → 指标 success / 状态事件 Success → 返回
       │    └─ 失败 → 指标 failure+fallback / 状态事件 Failure+SlowCall / 冷却计数
       ├─ 候选 2（mock-b:qwen-small）同上流程
       └─ 全部失败 → mapFinalFailure：504 timeout_* / 502 upstream_failed
           全被熔断/冷却跳过 → 503 circuit_open / cooldown_active
  ▼
GlobalExceptionHandler（统一错误体）
```

### 17.2 流式（含首字节边界与断连取消）

```
Client
  │ POST ... {"stream":true}
  ▼
ChatGatewayService.stream()                            ← 接线（12.3）
  └─ StreamProxy.streamChain(chain, ...)               ← 🖊 H6
       ├─ 候选 1：熔断/冷却检查通过
       │    ├─ OpenAIConnector.stream（首字节/空闲超时由 TimeoutGuard 定时器控制）← 🖊 H2
       │    ├─ 收到首个 chunk → started=true（首字节标记）→ 规范化 → consumer 转发
       │    ├─ 每 chunk：计量回调 → SseEmitter.send
       │    ├─ 客户端断开：send 抛 IOException → ClientDisconnectedException
       │    │    → StreamProxy 捕获 → connector 取消上游（session.cancel()）
       │    │    → 指标 stream_cancelled → 正常结束（不换候选、不发错误）
       │    ├─ 首字节前失败 → 记录失败 → 换候选 2（安全：还没输出内容）
       │    └─ 首字节后失败 → 502 stream_interrupted（安全边界：绝不重放）
       └─ 全部候选首字节前失败 → 502/503/504（同非流式映射）
  ▼
SseEmitter（data: 帧输出）
```

两个时序图的共同点：**决策链 → 执行器 → 单点记账（指标 + 状态事件）→ 客户端**，每一步都有日志与指标可回溯。

---

## 18. 测试计划

### 18.1 单元测试（JUnit + Mockito）

| 测试类 | 用例 | 断言要点 | 对应 |
|---|---|---|---|
| `execution/classify/FailureClassifierTest.java` | 429 + Retry-After | `HTTP_429`、retryable=true、retryAfterMs 正确 | 脚手架 |
| `execution/classify/FailureClassifierTest.java` | 500 / 400 / 网络异常 | `HTTP_5XX` retryable / `HTTP_OTHER` 不可重试 / `CONNECTION` | 脚手架 |
| `execution/retry/RetryExecutorTest.java` | 连接失败同实例重试一次 | 第二次调用成功即返回；超过一次不再重试 | H1 |
| `execution/retry/RetryExecutorTest.java` | 429 Retry-After 等待 | 等待 min(retryAfter, maxRetryAfter)；无 header 不等待 | H1 |
| `execution/retry/RetryExecutorTest.java` | 4xx 不重试 | 原样抛出，attempt 只调用一次 | H1 |
| `execution/retry/RetryExecutorTest.java` | 预算不足 | `sleepWithinBudget=false` → 抛 `TIMEOUT_TOTAL` | H1 |
| `execution/timeout/TimeoutGuardTest.java` | 总时长过期 | expired() 为 true；remainingMs 归零 | H2 |
| `execution/timeout/TimeoutGuardTest.java` | 预算内睡眠 | 预算足够 sleep 成功；不足返回 false | H2 |
| `execution/timeout/TimeoutGuardTest.java` | 定时器取消 | close 后 scheduleOnce 不再执行 | H2 |
| `execution/circuit/CircuitBreakerTest.java` | 样本不足不熔断 | minimumRequests 前一直放行 | H3 |
| `execution/circuit/CircuitBreakerTest.java` | 失败率超阈值打开 | allowRequest=false；事件 to=open | H3 |
| `execution/circuit/CircuitBreakerTest.java` | 半开成功关闭 | 探测成功后 CLOSED 且窗口清零 | H3 |
| `execution/circuit/CircuitBreakerTest.java` | 半开失败重新打开 | 探测失败后 OPEN 且重新计时 | H3 |
| `execution/circuit/CircuitBreakerTest.java` | 窗口滑动 | 旧失败被挤出后失败率回落 | H3 |
| `execution/cooldown/CooldownManagerTest.java` | 连续失败进入冷却 | 第 N 次失败后 isCooling=true | H4 |
| `execution/cooldown/CooldownManagerTest.java` | 到期恢复 + 探测翻倍 | 到期 false；探测失败后冷却时长翻倍；有上限 | H4 |
| `execution/cooldown/CooldownManagerTest.java` | 成功清零 | recordSuccess 后计数与探测态复位 | H4 |
| `execution/fallback/FallbackChainExecutorTest.java` | 主选失败 → 降级 | 结果来自链上第二个候选；fallback 指标 +1 | H5 |
| `execution/fallback/FallbackChainExecutorTest.java` | 熔断/冷却跳过 | 跳过候选不发起调用；全跳过 → 503 circuit_open/cooldown_active | H5 |
| `execution/fallback/FallbackChainExecutorTest.java` | 总时长超时 | 各候选耗时总和超 totalMs → 504 timeout_total | H5 |
| `execution/stream/StreamProxyTest.java` | SSE 规范化 | 缺 id/created/model 时补齐；object 固定 | H6 |
| `execution/stream/StreamProxyTest.java` | 错误事件透传 | 上游 error chunk → 客户端收到统一错误体后结束 | H6 |
| `execution/stream/StreamProxyTest.java` | 首字节后不降级 | 已 started 后失败 → 502 stream_interrupted，下一个候选不被调用 | H6 |
| `execution/stream/StreamProxyTest.java` | 客户端断开取消 | consumer 抛 ClientDisconnected → session.cancel() 被调用；无 failure 指标 | H6 |
| `decision/state/StateEventPipelineTest.java` | 事件聚合 | offer 后 flush() → ModelState 更新正确 | H7 |
| `decision/state/ModelStateStoreTest.java` | EWMA 收敛 | 连续成功样本后延迟接近样本值；错误率下降 | H7 |
| `decision/state/ModelStateStoreTest.java` | CooldownChange 渠道广播 | 事件到达后该渠道全部实例冷却态更新；未知渠道不抛异常 | H7 |
| `decision/state/ModelStateStoreTest.java` | 并发安全 | 多线程 offer + flush 不丢更新 | H7 |
| `core/service/ChatGatewayServiceTest.java` | 构造器/签名更新 | 新签名 + 链解析 + 执行器调用 | 接线 |

关键测试示例（代码段 S15，H3 / H6 完成后可跑）：

```java
// 路径：ai-gateway/src/test/java/com/aigateway/execution/circuit/CircuitBreakerTest.java
@Test
void halfOpenProbeFailure_shouldReopenCircuit() {
    CircuitBreaker cb = new CircuitBreaker("mock-a:qwen-large",
            new CircuitBreakerConfig(100, 5, 0.5, 10_000, 0.5, 30_000, 1));
    for (int i = 0; i < 5; i++) cb.record(false, 100, 10_000L); // 失败率 100% → OPEN

    assertThat(cb.state()).isEqualTo(CircuitState.OPEN);
    assertThat(cb.allowRequest()).isFalse();

    // 模拟打开期到期（把 openDurationMs 调成 0 的另一个实例更直接，这里用反射/短时长构造）
    CircuitBreaker halfOpen = new CircuitBreaker("mock-a:qwen-large",
            new CircuitBreakerConfig(100, 5, 0.5, 10_000, 0.5, 0, 1));
    for (int i = 0; i < 5; i++) halfOpen.record(false, 100, 10_000L);
    assertThat(halfOpen.allowRequest()).isTrue();          // 到期 → HALF_OPEN 放行探测
    halfOpen.record(false, 100, 10_000L);                  // 探测失败
    assertThat(halfOpen.state()).isEqualTo(CircuitState.OPEN);
    assertThat(halfOpen.allowRequest()).isFalse();         // 回到打开，快速失败
}
```

```java
// 路径：ai-gateway/src/test/java/com/aigateway/execution/stream/StreamProxyTest.java
@Test
void failureAfterFirstByte_shouldNotDegrade() {
    // 第一个候选：已输出 1 个 chunk 后失败（STREAM_INTERRUPTED）
    // 第二个候选：正常（若被调用则测试失败）
    List<ModelInstance> chain = List.of(failingAfterFirstByte("mock-a:qwen-large"),
            healthy("mock-b:qwen-small"));

    assertThatThrownBy(() -> proxy.streamChain(chain, request, "rid", Map.of(), sink))
            .isInstanceOf(GatewayException.class)
            .satisfies(e -> {
                assertThat(((GatewayException) e).getStatus()).isEqualTo(502);
                assertThat(((GatewayException) e).getType()).isEqualTo("stream_interrupted");
            });
    verify(connector, never()).stream(eq(chain.get(1)), any(), any(), any());
}
```

### 18.2 集成测试

`ai-gateway/src/test/java/com/aigateway/ChatGatewayIntegrationTest.java` 在 V1 四个场景（非流式 / 流式 / 故障切换 / 健康剔除）基础上，新增三个场景：

1. **场景 A（故障注入全景）**：`POST http://localhost:8001/mock/behavior {"errorRate":1.0,"errorStatus":500}` 后，非流式请求仍 200（降级到 mock-b）；`/actuator/prometheus` 出现 `gateway_fallback_events`，日志含“候选失败，降级 reason=HTTP_5XX”。
2. **场景 B（熔断与恢复）**：mock-a 持续 100% 故障，连续请求至熔断打开（`503 circuit_open`，请求快速失败不再等超时）；把故障率调回 0，等待半开探测成功，请求恢复 200，`gateway_circuit_breaker_events{to="close"}` 出现。
3. **场景 C（流式断连）**：发起流式请求并消费前几个 chunk 后断开（MockMvc 异步请求 cancel），断言 mock 侧 `GET /mock/behavior` 的 activeRequests 回到 0、`gateway_stream_cancelled` 计数 +1、网关无泄漏（无 502 错误日志）。

回归要求：V1 四个场景继续通过；V2 合并后其演示场景也必须通过（V2 决策链 + V3 执行链无回归）。

---

## 19. 演示与验收（curl 命令）

### 19.1 演示配置与启动

使用第 5.1 节的 `model.yml`（execution 全缺省即可，不需要特殊配置）。三个进程：

```bash
# 1) 启动两个 mock 实例
cd mock-model-server
mvn spring-boot:run -Dspring-boot.run.arguments=--server.port=8001,--mock.model=qwen-large &
mvn spring-boot:run -Dspring-boot.run.arguments=--server.port=8002,--mock.model=qwen-small &

# 2) 启动网关（演示配置已写在 model.yml）
cd ../ai-gateway
mvn spring-boot:run
```

### 19.2 演示 A：故障注入全景（重试 + 降级）

```bash
# 让 mock-a 100% 返回 500
curl -s -X POST http://localhost:8001/mock/behavior \
  -H 'Content-Type: application/json' -d '{"errorRate":1.0,"errorStatus":500}'

# 普通请求：A 失败 → 降级到 B 成功
curl -s http://localhost:8080/v1/chat/completions \
  -H 'Content-Type: application/json' \
  -d '{"model":"qwen","messages":[{"role":"user","content":"你好"}]}'

# 网关日志：
# [requestId=xxx] event=候选失败，降级 detail=qwen -> mock-a/qwen-large reason=HTTP_5XX
# [requestId=xxx] event=执行成功 detail=qwen -> mock-b/qwen-small

# 指标
curl -s http://localhost:8080/actuator/prometheus | grep -E 'gateway_(fallback|retry)_'
```

### 19.3 演示 A2：429 Retry-After（可选）

```bash
# mock-a 返回 429 + Retry-After: 2
curl -s -X POST http://localhost:8001/mock/behavior \
  -H 'Content-Type: application/json' \
  -d '{"errorRate":1.0,"errorStatus":429,"retryAfterSeconds":2}'

curl -s http://localhost:8080/v1/chat/completions \
  -H 'Content-Type: application/json' \
  -d '{"model":"qwen","messages":[{"role":"user","content":"你好"}]}'
# 网关日志：event=同实例重试 attempt=0 reason=HTTP_429 wait=2000ms
# 两次都 429 后降级到 mock-b；gateway_retry_total{reason="HTTP_429"} 计数 +1
```

### 19.4 演示 B：熔断与恢复

```bash
# mock-a 100% 失败持续打压（连续 10 次请求，窗口 5 个样本后熔断打开）
for i in $(seq 1 10); do
  curl -s -o /dev/null -w "%{http_code}\n" http://localhost:8080/v1/chat/completions \
    -H 'Content-Type: application/json' \
    -d '{"model":"qwen","messages":[{"role":"user","content":"你好"}]}'
done
# 只要 mock-b 健康，请求会一直 200（降级成功）；观察点在于日志与指标：
#   event=候选被熔断跳过（mock-a 不再被调用）
#   gateway_circuit_breaker_rejections 计数上升
# 想演示 503 circuit_open：把 mock-b 也置为 100% 故障并打到熔断，
# 之后请求快速失败（不再等上游超时）。

# 观察熔断指标
curl -s http://localhost:8080/actuator/prometheus | grep circuit
# gateway_circuit_breaker_events{to="open"} / gateway_circuit_breaker_rejections

# 恢复：故障率调回 0，等待 openDurationMs（默认 30s），再次请求
curl -s -X POST http://localhost:8001/mock/behavior \
  -H 'Content-Type: application/json' -d '{"errorRate":0.0}'
sleep 31
curl -s http://localhost:8080/v1/chat/completions \
  -H 'Content-Type: application/json' \
  -d '{"model":"qwen","messages":[{"role":"user","content":"你好"}]}'
# → 半开探测成功，熔断关闭：gateway_circuit_breaker_events{to="close"}
```

> 说明：熔断是“按实例跳过”，只要降级链里还有一个健康候选，请求仍然成功——这是正确的降级行为。想演示 `503 circuit_open`，把 mock-b 也打满故障（两个实例都熔断后请求才会快速失败）。冷却同理。

### 19.5 演示 C：流式断连（取消上游）

```bash
# 发起流式请求，2 秒后客户端主动断开
curl -N -s --max-time 2 http://localhost:8080/v1/chat/completions \
  -H 'Content-Type: application/json' \
  -d '{"model":"qwen","messages":[{"role":"user","content":"你好"}],"stream":true}'

# 网关日志：event=客户端断开，已取消上游
# mock 日志：[Mock qwen-large] 客户端断开，取消生成 requestId=xxx
# 指标：gateway_stream_cancelled 计数 +1；且没有 gateway_requests_total{result="failure"}
curl -s http://localhost:8080/actuator/prometheus | grep stream_cancelled
```

### 19.6 演示 D：渠道冷却（可选）

```bash
# mock-a 连续失败 5 次（errorStatus 500）
for i in $(seq 1 5); do
  curl -s -o /dev/null http://localhost:8080/v1/chat/completions \
    -H 'Content-Type: application/json' \
    -d '{"model":"qwen","messages":[{"role":"user","content":"你好"}]}'
done
# 网关日志：event=候选被冷却跳过（第 6 次起）
# 冷却 30s 到期后第一个请求是“恢复探测”：再失败 → 冷却 60s
curl -s http://localhost:8080/actuator/prometheus | grep cooldown
```

### 19.7 验收标准对照（与学习版文档一致）

| 学习版验收标准 | 证据 |
|---|---|
| 1. 重试只在安全时机发生（首字节后不重试） | `StreamProxyTest` 首字节边界用例 + 演示 C |
| 2. 熔断状态机转换可观测、可恢复 | `CircuitBreakerTest` + 演示 B + circuit 指标 |
| 3. 冷却/自动禁用与恢复符合配置 | `CooldownManagerTest` + 演示 D + cooldown 指标 |
| 4. 流式透传格式正确；客户端断开后上游被取消 | `StreamProxyTest` 规范化/取消用例 + 演示 C + mock 断连日志 |
| 5. 所有容错事件有日志与指标 | 第 15 节指标 + 第 17 节时序中的事件点 |
| 6. 版本 1（或版本 2）功能回归通过 | 集成测试 V1 四场景 + V2 合并后场景 |

---

## 20. 任务清单（按顺序执行）

| # | 任务 | 产出 | 验收 | 归属 |
|---|---|---|---|---|
| T1 | 配置绑定与启动校验 | `ExecutionPolicies` 系列 + `ExecutionPolicyManager` + 单测 | 旧配置可启动；非法参数启动失败且报错可读 | Codex |
| T2 | 失败分类 | `FailureType` / `Failure` / `UpstreamCallException` / `FailureClassifier` + 单测 | 429/5xx/4xx/网络/超时分类正确 | Codex |
| T3 | 分层超时 | `TimeoutGuard` + 调度器 Bean + 单测 | 总时长/预算睡眠/定时器取消用例全绿 | 🖊 H2 |
| T4 | 熔断 | `CircuitBreaker` + `CircuitBreakerRegistry` + 单测 | 状态机五类用例全绿；事件/指标发布 | 🖊 H3（Registry Codex） |
| T5 | 冷却 | `CooldownManager` + 单测 | 冷却/探测/翻倍/上限用例全绿 | 🖊 H4 |
| T6 | 重试执行器 | `RetryExecutor` + 单测 | 连接重试一次 / 429 Retry-After / 预算用例全绿 | 🖊 H1 |
| T7 | 非流式连接器改造 | `OpenAIConnector` 超时参数化 + 分类 + 单测 | mock 直接调用两种模式正常；超时抛结构化失败 | Codex |
| T8 | 降级链执行 | `ExecutionChainResolver` + `FallbackChainExecutor` + `ChatGatewayService` 接线 + 单测 | 降级/跳过/超时/503 用例全绿；V1 回归 | 🖊 H5（其余 Codex） |
| T9 | 流式治理 | `StreamProxy` + `StreamSession` + `ClientDisconnectedException` + `ChatChunk` 扩展 + Controller + 单测 | 规范化/错误事件/首字节边界/取消用例全绿 | 🖊 H6（其余 Codex） |
| T10 | 状态闭环 | `StateEvent` + `StateEventPipeline` + `ModelStateStore` 扩展 + 单测 | 事件聚合与 EWMA/熔断/冷却快照用例全绿 | 🖊 H7 |
| T11 | 指标扩展 | `GatewayMetrics` V3 增量 | `/actuator/prometheus` 可见全部新指标 | Codex |
| T12 | mock 故障注入 | `ModelProfile` / `ModelRegistry` / `BehaviorController` / `ChatController` 扩展 | 19.2~19.6 可复现 | Codex |
| T13 | 集成测试 | `ChatGatewayIntegrationTest` 场景 A/B/C + V1 回归 | 集成测试全绿 | 你 |
| T14 | 文档与演示 | model.yml 更新 / README / 演示命令 | 按第 19 节可完整走一遍 | 你 |

**手敲顺序建议**：H3 → H4 → H2 → H1 → H5 → H7 → H6。先写两个无 IO 的状态机（熔断、冷却），再写超时基建，然后重试、降级链、状态管道，最后写最复杂的流式代理——每步都有单测兜底，不会带病前进。

---

## 21. 风险与注意点

1. **首字节标记的竞态**：`started[0]` 必须在 `consumer.accept()` **之前**置位（先标记后转发），否则“发出 chunk 但标记未更新”时失败会被误判为可降级，客户端收到重复内容。
2. **取消 vs 失败的区分**：定时器关闭 body 流、客户端断开都会让 `readLine()` 抛 IOException；连接器必须用 `AtomicBoolean cancelled` 区分“被取消”（`CANCELLED`，不计数）与“真实失败”，否则客户端断开会污染熔断统计。
3. **关闭 InputStream 取消上游**：这是 JDK HttpClient 的既定语义（未消费完的响应体关闭即取消交换），但不要依赖 try-with-resources 隐式关闭——显式 `session.cancel()` 并打日志，演示 C 才可观测。
4. **熔断锁粒度**：`synchronized` 按实例，锁内只做数组读写与状态切换，不做 IO、不打大日志；窗口大小 100 时开销可忽略。
5. **冷却与熔断重复计数**：一次失败只允许“熔断记一次 + 冷却记一次 + 状态事件记一次”，统一收口在 `recordOutcome`（12.2 / 13.2），不要在多个 catch 里各记一遍。
6. **429 Retry-After 与总时长**：等待必须走 `TimeoutGuard.sleepWithinBudget`，防止上游 `Retry-After: 99999` 把整条链拖死；`maxRetryAfterMs` 是硬上限。
7. **状态管道单写者纪律**：`ModelStateStore.apply` 只允许被 `StateEventPipeline.flush()`（或测试）调用；请求线程只 `offer`，否则“单写者”形同虚设。
8. **V1 / V2 回归**：没有 `execution` 配置必须能启动（全缺省）；`ChatGatewayService` 构造器与签名变更后同步更新所有测试；V2 合并时 `ExecutionChainResolver` 是唯一需要改的适配点。
9. **`circuit_open` 与 `cooldown_active` 的语义**：它们只在“所有候选都被跳过、未发起任何上游调用”时返回；只要链上还有一个可尝试候选，请求就该继续尝试，而不是直接 503。
10. **测试 flaky**：熔断/冷却涉及时间，单测用“短时长配置（如 openDurationMs=0）”或注入时钟；集成测试用轮询断言而不是固定 sleep。
11. **SseEmitter 断连异常**：`emitter.send` 抛 IOException 不代表业务失败，不要在外层 catch 里记 failure 指标；`complete()` 在连接已关闭时可能抛异常，需容忍。
12. **手敲顺序**：不要一上来写 StreamProxy（H6）——它依赖超时、熔断、冷却、降级链的全部语义；按第 20 节顺序推进，每个 H 完成即跑对应单测。
13. **冷却回调轻量性**：`CooldownManager.Listener` 在 compute 回调内执行（11.2），只允许入队 / 原子计数等轻量动作，不得阻塞、不得抛异常——否则会把一次 `isCooling` / `recordFailure` 调用打挂。
