# 版本 4：详细实施计划（含关键代码段）

> 配套文档：《版本4-治理与计量（学习版）》讲“是什么、为什么”，本文档讲“怎么做”。
> 技术基线：Java 21、Spring Boot 3.2、Spring MVC + Java 21 虚拟线程（阻塞式）、Maven 多模块（ai-gateway / mock-model-server）。
> 前置依赖：版本 1、2 已完成并跑通；版本 3 按《版本3-详细实施计划.md》实施（本文档按 **V3 已合并** 为默认基线，第 2.1 节给出 V3 未合并时的对接方式）。
> 本文档的关键代码段是**实现参考**，允许按你的编码风格调整；但接口契约（配置结构、端点、错误体、指标名）请保持一致。
>
> ⚠️ 2026-08-10：当前工作区 V1 + V2 已落地（决策引擎 / 插件框架 / Scorer 信号动态调权 / BudgetFilter 占位均在），V3 仅有计划文档、代码未合并。版本 4 的治理代码放在 `governance/` 包（与设计文档的模块边界对齐），决策侧的预算降级复用 V2 的“插件写信号 → 过滤器消费 → Scorer 动态调权”机制；`ChatGatewayService` 只做“接线”改造，不把治理逻辑堆进主流程类。

---

## 0. 手敲清单与实施分工（先读）

版本 4 的代码分两类：**必须你手敲默写的核心（H1~H6）** 与 **Codex 已实现的脚手架**。本版本的核心是“令牌 → 限流 → 计量 → 预算扣减”这条治理链路，六个手敲点对应学习版文档 4.1~4.6 的六个功能模块。

> ⚠️ **编号独立声明**：V4 的 H1~H6 与 V2 计划的 H1~H6、V3 计划的 H1~H7 **均无对应关系**，各自维护独立编号；代码注释中的 `TODO Hx` 以文件所在模块区分，不要跨版本对照。

### 必须手敲默写（H1~H6）

| 编号 | 手敲内容 | 位置 | 为什么必须手敲 |
|---|---|---|---|
| H1 | 令牌管理 `TokenManager`（创建随机令牌 / 哈希存储 / 校验时序 / 吊销） | 第 8 节 S6 | “凭证只能存哈希、明文只出现一次”是身份管理的核心心法 |
| H2 | 限流器 `RateLimiter`（环形时间桶滑动窗口 / 原子计数 / 多维度 / Token 占位修正） | 第 9 节 S7 | “管速度”的双层治理第一半，无锁读 + 原子写是并发点睛 |
| H3 | 预算管理器 `BudgetManager`（预检 → 扣减 → 结算 / 周期惰性重置 / CAS 防超扣 / 预警） | 第 10 节 S8 | “管总量”的双层治理第二半，**并发下不超扣**是版本 4 招牌 |
| H4 | Token 计量 `TokenMeter`（近似算法 / 单价表 / 流式增量累计 / 误差标注） | 第 11 节 S9 | “按字数收钱”的收银机，流式边出字边计费是难点 |
| H5 | 预算降级闭环 `BudgetFilter` 真实化（信号写入 / 逐候选淘汰 / 与 Scorer 动态调权对接） | 第 12 节 S10 | “花完钱自动换便宜餐厅”，信号驱动的自适应调度是本项目亮点 |
| H6 | 用量落库 `UsageLedger`（内存缓冲 / 定时批量写 H2 / 失败重试） | 第 13 节 S11 | “缓存 + 批量落库”避免每请求一次磁盘 IO，账本不拖慢收银 |

手敲方法：先合上文档自己写 → 写不出来再看代码段 → 看完再自己重写一遍，直到能默写。

手敲类路径：
- H1 `ai-gateway/src/main/java/com/aigateway/governance/token/TokenManager.java`
- H2 `ai-gateway/src/main/java/com/aigateway/governance/ratelimit/RateLimiter.java`
- H3 `ai-gateway/src/main/java/com/aigateway/governance/budget/BudgetManager.java`
- H4 `ai-gateway/src/main/java/com/aigateway/governance/meter/TokenMeter.java`
- H5 `ai-gateway/src/main/java/com/aigateway/decision/filter/BudgetFilter.java`（改造现有占位）
- H6 `ai-gateway/src/main/java/com/aigateway/governance/ledger/UsageLedger.java`

### Codex 已实现（脚手架，不需要手敲）

配置绑定（`GovernanceProperties`）、持久化（`SchemaInitializer` 建表 + `TokenDao` / `BudgetDao` / `UsageDao` / `ChannelDao`）、公共估算器 `TokenEstimator`（从 Scorer 抽取，口径一致）、治理插件 `GovernanceBootstrapPlugin`（写信号）、`GovernanceFilter`（鉴权 + QPS/RPM 限流）、`GovernanceService`（模型范围校验 + Token 速率占位 + 预检 + 扣减编排）、`ChannelStore`（渠道 CRUD + 启停）、管理端点 `GovernanceAdminController`、`ChatGatewayService` 治理接线、`GatewayMetrics` 扩展、`MeteringCallback` 签名对齐与真实实现、mock 流式 usage 补齐、单测骨架。

代码中手敲部分留 `TODO H1~H6` 标记与说明注释，提示你该写哪一段；未完成前调用对应功能会直接报错。

---

## 1. 目标与范围

版本 4 完成后，系统支持：

- **持久化基础**：嵌入式 H2（文件模式）零外部依赖；渠道 / 令牌 / 预算 / 用量四张表，建表幂等；用量记录内存缓冲 + 定时批量落库。
- **令牌管理**：创建令牌返回**明文仅一次**，库中只存 SHA-256 哈希；支持额度（TOKEN/COST）、模型范围、IP 白名单、过期时间、启用状态；请求级校验时序（存在 → 启用 → 未过期 → IP → 模型范围 → 预检额度）；吊销即时生效。
- **双层治理**：
  - **限流（管速度）**：QPS / RPM 滑动窗口（环形时间桶），维度：令牌 / 模型 / 渠道 / 全局；Token 速率限流用“预估输入占位 + 实际用量修正”。
  - **配额（管总量）**：令牌额度（总量不重置）+ 预算（周期重置：小时/天/月），作用域：模型 / 渠道 / 全局；超额 → 拒绝或自动降级。
- **Token 计量与成本**：非流式优先取上游 `usage`、缺失用近似；流式按 chunk 增量累计输出 token、输入按预估；近似算法明确标注误差（CJK 约 1 字/token，其他约 4 字符/token）；成本 = `tokenIn/1000 × priceIn + tokenOut/1000 × priceOut`（单价表已有）。
- **预算闭环**：预检 → 成功才扣减（结算）→ 周期自动重置；70% / 90% 预警（日志 + 指标，不阻断）；超额策略可配 `REJECT` / `DEGRADE`，降级走“信号 → BudgetFilter 淘汰 → Scorer 动态调权”的既有机制。
- **渠道管理**：内部端点支持创建 / 启停 / 权重调整 / 软删除，状态即时生效（内存缓存原子替换）；渠道级配额（预算作用域 CHANNEL）。
- **内部管理端点**：令牌 / 预算 / 渠道 / 用量查询（V5 升级为带管理密钥的 Admin API）。
- **版本 3 功能回归**：重试 / 熔断 / 冷却 / 降级链 / 流式治理不回退。

**非目标**（版本 4 不做什么）：

- 不实现配置热更新与带鉴权的 Admin API 对外接口（版本 5；本版本管理端点免鉴权、仅供演示与学习）。
- 不实现管理后台看板（版本 6）。
- 不做多租户、兑换码、充值（保持轻量）。
- 不做 Redis 分布式限流 / 配额（远期；本版本单实例内存实现 + 日志重放）。
- 不引入精确 tokenizer（版本 6 可选；本版本近似 + 误差标注）。
- 不做预算的持久化对账补偿（内存扣减优先，对账周期落库；重启丢失未对账窗口，文档标注）。

---

## 2. 与现有代码的关系（先盘点，再动手）

| 现有文件 | 现状 | 处理方式 |
|---|---|---|
| `ChatGatewayService` | V2 主流程：决策 + 执行主选 + 最小降级（V3 合并且并入后为链执行器） | 改造为“鉴权预检 → 决策 → 执行 → 计量 → 扣减 → 落库”的治理接线（第 14 节） |
| `BudgetFilter` | V4 占位（order=20，第 4 道关卡，直接放行） | 填充真实预算淘汰逻辑（🖊 H5）：渠道/模型预算超限与令牌额度不足的候选淘汰 |
| `Scorer` | 已有 `estimateInputTokens` / `estimateTextTokens`（CJK 1 字/token、其他 4 字符/token）与 `budget_remaining_ratio` 信号动态调权 | 估算逻辑抽取为公共 `TokenEstimator`（governance），Scorer 改为复用；信号动态调权**零改动**，V4 只需写入该信号 |
| `Signals` | 已有 set / setIfAbsent / getString / getStringSet / snapshot | 治理信号载体，无需改动 |
| `PluginContext` | 决策引擎在 BEFORE_DECISION 插件之后跑 FilterChain | V4 治理信号由治理插件在 BEFORE_DECISION 写入，BudgetFilter 消费 |
| `PluginEngine` / `PluginRegistry` | V2 插件框架 | 新增治理插件挂载配置（`governance-bootstrap`），框架零改动 |
| `GatewayProperties` | health / decision / channels / models / policies / plugins | 新增 `gateway.governance` 配置段（第 5 节） |
| `ModelRegistry` | 别名 → 候选 + 渠道表 + 启动校验 | 保留不动；V4 渠道管理另建 `ChannelStore`（启动时与 model.yml 渠道合并，DB 覆盖同 id 的权重/启停） |
| `ModelInstance` | record，已带 priceIn / priceOut / qualityScore / latencyProfileMs | 保留不动（单价表直接复用 priceIn/priceOut） |
| `HealthChecker` | 渠道级健康快照 | 保留不动；`AvailabilityFilter` 增加渠道启停检查（ChannelStore.isEnabled） |
| `AvailabilityFilter` | 第 2 道：健康 + 权重过滤 | 增加“渠道已停用 → 淘汰”检查（一行，Codex） |
| `ChatCompletionController` | 非流式 / 流式 + SseEmitter + metadata 提取 | metadata 增加 `x-gateway-token-id`（GovernanceFilter 写入 request attribute，Controller 透传） |
| `ChatCompletion.Usage` | promptTokens / completionTokens / totalTokens | 保留不动（非流式计量输入） |
| `ChatChunk` | V3 增加可选 usage / error 字段 | V4 流式计量消费 `usage`（结束 chunk）与 `choices[].delta.content`（增量文本） |
| `MeteringCallback` | V3 计划的空实现（onChunk / onFinish 无上下文） | 签名扩展为带 `MeteringContext`（requestId / instance / request / tokenId），V4 提供真实累加实现（第 14.3 节） |
| `GatewayMetrics` | success / failure / streamChunk / decision / filterDrops | 增加治理指标（第 17 节） |
| `GatewayException` | 统一业务异常 | 保留不动；V4 新增错误码见 17.2 |
| `RequestIdFilter` | Filter（ATTR=requestId，无 @Order） | 保留不动；GovernanceFilter 用 @Order(2) 注册，两 Filter 执行顺序由 Spring 决定，GovernanceFilter **不依赖 requestId**，顺序无功能影响 |
| mock-model-server | 非流式已带 usage（与网关同口径估算）；流式结束 chunk 无 usage | 流式结束 chunk 补齐 usage（Codex，一行）；其余不动 |
| 测试 | V2 单测 + 集成测试 | 新增治理六模块单测 + 三个演示场景集成测试 + V3 回归 |

> 路径前缀：`ai-gateway/src/main/java/com/aigateway/`。治理代码放 `governance/` 包（token / ratelimit / budget / meter / ledger / persistence / api 子包），与设计文档的模块边界（governance）对齐。

### 2.1 与版本 3 的关系（重要）

版本 4 的学习版文档要求“预算降级需要版本 3 的降级链”，但 V4 的预算降级实际走**决策层**（BudgetFilter 淘汰候选），不依赖 V3 的执行层；V4 对 V3 的真正依赖只有两处：

1. **流式计量回调**：V3 的 `StreamProxy` 逐 chunk 触发 `MeteringCallback`（onChunk / onFinish），V4 把空实现替换为真实累加（第 14.3 节）。**V3 未合并时**：流式计量退化为“结束 chunk 的 usage + 近似补齐”，非流式计量与预算降级不受影响。
2. **降级链执行**：V3 的 `FallbackChainExecutor` / `StreamProxy` 接收候选链并执行。V4 的接线代码（第 14 节）依赖 V3 的 `ChatGatewayService` 签名；**V3 未合并时**：在 V2 版 `ChatGatewayService` 上直接加治理环节（第 14 节给出两版接线，实施时按当前代码选择其一）。

合并顺序建议：**先合并 V3，再实施 V4**。V4 的代码段按 V3 已合并编写；若 V3 未合并就启动 V4，需要同步改动的只有第 14 节的接线类。

---

## 3. 工程结构与新增文件

```
ai-gateway/src/main/java/com/aigateway/
├── GatewayApplication.java                    （保留）
├── api/
│   ├── controller/ChatCompletionController.java（改造：metadata 透传 tokenId）
│   ├── controller/GovernanceAdminController.java（新增：/v1/admin/* 内部管理端点）
│   └── error/GlobalExceptionHandler.java       （保留，错误表增加 V4 行）
├── core/
│   ├── exception/GatewayException.java         （保留）
│   └── service/ChatGatewayService.java         （改造：治理接线，见第 14 节）
├── decision/
│   ├── filter/BudgetFilter.java                （🖊 H5 改造：真实预算淘汰）
│   └── ...（其余保留不动）
├── execution/
│   ├── stream/MeteringCallback.java            （改造：签名对齐 V4，见 14.3）
│   └── ...（其余保留不动）
├── governance/                                 （新增：V4 主战场）
│   ├── model/
│   │   ├── ApiToken.java                       （新增：令牌领域对象）
│   │   ├── TokenStatus.java                    （新增：校验结果分类）
│   │   ├── Budget.java                         （新增：预算定义 + 计数）
│   │   ├── BudgetScope.java                    （新增：MODEL / CHANNEL / GLOBAL）
│   │   ├── BudgetPeriod.java                   （新增：HOURLY / DAILY / MONTHLY）
│   │   ├── LimitType.java                      （新增：TOKEN / COST）
│   │   ├── UsageRecord.java                    （新增：单次用量记录）
│   │   └── MeteredUsage.java                   （新增：计量结果 tokenIn/tokenOut/cost）
│   ├── token/TokenManager.java                 （🖊 H1）
│   ├── ratelimit/
│   │   ├── RateLimiter.java                    （🖊 H2）
│   │   ├── RateLimitKey.java                   （新增：维度 + key 封装）
│   │   └── RateLimitRule.java                  （新增：limit / windowMs 规则）
│   ├── budget/BudgetManager.java               （🖊 H3）
│   ├── meter/
│   │   ├── TokenEstimator.java                 （新增：公共近似估算器，从 Scorer 抽取）
│   │   └── TokenMeter.java                     （🖊 H4）
│   ├── ledger/UsageLedger.java                 （🖊 H6）
│   ├── persistence/
│   │   ├── SchemaInitializer.java              （新增：幂等建表 + 索引）
│   │   ├── TokenDao.java                       （新增：令牌表读写）
│   │   ├── BudgetDao.java                      （新增：预算表读写）
│   │   ├── UsageDao.java                       （新增：用量批量写入 + 汇总查询）
│   │   └── ChannelDao.java                     （新增：渠道表读写）
│   ├── channel/ChannelStore.java               （新增：渠道 CRUD + 启停 + 合并）
│   ├── plugin/GovernanceBootstrapPlugin.java   （新增：BEFORE_DECISION 写治理信号）
│   ├── filter/GovernanceFilter.java            （新增：鉴权 + QPS/RPM 限流）
│   ├── service/GovernanceService.java          （新增：模型范围/Token 速率/预检/扣减编排）
│   └── config/GovernanceProperties.java        （新增：gateway.governance 配置绑定）
├── infra/
│   ├── config/GatewayProperties.java           （改造：governance 段）
│   └── config/GovernanceBeans.java             （新增：DAO / Manager / 定时器装配）
└── observability/GatewayMetrics.java           （扩展：治理指标）
```

mock-model-server 修改（Codex）：

```
mock-model-server/src/main/java/com/aigateway/mock/controller/ChatController.java
  └─ buildChunk：结束 chunk 补齐 usage（prompt_tokens=estimateTokens(request)、completion_tokens=已发 token 数）
```

测试新增（`ai-gateway/src/test/java/com/aigateway/`）：

```
governance/token/TokenManagerTest.java
governance/ratelimit/RateLimiterTest.java
governance/budget/BudgetManagerTest.java
governance/meter/TokenMeterTest.java
governance/ledger/UsageLedgerTest.java
decision/filter/BudgetFilterTest.java
governance/persistence/SchemaInitializerTest.java（启动可用性）
governance/filter/GovernanceFilterTest.java（MockMvc：鉴权 / 限流 / 放行）
```

---

## 4. 依赖（pom.xml）

```xml
<!-- ai-gateway/pom.xml 新增两项（其余不动） -->
<dependency>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-jdbc</artifactId>
</dependency>
<dependency>
    <groupId>com.h2database</groupId>
    <artifactId>h2</artifactId>
    <scope>runtime</scope>
</dependency>
```

理由（面试可讲）：

- **H2 文件模式**：零安装、一条命令跑起来，学习项目不引外部数据库；SQL 与主流数据库兼容，`JdbcTemplate` 预留方言隔离，以后可平滑迁 PostgreSQL；
- **JdbcTemplate 而非 JPA**：建表 SQL 显式可控（方言隔离的底气）、批量写入 `batchUpdate` 天然支持、无实体映射开销——学习“缓存 + 批量落库”用 JDBC 最直白；
- 不需要 Flyway：四张表用幂等 `CREATE TABLE IF NOT EXISTS` 起步，字段变更走版本化迁移列（`schema_version` 预留，V5 再启用），本版本不做复杂迁移。

---

## 5. 配置设计（关键代码段 S1）

### 5.1 `model.yml`（V4 完整结构）

> 路径：`ai-gateway/src/main/resources/model.yml`

在 V2 基础上新增 `gateway.governance`（治理参数）与 `gateway.tokens`（启动种子令牌，演示用，可空）。V3 合并后追加的 `execution` 段与本版本互不干扰，可共存。

```yaml
gateway:
  health:                       # V1 保留
    intervalSeconds: 5
    timeoutMs: 2000
    consecutiveFailures: 3

  decision:                     # V2 保留
    logSize: 100
    ewmaAlpha: 0.3
    errorRateAlpha: 0.1
    defaultOutputTokens: 256

  governance:                   # ── V4 新增 ──
    enabled: true               # 总开关：false = 跳过鉴权与限流（演示旁路用）
    db:
      url: jdbc:h2:file:./data/gateway    # 文件模式；测试用 jdbc:h2:mem:gateway
      user: sa
      password: ""
    estimation:                 # 近似计量系数（CJK 1 字/token，其他约 4 字符/token）
      charsPerToken: 4.0
      cjkTokenPerChar: 1.0
    rateLimit:                  # 限流（管速度）
      defaultQps: 100           # 令牌级缺省 QPS（滑动窗口 1s）
      defaultRpm: 6000          # 令牌级缺省 RPM（滑动窗口 60s）
      tokenRate:                # Token 速率限流（可选）：预扣预估输入、结束按实际修正
        enabled: false
        perSecondTokens: 20000
    budget:                     # 配额（管总量）
      warnRatios: [ 0.7, 0.9 ] # 预警阈值（软预算，不阻断）
      exceedAction: REJECT      # 超额动作：REJECT（拒绝）/ DEGRADE（降级到低成本候选）
    usageFlush:                 # 用量批量落库
      intervalMs: 5000          # 定时 flush 周期
      batchSize: 100            # 单批最大条数（达到即刷）

  channels:                     # V1 保留（V4 ChannelStore 启动时合并，DB 状态覆盖同 id）
    - id: mock-a
      provider: openai-compatible
      baseUrl: http://localhost:8001
      credentialsRef: ""
      weight: 10
    - id: mock-b
      provider: openai-compatible
      baseUrl: http://localhost:8002
      credentialsRef: ""
      weight: 5

  models:                       # V2 保留（价格/质量/延迟画像 + capability）
    # ⚠️ V4 价格调整：qwen-large 贵且好、qwen-small 便宜且快——
    #    成本降级演示（21.4）依赖「低成本候选」真实存在（旧配置价格反了）
    - alias: qwen
      strategy: simple-task
      candidates:
        - channelId: mock-a
          model: qwen-large
          weight: 8
          priceIn: 0.002
          priceOut: 0.004          # 单次（256 输出 token）约 $0.001
          qualityScore: 0.9
          latencyProfileMs: 2000
          capability: { contextLength: 128000, multimodal: false, toolCallSupport: true,
                        codingAbility: MEDIUM, reasoningLevel: HIGH, qualityLevel: QUALITY_HIGH }
        - channelId: mock-b
          model: qwen-small
          weight: 2
          priceIn: 0.0005
          priceOut: 0.001          # 单次（256 输出 token）约 $0.00026
          qualityScore: 0.4
          latencyProfileMs: 200
          capability: { contextLength: 32000, multimodal: false, toolCallSupport: false,
                        codingAbility: LOW, reasoningLevel: LOW, qualityLevel: FAST }
    - alias: deepseek-code
      strategy: quality-first
      candidates:
        - channelId: mock-b
          model: deepseek-code
          weight: 1
          priceIn: 0.002
          priceOut: 0.004
          qualityScore: 0.8
          latencyProfileMs: 500
          capability: { contextLength: 64000, multimodal: false, toolCallSupport: true,
                        codingAbility: MAX, reasoningLevel: MEDIUM, qualityLevel: QUALITY_MEDIUM }

  policies:                     # V2 保留
    - name: balanced
      type: MULTI_OBJECTIVE
      weights: { latency: 0.25, cost: 0.25, quality: 0.25, health: 0.25 }
    - name: cost-first
      type: MULTI_OBJECTIVE
      weights: { latency: 0.1, cost: 0.6, quality: 0.2, health: 0.1 }
    - name: simple-task
      type: CONDITIONAL
      defaultStrategy: balanced
      rules:
        - when: { signal: task_complexity, op: EQ, value: COMPLEX }
          select: [ { channelId: mock-a, model: qwen-large } ]

  plugins:                      # V2 保留 + V4 治理插件
    - name: complexity-recognizer
      scope: GLOBAL
      order: 30
      config: { keywordRegex: "代码|算法|重构|设计" }
    - name: canary
      scope: ROUTE
      scopeValue: qwen
      order: 20
      config: { stableInstances: [ "mock-a:qwen-large" ], canaryInstances: [ "mock-b:qwen-small" ] }
    - name: governance-bootstrap      # ── V4 新增：BEFORE_DECISION 写治理信号 ──
      scope: GLOBAL
      order: 100                     # 必须最先执行（数字大先执行），信号要赶在过滤器之前
      config: {}

  tokens:                       # ── V4 新增：启动种子令牌（演示用，可空）──
    - name: demo
      quotaLimit: 1.0           # 额度上限（美元，-1 = 无限）；TOKEN 类型则写 token 数
      quotaType: COST
      modelScope: ""            # 空 = 不限；逗号分隔的 alias 列表
      ipWhitelist: ""           # 空 = 不限；逗号分隔的 IP 列表
      expiresAt: -1             # epoch 毫秒；-1 = 永不过期
      enabled: true
```

### 5.2 新配置字段速查

| 配置 | 说明 | 缺省 |
|---|---|---|
| `governance.enabled` | 治理总开关（false 跳过鉴权与限流） | `true` |
| `governance.db.url` | H2 JDBC URL（文件 / 内存） | `jdbc:h2:file:./data/gateway` |
| `governance.db.user / password` | H2 账号 | `sa / 空` |
| `governance.estimation.charsPerToken` | 非 CJK 字符 → token 换算系数 | `4.0` |
| `governance.estimation.cjkTokenPerChar` | CJK 每字 token 数 | `1.0` |
| `governance.rateLimit.defaultQps` | 令牌级缺省 QPS（1s 滑动窗口） | `100` |
| `governance.rateLimit.defaultRpm` | 令牌级缺省 RPM（60s 滑动窗口） | `6000` |
| `governance.rateLimit.tokenRate.enabled` | Token 速率限流开关 | `false` |
| `governance.rateLimit.tokenRate.perSecondTokens` | Token 速率上限（每秒 token） | `20000` |
| `governance.budget.warnRatios` | 预警阈值列表（软预算） | `[0.7, 0.9]` |
| `governance.budget.exceedAction` | 超额动作 `REJECT` / `DEGRADE` | `REJECT` |
| `governance.usageFlush.intervalMs` | 用量批量落库周期 | `5000` |
| `governance.usageFlush.batchSize` | 单批最大条数 | `100` |
| `tokens[]` | 启动种子令牌（创建即入库） | 空 |

兼容性约定：版本 2 的旧 `model.yml`（没有 `governance` 段、没有 `tokens`）**必须能直接启动**——治理默认开启但无令牌可校验？**不行**：治理开启时必须存在至少一个令牌，否则所有请求 401。因此约定：`governance.enabled=true` 且无任何令牌（配置种子为空且库中为空）时，**自动创建一个内置演示令牌** `agw_demo_<16 hex>` 并打印在启动日志里（演示 A/B/C 直接可用）；`enabled=false` 则完全旁路（V1~V3 行为不回退）。

### 5.3 请求级治理信息（metadata）

`GovernanceFilter` 校验通过后，把令牌 ID 写入 request attribute，Controller 的 `metadata()` 提取为内部 key（不以 `x-` 开头对外暴露，只在内网传递）：

| metadata key | 来源 | 说明 |
|---|---|---|
| `x-gateway-token-id` | GovernanceFilter 写入 attribute | 请求所持令牌 ID（治理插件 / BudgetFilter / 计量共用） |

客户端请求体 / 响应体不出现令牌 ID。剩余额度比率不走 metadata——由 `GovernanceBootstrapPlugin` 直接写进 signals（`governance.token_remaining_ratio`，12.3 节），链路更短。

---

## 6. 领域模型（关键代码段 S2）

> 路径：`ai-gateway/src/main/java/com/aigateway/governance/model/`

### 6.1 令牌（`ApiToken` / `TokenStatus`）

```java
// 路径：governance/model/ApiToken.java
/**
 * 令牌领域对象（对应 New API 的 ApiToken）：
 * - 明文凭证只在创建时出现一次，库里只存 tokenHash（SHA-256 十六进制）；
 * - quotaLimit 是「总量额度」：COST 单位为美元、TOKEN 单位为 token 数；-1 = 无限；
 * - 与 Budget（周期配额）的区别：令牌额度不按周期重置，花完即止（New API 语义）。
 */
public record ApiToken(
        String id,             // 管理 ID（前缀 agw_ + 8 hex，如 agw_3f9a2b1c）
        String name,           // 显示名（脱敏列表用）
        String tokenHash,      // SHA-256(明文 token) 十六进制，唯一索引
        double quotaLimit,     // 额度上限；-1 = 无限
        String quotaType,      // COST / TOKEN
        String modelScope,     // 逗号分隔的 alias；空 = 不限
        String ipWhitelist,    // 逗号分隔的 IP；空 = 不限
        long expiresAt,        // epoch 毫秒；-1 = 永不过期
        boolean enabled,
        long createdAt
) {
    /** 是否无限额度 */
    public boolean unlimited() { return quotaLimit < 0; }
}
```

```java
// 路径：governance/model/TokenStatus.java
/** 令牌校验结果分类：校验失败的类型决定错误码与 HTTP 状态（第 17.2 节） */
public enum TokenStatus {
    VALID,                 // 通过：继续走限流与决策
    MISSING,               // 未携带 Authorization 头 → 401 invalid_token
    UNKNOWN,               // 令牌不存在 → 401 invalid_token
    DISABLED,              // 已吊销/停用 → 403 token_disabled
    EXPIRED,               // 已过期 → 403 token_expired
    IP_DENIED,             // IP 不在白名单 → 403 token_ip_denied
    MODEL_FORBIDDEN,       // 请求模型不在 modelScope → 403 token_model_forbidden
    QUOTA_EXHAUSTED        // 额度预检不足 → 429 budget_exceeded
}
```

### 6.2 预算（`Budget` / 枚举）

```java
// 路径：governance/model/BudgetScope.java
/** 预算作用域：配额按谁统计 */
public enum BudgetScope { MODEL, CHANNEL, GLOBAL }

// 路径：governance/model/BudgetPeriod.java
/** 预算周期：到期自动重置（惰性重置见 H3） */
public enum BudgetPeriod {
    HOURLY, DAILY, MONTHLY;
    /** 当前周期标识：HOURLY=yyyyMMddHH、DAILY=yyyyMMdd、MONTHLY=yyyyMM（周期切换判据） */
    public String periodId(long nowMs) {
        java.time.ZonedDateTime t = java.time.ZonedDateTime.ofInstant(
                java.time.Instant.ofEpochMilli(nowMs), java.time.ZoneId.systemDefault());
        return switch (this) {
            case HOURLY -> t.format(java.time.format.DateTimeFormatter.ofPattern("yyyyMMddHH"));
            case DAILY -> t.format(java.time.format.DateTimeFormatter.ofPattern("yyyyMMdd"));
            case MONTHLY -> t.format(java.time.format.DateTimeFormatter.ofPattern("yyyyMM"));
        };
    }
}

// 路径：governance/model/LimitType.java
/** 预算限额类型：按 token 总量还是按成本金额 */
public enum LimitType { TOKEN, COST }

// 路径：governance/model/Budget.java
/**
 * 预算定义 + 计数（对应 LiteLLM budget / Envoy QuotaPolicy）：
 * - limit 是周期上限；used 是当前周期已用量；
 * - COST 类型内部按「微美元」整数计数（double 有精度问题，结算时再转美元）；
 * - used 由 BudgetManager 用 CAS 原子扣减（第 10 节 H3），本类只做状态容器。
 */
public final class Budget {

    private final BudgetScope scope;
    private final String scopeValue;      // MODEL=alias、CHANNEL=channelId、GLOBAL=""
    private final LimitType limitType;
    private final BudgetPeriod period;
    private final double limit;           // TOKEN=token 数；COST=美元
    private final java.util.concurrent.atomic.AtomicLong usedMicro = new java.util.concurrent.atomic.AtomicLong();
    private volatile String currentPeriodId;   // 惰性重置判据

    public Budget(BudgetScope scope, String scopeValue, LimitType limitType,
                  BudgetPeriod period, double limit) {
        this.scope = scope;
        this.scopeValue = scopeValue;
        this.limitType = limitType;
        this.period = period;
        this.limit = limit;
        this.currentPeriodId = period.periodId(System.currentTimeMillis());
    }

    public BudgetScope scope() { return scope; }
    public String scopeValue() { return scopeValue; }
    public LimitType limitType() { return limitType; }
    public BudgetPeriod period() { return period; }
    public double limit() { return limit; }

    /** 内部计数句柄（BudgetManager 的扣减/重置用；锁内读写） */
    public java.util.concurrent.atomic.AtomicLong usedMicro() { return usedMicro; }

    /** 当前已用量（展示口径；COST 转回美元） */
    public double used() {
        return limitType == LimitType.COST ? usedMicro.get() / 1_000_000.0 : usedMicro.get();
    }

    /** 剩余用量（预检用）；为负按 0（不提供负余额，扣减时由 CAS 保证不超扣） */
    public double remaining() { return Math.max(0, limit - used()); }

    /** 剩余比率 0~1（Scorer 的 budget_remaining_ratio 信号输入） */
    public double remainingRatio() {
        return limit <= 0 ? 0 : Math.max(0, Math.min(1, remaining() / limit));
    }

    /** 转为内部整数计数单位：TOKEN=token 数、COST=微美元 */
    public long toUnit(double amount) {
        return limitType == LimitType.COST ? Math.round(amount * 1_000_000) : Math.round(amount);
    }

    /** 周期是否已切换（BudgetManager 每次读写前调用，命中即惰性重置） */
    public boolean periodChanged(long nowMs) {
        return !currentPeriodId.equals(period.periodId(nowMs));
    }

    /** 惰性重置：新周期从 0 开始 */
    public void resetPeriod(String newPeriodId) {
        usedMicro.set(0);
        currentPeriodId = newPeriodId;
    }

    public String periodId() { return currentPeriodId; }
}
```

### 6.3 用量与计量（`UsageRecord` / `MeteredUsage`）

```java
// 路径：governance/model/MeteredUsage.java
/** 计量结果：一次成功请求的 token 与成本（TokenMeter 产出，BudgetManager 消费） */
public record MeteredUsage(
        long tokenIn,
        long tokenOut,
        double cost,          // 美元
        boolean estimated     // true = 使用了近似算法（usage 缺失）
) {
    public long totalTokens() { return tokenIn + tokenOut; }
}

// 路径：governance/model/UsageRecord.java
/** 单次用量记录（异步批量落库；字段与 usage_record 表一一对应） */
public record UsageRecord(
        String requestId,
        String tokenId,
        String alias,
        String channelId,
        String model,
        long tokenIn,
        long tokenOut,
        double cost,
        long latencyMs,
        String result,        // success / failure（失败不扣预算但留痕）
        long createdAt
) {}
```

### 6.4 限流（`RateLimitKey` / `RateLimitRule`）

```java
// 路径：governance/ratelimit/RateLimitKey.java
/** 限流维度 + key：同一维度同一 key 共享一个滑动窗口桶 */
public record RateLimitKey(String dimension, String key) {
    public static RateLimitKey token(String tokenId)  { return new RateLimitKey("token", tokenId); }
    public static RateLimitKey model(String alias)    { return new RateLimitKey("model", alias); }
    public static RateLimitKey channel(String chId)   { return new RateLimitKey("channel", chId); }
    public static RateLimitKey global()               { return new RateLimitKey("global", ""); }
}

// 路径：governance/ratelimit/RateLimitRule.java
/** 一条限流规则：窗口内最多 limit 个「单位」（1 请求 或 1 token） */
public record RateLimitRule(
        RateLimitKey key,
        long windowMs,       // 窗口长度：QPS=1000、RPM=60000
        long limit,
        boolean tokenMode    // true = 按 token 计数（预扣预估输入，结束修正）
) {}
```

---

## 7. 持久化基础（关键代码段 S3、S4）

> 路径：`ai-gateway/src/main/java/com/aigateway/governance/persistence/`。本节全部为 Codex 脚手架，但**建表 SQL 与“缓存 + 批量落库”的取舍必须读懂**（面试讲得出）。

### 7.1 表结构（代码段 S3，Codex）

```sql
-- 路径：ai-gateway/src/main/resources/schema.sql（SchemaInitializer 启动时执行，幂等）

CREATE TABLE IF NOT EXISTS channel (
    id             VARCHAR(64)  PRIMARY KEY,
    provider       VARCHAR(32),
    base_url       VARCHAR(512),
    credentials_ref VARCHAR(128),
    weight         INT          NOT NULL DEFAULT 1,
    enabled        BOOLEAN      NOT NULL DEFAULT TRUE,
    deleted        BOOLEAN      NOT NULL DEFAULT FALSE,  -- 软删除（有历史用量时保护）
    created_at     TIMESTAMP    NOT NULL
);

CREATE TABLE IF NOT EXISTS api_token (
    id             VARCHAR(64)  PRIMARY KEY,             -- agw_ + 8 hex
    token_hash     VARCHAR(64)  NOT NULL UNIQUE,         -- SHA-256 十六进制（凭证只存哈希）
    name           VARCHAR(128),
    quota_limit    DOUBLE       NOT NULL DEFAULT -1,     -- -1 = 无限
    quota_type     VARCHAR(16)  NOT NULL DEFAULT 'COST',
    model_scope    VARCHAR(512) NOT NULL DEFAULT '',
    ip_whitelist   VARCHAR(512) NOT NULL DEFAULT '',
    expires_at     BIGINT       NOT NULL DEFAULT -1,     -- epoch 毫秒；-1 = 永不过期
    enabled        BOOLEAN      NOT NULL DEFAULT TRUE,
    created_at     TIMESTAMP    NOT NULL,
    revoked_at     TIMESTAMP                              -- 吊销时间；NULL = 未吊销
);

CREATE TABLE IF NOT EXISTS budget (
    scope          VARCHAR(16)  NOT NULL,                -- MODEL / CHANNEL / GLOBAL
    scope_value    VARCHAR(128) NOT NULL,                -- MODEL=alias、CHANNEL=channelId、GLOBAL=''
    limit_type     VARCHAR(16)  NOT NULL,                -- TOKEN / COST
    period         VARCHAR(16)  NOT NULL,                -- HOURLY / DAILY / MONTHLY
    limit_value    DOUBLE       NOT NULL,
    used_value     DOUBLE       NOT NULL DEFAULT 0,      -- 对账用的已用量快照（内存计数为主）
    PRIMARY KEY (scope, scope_value, limit_type, period, limit_value)
);

CREATE TABLE IF NOT EXISTS usage_record (
    request_id     VARCHAR(64)  PRIMARY KEY,
    token_id       VARCHAR(64),
    alias          VARCHAR(64),
    channel_id     VARCHAR(64),
    model          VARCHAR(64),
    token_in       BIGINT       NOT NULL DEFAULT 0,
    token_out      BIGINT       NOT NULL DEFAULT 0,
    cost           DOUBLE       NOT NULL DEFAULT 0,
    latency_ms     BIGINT       NOT NULL DEFAULT 0,
    result         VARCHAR(16)  NOT NULL,                -- success / failure
    created_at     TIMESTAMP    NOT NULL
);

CREATE INDEX IF NOT EXISTS idx_usage_token_time ON usage_record (token_id, created_at);
CREATE INDEX IF NOT EXISTS idx_usage_model_time ON usage_record (model, created_at);
```

设计要点：

- **四张表最小集**：渠道表（供应商档案）、令牌表（会员卡）、预算表（周期配额）、用量表（账本流水）；审计表留 V5 管理面再加；
- **凭证只存哈希**：`token_hash` 唯一索引，数据库泄露也拿不到可用凭证（密码存储思路）；
- **预算 used 双写**：内存 `AtomicLong` 是热路径（预检/扣减），`used_value` 是慢路径对账快照（定时落库 + 重启恢复），两者口径一致、允许短暂偏差（文档标注）；
- **软删除**：渠道 `deleted` 标志，有历史用量时保护性删除。

### 7.2 DAO（代码段 S4，Codex）

```java
// 路径：governance/persistence/TokenDao.java
@Repository
public class TokenDao {

    private final JdbcTemplate jdbc;

    public TokenDao(JdbcTemplate jdbc) { this.jdbc = jdbc; }

    /** 插入令牌（仅创建时调用） */
    public void insert(ApiToken t) {
        jdbc.update("""
                INSERT INTO api_token (id, token_hash, name, quota_limit, quota_type,
                                       model_scope, ip_whitelist, expires_at, enabled, created_at)
                VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?)
                """, t.id(), t.tokenHash(), t.name(), t.quotaLimit(), t.quotaType(),
                t.modelScope(), t.ipWhitelist(), t.expiresAt(), t.enabled(),
                new Timestamp(t.createdAt()));
    }

    /** 按哈希查令牌（启动预热 + 缓存未命中兜底）；无则返回 null */
    public ApiToken findByHash(String tokenHash) {
        return jdbc.query("SELECT * FROM api_token WHERE token_hash = ? AND revoked_at IS NULL",
                this::mapRow, tokenHash).stream().findFirst().orElse(null);
    }

    /** 吊销：标记 revoked_at；enabled 同时置 false（缓存失效靠内存删除） */
    public void revoke(String id) {
        jdbc.update("UPDATE api_token SET revoked_at = ?, enabled = FALSE WHERE id = ?",
                new Timestamp(System.currentTimeMillis()), id);
    }

    /** 全量列表（脱敏显示用：不返回哈希） */
    public List<ApiToken> findAll() {
        return jdbc.query("SELECT * FROM api_token", this::mapRow);
    }

    private ApiToken mapRow(ResultSet rs, int i) throws SQLException {
        return new ApiToken(
                rs.getString("id"), rs.getString("name"), rs.getString("token_hash"),
                rs.getDouble("quota_limit"), rs.getString("quota_type"),
                rs.getString("model_scope"), rs.getString("ip_whitelist"),
                rs.getLong("expires_at"), rs.getBoolean("enabled"),
                rs.getTimestamp("created_at").getTime());
    }
}
```

> `BudgetDao`（按主键 upsert 预算定义、更新/读取 used_value 快照）、`ChannelDao`（渠道 CRUD + 软删除）结构同款，由 Codex 按上表字段实现，本节不重复贴码。
>
> `UsageDao` 两个查询的**返回列契约**（`TokenUsageStore.restore` 与 `/v1/admin/usage` 都依赖它，字段名必须一致）：
> - `List<Map<String, Object>> sumByTokenId()`：`SELECT token_id, SUM(token_in) + SUM(token_out) AS total_tokens, SUM(cost) AS total_cost FROM usage_record GROUP BY token_id`——`total_tokens`（token 数）与 `total_cost`（美元）同时返回，由调用方按令牌 quotaType 选量纲；
> - `Map<String, Object> summarize(String tokenId, String model, int days)`：同一 `GROUP BY` 语义 + `WHERE created_at >= ?` 过滤，返回 `token_in / token_out / cost / count` 汇总（管理端点展示用）。

### 7.3 `SchemaInitializer` 启动迁移（代码段 S4b，Codex）

```java
// 路径：governance/persistence/SchemaInitializer.java
/** 启动时执行 schema.sql（幂等）；预留 schema_version 表，V5 热更新启用版本化迁移 */
@Component
public class SchemaInitializer implements InitializingBean {

    private final JdbcTemplate jdbc;

    public SchemaInitializer(JdbcTemplate jdbc) { this.jdbc = jdbc; }

    @Override
    public void afterPropertiesSet() {
        // 读取 classpath:schema.sql 逐条执行（文件内全部是幂等 DDL）
        Resource res = new ClassPathResource("schema.sql");
        try (var reader = new BufferedReader(
                new InputStreamReader(res.getInputStream(), StandardCharsets.UTF_8))) {
            String sql = reader.lines()
                    .filter(l -> !l.isBlank() && !l.trim().startsWith("--"))
                    .collect(Collectors.joining("\n"));
            // H2 支持单语句 execute：按分号切分逐条执行（简单实现，不引 Flyway）
            for (String stmt : sql.split(";")) {
                if (!stmt.isBlank()) jdbc.execute(stmt);
            }
        } catch (IOException e) {
            throw new IllegalStateException("加载 schema.sql 失败", e);
        }
    }
}
```

> 启动顺序约束：`SchemaInitializer` 必须早于 `TokenManager` / `ChannelStore` / `BudgetManager` 的初始化（Spring `@DependsOn` 或让它们从 DAO 懒加载——Codex 在 `GovernanceBeans` 装配时处理，此处不展开）。

---

## 8. 令牌管理（关键代码段 S5，🖊 H1）

> 路径：`ai-gateway/src/main/java/com/aigateway/governance/token/TokenManager.java`

> 🖊 **手敲 H1**：本节全部手敲；`ApiToken` / `TokenStatus` / `TokenDao` 为脚手架。

### 8.1 令牌生命周期（先背下来）

```
创建：随机生成明文 token（agw_<32 hex>）→ SHA-256 哈希入库 → 明文只返回这一次
校验（每请求）：取 header → 按哈希查缓存 → 存在 → 启用 → 未过期 → IP → 模型范围 → 预检额度
吊销：删缓存 + 库标记 revoked_at；在途请求不追溯
```

### 8.2 主逻辑（代码段 S5，🖊 H1 全部手敲）

```java
// 路径：ai-gateway/src/main/java/com/aigateway/governance/token/TokenManager.java
package com.aigateway.governance.token;

/**
 * 令牌管理器：凭证的唯一守门人。
 *
 * 设计语义（对照学习版文档 4.3 / New API 令牌管理）：
 * 1. 创建时生成随机明文 token，**只返回一次**；库中只存 SHA-256 哈希——
 *    数据库泄露也拿不到可用凭证（密码存储思路）；
 * 2. 校验走「查缓存 → 逐条核对」的时序，全部通过才算 VALID；
 *    模型范围与额度预检需要请求上下文（alias / 预估用量），由 GovernanceService
 *    在校验后另行执行（本类只做令牌自身的检查）；
 * 3. 吊销 = 删缓存 + 库标记，即时生效；已发出的在途请求不追溯（New API 语义）；
 * 4. 缓存是热路径（读多写少），DB 是慢路径；校验失败不写缓存（防脏条目）。
 */
@Component
public class TokenManager {

    private static final Logger log = LoggerFactory.getLogger(TokenManager.class);
    /** 明文前缀：agw_ 便于日志识别与脱敏展示（agw_****） */
    public static final String PREFIX = "agw_";

    private final TokenDao dao;
    /** tokenHash -> ApiToken（热路径缓存；吊销时删除） */
    private final Map<String, ApiToken> cache = new ConcurrentHashMap<>();

    public TokenManager(TokenDao dao) {
        this.dao = dao;
        warmup();
    }

    /** 启动预热：全量加载到缓存（令牌量级小，全量加载最简单） */
    private void warmup() {
        for (ApiToken t : dao.findAll()) {
            cache.put(t.tokenHash(), t);
        }
        log.info("令牌缓存预热完成，共 {} 个", cache.size());
    }

    /**
     * 创建令牌：生成明文 → 哈希入库 → 返回（明文只在返回值里出现一次）。
     * @return record CreateResult(ApiToken token, String plain) —— 明文只此一份
     */
    public CreateResult create(String name, double quotaLimit, String quotaType,
                               String modelScope, String ipWhitelist, long expiresAt) {
        String id = PREFIX + randomHex(8);
        String plain = PREFIX + randomHex(32);           // 明文凭证
        ApiToken token = new ApiToken(id, name, sha256(plain), quotaLimit,
                quotaType, modelScope == null ? "" : modelScope,
                ipWhitelist == null ? "" : ipWhitelist,
                expiresAt, true, System.currentTimeMillis());
        dao.insert(token);
        cache.put(token.tokenHash(), token);
        log.info("令牌创建 id={} name={}（明文不再可见）", id, name);
        return new CreateResult(token, plain);
    }

    /** 创建结果：token 是管理对象（可查列表），plain 是唯一一次明文 */
    public record CreateResult(ApiToken token, String plain) {}

    /**
     * 校验一个原始令牌：只查令牌自身的状态（存在/启用/过期/IP）。
     * 模型范围与额度预检由调用方（GovernanceService）基于返回结果继续。
     */
    public TokenStatus validate(String plainToken, String clientIp) {
        if (plainToken == null || plainToken.isBlank()) {
            return TokenStatus.MISSING;
        }
        String hash = sha256(plainToken.trim());
        ApiToken t = cache.get(hash);
        if (t == null) {
            // 缓存未命中兜底查库（可能预热后被管理端点新增）；仍无 → 不存在
            t = dao.findByHash(hash);
            if (t == null) {
                return TokenStatus.UNKNOWN;
            }
            cache.put(hash, t);
        }
        if (!t.enabled()) {
            return TokenStatus.DISABLED;
        }
        if (t.expiresAt() >= 0 && System.currentTimeMillis() > t.expiresAt()) {
            return TokenStatus.EXPIRED;
        }
        if (!ipAllowed(t, clientIp)) {
            return TokenStatus.IP_DENIED;
        }
        return TokenStatus.VALID;
    }

    /** 吊销：删缓存 + 库标记（在途请求不追溯） */
    public void revoke(String tokenId) {
        ApiToken t = dao.findAll().stream()
                .filter(x -> x.id().equals(tokenId)).findFirst().orElse(null);
        if (t == null) {
            throw new GatewayException(404, "token_not_found", "令牌不存在: " + tokenId);
        }
        dao.revoke(tokenId);
        cache.remove(t.tokenHash());
        log.info("令牌吊销 id={}", tokenId);
    }

    /** 按管理 ID 查令牌（管理端点/脱敏列表用） */
    public Optional<ApiToken> findById(String tokenId) {
        return cache.values().stream().filter(t -> t.id().equals(tokenId)).findFirst()
                .or(() -> dao.findAll().stream().filter(t -> t.id().equals(tokenId)).findFirst());
    }

    /** 全部令牌（脱敏列表：只暴露 id / name / 前缀，不暴露哈希与明文） */
    public List<ApiToken> allTokens() {
        return List.copyOf(cache.values());
    }

    /** 模型范围校验：modelScope 为空放行，否则 alias 必须在列表中 */
    public boolean modelAllowed(ApiToken token, String alias) {
        if (token.modelScope() == null || token.modelScope().isBlank()) return true;
        return Arrays.stream(token.modelScope().split(","))
                .map(String::trim).anyMatch(alias::equals);
    }

    /** IP 白名单：空放行；否则精确匹配（本版本不做 CIDR，文档标注） */
    private static boolean ipAllowed(ApiToken token, String clientIp) {
        if (token.ipWhitelist() == null || token.ipWhitelist().isBlank()) return true;
        if (clientIp == null) return false;
        return Arrays.stream(token.ipWhitelist().split(","))
                .map(String::trim).anyMatch(clientIp::equals);
    }

    /** SHA-256 十六进制（凭证只存哈希） */
    public static String sha256(String input) {
        try {
            MessageDigest md = MessageDigest.getInstance("SHA-256");
            byte[] digest = md.digest(input.getBytes(StandardCharsets.UTF_8));
            return HexFormat.of().formatHex(digest);
        } catch (NoSuchAlgorithmException e) {
            throw new IllegalStateException("SHA-256 不可用", e);
        }
    }

    /** 随机十六进制串（SecureRandom 保证不可预测） */
    private static String randomHex(int bytes) {
        byte[] buf = new byte[bytes];
        new SecureRandom().nextBytes(buf);
        return HexFormat.of().formatHex(buf);
    }
}
```

设计要点：

- **哈希入库 + 明文一次**：`sha256` 用 `MessageDigest` + `HexFormat`（JDK 自带，零依赖）；`CreateResult` 保证明文只在创建响应里出现一次；
- **缓存读多写少**：校验命中缓存零 IO；吊销删缓存即失效；校验失败不写缓存，防止把“不存在”缓存成脏数据；
- **白名单不做 CIDR**：IP 精确匹配即可演示，CIDR 前缀匹配留 V5 管理面（文档标注，面试时说明取舍）；
- **模型范围与额度预检分离**：本类只做“你是谁”的检查；“你能用哪个模型、还剩多少钱”由 `GovernanceService`（14.1）接着做——职责单一，测试也好写。

### 8.3 单测（H1 完成后可跑）

```java
// 路径：ai-gateway/src/test/java/com/aigateway/governance/token/TokenManagerTest.java
@Test
void createThenValidate_plainOnlyOnce() {
    TokenManager mgr = new TokenManager(mock(TokenDao.class));
    TokenManager.CreateResult r = mgr.create("demo", -1, "COST", "", "", -1);

    assertThat(r.plain()).startsWith("agw_");
    // 明文在库中不存在：库里只有哈希
    verify(dao).insert(argThat(t -> t.tokenHash().equals(TokenManager.sha256(r.plain()))
            && !t.tokenHash().equals(r.plain())));
    // 用明文可校验通过
    assertThat(mgr.validate(r.plain(), "127.0.0.1")).isEqualTo(TokenStatus.VALID);
}

@Test
void validate_rejectsExpiredAndDisabledAndIp() {
    TokenManager mgr = new TokenManager(mock(TokenDao.class));
    var r = mgr.create("t", -1, "COST", "", "1.2.3.4",
            System.currentTimeMillis() - 1000);   // 已过期

    assertThat(mgr.validate(r.plain(), "1.2.3.4")).isEqualTo(TokenStatus.EXPIRED);

    var r2 = mgr.create("t2", -1, "COST", "", "1.2.3.4", -1);
    mgr.revoke(r2.token().id());                  // 吊销
    assertThat(mgr.validate(r2.plain(), "1.2.3.4")).isEqualTo(TokenStatus.DISABLED);

    var r3 = mgr.create("t3", -1, "COST", "", "1.2.3.4", -1);
    assertThat(mgr.validate(r3.plain(), "9.9.9.9")).isEqualTo(TokenStatus.IP_DENIED);
}

@Test
void modelScope_limitsAlias() {
    TokenManager mgr = new TokenManager(mock(TokenDao.class));
    var r = mgr.create("t", -1, "COST", "qwen", "", -1);
    assertThat(mgr.modelAllowed(r.token(), "qwen")).isTrue();
    assertThat(mgr.modelAllowed(r.token(), "deepseek-code")).isFalse();
}
```

---

## 9. 限流器（关键代码段 S6，🖊 H2）

> 路径：`ai-gateway/src/main/java/com/aigateway/governance/ratelimit/RateLimiter.java`

> 🖊 **手敲 H2**：本节全部手敲；`RateLimitKey` / `RateLimitRule` 为脚手架。

### 9.1 滑动窗口设计（先背下来）

```
窗口 = 10 个等宽子桶（环形数组）。请求到达：
1. 计算当前子桶下标；窗口滑过 k 个子桶 → 途经的子桶计数清零（旧样本自然过期）；
2. 窗口和 = 10 个子桶计数之和（O(1) 滑动窗口，不用存每个请求的时间戳）；
3. 窗口和 + cost ≤ limit → 放行并累加；否则拒绝。
```

为什么用环形子桶而不是时间戳队列：

- **无锁读 + 原子写**：写入只碰当前子桶（原子计数），读和是数组遍历——并发下吞吐远高于维护时间戳 Deque 加锁；
- **误差有界**：固定 10 个 100ms 子桶（1s 窗口）的滑窗精度 ±100ms，可配 `SLOTS` 调整（越细越准、内存越大）；
- 锁粒度 = 每 key 的桶（`synchronized(bucket)`），不是全局锁——不同令牌互不阻塞。

### 9.2 主逻辑（代码段 S6，🖊 H2 全部手敲）

```java
// 路径：ai-gateway/src/main/java/com/aigateway/governance/ratelimit/RateLimiter.java
package com.aigateway.governance.ratelimit;

/**
 * 滑动窗口限流器：按 key（令牌/模型/渠道/全局）独立计数。
 *
 * 两种模式（学习版 4.4 的双层治理第一半）：
 * - 普通模式：cost = 1，限制请求次数（QPS / RPM）；
 * - tokenMode：cost = 预估输入 token，请求结束时用实际用量 adjust 修正——
 *   解决“流式请求结束前不知道总量”的问题（预扣占位 + 结算修正）。
 *
 * 学习要点：
 * - 环形子桶滑动窗口：窗口和 O(1) 可算，旧样本随滑窗自动清零；
 * - synchronized 锁粒度 = 单个桶（每 key），多 key 互不阻塞；
 * - adjust 可为负：结算时用“实际 - 预估”回补，保证长期口径正确；
 * - 拒绝不计数：被拒请求没进窗口，窗口只统计真正放行的那部分。
 */
@Component
public class RateLimiter {

    private static final int SLOTS = 10;   // 子桶数：窗口被切成 10 段
    private final Map<BucketKey, Bucket> buckets = new ConcurrentHashMap<>();

    /** 桶的定位键：同 key 不同窗口（QPS 与 RPM）是独立桶 */
    private record BucketKey(RateLimitKey key, long windowMs) {}

    /** 一个滑动窗口桶：环形计数 + 惰性滑窗 */
    private static final class Bucket {
        final long windowMs;
        final long limit;
        final long[] counts = new long[SLOTS];
        int currentSlot;                 // 当前子桶下标
        long currentSlotStartMs;         // 当前子桶的开始时刻

        Bucket(long windowMs, long limit) {
            this.windowMs = windowMs;
            this.limit = limit;
            this.currentSlotStartMs = System.currentTimeMillis();
        }

        /** 窗口内已放行的总量（10 个槽求和） */
        long windowSum() {
            long sum = 0;
            for (long c : counts) sum += c;
            return sum;
        }
    }

    /**
     * 尝试放行：cost = 请求数（1）或预估 token 数。
     * @return true 放行；false 超限（调用方回 429）
     */
    public boolean tryAcquire(RateLimitRule rule, long cost) {
        Bucket bucket = buckets.computeIfAbsent(
                new BucketKey(rule.key(), rule.windowMs()),
                k -> new Bucket(rule.windowMs(), rule.limit()));
        synchronized (bucket) {
            slide(bucket, System.currentTimeMillis());
            long sum = bucket.windowSum();
            if (sum + cost > bucket.limit) {
                return false;                       // 拒绝：不计入窗口
            }
            bucket.counts[bucket.currentSlot] += cost;
            return true;
        }
    }

    /**
     * 结算修正：请求结束后用「实际 - 预估」回补（tokenMode 专用）。
     * delta 为负 = 实际少于预估（占位过多，回吐）；为正 = 实际超出预估（补记）。
     */
    public void adjust(RateLimitKey key, long windowMs, long delta) {
        Bucket bucket = buckets.get(new BucketKey(key, windowMs));
        if (bucket == null) return;                 // 从未占位过：无需修正
        synchronized (bucket) {
            slide(bucket, System.currentTimeMillis());
            // 只修正当前槽；跨槽误差在窗口尺度上自动弥合
            bucket.counts[bucket.currentSlot] = Math.max(0,
                    bucket.counts[bucket.currentSlot] + delta);
        }
    }

    /** 惰性滑窗：时间前进了 k 个子桶 → 途经槽清零，指针前移（旧样本过期） */
    private static void slide(Bucket bucket, long nowMs) {
        long elapsed = nowMs - bucket.currentSlotStartMs;
        long step = elapsed / (bucket.windowMs / SLOTS);   // 滑过多少个子桶
        if (step <= 0) return;
        if (step >= SLOTS) {
            // 一整个窗口都过去了：全部清零（等价于全新窗口）
            java.util.Arrays.fill(bucket.counts, 0);
            bucket.currentSlot = 0;
        } else {
            for (int i = 1; i <= step; i++) {
                int idx = (bucket.currentSlot + i) % SLOTS;
                bucket.counts[idx] = 0;
            }
            bucket.currentSlot = (int) ((bucket.currentSlot + step) % SLOTS);
        }
        bucket.currentSlotStartMs = nowMs - (elapsed % (bucket.windowMs / SLOTS));
    }

    /** 当前窗口内的累计值（测试/指标用；线程安全：synchronized 读） */
    public long windowCount(RateLimitKey key, long windowMs) {
        Bucket bucket = buckets.get(new BucketKey(key, windowMs));
        if (bucket == null) return 0;
        synchronized (bucket) {
            slide(bucket, System.currentTimeMillis());
            return bucket.windowSum();
        }
    }
}
```

设计要点：

- **拒绝不计数**：被 429 的请求不占用窗口额度，避免“越拒绝越堵死”（与熔断 OPEN 期不采样同思想）；
- **`slide` 的正确性**：`step >= SLOTS` 时全清（等价新窗口）；否则只清途经槽。`currentSlotStartMs` 用余数校准，保证多次小步滑动累计正确；
- **QPS 与 RPM 是独立桶**：`BucketKey = key + windowMs`，同一令牌的 1s 窗与 60s 窗互不干扰（双层限流语义：短窗管突发、长窗管总量）；
- **tokenMode 修正**：`adjust` 只改当前槽，跨槽误差在窗口尺度自动弥合——不为“精确到毫秒”牺牲复杂度。

### 9.3 单测（H2 完成后可跑）

```java
// 路径：ai-gateway/src/test/java/com/aigateway/governance/ratelimit/RateLimiterTest.java
@Test
void sixthConcurrentRequest_rejected() {
    RateLimiter limiter = new RateLimiter();
    RateLimitRule rule = new RateLimitRule(RateLimitKey.token("tok-1"), 1000, 5, false);

    for (int i = 0; i < 5; i++) assertThat(limiter.tryAcquire(rule, 1)).isTrue();
    assertThat(limiter.tryAcquire(rule, 1)).isFalse();          // 第 6 个 → 429
    assertThat(limiter.windowCount(RateLimitKey.token("tok-1"), 1000)).isEqualTo(5);
}

@Test
void windowRollsOver_afterTimeout() throws InterruptedException {
    RateLimiter limiter = new RateLimiter();
    RateLimitRule rule = new RateLimitRule(RateLimitKey.token("tok-2"), 100, 2, false);

    limiter.tryAcquire(rule, 1);
    limiter.tryAcquire(rule, 1);
    assertThat(limiter.tryAcquire(rule, 1)).isFalse();
    Thread.sleep(120);                                          // 窗口过去
    assertThat(limiter.tryAcquire(rule, 1)).isTrue();           // 计数恢复
}

@Test
void tokenMode_placeholderThenAdjust() {
    RateLimiter limiter = new RateLimiter();
    RateLimitRule rule = new RateLimitRule(RateLimitKey.token("tok-3"), 1000, 100, true);

    assertThat(limiter.tryAcquire(rule, 60)).isTrue();          // 预扣预估 60
    assertThat(limiter.tryAcquire(rule, 50)).isFalse();         // 60+50 > 100
    limiter.adjust(RateLimitKey.token("tok-3"), 1000, -20);     // 实际 40：回吐 20
    assertThat(limiter.tryAcquire(rule, 50)).isTrue();          // 40+50 ≤ 100
}

@Test
void dimensions_areIsolated() {
    RateLimiter limiter = new RateLimiter();
    RateLimitRule a = new RateLimitRule(RateLimitKey.token("tok-a"), 1000, 1, false);
    RateLimitRule b = new RateLimitRule(RateLimitKey.token("tok-b"), 1000, 1, false);
    assertThat(limiter.tryAcquire(a, 1)).isTrue();
    assertThat(limiter.tryAcquire(b, 1)).isTrue();              // 互不影响
    assertThat(limiter.tryAcquire(a, 1)).isFalse();
}
```

---

## 10. 预算管理器（关键代码段 S7，🖊 H3）

> 路径：`ai-gateway/src/main/java/com/aigateway/governance/budget/BudgetManager.java`

> 🖊 **手敲 H3**：本节全部手敲；`Budget` / `BudgetScope` / `BudgetPeriod` / `LimitType` / `BudgetDao` 为脚手架。

### 10.1 预检 → 扣减 → 结算（先背下来）

```
请求开始：precheck（余额 > 0？）        → 不足 → 429 budget_exceeded / 降级信号
请求结束：consume（按实际用量原子扣减）  → 只有成功响应才扣钱（失败不结算）
周期切换：惰性重置（读写前检查周期号，变了从 0 开始）
预警：扣减后跨过 70% / 90% 阈值 → 日志 + 指标（软预算，不阻断）
```

为什么“预检与扣减分开”（学习版 Q2）：请求开始前不知道实际用量（尤其流式），只能先看余额够不够放行；结束时按实际算账。中间“预检通过但请求失败”——失败就不扣钱。

### 10.2 主逻辑（代码段 S7，🖊 H3 全部手敲）

```java
// 路径：ai-gateway/src/main/java/com/aigateway/governance/budget/BudgetManager.java
package com.aigateway.governance.budget;

/**
 * 预算管理器：管「总量」的第二层治理（对照学习版 4.6 / LiteLLM budget）。
 *
 * 设计语义：
 * 1. 内存预算对象是热路径（预检 / 扣减 O(1)），DB 的 used_value 是慢路径对账快照
 *    （由 UsageLedger 的 flush 顺带落库，重启后从库恢复）；
 * 2. 扣减用「小锁 + 比较写」：锁粒度 = 单个预算对象（不是全局、不是整个请求），
 *    并发扣同一预算串行化，不同预算互不阻塞——这就是「并发下不超扣」的实现；
 * 3. 周期惰性重置：periodChanged 才重置，不跑定时任务扫表；
 * 4. 预警是软预算：跨过 warnRatios 阈值记日志 + 指标，不阻断请求；
 * 5. 超额动作（REJECT / DEGRADE）由调用方按 exceedAction 配置执行，
 *    本类只回报 ConsumeResult 与预检结果。
 */
@Component
public class BudgetManager {

    private static final Logger log = LoggerFactory.getLogger(BudgetManager.class);

    /** 预算定位键：scope + scopeValue（同 scope 同 value 只允许一个限额定义） */
    private record BudgetKey(BudgetScope scope, String scopeValue) {}

    private final BudgetDao dao;
    private final List<Double> warnRatios;      // 如 [0.7, 0.9]
    private final GatewayMetrics metrics;
    private final Map<BudgetKey, Budget> budgets = new ConcurrentHashMap<>();
    private final Map<BudgetKey, Integer> warnedLevel = new ConcurrentHashMap<>(); // 已触发到第几档

    public BudgetManager(BudgetDao dao, List<Double> warnRatios, GatewayMetrics metrics) {
        this.dao = dao;
        this.warnRatios = warnRatios.stream().sorted().toList();
        this.metrics = metrics;
        loadFromDb();
    }

    /** 启动加载：DB 里的预算定义 + used 快照（丢掉的未对账窗口是已知取舍） */
    private void loadFromDb() {
        for (Budget b : dao.findAll()) {
            budgets.put(new BudgetKey(b.scope(), b.scopeValue()), b);
        }
        log.info("预算加载完成，共 {} 条", budgets.size());
    }

    /** 创建或替换一个预算定义（管理端点调用；同 key 覆盖） */
    public Budget upsert(BudgetScope scope, String scopeValue, LimitType limitType,
                         BudgetPeriod period, double limit) {
        if (limit <= 0) {
            throw new GatewayException(400, "invalid_request", "预算上限必须 > 0");
        }
        Budget b = new Budget(scope, scopeValue, limitType, period, limit);
        budgets.put(new BudgetKey(scope, scopeValue), b);
        warnedLevel.put(new BudgetKey(scope, scopeValue), 0);
        dao.upsert(b);
        log.info("预算设置 scope={} value={} type={} period={} limit={}",
                scope, scopeValue, limitType, period, limit);
        return b;
    }

    /**
     * 预检：当前周期剩余是否 >= 本次预估用量（不改变任何状态）。
     * 两个量纲参数（tokenAmount / costAmount）由预算自身的 limitType 决定用哪个——
     * 调用方无脑传双值，本方法按预算配置换算，避免「令牌是 COST、预算是 TOKEN」
     * 时把美元当 token 数扣的量纲错乱。
     * @return true = 可放行
     */
    public boolean precheck(BudgetScope scope, String scopeValue,
                            long tokenAmount, double costAmount) {
        Budget b = budgets.get(new BudgetKey(scope, scopeValue));
        if (b == null) return true;                 // 未配置预算 = 不限
        synchronized (b) {
            lazyResetIfNeeded(b);
            long unit = b.toUnit(b.limitType() == LimitType.TOKEN ? tokenAmount : costAmount);
            long limitUnit = b.toUnit(b.limit());
            // 整数比较（微美元/token），不经 double 往返，无精度损失
            return b.usedMicro().get() + unit <= limitUnit;
        }
    }

    /** 剩余比率（Scorer 的 budget_remaining_ratio 信号输入）；未配置预算返回 1.0 */
    public double remainingRatio(BudgetScope scope, String scopeValue) {
        Budget b = budgets.get(new BudgetKey(scope, scopeValue));
        if (b == null) return 1.0;
        synchronized (b) {
            lazyResetIfNeeded(b);
            return b.remainingRatio();
        }
    }

    /**
     * 结算扣减：按实际用量原子扣减（只有成功响应才调用）。
     * 与 precheck 同量纲约定：tokenAmount / costAmount 双值传入，按预算 limitType 取用。
     * @return OK = 扣减成功；EXCEEDED = 超出上限（拒绝，调用方回 429 budget_exceeded）
     */
    public ConsumeResult consume(BudgetScope scope, String scopeValue,
                                 long tokenAmount, double costAmount) {
        Budget b = budgets.get(new BudgetKey(scope, scopeValue));
        if (b == null) return ConsumeResult.ok(1.0);   // 未配置 = 不扣
        synchronized (b) {
            lazyResetIfNeeded(b);
            long unit = b.toUnit(b.limitType() == LimitType.TOKEN ? tokenAmount : costAmount);
            long limitUnit = b.toUnit(b.limit());
            long used = b.usedMicro().get();
            if (used + unit > limitUnit) {
                return ConsumeResult.exceeded(b.remainingRatio());
            }
            b.usedMicro().set(used + unit);
        }
        checkWarnings(scope, scopeValue, b);
        return ConsumeResult.ok(b.remainingRatio());
    }

    /** 扣减结果：remainingRatio 供上层写信号/指标 */
    public record ConsumeResult(boolean ok, double remainingRatio) {
        static ConsumeResult ok(double ratio)      { return new ConsumeResult(true, ratio); }
        static ConsumeResult exceeded(double ratio) { return new ConsumeResult(false, ratio); }
    }

    /** 预警：跨过第 i 档阈值（70% / 90%）才记一次（避免每请求重复刷日志） */
    private void checkWarnings(BudgetScope scope, String scopeValue, Budget b) {
        double ratio = b.remainingRatio();
        double usedRatio = 1 - ratio;
        BudgetKey key = new BudgetKey(scope, scopeValue);
        int level = warnedLevel.getOrDefault(key, 0);
        int next = 0;
        for (int i = level; i < warnRatios.size(); i++) {
            if (usedRatio >= warnRatios.get(i)) next = i + 1;
        }
        if (next > level) {
            warnedLevel.put(key, next);
            // 软预算：只记录，不阻断（学习版 4.6 预警语义）
            log.warn("预算预警 scope={} value={} 已用 {}%（上限 {}）",
                    scope, scopeValue, Math.round(usedRatio * 100), b.limit());
            metrics.budgetWarning(scope.name(), scopeValue);
        }
    }

    /** 周期切换的惰性重置：读写预算前调用（在 synchronized 内） */
    private static void lazyResetIfNeeded(Budget b) {
        if (b.periodChanged(System.currentTimeMillis())) {
            b.resetPeriod(b.period().periodId(System.currentTimeMillis()));
        }
    }

    /** 对账落库：把内存 used 快照写回 DB（由 UsageLedger 定时 flush 触发） */
    public void flushUsed() {
        for (Map.Entry<BudgetKey, Budget> e : budgets.entrySet()) {
            Budget b = e.getValue();
            synchronized (b) {
                lazyResetIfNeeded(b);
                dao.updateUsed(b.scope(), b.scopeValue(), b.limitType(), b.period(), b.used());
            }
        }
    }

    /** 全部预算（管理端点列表用） */
    public List<Budget> allBudgets() {
        return List.copyOf(budgets.values());
    }
}
```

> `metrics.budgetWarning(...)` 见 17.1 的 `GatewayMetrics` 扩展。

设计要点：

- **小锁 + 原子计数**：锁粒度 = 单预算对象，锁内只做“周期重置 + 读改写”三项轻量操作，绝不包住请求流程（学习版 4.4 并发安全条目）；
- **不超扣的保证**：`used + unit > limitUnit` 在锁内比较后写——两个并发请求的扣减串行化，永远不会有第二笔把 used 推过上限（对比“先读、再算、后写”三步分开的经典错误）；
- **失败不结算**：`consume` 只被成功路径调用（14.2 接线），预检通过但失败的请求不扣钱；
- **对账是慢路径**：内存扣减即时生效，`flushUsed` 定时把快照写库——重启丢的是未对账窗口，而不是全部账目。

### 10.3 单测（H3 完成后可跑）

```java
// 路径：ai-gateway/src/test/java/com/aigateway/governance/budget/BudgetManagerTest.java
@Test
void concurrentConsume_neverExceedsLimit() throws InterruptedException {
    BudgetManager mgr = new BudgetManager(mock(BudgetDao.class), List.of(0.7, 0.9), mock(GatewayMetrics.class));
    mgr.upsert(BudgetScope.GLOBAL, "", LimitType.TOKEN, BudgetPeriod.DAILY, 100);

    // 20 个线程各扣 10：总扣 200，上限 100 → 恰好 10 个成功
    int threads = 20;
    ExecutorService pool = Executors.newFixedThreadPool(threads);
    CountDownLatch start = new CountDownLatch(1);
    List<Future<Boolean>> futures = new ArrayList<>();
    for (int i = 0; i < threads; i++) {
        futures.add(pool.submit(() -> {
            start.await();
            return mgr.consume(BudgetScope.GLOBAL, "", 10, 0).ok();
        }));
    }
    start.countDown();
    long ok = futures.stream().filter(f -> {
        try { return f.get(); } catch (Exception e) { throw new RuntimeException(e); }
    }).count();
    pool.shutdown();

    assertThat(ok).isEqualTo(10);                       // 并发下不超扣
    assertThat(mgr.remainingRatio(BudgetScope.GLOBAL, "")).isEqualTo(0.0);
}

@Test
void periodRollover_resetsUsage() {
    BudgetManager mgr = new BudgetManager(mock(BudgetDao.class), List.of(), mock(GatewayMetrics.class));
    mgr.upsert(BudgetScope.GLOBAL, "", LimitType.TOKEN, BudgetPeriod.HOURLY, 100);
    mgr.consume(BudgetScope.GLOBAL, "", 60, 0);
    assertThat(mgr.remainingRatio(BudgetScope.GLOBAL, "")).isEqualTo(0.4);

    // 直接改 Budget 的周期号模拟跨小时（避免真实等待）
    Budget b = mgr.allBudgets().get(0);
    b.resetPeriod("2026081000");                        // 上一个周期号
    assertThat(mgr.remainingRatio(BudgetScope.GLOBAL, "")).isEqualTo(1.0); // 惰性重置
}

@Test
void precheck_doesNotDeduct() {
    BudgetManager mgr = new BudgetManager(mock(BudgetDao.class), List.of(), mock(GatewayMetrics.class));
    mgr.upsert(BudgetScope.GLOBAL, "", LimitType.TOKEN, BudgetPeriod.DAILY, 100);
    assertThat(mgr.precheck(BudgetScope.GLOBAL, "", 100, 0)).isTrue();
    assertThat(mgr.precheck(BudgetScope.GLOBAL, "", 1, 0)).isFalse();   // 预检不改状态
    assertThat(mgr.remainingRatio(BudgetScope.GLOBAL, "")).isEqualTo(1.0);
}
```

---

## 11. Token 计量与成本（关键代码段 S8，🖊 H4）

> 路径：`ai-gateway/src/main/java/com/aigateway/governance/meter/`

> 🖊 **手敲 H4**：`TokenMeter` 全部手敲；`TokenEstimator`（公共估算器，从 Scorer 抽取）与 `MeteringCallback` 签名对齐为 Codex 脚手架。

### 11.1 计量口径（先背下来）

```
非流式：上游 usage 优先（prompt/completion/total 全有才用）→ 缺失用近似：
        输入 = 请求 messages 文本估算；输出 = 响应文本估算
流式：输入 = 请求文本估算（发出前就知道）；输出 = 逐 chunk 增量累计
      （结束 chunk 携带 usage 时用真实 completion_tokens 覆盖）
近似算法（误差明确标注）：CJK 约 1 字/token；其他约 4 字符/token
成本：cost = tokenIn/1000 × priceIn + tokenOut/1000 × priceOut（单价表 = ModelInstance）
```

为什么近似（学习版 Q3）：Java 没有官方 tokenizer，精确计数要么引第三方库要么自己实现 BPE，复杂度高；近似算法误差可控、文档写明误差范围（验收：误差 < 15%）。精确化（引入 jtokkit）列为版本 6 可选能力。

### 11.2 公共估算器（代码段 S8a，Codex）

```java
// 路径：ai-gateway/src/main/java/com/aigateway/governance/meter/TokenEstimator.java
/** 文本 → token 近似估算（从 Scorer 抽取，口径与 mock 服务端一致）：
 *  CJK 字符约 cjkTokenPerChar 字/token（默认 1），其余约 charsPerToken 字符/token（默认 4）。
 *  V4 起 Scorer / CapabilityFilter / TokenMeter 统一复用本类，消除口径漂移。 */
public final class TokenEstimator {

    private TokenEstimator() {}

    public static long estimateTextTokens(String text, double charsPerToken, double cjkTokenPerChar) {
        long cjk = 0, other = 0;
        for (char c : text.toCharArray()) {
            if (Character.UnicodeScript.of(c) == Character.UnicodeScript.HAN) cjk++;
            else other++;
        }
        return (long) (cjk * cjkTokenPerChar) + (long) Math.ceil(other / charsPerToken);
    }

    public static long estimateInputTokens(ChatRequest request,
                                           double charsPerToken, double cjkTokenPerChar) {
        if (request.messages() == null) return 1;
        long tokens = request.messages().stream()
                .map(ChatRequest.Message::content)
                .filter(s -> s != null)
                .mapToLong(s -> estimateTextTokens(s, charsPerToken, cjkTokenPerChar))
                .sum();
        return Math.max(1, tokens);
    }
}
```

> 脚手架改动：`Scorer` 的两个私有估算方法替换为调用 `TokenEstimator`（系数取 `GovernanceProperties.estimation` 的缺省值），`CapabilityFilter` 同款处理——行为不变，只统一口径。

### 11.3 `TokenMeter` 主逻辑（代码段 S8，🖊 H4 全部手敲）

```java
// 路径：ai-gateway/src/main/java/com/aigateway/governance/meter/TokenMeter.java
package com.aigateway.governance.meter;

/**
 * Token 计量器：把一次请求的真实/近似用量换算成 MeteredUsage（收银机）。
 *
 * 两种入口：
 * - meterNonStream：非流式，响应后一次性计量（usage 优先，缺失近似）；
 * - 流式：实现 MeteringCallback（第 14.3 节），onChunk 增量累计输出 token，
 *   结束 chunk 有 usage 时用真实值覆盖，onFinish 产出 MeteredUsage。
 *
 * 误差说明（学习版 4.5 / 验收 2）：近似算法按字符数与语言系数估算，
 * 中文约 1 字/token、英文约 4 字符/token；流式输出按 chunk 文本增量累计，
 * 结束 chunk 的 usage（mock 与多数真实上游会带）优先，误差 < 15% 达标。
 */
@Component
public class TokenMeter implements MeteringCallback {

    private final double charsPerToken;
    private final double cjkTokenPerChar;
    private final Map<String, StreamTracker> trackers = new ConcurrentHashMap<>();
    /** 已结算结果：requestId -> MeteredResult（onFinish 写入，GovernanceService.takeResult 取走） */
    private final Map<String, MeteredResult> finished = new ConcurrentHashMap<>();

    /** 结算结果：携带实际执行实例（MeteringContext 里是真实候选，
     *  不是「首个候选」——预算扣减必须按实际走了谁扣，审查修正 #4） */
    public record MeteredResult(ModelInstance instance, MeteredUsage usage) {}

    public TokenMeter(GovernanceProperties props) {
        this.charsPerToken = props.getEstimation().getCharsPerToken();
        this.cjkTokenPerChar = props.getEstimation().getCjkTokenPerChar();
    }

    /** 非流式计量：usage 三个字段齐全则用真实值，否则近似补齐 */
    public MeteredUsage meterNonStream(ModelInstance inst, ChatRequest request,
                                       ChatCompletion completion) {
        boolean estimated = false;
        long tokenIn;
        long tokenOut;
        if (completion.usage() != null
                && completion.usage().totalTokens() > 0) {
            tokenIn = completion.usage().promptTokens();
            tokenOut = completion.usage().completionTokens();
        } else {
            estimated = true;
            tokenIn = estimateInput(request);
            tokenOut = estimateOutputText(responseText(completion));
        }
        return new MeteredUsage(tokenIn, tokenOut, cost(inst, tokenIn, tokenOut), estimated);
    }

    // ── MeteringCallback：流式增量计量 ──

    @Override
    public void onChunk(MeteringContext ctx, ChatChunk chunk) {
        StreamTracker tracker = trackers.computeIfAbsent(ctx.requestId(), k -> new StreamTracker(ctx));
        tracker.accumulate(chunk);        // 增量累计 + 结束 chunk usage 覆盖
    }

    @Override
    public MeteredUsage onFinish(MeteringContext ctx, ChatCompletion.Usage usage, boolean success) {
        StreamTracker tracker = trackers.remove(ctx.requestId());
        MeteredResult result;
        if (tracker == null) {
            // 异常路径兜底：按请求文本估算输入，输出 0（失败请求不扣预算，仅留痕）
            long in = estimateInput(ctx.request());
            result = new MeteredResult(ctx.instance(),
                    new MeteredUsage(in, 0, cost(ctx.instance(), in, 0), true));
        } else {
            if (usage != null && usage.totalTokens() > 0) {
                tracker.applyFinalUsage(usage);            // 真实 usage 覆盖累计值
            }
            MeteredUsage u = tracker.result(ctx.instance());
            if (!success) {
                // 失败请求：仍留痕（result=failure），但不扣预算（成本归零）
                u = new MeteredUsage(u.tokenIn(), u.tokenOut(), 0, u.estimated());
            }
            result = new MeteredResult(ctx.instance(), u); // 真实候选实例
        }
        if (success) {
            finished.put(ctx.requestId(), result);         // 供 GovernanceService.takeResult 取走
        }
        // 失败请求不写 finished：catch 路径只留痕不结算，写进去会残留（审查修正 #6）
        return result.usage();
    }

    /** 流结束后取最终计量结果（从 finished 移除；无则用 fallback 实例按文本估算兜底） */
    public MeteredResult takeResult(String requestId, ModelInstance fallback, ChatRequest request) {
        MeteredResult result = finished.remove(requestId);
        if (result == null) {
            long in = estimateInput(request);
            result = new MeteredResult(fallback,
                    new MeteredUsage(in, 0, cost(fallback, in, 0), true));
        }
        return result;
    }

    /** 流式累计器：一个请求一个实例（按 requestId 索引） */
    private final class StreamTracker {
        private final MeteringContext ctx;
        private long tokenOut;
        private boolean hasRealUsage;

        StreamTracker(MeteringContext ctx) { this.ctx = ctx; }

        void accumulate(ChatChunk chunk) {
            if (chunk.choices() == null) return;
            for (ChatChunk.ChunkChoice choice : chunk.choices()) {
                if (choice.delta() != null && choice.delta().content() != null
                        && !choice.delta().content().isEmpty()) {
                    tokenOut += TokenEstimator.estimateTextTokens(
                            choice.delta().content(), charsPerToken, cjkTokenPerChar);
                }
            }
        }

        void applyFinalUsage(ChatCompletion.Usage usage) {
            tokenOut = usage.completionTokens();       // 真实值覆盖近似累计
            hasRealUsage = true;
        }

        MeteredUsage result(ModelInstance inst) {
            long in = estimateInput(ctx.request());
            return new MeteredUsage(in, tokenOut,
                    cost(inst, in, tokenOut), !hasRealUsage);
        }
    }

    /** 成本核算：单价表（每 1K tokens）× 实际用量 */
    public double cost(ModelInstance inst, long tokenIn, long tokenOut) {
        return tokenIn / 1000.0 * inst.priceIn() + tokenOut / 1000.0 * inst.priceOut();
    }

    private long estimateInput(ChatRequest request) {
        return TokenEstimator.estimateInputTokens(request, charsPerToken, cjkTokenPerChar);
    }

    private long estimateOutputText(String text) {
        return text == null || text.isBlank() ? 0
                : TokenEstimator.estimateTextTokens(text, charsPerToken, cjkTokenPerChar);
    }

    /** 非流式响应全文拼起来（近似输出用） */
    private static String responseText(ChatCompletion completion) {
        if (completion.choices() == null) return "";
        return completion.choices().stream()
                .map(c -> c.message() == null ? "" : c.message().content())
                .filter(s -> s != null)
                .collect(Collectors.joining());
    }
}
```

> `MeteringContext` 与 `MeteringCallback` 的 V4 定义见第 14.3 节（V3 计划中的无上下文空实现按本节签名对齐）。

### 11.4 单测（H4 完成后可跑）

```java
// 路径：ai-gateway/src/test/java/com/aigateway/governance/meter/TokenMeterTest.java
@Test
void nonStream_usesUpstreamUsage_whenPresent() {
    TokenMeter meter = new TokenMeter(testProps());
    ModelInstance inst = instance(0.001, 0.002);
    ChatCompletion completion = new ChatCompletion("id", "chat.completion", 0, "m",
            List.of(), new ChatCompletion.Usage(100, 200, 300));

    MeteredUsage u = meter.meterNonStream(inst, request("你好"), completion);
    assertThat(u.tokenIn()).isEqualTo(100);
    assertThat(u.tokenOut()).isEqualTo(200);
    assertThat(u.estimated()).isFalse();
    assertThat(u.cost()).isCloseTo(100 / 1000.0 * 0.001 + 200 / 1000.0 * 0.002, within(1e-9));
}

@Test
void nonStream_fallsBackToEstimate_whenUsageMissing() {
    TokenMeter meter = new TokenMeter(testProps());
    ModelInstance inst = instance(0.001, 0.002);
    ChatCompletion completion = new ChatCompletion("id", "chat.completion", 0, "m",
            List.of(new ChatCompletion.Choice(0,
                    new ChatCompletion.Choice.Message("assistant", "你好，我是助手"), "stop")),
            null);

    MeteredUsage u = meter.meterNonStream(inst, request("你好"), completion);
    assertThat(u.estimated()).isTrue();
    assertThat(u.tokenIn()).isGreaterThanOrEqualTo(1);     // “你好”约 2 token
    assertThat(u.tokenOut()).isGreaterThanOrEqualTo(1);
}

@Test
void stream_accumulatesDeltas() {
    TokenMeter meter = new TokenMeter(testProps());
    MeteringContext ctx = ctx("rid-1");
    ChatChunk c1 = chunk("你");
    ChatChunk c2 = chunk("好");
    meter.onChunk(ctx, c1);
    meter.onChunk(ctx, c2);

    // 结束 chunk 带真实 usage → 覆盖累计
    MeteredUsage u = meter.onFinish(ctx, new ChatCompletion.Usage(10, 5, 15), true);
    assertThat(u.tokenOut()).isEqualTo(5);
    assertThat(u.estimated()).isFalse();
}

@Test
void failureRequest_hasZeroCost() {
    TokenMeter meter = new TokenMeter(testProps());
    MeteringContext ctx = ctx("rid-2");
    meter.onChunk(ctx, chunk("好"));
    MeteredUsage u = meter.onFinish(ctx, null, false);      // 失败
    assertThat(u.cost()).isZero();                          // 失败不扣钱
}

// —— 测试辅助 ——
private static GovernanceProperties testProps() {
    GovernanceProperties props = new GovernanceProperties();
    props.getEstimation().setCharsPerToken(4.0);
    props.getEstimation().setCjkTokenPerChar(1.0);
    return props;
}

private static ModelInstance instance(double priceIn, double priceOut) {
    return new ModelInstance("mock-a:qwen", "qwen", "mock-a", "qwen",
            1, null, priceIn, priceOut, 0.5, 1000);
}

private static ChatRequest request(String content) {
    return new ChatRequest("qwen",
            List.of(new ChatRequest.Message("user", content)), false, null, null);
}

private static MeteringContext ctx(String rid) {
    return new MeteringContext(rid, instance(0.001, 0.002), request("你好"), "agw_00000001");
}

private static ChatChunk chunk(String delta) {
    return new ChatChunk("id-1", "chat.completion.chunk", 0, "qwen",
            List.of(new ChatChunk.ChunkChoice(0,
                    new ChatChunk.ChunkChoice.Delta(delta), null)));
}
```

---

## 12. 预算降级闭环（关键代码段 S9，🖊 H5）

> 路径：`ai-gateway/src/main/java/com/aigateway/decision/filter/BudgetFilter.java`（改造现有占位）+ `ai-gateway/src/main/java/com/aigateway/governance/plugin/GovernanceBootstrapPlugin.java`

> 🖊 **手敲 H5**：`BudgetFilter` 的淘汰逻辑全部手敲；`GovernanceBootstrapPlugin`、`TokenUsageStore`（令牌额度内存账本）为 Codex 脚手架。

### 12.1 信号链路（先背下来）

```
GovernanceBootstrapPlugin（BEFORE_DECISION，order=100）
  ├─ 写 governance.token_id                 → BudgetFilter 查令牌额度
  ├─ 写 governance.token_remaining_ratio    → Scorer 动态调权备用
  └─ 写 governance.budget_exceeded_channels/models（渠道/模型预算触顶集合）
        ↓
BudgetFilter（第 4 道关卡，order=20）
  ├─ 渠道预算触顶的候选 → 淘汰
  ├─ 模型预算触顶的候选 → 淘汰
  └─ 令牌额度付不起的候选 → 淘汰（DEGRADE 模式的自动降级核心）
        ↓
Scorer（读 budget_remaining_ratio 信号 → 成本权重自动放大，**零改动**）
```

这就是“预算插件写信号、决策链消费”的完整闭环：不需要改执行层、不需要改 Scorer，全靠 V2 的信号机制拼装——学习版 4.6 说的“预算插件写信号，版本 3 降级链消费”在这里落地为“预算信号 + 决策链淘汰”。

### 12.2 令牌额度账本（代码段 S9a，Codex）

```java
// 路径：ai-gateway/src/main/java/com/aigateway/governance/budget/TokenUsageStore.java
/**
 * 令牌额度账本（内存热路径）：
 * - COST 类型按微美元累计（整数防精度问题），TOKEN 类型按 token 数累计；
 * - 启动时从 usage_record 按 tokenId 聚合恢复（账本即用量表，重启不丢账；
 *   无需在 api_token 表冗余 used_value 快照列——用量表就是持久化账本）；
 * - consume 用 CAS 循环防超扣（与 BudgetManager 同思想）。
 */
@Component
public class TokenUsageStore {

    private final TokenDao tokenDao;
    private final UsageDao usageDao;
    private final Map<String, AtomicLong> used = new ConcurrentHashMap<>(); // tokenId -> 微美元/token 数

    public TokenUsageStore(TokenDao tokenDao, UsageDao usageDao) {
        this.tokenDao = tokenDao;
        this.usageDao = usageDao;
        restore();
    }

    /**
     * 重启恢复：用量表 SUM 即令牌账本（验收标准 1「重启不丢账」的落点）。
     * 换算口径（审查修正 #2）：restore 必须按令牌 quotaType 分支——
     * COST 令牌从 SUM(cost) 转微美元、TOKEN 令牌从 SUM(token_in + token_out) 取整；
     * 不按 quotaType 换算会把美元当 token 数（或反之），重启后额度对不上账。
     */
    private void restore() {
        Map<String, ApiToken> tokens = tokenDao.findAll().stream()
                .collect(Collectors.toMap(ApiToken::id, t -> t));
        for (Map<String, Object> row : usageDao.sumByTokenId()) {
            String tokenId = (String) row.get("token_id");
            ApiToken token = tokens.get(tokenId);
            if (token == null) continue;                     // 已吊销/删除的令牌跳过
            long unit = "TOKEN".equals(token.quotaType())
                    ? ((Number) row.get("total_tokens")).longValue()
                    : Math.round(((Number) row.get("total_cost")).doubleValue() * 1_000_000);
            used.computeIfAbsent(tokenId, k -> new AtomicLong()).addAndGet(unit);
        }
    }

    /** 令牌剩余额度（quotaLimit - 已用）；返回负数表示已超支（预检用） */
    public double remaining(ApiToken token) {
        if (token.unlimited()) return Double.MAX_VALUE;
        long unit = used.computeIfAbsent(token.id(), k -> new AtomicLong()).get();
        return limitToUnit(token) - unit;
    }

    /** 剩余比率 0~1（Scorer 信号）；无限额度恒为 1 */
    public double remainingRatio(ApiToken token) {
        if (token.unlimited()) return 1.0;
        long limit = limitToUnit(token);
        long u = used.computeIfAbsent(token.id(), k -> new AtomicLong()).get();
        return limit <= 0 ? 0 : Math.max(0, Math.min(1, (limit - u) / (double) limit));
    }

    /** 能否支付 amount（预检）；amount 单位与 quotaType 一致 */
    public boolean canPay(ApiToken token, double amount) {
        return remaining(token) >= toUnit(token, amount);
    }

    /** 结算扣减：CAS 循环，不超扣 */
    public boolean consume(ApiToken token, double amount) {
        if (token.unlimited()) return true;
        AtomicLong counter = used.computeIfAbsent(token.id(), k -> new AtomicLong());
        long unit = toUnit(token, amount);
        long limit = limitToUnit(token);
        while (true) {
            long cur = counter.get();
            if (cur + unit > limit) return false;      // 超限：拒绝（由调用方回 429）
            if (counter.compareAndSet(cur, cur + unit)) return true;
        }
    }

    private long limitToUnit(ApiToken token) {
        return toUnit(token, token.quotaLimit());
    }

    private static long toUnit(ApiToken token, double amount) {
        return "TOKEN".equals(token.quotaType())
                ? Math.round(amount)
                : Math.round(amount * 1_000_000);       // COST → 微美元
    }
}
```

### 12.3 治理插件写信号（代码段 S9b，Codex）

```java
// 路径：ai-gateway/src/main/java/com/aigateway/governance/plugin/GovernanceBootstrapPlugin.java
/**
 * 治理插件（BEFORE_DECISION，order=100 最先执行）：
 * 把治理状态写进信号，供 BudgetFilter（H5）与 Scorer 消费。
 * 不淘汰任何候选——淘汰动作统一收敛在 BudgetFilter，插件只负责“情报”。
 */
@Component
public class GovernanceBootstrapPlugin implements GatewayPlugin {

    private final TokenManager tokenManager;
    private final TokenUsageStore usageStore;
    private final BudgetManager budgetManager;

    public GovernanceBootstrapPlugin(TokenManager tokenManager, TokenUsageStore usageStore,
                                     BudgetManager budgetManager) {
        this.tokenManager = tokenManager;
        this.usageStore = usageStore;
        this.budgetManager = budgetManager;
    }

    @Override
    public String name() { return "governance-bootstrap"; }

    @Override
    public PluginScope scope() { return PluginScope.GLOBAL; }

    @Override
    public PluginPhase phase() { return PluginPhase.BEFORE_DECISION; }

    @Override
    public int order() { return 100; }     // 数字大先执行：必须赶在过滤器读信号之前

    @Override
    public void beforeDecision(PluginContext ctx) {
        Signals s = ctx.signals();
        // 1. 令牌情报：GovernanceFilter 已把 tokenId 放进 metadata
        String tokenId = ctx.metadata().get("x-gateway-token-id");
        if (tokenId != null) {
            tokenManager.findById(tokenId).ifPresent(token -> {
                s.set("governance.token_id", tokenId);
                s.set("governance.token_remaining_ratio", usageStore.remainingRatio(token));
            });
        }
        // 2. 渠道/模型预算触顶集合：候选逐个查预算（预算未配置 = 不限）
        Set<String> channels = new HashSet<>();
        Set<String> models = new HashSet<>();
        for (ModelInstance inst : ctx.candidates()) {
            if (budgetManager.remainingRatio(BudgetScope.CHANNEL, inst.channelId()) <= 0) {
                channels.add(inst.channelId());
            }
            if (budgetManager.remainingRatio(BudgetScope.MODEL, inst.model()) <= 0) {
                models.add(inst.model());
            }
        }
        if (!channels.isEmpty()) s.set("governance.budget_exceeded_channels", channels);
        if (!models.isEmpty()) s.set("governance.budget_exceeded_models", models);
    }
}
```

### 12.4 `BudgetFilter` 主逻辑（代码段 S9，🖊 H5 全部手敲）

```java
// 路径：ai-gateway/src/main/java/com/aigateway/decision/filter/BudgetFilter.java
package com.aigateway.decision.filter;

/**
 * 第 4 道：预算过滤（V4 真实化，替代占位放行）。
 *
 * 淘汰规则（对照学习版 4.6 超额策略 / Envoy QuotaPolicy）：
 * 1. 渠道预算触顶（governance.budget_exceeded_channels）→ 淘汰该渠道候选；
 * 2. 模型预算触顶（governance.budget_exceeded_models）→ 淘汰该模型候选；
 * 3. 令牌额度付不起预估成本 → 淘汰（DEGRADE 模式的自动降级核心：贵的淘汰，便宜的自然上位）；
 * 4. 全部淘汰 → 429 budget_exceeded（比 FilterChain 的 503 no_available_model 语义更准确）；
 * 5. 写 budget_remaining_ratio 信号 → Scorer 动态调权自动生效（成本权重放大，零改动）。
 *
 * 预估成本口径与 Scorer 一致：输入 = 请求文本估算，输出 = maxTokens 或 defaultOutputTokens。
 */
@Component
public class BudgetFilter implements CandidateFilter {

    private final TokenUsageStore usageStore;
    private final TokenManager tokenManager;
    private final GovernanceProperties props;
    private final GatewayMetrics metrics;

    public BudgetFilter(TokenUsageStore usageStore, TokenManager tokenManager,
                        GovernanceProperties props, GatewayMetrics metrics) {
        this.usageStore = usageStore;
        this.tokenManager = tokenManager;
        this.props = props;
        this.metrics = metrics;
    }

    public int order() { return 20; }

    public String name() { return "budget"; }

    public List<ModelInstance> apply(ChatRequest request, PluginContext ctx,
                                     List<ModelInstance> candidates) {
        Signals s = ctx.signals();
        Set<String> exceededChannels = s.getStringSet("governance.budget_exceeded_channels")
                .orElse(Set.of());
        Set<String> exceededModels = s.getStringSet("governance.budget_exceeded_models")
                .orElse(Set.of());
        String tokenId = s.getString("governance.token_id").orElse(null);
        ApiToken token = tokenId == null ? null
                : tokenManager.findById(tokenId).orElse(null);

        List<ModelInstance> kept = new ArrayList<>();
        for (ModelInstance inst : candidates) {
            if (exceededChannels.contains(inst.channelId())) continue;   // 渠道预算触顶
            if (exceededModels.contains(inst.model())) continue;         // 模型预算触顶
            if (token != null && !usageStore.canPay(token, estimateCost(inst, request))) {
                continue;                                                // 令牌额度付不起（自动降级）
            }
            kept.add(inst);
        }

        if (kept.isEmpty()) {
            // 全淘汰也要埋点：FilterChain 的 filterDrops 只在 apply 正常返回后计数，
            // 这里抛异常前补一次，避免“全淘汰场景无淘汰指标”（审查修正 #9）
            metrics.filterDrops(ctx.alias(), name(), candidates.size());
            metrics.budgetRejected("token");
            // 过滤链 fail-closed 之前先给出更准确的语义：预算花完了
            throw new GatewayException(429, "budget_exceeded",
                    "预算/额度不足，没有可用的低成本候选");
        }

        // 写剩余比率信号 → Scorer 成本权重放大（预算紧张时自动省着花）
        double ratio = token != null ? usageStore.remainingRatio(token) : 1.0;
        ctx.signals().set("budget_remaining_ratio", ratio);
        return kept;
    }

    /** 预估成本：与 Scorer.estimateCost 同口径（单价表 × 预估 token） */
    private double estimateCost(ModelInstance inst, ChatRequest request) {
        double charsPerToken = props.getEstimation().getCharsPerToken();
        double cjkTokenPerChar = props.getEstimation().getCjkTokenPerChar();
        long inputTokens = TokenEstimator.estimateInputTokens(request, charsPerToken, cjkTokenPerChar);
        int outputTokens = request.maxTokens() != null ? request.maxTokens()
                : props.getDefaultOutputTokens();   // 复用 decision.defaultOutputTokens
        return inputTokens / 1000.0 * inst.priceIn()
                + outputTokens / 1000.0 * inst.priceOut();
    }
}
```

> `GovernanceProperties.getDefaultOutputTokens()` 由 Codex 提供（读取 `gateway.decision.defaultOutputTokens`，避免两处配置）。

### 12.5 单测（H5 完成后可跑）

```java
// 路径：ai-gateway/src/test/java/com/aigateway/decision/filter/BudgetFilterTest.java
class BudgetFilterTest {

    private TokenManager tokenManager;
    private TokenUsageStore store;
    private BudgetFilter filter;

    @BeforeEach
    void setUp() {
        tokenManager = mock(TokenManager.class);
        store = new TokenUsageStore(mock(TokenDao.class), mock(UsageDao.class));
        filter = new BudgetFilter(store, tokenManager, testProps(), mock(GatewayMetrics.class));
    }

    @Test
    void channelBudgetExceeded_removesChannelCandidates() {
        PluginContext ctx = ctxWithCandidates(instance("mock-a:qwen-large"),
                instance("mock-b:qwen-small"));
        ctx.signals().set("governance.budget_exceeded_channels", Set.of("mock-a"));

        List<ModelInstance> kept = filter.apply(request("你好"), ctx, ctx.candidates());
        assertThat(kept).extracting(ModelInstance::channelId).containsExactly("mock-b");
    }

    @Test
    void insufficientTokenQuota_keepsOnlyAffordable() {
        // 令牌剩余 $0.0005：qwen-large 预估 ~$0.001（付不起）、qwen-small 预估 ~$0.00026（付得起）
        ApiToken token = new ApiToken("agw_00000001", "t", "hash", 0.0005, "COST", "", "", -1, true, 0);
        PluginContext ctx = ctxWithCandidates(instance("mock-a:qwen-large", 0.002, 0.004),
                instance("mock-b:qwen-small", 0.0005, 0.001));
        ctx.signals().set("governance.token_id", token.id());
        when(tokenManager.findById(token.id())).thenReturn(Optional.of(token));

        List<ModelInstance> kept = filter.apply(request("你好"), ctx, ctx.candidates());
        assertThat(kept).extracting(ModelInstance::instanceId).containsExactly("mock-b:qwen-small");
        // 信号已写入：Scorer 动态调权自动生效
        assertThat(ctx.signals().get("budget_remaining_ratio")).hasValueSatisfying(v ->
                assertThat(((Number) v).doubleValue()).isCloseTo(1.0, within(0.001)));
    }

    @Test
    void allDropped_returnsBudgetExceeded() {
        // 额度 $0.0001：两个候选都付不起 → 全淘汰
        ApiToken token = new ApiToken("agw_00000001", "t", "hash", 0.0001, "COST", "", "", -1, true, 0);
        PluginContext ctx = ctxWithCandidates(instance("mock-a:qwen-large", 0.001, 0.002));
        ctx.signals().set("governance.token_id", token.id());
        when(tokenManager.findById(token.id())).thenReturn(Optional.of(token));

        assertThatThrownBy(() -> filter.apply(request("你好"), ctx, ctx.candidates()))
                .isInstanceOf(GatewayException.class)
                .satisfies(e -> {
                    assertThat(((GatewayException) e).getStatus()).isEqualTo(429);
                    assertThat(((GatewayException) e).getType()).isEqualTo("budget_exceeded");
                });
    }
}
```

---

## 13. 用量落库（关键代码段 S10，🖊 H6）

> 路径：`ai-gateway/src/main/java/com/aigateway/governance/ledger/UsageLedger.java`

> 🖊 **手敲 H6**：本节全部手敲；`UsageRecord` / `UsageDao`（batchUpdate）为脚手架。

### 13.1 为什么批量落库（先背下来）

每次请求一次磁盘 IO 会在高并发下把账本变成瓶颈（学习版 4.1：“用量记录先写内存环形缓冲、定时批量落库，避免每次请求一次磁盘 IO”）。缓存 + 批量：

```
请求线程 → queue.offer（无锁入队，O(1)，不碰磁盘）
定时线程 → flush：攒到 batchSize 或 intervalMs 到期 → 一次 batchUpdate 写 N 条
```

### 13.2 主逻辑（代码段 S10，🖊 H6 全部手敲）

```java
// 路径：ai-gateway/src/main/java/com/aigateway/governance/ledger/UsageLedger.java
package com.aigateway.governance.ledger;

/**
 * 用量账本：内存队列 + 定时批量落库（学习版 4.1 的“缓存 + 批量落库”）。
 *
 * 设计语义：
 * 1. record() 只入队：请求线程零磁盘 IO，无锁入队不阻塞转发；
 * 2. flush() 批量写库：一次事务写 N 条（batchSize 上限），失败整批放回队尾重试
 *   （代价：重复窗口内最多丢一批，可接受——账本允许最终一致）；
 * 3. 顺带触发 BudgetManager.flushUsed()：预算 used 快照与用量同频对账；
 * 4. 复用全局 gatewayScheduler（V3 9.2 的 gw-timer），不另起线程池。
 */
@Component
public class UsageLedger {

    private static final Logger log = LoggerFactory.getLogger(UsageLedger.class);

    private final UsageDao dao;
    private final BudgetManager budgetManager;
    private final int batchSize;
    private final Queue<UsageRecord> queue = new ConcurrentLinkedQueue<>();

    public UsageLedger(UsageDao dao, BudgetManager budgetManager,
                       GovernanceProperties props, ScheduledExecutorService scheduler) {
        this.dao = dao;
        this.budgetManager = budgetManager;
        this.batchSize = props.getUsageFlush().getBatchSize();
        scheduler.scheduleAtFixedRate(this::flush,
                props.getUsageFlush().getIntervalMs(),
                props.getUsageFlush().getIntervalMs(), TimeUnit.MILLISECONDS);
    }

    /** 请求线程入口：只入队（失败请求也留痕，result=failure） */
    public void record(UsageRecord r) {
        queue.offer(r);
    }

    /** 批量落库（定时线程调用；测试可直接调用做确定性断言） */
    public void flush() {
        List<UsageRecord> batch = drain(batchSize);
        if (!batch.isEmpty()) {
            try {
                dao.batchInsert(batch);
            } catch (Exception e) {
                // 落库失败：整批放回队尾，下一轮重试（不丢账，只可能延迟）
                log.error("用量批量落库失败，{} 条待重试: {}", batch.size(), e.toString());
                batch.forEach(queue::offer);
                return;                                   // 本次跳过对账，避免 budget 快照与用量不同频
            }
        }
        budgetManager.flushUsed();                        // 预算 used 快照顺带对账
    }

    /** 从队头取最多 max 条（不删除 —— 失败时整体放回） */
    private List<UsageRecord> drain(int max) {
        List<UsageRecord> batch = new ArrayList<>(max);
        UsageRecord r;
        while (batch.size() < max && (r = queue.poll()) != null) {
            batch.add(r);
        }
        return batch;
    }

    /** 待落库条数（指标用） */
    public int pending() { return queue.size(); }
}
```

### 13.3 单测（H6 完成后可跑）

```java
// 路径：ai-gateway/src/test/java/com/aigateway/governance/ledger/UsageLedgerTest.java
class UsageLedgerTest {

    private UsageDao dao;
    private UsageLedger ledger;

    @BeforeEach
    void setUp() {
        dao = mock(UsageDao.class);
        // intervalMs=100（不重要，测试直接调 flush）、batchSize=50
        GovernanceProperties props = new GovernanceProperties();
        props.getUsageFlush().setIntervalMs(100);
        props.getUsageFlush().setBatchSize(50);
        ledger = new UsageLedger(dao, mock(BudgetManager.class), props, scheduler());
    }

    @Test
    void flush_writesBatchInOneCall() {
        UsageRecord r1 = record("r1"), r2 = record("r2");
        ledger.record(r1);
        ledger.record(r2);
        ledger.flush();

        verify(dao).batchInsert(List.of(r1, r2));         // 一次批量写两条
        assertThat(ledger.pending()).isZero();
    }

    @Test
    void flush_failure_returnsRecordsToQueue() {
        doThrow(new RuntimeException("db down")).when(dao).batchInsert(anyList());
        ledger.record(record("r1"));
        ledger.flush();
        assertThat(ledger.pending()).isEqualTo(1);        // 失败放回，等待重试
    }

    // —— 测试辅助 ——
    private static UsageRecord record(String rid) {
        return new UsageRecord(rid, "agw_00000001", "qwen", "mock-a", "qwen-large",
                10, 20, 0.0001, 100, "success", System.currentTimeMillis());
    }

    private static ScheduledExecutorService scheduler() {
        return Executors.newSingleThreadScheduledExecutor();
    }
}
```

---

## 14. 治理接线（关键代码段 S11，Codex）

> 本节全部为 Codex 脚手架，但它是把 H1~H6 拼成一条链的关键：**鉴权与限流在 Filter 层（不读 body），模型范围 / Token 速率 / 预检 / 计量 / 扣减在服务层（有 ChatRequest）**。

### 14.1 `GovernanceFilter`（鉴权 + QPS/RPM 限流）

```java
// 路径：ai-gateway/src/main/java/com/aigateway/governance/filter/GovernanceFilter.java
/**
 * 治理过滤器（挂 RequestIdFilter 之后，@Order(2)）：
 * 1. 解析 Authorization: Bearer <token> → TokenManager.resolve（H1）→ 状态分类错误映射；
 * 2. 令牌级 QPS / RPM 限流（H2）——不读请求体，因此 Filter 层只做这两件事；
 * 3. 校验通过把 tokenId 写入 request attribute，Controller 透传进 metadata。
 *
 * 设计取舍（面试可讲）：
 * - 模型范围、Token 速率、预算预检需要请求体（model / messages），放服务层做；
 * - Filter 层保持「零 body 读取」，不包装请求流，避免缓存 body 的内存开销；
 * - governance.enabled=false 时整体旁路，V1~V3 行为原样。
 */
@Component
@Order(2)
public class GovernanceFilter implements Filter {

    public static final String ATTR_TOKEN_ID = "governance.tokenId";

    private final GovernanceProperties props;
    private final TokenManager tokenManager;
    private final RateLimiter rateLimiter;

    public GovernanceFilter(GovernanceProperties props, TokenManager tokenManager,
                            RateLimiter rateLimiter) {
        this.props = props;
        this.tokenManager = tokenManager;
        this.rateLimiter = rateLimiter;
    }

    @Override
    public void doFilter(ServletRequest req, ServletResponse res, FilterChain chain)
            throws IOException, ServletException {
        HttpServletRequest httpReq = (HttpServletRequest) req;
        if (!props.isEnabled() || !httpReq.getRequestURI().equals("/v1/chat/completions")) {
            chain.doFilter(req, res);
            return;
        }

        // 1. 鉴权：Bearer token → 令牌对象（存在/启用/过期/IP 已核）→ 错误映射
        String plain = bearer(httpReq.getHeader("Authorization"));
        ApiToken token = tokenManager.resolve(plain, clientIp(httpReq))
                .orElse(null);
        if (token == null) {
            TokenStatus status = tokenManager.validate(plain, clientIp(httpReq));
            writeError((HttpServletResponse) res, status);
            return;
        }
        // 2. 令牌级 QPS / RPM 限流（短窗管突发、长窗管总量）
        RateLimitRule qps = new RateLimitRule(RateLimitKey.token(token.id()),
                TimeUnit.SECONDS.toMillis(1), props.getRateLimit().getDefaultQps(), false);
        RateLimitRule rpm = new RateLimitRule(RateLimitKey.token(token.id()),
                TimeUnit.MINUTES.toMillis(1), props.getRateLimit().getDefaultRpm(), false);
        if (!rateLimiter.tryAcquire(qps, 1) || !rateLimiter.tryAcquire(rpm, 1)) {
            metrics.rateLimited("token");
            writeError((HttpServletResponse) res, TokenStatus.RATE_LIMITED);
            return;
        }
        // 3. 写 attribute → Controller metadata 透传
        httpReq.setAttribute(ATTR_TOKEN_ID, token.id());
        chain.doFilter(req, res);
    }

    /** Bearer 解析：无头/格式错都返回 null（后续走 MISSING/UNKNOWN 分类） */
    private static String bearer(String authorization) {
        if (authorization == null || !authorization.startsWith("Bearer ")) return null;
        String v = authorization.substring(7).trim();
        return v.isEmpty() ? null : v;
    }

    /** 客户端 IP（X-Forwarded-For 首段，演示环境直连取 remoteAddr） */
    private static String clientIp(HttpServletRequest req) {
        String fwd = req.getHeader("X-Forwarded-For");
        if (fwd != null && !fwd.isBlank()) return fwd.split(",")[0].trim();
        return req.getRemoteAddr();
    }

    /** 校验失败 → 统一错误体（错误码表见 17.2；RATE_LIMITED 带 Retry-After） */
    private static void writeError(HttpServletResponse res, TokenStatus status) throws IOException {
        int httpStatus = switch (status) {
            case MISSING, UNKNOWN -> 401;
            case DISABLED, EXPIRED, IP_DENIED, MODEL_FORBIDDEN -> 403;
            case QUOTA_EXHAUSTED, RATE_LIMITED -> 429;
            default -> 500;
        };
        String type = switch (status) {
            case MISSING, UNKNOWN -> "invalid_token";
            case DISABLED -> "token_disabled";
            case EXPIRED -> "token_expired";
            case IP_DENIED -> "token_ip_denied";
            case MODEL_FORBIDDEN -> "token_model_forbidden";
            case QUOTA_EXHAUSTED -> "budget_exceeded";
            case RATE_LIMITED -> "rate_limit_exceeded";
            default -> "internal_error";
        };
        if (status == TokenStatus.RATE_LIMITED) {
            res.setHeader("Retry-After", "1");     // 演示友好：1 秒后重试
        }
        res.setStatus(httpStatus);
        res.setContentType("application/json");
        res.getWriter().write("{\"type\":\"" + type + "\",\"message\":\""
                + describe(status) + "\"}");
    }

    private static String describe(TokenStatus s) {
        return switch (s) {
            case MISSING -> "缺少 Authorization: Bearer 令牌";
            case UNKNOWN -> "令牌不存在或已吊销";
            case DISABLED -> "令牌已被禁用";
            case EXPIRED -> "令牌已过期";
            case IP_DENIED -> "来源 IP 不在白名单";
            case MODEL_FORBIDDEN -> "该令牌无权调用此模型";
            case QUOTA_EXHAUSTED -> "令牌额度已用尽";
            case RATE_LIMITED -> "请求频率超限";
            default -> "未知错误";
        };
    }
}
```

> ⚠️ **`TokenManager.resolve` 与 `RATE_LIMITED` 补充**（Codex 脚手架）：
> 1. `TokenManager.validate` 只返回状态分类；Filter 还需要令牌对象来构造限流 key。给 `TokenManager` 增加 `Optional<ApiToken> resolve(String plainToken, String clientIp)`——内部复用 validate 的状态检查，校验通过才返回令牌对象，避免两处校验逻辑漂移；
> 2. `TokenStatus` 枚举增加 `RATE_LIMITED` 值（6.1 追加一行）；`writeError` 已覆盖。

### 14.2 `GovernanceService`（预检 / 结算编排）

```java
// 路径：ai-gateway/src/main/java/com/aigateway/governance/service/GovernanceService.java
/**
 * 治理编排服务：在 ChatGatewayService 外面包一层「预检 → 执行 → 计量 → 结算 → 留痕」。
 *
 * 时序（对照学习版 4.4 / 设计文档 4.1 请求生命周期）：
 * 1. precheck：模型范围（H1）+ 令牌额度预检（12.2）——REJECT 模式不足直接 429；
 * 2. 执行：委托 ChatGatewayService（V3 合并后 = 决策 + 降级链执行）；
 * 3. 计量：非流式 meterNonStream（H4）；流式由 StreamProxy 的 MeteringCallback 增量累计；
 * 4. 结算：只有成功响应才扣减——令牌额度 + 实际使用的渠道/模型预算（H3）；
 * 5. 留痕：UsageLedger.record 入队（H6），失败请求也记（result=failure，成本 0）。
 */
@Service
public class GovernanceService {

    private final ChatGatewayService gateway;
    private final TokenManager tokenManager;
    private final TokenUsageStore usageStore;
    private final BudgetManager budgetManager;
    private final TokenMeter meter;
    private final UsageLedger ledger;
    private final RateLimiter rateLimiter;
    private final GovernanceProperties props;
    private final ModelRegistry registry;
    private final GatewayMetrics metrics;

    public GovernanceService(ChatGatewayService gateway, TokenManager tokenManager,
                             TokenUsageStore usageStore, BudgetManager budgetManager,
                             TokenMeter meter, UsageLedger ledger, RateLimiter rateLimiter,
                             GovernanceProperties props, ModelRegistry registry,
                             GatewayMetrics metrics) {
        this.gateway = gateway;
        this.tokenManager = tokenManager;
        this.usageStore = usageStore;
        this.budgetManager = budgetManager;
        this.meter = meter;
        this.ledger = ledger;
        this.rateLimiter = rateLimiter;
        this.props = props;
        this.registry = registry;
        this.metrics = metrics;
    }

    /** 非流式入口：治理化 complete */
    public ChatCompletion complete(ChatRequest request, String requestId,
                                   Map<String, String> metadata) {
        if (!props.isEnabled()) {
            // 旁路：enabled=false 时不做任何治理，V1~V3 行为原样（审查修正 #5）
            return gateway.complete(request, requestId, metadata).completion();
        }
        ApiToken token = precheck(request, metadata);
        long start = System.nanoTime();
        try {
            long estIn = tokenRateAcquire(token, request);  // Token 速率占位（可选）
            ChatResult result = gateway.complete(request, requestId, metadata);
            MeteredUsage usage = meter.meterNonStream(
                    result.instance(), request, result.completion());
            settle(token, result, usage, true, elapsedMs(start), requestId);
            tokenRateAdjust(token, usage, estIn);           // 实际用量修正
            return result.completion();
        } catch (Exception e) {
            ledger.record(failureRecord(request, requestId, token, elapsedMs(start)));
            throw e;
        }
    }

    /** 流式入口：治理化 stream（计量靠 MeteringCallback 增量累计） */
    public void stream(ChatRequest request, String requestId, Map<String, String> metadata,
                       Consumer<ChatChunk> consumer) {
        if (!props.isEnabled()) {
            gateway.stream(request, requestId, metadata, consumer);   // 旁路
            return;
        }
        ApiToken token = precheck(request, metadata);
        long start = System.nanoTime();
        try {
            long estIn = tokenRateAcquire(token, request);  // Token 速率占位（可选）
            gateway.stream(request, requestId, metadata, consumer);
            // 流已正常结束：取流式计量结果——携带**实际执行实例**
            // （onFinish 的 MeteringContext 里是真实候选，不是首个候选；审查修正 #4）
            ModelInstance fallback = registry.findByAlias(request.model()).get(0);
            TokenMeter.MeteredResult mr = meter.takeResult(requestId, fallback, request);
            settle(token, new ChatResult(null, mr.instance()), mr.usage(),
                    true, elapsedMs(start), requestId);
            tokenRateAdjust(token, mr.usage(), estIn);      // 实际用量修正
        } catch (Exception e) {
            ledger.record(failureRecord(request, requestId, token, elapsedMs(start)));
            throw e;
        }
    }

    /** 预检：模型范围 + 令牌额度（REJECT 模式）；DEGRADE 模式额度不足只写信号不拦 */
    private ApiToken precheck(ChatRequest request, Map<String, String> metadata) {
        String tokenId = metadata.get("x-gateway-token-id");
        ApiToken token = tokenId == null ? null
                : tokenManager.findById(tokenId).orElse(null);
        if (token == null) {
            throw new GatewayException(401, "invalid_token", "请求未携带有效令牌");
        }
        if (!tokenManager.modelAllowed(token, request.model())) {
            throw new GatewayException(403, "token_model_forbidden",
                    "令牌无权调用模型: " + request.model());
        }
        // 额度预检（REJECT 模式硬拦；DEGRADE 模式放行，交给 BudgetFilter 自动降级）
        if ("REJECT".equals(props.getBudget().getExceedAction())
                && !usageStore.canPay(token, estimateAmount(token, request))) {
            throw new GatewayException(429, "budget_exceeded", "令牌额度不足");
        }
        return token;
    }

    /** 结算：成功才扣钱（预检通过但失败 → 不扣减，学习版 Q2）。
     *  量纲约定（审查修正 #1）：令牌额度按令牌 quotaType 换算；
     *  预算按预算自身 limitType 取用（TOKEN 预算扣 totalTokens、COST 预算扣 cost）——
     *  调用方对预算无脑传双值，由 BudgetManager 内部选择，杜绝量纲错乱。 */
    private void settle(ApiToken token, ChatResult result, MeteredUsage usage,
                        boolean success, long latencyMs, String requestId) {
        // 令牌额度：COST → 美元；TOKEN → 总 token 数
        double tokenAmount = "TOKEN".equals(token.quotaType())
                ? usage.totalTokens() : usage.cost();
        usageStore.consume(token, tokenAmount);                // 令牌额度
        if (result.instance() != null) {
            // 预算：双值传入，预算内部按 limitType 选量纲
            budgetManager.consume(BudgetScope.CHANNEL, result.instance().channelId(),
                    usage.totalTokens(), usage.cost());
            budgetManager.consume(BudgetScope.MODEL, result.instance().model(),
                    usage.totalTokens(), usage.cost());
            budgetManager.consume(BudgetScope.GLOBAL, "",
                    usage.totalTokens(), usage.cost());
        }
        ledger.record(new UsageRecord(requestId, token.id(),
                requestAliasOf(result), instanceChannelOf(result), instanceModelOf(result),
                usage.tokenIn(), usage.tokenOut(), usage.cost(), latencyMs,
                success ? "success" : "failure", System.currentTimeMillis()));
        metrics.usage(usage, success);
    }

    /** 失败留痕：instance 未知，渠道/模型记空（账本按 tokenId 汇总不受影响） */
    private UsageRecord failureRecord(ChatRequest request, String requestId,
                                      ApiToken token, long latencyMs) {
        return new UsageRecord(requestId, token.id(), request.model(), "", "",
                0, 0, 0, latencyMs, "failure", System.currentTimeMillis());
    }

    /** 辅助取值（result.instance() 为 null 时返回占位） */
    private static String requestAliasOf(ChatResult r) {
        return r.instance() != null ? r.instance().alias() : "unknown";
    }
    private static String instanceChannelOf(ChatResult r) {
        return r.instance() != null ? r.instance().channelId() : "";
    }
    private static String instanceModelOf(ChatResult r) {
        return r.instance() != null ? r.instance().model() : "";
    }

    /** 耗时换算：nanoTime 差值 → 毫秒（与 V1~V3 同款，不受系统改时间影响） */
    private static long elapsedMs(long startNanos) {
        return TimeUnit.NANOSECONDS.toMillis(System.nanoTime() - startNanos);
    }
}
```

> `ChatResult` 与 `ChatGatewayService` 的契约调整见 14.4。`metrics.usage(usage, success)` 见 17.1。

### 14.3 计量回调契约（V3 对齐）

V3 计划中的 `MeteringCallback` 是无上下文空实现；V4 把它扩展为**带上下文的计量契约**（V3 代码未落地，以本签名为准）：

```java
// 路径：ai-gateway/src/main/java/com/aigateway/execution/stream/MeteringCallback.java（改造）
/** 计量上下文：一次流式请求的身份 + 请求 + 实例（StreamProxy 构造后传给回调） */
public record MeteringContext(String requestId, ModelInstance instance,
                              ChatRequest request, String tokenId) {}

/** V4 计量契约：onChunk 增量累计，onFinish 结算（失败不扣预算，见 TokenMeter） */
public interface MeteringCallback {
    default void onChunk(MeteringContext ctx, ChatChunk chunk) {}

    /**
     * 流结束结算：返回 MeteredUsage（StreamProxy 忽略返回值；
     * TokenMeter 会把**成功**请求的结果写入内部 finished 表，
     * 供 GovernanceService.takeResult 取走——失败请求不写，避免残留；
     * 未实现计量时默认返回 null，GovernanceService 按估算兜底）。
     */
    default MeteredUsage onFinish(MeteringContext ctx, ChatCompletion.Usage usage, boolean success) {
        return null;
    }
}
```

`StreamProxy`（V3 计划 13.2）对接修改（Codex）：

- 在 `streamOnce` 的 chunk 回调处，把 `metering.onChunk(normalized)` 改为：
  ```java
  metering.onChunk(new MeteringContext(requestId, candidate, request,
          metadata.getOrDefault("x-gateway-token-id", "")), normalized);
  ```
- 流结束（`streamChain` 的 SUCCESS 分支）处，把 `metering.onFinish(usageOrApproximate(...))` 改为：
  ```java
  metering.onFinish(new MeteringContext(requestId, candidate, request,
          metadata.getOrDefault("x-gateway-token-id", "")),
          usageOrApproximate(candidate, request), true);
  ```
- 首字节后失败（`stream_interrupted` 分支）也补一次 `onFinish(..., false)`，让 TokenMeter 释放 tracker 并留失败痕迹。

> `TokenMeter.takeResult(requestId, fallback, request)` 已定义在 11.3，此处不再重复。

### 14.4 `ChatGatewayService` 契约调整（V3 合并后）

V3 合并后 `FallbackChainExecutor.execute` / `StreamProxy.streamChain` 已存在，V4 只做两处契约扩展：

```java
// 路径：ai-gateway/src/main/java/com/aigateway/core/service/ChatGatewayService.java（改造）
/** 执行结果携带实际使用的实例（V4 计量需要 instance 的单价表与渠道/模型维度） */
public record ChatResult(ChatCompletion completion, ModelInstance instance) {}

// FallbackChainExecutor.execute 返回值：ChatCompletion → ChatResult
// （成功分支 return new ChatResult(result, candidate)，其余逻辑零改动）
```

`ChatGatewayService` 的 `complete` / `stream` 签名不变（返回类型从 `ChatCompletion` 改为 `ChatResult`；`stream` 为 void 不变），Controller 调用处改一行（`gatewayService.complete(...).completion()`）。

> **V3 未合并时的备路径**（V4 实施时若 V3 仍未落地）：`ChatGatewayService` 保持 V2 版返回 `ChatCompletion`，`GovernanceService` 退化为「按 `completion.model()` 从 `ModelRegistry.findByAlias(request.model())` 反查 instance，找不到用第一个候选」；流式计量退化为「结束 chunk usage + 文本近似」。预算降级（BudgetFilter）不受影响——它在决策链上，与执行层无关。两条路径的接口层（Controller / 端点 / 配置）完全一致。

### 14.5 预检 / Token 速率的量纲统一

`TokenUsageStore.canPay` 的 `amount` 与令牌 `quotaType` 必须同量纲：`quotaType=COST` 传成本（美元）、`quotaType=TOKEN` 传总 token 数。`GovernanceService.precheck` 的 `estimateAmount(request)` 返回成本，因此 **TOKEN 类型令牌的预检与结算要换算**：

```java
// GovernanceService 内（Codex）
/** 预检额度估算：与 BudgetFilter 同口径（决策 defaultOutputTokens）；
 *  COST 令牌 → 预估成本（用首个候选单价近似）；TOKEN 令牌 → 预估总 token */
private double estimateAmount(ApiToken token, ChatRequest request) {
    double charsPerToken = props.getEstimation().getCharsPerToken();
    long input = TokenEstimator.estimateInputTokens(request, charsPerToken,
            props.getEstimation().getCjkTokenPerChar());
    int output = request.maxTokens() != null ? request.maxTokens()
            : props.getDefaultOutputTokens();
    ModelInstance probe = registry.findByAlias(request.model()).get(0);
    return "TOKEN".equals(token.quotaType())
            ? input + output
            : input / 1000.0 * probe.priceIn() + output / 1000.0 * probe.priceOut();
}

/** Token 速率限流（可选开关）：请求开始用预估输入 token 占位；
 *  返回预估输入 token 数（未启用返回 0，供结束修正用） */
private long tokenRateAcquire(ApiToken token, ChatRequest request) {
    if (!props.getRateLimit().getTokenRate().isEnabled()) return 0;
    long est = TokenEstimator.estimateInputTokens(request,
            props.getEstimation().getCharsPerToken(),
            props.getEstimation().getCjkTokenPerChar());
    RateLimitRule rule = new RateLimitRule(RateLimitKey.token(token.id()),
            TimeUnit.SECONDS.toMillis(1),
            props.getRateLimit().getTokenRate().getPerSecondTokens(), true);
    if (!rateLimiter.tryAcquire(rule, est)) {
        metrics.rateLimited("token_rate");
        throw new GatewayException(429, "rate_limit_exceeded", "Token 速率超限");
    }
    return est;
}

/** Token 速率修正：把占位量换成实际量（实际输入 - 预估输入，可负 = 回吐） */
private void tokenRateAdjust(ApiToken token, MeteredUsage usage, long estIn) {
    if (!props.getRateLimit().getTokenRate().isEnabled()) return;
    long delta = usage.tokenIn() - estIn;
    if (delta != 0) {
        rateLimiter.adjust(RateLimitKey.token(token.id()),
                TimeUnit.SECONDS.toMillis(1), delta);
    }
}
```

---

## 15. 内部管理端点（关键代码段 S12，Codex）

> 路径：`ai-gateway/src/main/java/com/aigateway/api/controller/GovernanceAdminController.java`
> 本版本管理端点**免鉴权**（V5 升级为带管理密钥的 Admin API，学习版第 8 节明确“本版本先提供内部服务”）。

```java
/**
 * 内部管理端点（/v1/admin/*）：令牌 / 预算 / 用量 / 渠道的创建与查询。
 * ⚠️ V5 将整体替换为带鉴权的 Admin API，本版本不设计鉴权（演示与学习用）。
 */
@RestController
@RequestMapping("/v1/admin")
public class GovernanceAdminController {

    private final TokenManager tokenManager;
    private final BudgetManager budgetManager;
    private final UsageDao usageDao;
    private final ChannelStore channelStore;

    // ── 令牌管理 ──

    /** 创建令牌：明文只出现在本次响应（一次性） */
    @PostMapping("/tokens")
    public Map<String, Object> createToken(@RequestBody CreateTokenReq req) {
        TokenManager.CreateResult r = tokenManager.create(req.name(), req.quotaLimit(),
                req.quotaType(), req.modelScope(), req.ipWhitelist(), req.expiresAt());
        return Map.of(
                "token", publicView(r.token()),
                "plain", r.plain());              // ← 明文仅此一次
    }

    /** 令牌列表：脱敏（id / name / 前缀 / 额度 / 状态，无哈希、无明文） */
    @GetMapping("/tokens")
    public List<Map<String, Object>> listTokens() {
        return tokenManager.allTokens().stream()
                .map(this::publicView)
                .toList();
    }

    /** 吊销：删缓存 + 库标记，即时生效 */
    @PostMapping("/tokens/{id}/revoke")
    public Map<String, String> revokeToken(@PathVariable String id) {
        tokenManager.revoke(id);
        return Map.of("result", "ok");
    }

    // ── 预算管理 ──

    @PostMapping("/budgets")
    public Budget createBudget(@RequestBody CreateBudgetReq req) {
        return budgetManager.upsert(BudgetScope.valueOf(req.scope()),
                req.scopeValue(), LimitType.valueOf(req.limitType()),
                BudgetPeriod.valueOf(req.period()), req.limit());
    }

    @GetMapping("/budgets")
    public List<Budget> listBudgets() {
        return budgetManager.allBudgets();
    }

    // ── 用量查询 ──

    /** 按令牌汇总用量与成本（账本查询，演示 A 的“用量记录可查”） */
    @GetMapping("/usage")
    public Map<String, Object> usage(@RequestParam(required = false) String tokenId,
                                     @RequestParam(required = false) String model,
                                     @RequestParam(defaultValue = "7") int days) {
        return usageDao.summarize(tokenId, model, days);
    }

    // ── 渠道管理（16 节）──

    @GetMapping("/channels")
    public List<ChannelRecord> listChannels() { return channelStore.all(); }

    @PostMapping("/channels")
    public ChannelRecord createChannel(@RequestBody CreateChannelReq req) {
        return channelStore.create(req.id(), req.provider(), req.baseUrl(),
                req.credentialsRef(), req.weight());
    }

    @PostMapping("/channels/{id}/status")
    public ChannelRecord setChannelStatus(@PathVariable String id, @RequestBody StatusReq req) {
        return channelStore.setEnabled(id, req.enabled());
    }

    @PostMapping("/channels/{id}/weight")
    public ChannelRecord setChannelWeight(@PathVariable String id, @RequestBody WeightReq req) {
        return channelStore.setWeight(id, req.weight());
    }

    @DeleteMapping("/channels/{id}")
    public Map<String, String> deleteChannel(@PathVariable String id) {
        channelStore.delete(id);                       // 有历史用量 → 软删除
        return Map.of("result", "ok");
    }

    /** 脱敏视图：id / name / 前缀（agw_****）/ 额度 / 状态；不暴露哈希与明文 */
    private Map<String, Object> publicView(ApiToken t) {
        return Map.of(
                "id", t.id(),
                "name", t.name(),
                "display", t.id() + "****",            // 前缀展示（New API 风格）
                "quotaLimit", t.quotaLimit(),
                "quotaType", t.quotaType(),
                "modelScope", t.modelScope(),
                "ipWhitelist", t.ipWhitelist(),
                "expiresAt", t.expiresAt(),
                "enabled", t.enabled());
    }
}
```

> DTO（`CreateTokenReq` / `CreateBudgetReq` / `CreateChannelReq` / `StatusReq` / `WeightReq`）为普通 record（字段名与 JSON 一一对应），Codex 补齐；`UsageDao.summarize` 用 `GROUP BY` 聚合（7.2 已提）。

---

## 16. 渠道管理（关键代码段 S13，Codex）

> 路径：`ai-gateway/src/main/java/com/aigateway/governance/channel/ChannelStore.java`

渠道档案升级（学习版 4.2）：配置渠道落库 + 运行时 CRUD + 启停即时生效 + 软删除保护。

```java
/**
 * 渠道存储：内存缓存（原子替换引用）+ DB 持久化。
 *
 * 启动合并规则：model.yml 渠道作为种子写库（不存在才插入），
 * DB 中同 id 的 weight / enabled 覆盖配置值——运行期改过的不被重启冲掉。
 * 变更生效：所有写操作先改内存（ConcurrentHashMap 原子替换）再落库，
 * 在途请求读到的要么是旧引用要么是新引用，不会读到半改状态。
 */
@Component
public class ChannelStore {

    private final ChannelDao dao;
    private final Map<String, ChannelRecord> channels = new ConcurrentHashMap<>();

    public ChannelStore(ChannelDao dao, GatewayProperties props) {
        this.dao = dao;
        seedFromConfig(props);           // 配置种子入库（不存在才插入）
        loadFromDb();                    // DB 覆盖同 id 的 weight / enabled
    }

    /** 创建渠道（id 冲突报 409）；候选要引用它需重启加载（文档标注） */
    public ChannelRecord create(String id, String provider, String baseUrl,
                                String credentialsRef, int weight) {
        if (channels.containsKey(id)) {
            throw new GatewayException(409, "channel_exists", "渠道已存在: " + id);
        }
        ChannelRecord rec = new ChannelRecord(id, provider, baseUrl,
                credentialsRef, weight, true, false, System.currentTimeMillis());
        channels.put(id, rec);
        dao.insert(rec);
        return rec;
    }

    /** 启停：即时生效（AvailabilityFilter 消费 isEnabled） */
    public ChannelRecord setEnabled(String id, boolean enabled) {
        ChannelRecord old = require(id);
        ChannelRecord next = old.withEnabled(enabled);
        channels.put(id, next);          // 原子替换：在途请求读旧值，新请求读新值
        dao.updateStatus(id, enabled);
        return next;
    }

    public ChannelRecord setWeight(String id, int weight) {
        ChannelRecord old = require(id);
        ChannelRecord next = old.withWeight(weight);
        channels.put(id, next);
        dao.updateWeight(id, weight);
        return next;
    }

    /** 删除：有用量记录 → 软删除（deleted=true），否则物理删除 */
    public void delete(String id) {
        ChannelRecord old = require(id);
        if (dao.hasUsage(id)) {
            channels.put(id, old.withDeleted(true));
            dao.softDelete(id);
        } else {
            channels.remove(id);
            dao.delete(id);
        }
    }

    public boolean isEnabled(String id) {
        ChannelRecord rec = channels.get(id);
        return rec != null && rec.enabled() && !rec.deleted();
    }

    public Optional<ChannelRecord> find(String id) { return Optional.ofNullable(channels.get(id)); }

    public List<ChannelRecord> all() {
        return channels.values().stream()
                .filter(r -> !r.deleted())
                .toList();
    }

    private ChannelRecord require(String id) {
        ChannelRecord rec = channels.get(id);
        if (rec == null) {
            throw new GatewayException(404, "channel_not_found", "渠道不存在: " + id);
        }
        return rec;
    }
}
```

`AvailabilityFilter` 对接（一行，Codex）：

```java
// decision/filter/AvailabilityFilter.java（改造）
// 增加渠道启停检查：停用/软删除的渠道候选直接淘汰
.filter(c -> channelStore.isEnabled(c.channelId()) && healthChecker.isHealthy(c.channelId())
        && c.weight() > 0)
```

> `ChannelRecord`（record：id / provider / baseUrl / credentialsRef / weight / enabled / deleted / createdAt + withXxx 副本方法）与 `ChannelDao`（insert / updateStatus / updateWeight / softDelete / delete / hasUsage）为 Codex 脚手架；`hasUsage` 查 `usage_record` 是否存在该 channel_id。

---

## 17. 指标与错误码（关键代码段 S14，Codex）

### 17.1 `GatewayMetrics` 扩展

```java
// 路径：ai-gateway/src/main/java/com/aigateway/observability/GatewayMetrics.java
// V1~V3 指标保留，以下为 V4 增量：

/** 令牌生命周期：gateway_tokens_created_total / gateway_tokens_revoked_total */
public void tokenCreated() { Counter.builder("gateway.tokens.created").register(meterRegistry).increment(); }
public void tokenRevoked() { Counter.builder("gateway.tokens.revoked").register(meterRegistry).increment(); }

/** 限流拒绝：gateway_rate_limit_rejections_total{dimension} */
public void rateLimited(String dimension) {
    Counter.builder("gateway.rate.limit.rejections")
            .tag("dimension", dimension)
            .register(meterRegistry).increment();
}

/** 预算预警：gateway_budget_warnings_total{scope,value}（软预算，不阻断） */
public void budgetWarning(String scope, String value) {
    Counter.builder("gateway.budget.warnings")
            .tag("scope", scope).tag("value", value)
            .register(meterRegistry).increment();
}

/** 预算拒绝：gateway_budget_rejections_total{scope}（429 budget_exceeded 次数） */
public void budgetRejected(String scope) {
    Counter.builder("gateway.budget.rejections")
            .tag("scope", scope)
            .register(meterRegistry).increment();
}

/** 用量与成本：gateway_usage_tokens_total{type}（in/out 累计）、gateway_usage_cost_total（美元） */
public void usage(MeteredUsage usage, boolean success) {
    Counter.builder("gateway.usage.tokens").tag("type", "in")
            .register(meterRegistry).increment(usage.tokenIn());
    Counter.builder("gateway.usage.tokens").tag("type", "out")
            .register(meterRegistry).increment(usage.tokenOut());
    Counter.builder("gateway.usage.cost")
            .tag("result", success ? "success" : "failure")
            .register(meterRegistry).increment(usage.cost());
}

/** 近似计量打点：gateway_meter_estimates_total{type}（estimated 次数 / 误差审计用） */
public void meterEstimated(boolean estimated) {
    if (estimated) {
        Counter.builder("gateway.meter.estimates").tag("type", "approximate")
                .register(meterRegistry).increment();
    }
}

/** 待落库积压：gateway_ledger_pending（gauge，UsageLedger 定时上报） */
public void ledgerPending(int pending) {
    Gauge.builder("gateway.ledger.pending", () -> pending)
            .register(meterRegistry);
}
```

### 17.2 错误码约定（V4 增量）

| type | HTTP | 触发场景 | 归属 |
|---|---|---|---|
| `invalid_token` | 401 | 缺 Authorization 头 / 令牌不存在或已吊销 | V4 |
| `token_disabled` | 403 | 令牌被禁用 | V4 |
| `token_expired` | 403 | 令牌已过期 | V4 |
| `token_ip_denied` | 403 | 来源 IP 不在白名单 | V4 |
| `token_model_forbidden` | 403 | 请求模型不在令牌 modelScope | V4 |
| `rate_limit_exceeded` | 429 | 限流超限（带 Retry-After 头） | V4 |
| `budget_exceeded` | 429 | 令牌额度 / 预算不足（含 BudgetFilter 全淘汰） | V4 |
| `channel_exists` | 409 | 创建重复渠道 | V4 |
| `channel_not_found` | 404 | 渠道不存在 | V4 |
| `token_not_found` | 404 | 吊销不存在的令牌 | V4 |
| `invalid_request` | 400 | 管理端点参数非法（预算上限 ≤ 0 等） | V4 |
| V1~V3 全部错误码 | — | 保留不回退 | — |

---

## 18. mock 服务端修改（关键代码段 S15，Codex）

mock 只需一处小改：**流式结束 chunk 补齐 usage**（非流式已带 usage）。

```java
// mock-model-server/src/main/java/com/aigateway/mock/controller/ChatController.java（改造）
// buildChunk(id, model, delta, isLast, usage)：isLast 时附加 usage（与网关同口径估算）。
// usage 由调用方（chatStream 方法内）计算传入——方法参数只有 id/model/delta/isLast，
// 不能在这里引用方法外的 request / content（审查修正 #11）。
private Map<String, Object> buildChunk(String id, String model, String delta,
                                       boolean isLast, Map<String, Object> usage) {
    Map<String, Object> chunk = new LinkedHashMap<>();
    chunk.put("id", id);
    chunk.put("object", "chat.completion.chunk");
    chunk.put("created", System.currentTimeMillis() / 1000);
    chunk.put("model", model);
    chunk.put("choices", List.of(Map.of(
            "index", 0,
            "delta", Map.of("role", "assistant", "content", delta),
            "finish_reason", isLast ? "stop" : null)));
    if (isLast) {
        chunk.put("usage", usage);
    }
    return chunk;
}

// 调用处（chatStream 方法内，request 是方法参数、content 是本次生成全文）：
// 结束 chunk：
emitter.send(buildChunk(requestId, profile.name(), "", true, Map.of(
        "prompt_tokens", estimateTokens(request),
        "completion_tokens", estimateTextTokens(content))));
// 中间 chunk：usage 传 null（与 V1 行为一致，只最后一块带 usage）
emitter.send(buildChunk(requestId, profile.name(), token, false, null));
```

> `estimateTokens(request)` / `estimateTextTokens(content)` 已存在（170 行起），直接复用。这样流式请求的结束 chunk 携带真实口径 usage，网关流式计量（H4）用真实值覆盖增量累计，演示误差可控。

---

## 19. 一次请求的完整时序

### 19.1 非流式（含鉴权 / 限流 / 计量 / 扣减）

```
Client
  │ POST /v1/chat/completions {model:"qwen", messages:[...]}
  │   Authorization: Bearer agw_xxxx
  ▼
RequestIdFilter（生成 requestId）→ GovernanceFilter（@Order(2)）
  ├─ TokenManager.resolve（H1：存在/启用/过期/IP）→ 401/403
  ├─ RateLimiter QPS + RPM（H2：1s 窗 + 60s 窗）→ 429 + Retry-After
  └─ 写 attribute governance.tokenId
  ▼
ChatCompletionController（metadata 透传 x-gateway-token-id）
  ▼
GovernanceService.complete（14.2）
  ├─ precheck：模型范围（H1）+ 令牌额度预检（REJECT 模式）→ 429 budget_exceeded
  ├─ ChatGatewayService.complete → DecisionEngine
  │    ├─ BEFORE_DECISION：GovernanceBootstrapPlugin 写信号（12.3）
  │    ├─ FilterChain：BudgetFilter 淘汰超预算候选（🖊 H5）→ 全淘汰 429
  │    └─ Scorer：读 budget_remaining_ratio → 成本权重放大（零改动）
  ├─ 执行：FallbackChainExecutor（V3）→ ChatResult(completion, instance)
  ├─ 计量：TokenMeter.meterNonStream（H4：usage 优先 / 近似）
  ├─ 结算：TokenUsageStore.consume + BudgetManager.consume（渠道/模型/全局，H3）
  └─ UsageLedger.record（H6：入队，定时批量落库）
  ▼
GlobalExceptionHandler（统一错误体）
```

### 19.2 流式（增量计量）

```
Client（同上鉴权链路）
  ▼
GovernanceService.stream
  ├─ precheck（同上）
  ├─ ChatGatewayService.stream → StreamProxy（V3）
  │    └─ 每 chunk：metering.onChunk(MeteringContext, chunk)（H4：增量累计输出 token）
  │         └─ 结束 chunk：usage 真实值覆盖（mock 已补齐）
  │         └─ onFinish(ctx, usage, true/false)：TokenMeter 结算 tracker
  ├─ 流正常结束 → meter.takeResult(requestId, fallback, request)（携带实际实例）→ 结算 + 留痕（同上）
  └─ 失败 → ledger.record(failure)（成本 0，不扣预算）
  ▼
SseEmitter（data: 帧输出）
```

两个时序图的共同点：**Filter 门禁 → 服务层预检 → 决策（预算信号链）→ 执行 → 计量 → 只对成功结算 → 批量落库**，每步都有日志与指标可回溯。

---

## 20. 测试计划

### 20.1 单元测试（JUnit + Mockito）

| 测试类 | 用例 | 断言要点 | 对应 |
|---|---|---|---|
| `governance/token/TokenManagerTest.java` | 创建明文仅一次 | 库中只有哈希；明文可校验 | H1 |
| `governance/token/TokenManagerTest.java` | 过期 / 吊销 / IP 拒绝 | 分类正确 | H1 |
| `governance/token/TokenManagerTest.java` | 模型范围 | modelAllowed 正确 | H1 |
| `governance/ratelimit/RateLimiterTest.java` | 第 N+1 个请求拒绝 | 窗口计数与拒绝一致 | H2 |
| `governance/ratelimit/RateLimiterTest.java` | 窗口滚动恢复 | 超窗后计数恢复 | H2 |
| `governance/ratelimit/RateLimiterTest.java` | Token 占位修正 | 预扣 → adjust 回吐 → 放行 | H2 |
| `governance/ratelimit/RateLimiterTest.java` | 维度隔离 | 不同 key 互不影响 | H2 |
| `governance/budget/BudgetManagerTest.java` | 并发不超扣 | 20 线程扣 200 上限 100 → 恰好 10 成功 | H3 |
| `governance/budget/BudgetManagerTest.java` | 周期惰性重置 | 换周期号后 used 归零 | H3 |
| `governance/budget/BudgetManagerTest.java` | 预检不改状态 | precheck 后剩余不变 | H3 |
| `governance/meter/TokenMeterTest.java` | usage 优先 | 真实值不被近似覆盖 | H4 |
| `governance/meter/TokenMeterTest.java` | 缺失回退近似 | estimated=true；误差在标注范围 | H4 |
| `governance/meter/TokenMeterTest.java` | 流式增量 + usage 覆盖 | 结束 chunk 覆盖累计 | H4 |
| `governance/meter/TokenMeterTest.java` | 失败成本归零 | 失败请求 cost=0 | H4 |
| `decision/filter/BudgetFilterTest.java` | 渠道/模型超限淘汰 | 淘汰集合正确 | H5 |
| `decision/filter/BudgetFilterTest.java` | 额度不足只留付得起 | 低成本候选留下；信号写入 | H5 |
| `decision/filter/BudgetFilterTest.java` | 全淘汰 429 | budget_exceeded 错误码 | H5 |
| `governance/ledger/UsageLedgerTest.java` | 批量落库一次 | batchInsert 一次 + 对账 | H6 |
| `governance/ledger/UsageLedgerTest.java` | 失败放回重试 | 落库失败 pending 不变 | H6 |
| `governance/filter/GovernanceFilterTest.java` | MockMvc 鉴权 | 无头 401 / 坏令牌 401 / 好令牌放行 | 接线 |
| `governance/filter/GovernanceFilterTest.java` | MockMvc 限流 | 超限 429 + Retry-After | 接线 |
| `governance/service/GovernanceServiceTest.java` | 成功结算 | 扣减 + 留痕 + 指标 | 接线 |
| `governance/service/GovernanceServiceTest.java` | 失败不扣钱 | 失败只留痕不扣减 | 接线 |
| `governance/service/GovernanceServiceTest.java` | REJECT 预检拦截 | 额度不足 429 | 接线 |
| `governance/persistence/SchemaInitializerTest.java` | 幂等建表 | 两次启动不报错；四张表存在 | 脚手架 |

### 20.2 集成测试

`ai-gateway/src/test/java/com/aigateway/ChatGatewayIntegrationTest.java` 在 V1~V3 场景基础上新增三个场景（对齐学习版演示）：

1. **场景 A（令牌额度）**：创建额度 $0.0015 的令牌 → 连续调用高成本模型（qwen-large 单次约 $0.001）→ 第一次成功、第二次自动降级到 qwen-small（日志含“预算”与降级事件）→ 用量查询端点可见两条记录与成本。若 `exceedAction=REJECT`，则第二次直接 429 `budget_exceeded`。
2. **场景 B（限流）**：`rateLimit.defaultQps=5` 的令牌 → 并发第 6 个请求 429 `rate_limit_exceeded`（错误体含限流原因、响应带 Retry-After）→ 等待窗口结束后计数恢复；`gateway_rate_limit_rejections_total{dimension="token"}` 计数上升。
3. **场景 C（预算降级）**：设置模型预算（如 MODEL=qwen-large, COST, DAILY, limit=0.05）→ 高成本模型预算触顶后，同一请求自动改走 qwen-small（决策日志显示候选过滤淘汰 qwen-large），调用方无感知；`gateway_budget_rejections_total` / `gateway_budget_warnings_total` 出现。

回归要求：V1 四场景 + V2 场景 + V3 场景继续通过；治理 `enabled=false` 时全部场景原样可跑。

---

## 21. 演示与验收（curl 命令）

### 21.1 启动与种子令牌

```bash
# 1) 启动两个 mock 实例（V1/V3 同款）
cd mock-model-server
mvn spring-boot:run -Dspring-boot.run.arguments=--server.port=8001,--mock.model=qwen-large &
mvn spring-boot:run -Dspring-boot.run.arguments=--server.port=8002,--mock.model=qwen-small &

# 2) 启动网关（model.yml 已含 governance 段与种子令牌 demo）
cd ../ai-gateway
mvn spring-boot:run
# 启动日志应出现：
#   令牌缓存预热完成，共 1 个
#   预算加载完成，共 0 条
#   H2 文件库位于 ./data/gateway（首次启动自动建表）
```

### 21.2 演示 A：令牌额度（COST 型，REJECT 模式）

```bash
# 创建额度 $0.0015 的令牌（明文只出现一次！）
curl -s -X POST http://localhost:8080/v1/admin/tokens \
  -H 'Content-Type: application/json' \
  -d '{"name":"demo-a","quotaLimit":0.0015,"quotaType":"COST","modelScope":"qwen","ipWhitelist":"","expiresAt":-1}'
# → {"token":{...},"plain":"agw_3f9a...c2b1"}   ← 明文仅此一次，抄下来

# 连续调用 qwen-large（单次约 $0.001：输入 2 token + 输出 256 token）
TOKEN=agw_3f9a...c2b1
for i in 1 2 3; do
  curl -s http://localhost:8080/v1/chat/completions \
    -H "Authorization: Bearer $TOKEN" -H 'Content-Type: application/json' \
    -d '{"model":"qwen","messages":[{"role":"user","content":"你好"}]}'
done
# 第 1 次：200（花费约 $0.001，剩余约 $0.0005）
# 第 2 次：429 {"type":"budget_exceeded",...}（REJECT 模式预检拦截）

# 用量可查
curl -s "http://localhost:8080/v1/admin/usage?tokenId=agw_3f9a" | jq
# → 该令牌 token_in / token_out / cost 汇总，与扣减一致
```

### 21.3 演示 B：限流（QPS=5）

```bash
# 把网关配置 defaultQps 改为 5 后重启（或直接演示缺省值 100 不易触发，建议改配置）
# 并发 6 个请求：
for i in $(seq 1 6); do
  curl -s -o /dev/null -w "%{http_code}\n" http://localhost:8080/v1/chat/completions \
    -H "Authorization: Bearer $TOKEN" -H 'Content-Type: application/json' \
    -d '{"model":"qwen","messages":[{"role":"user","content":"你好"}]}' &
done
wait
# → 输出 5 个 200 + 1 个 429
# 第 6 个响应头带 Retry-After: 1

# 指标
curl -s http://localhost:8080/actuator/prometheus | grep rate_limit
# gateway_rate_limit_rejections_total{dimension="token"}
```

### 21.4 演示 C：成本降级（DEGRADE 模式）

```bash
# 1) 创建额度 $0.0015 的令牌：够第一次 qwen-large（约 $0.001），不够第二次
curl -s -X POST http://localhost:8080/v1/admin/tokens \
  -H 'Content-Type: application/json' \
  -d '{"name":"demo-c","quotaLimit":0.0015,"quotaType":"COST","modelScope":"qwen","ipWhitelist":"","expiresAt":-1}'

# 2) 把 exceedAction 改为 DEGRADE（model.yml 配好后重启），连续请求 qwen
for i in 1 2; do
  curl -s http://localhost:8080/v1/chat/completions \
    -H "Authorization: Bearer $TOKEN_C" -H 'Content-Type: application/json' \
    -d '{"model":"qwen","messages":[{"role":"user","content":"你好"}]}'
done
# 第 1 次：200（剩余额度够付，决策可能仍选 qwen-large）
# 第 2 次：200，但网关日志显示自动降级（调用方无感知）：
#   event=候选过滤 detail=budget 淘汰 1 个，剩余 1 个   （qwen-large 付不起被淘汰）
#   event=decision ... primary=mock-b:qwen-small        （低成本候选自然上位）

# 3) 预算（模型维度）演示：给 qwen-large 设日预算 $0.002（约两次调用）
curl -s -X POST http://localhost:8080/v1/admin/budgets \
  -H 'Content-Type: application/json' \
  -d '{"scope":"MODEL","scopeValue":"qwen-large","limitType":"COST","period":"DAILY","limit":0.002}'
# 打到超限后：候选过滤淘汰 qwen-large → 自动走 qwen-small
# 指标：gateway_budget_warnings_total / gateway_budget_rejections_total
```

### 21.5 演示 D：重启不丢账

```bash
# 1) 记录当前用量：curl /v1/admin/usage
# 2) Ctrl-C 停网关，重新 mvn spring-boot:run
# 3) 再查 /v1/admin/usage → 记录仍在（H2 文件库）
# 4) 令牌仍可用；吊销的令牌仍不可用
# 日志：令牌缓存预热完成，共 N 个（与库中一致）
```

### 21.6 验收标准对照（与学习版文档一致）

| 学习版验收标准 | 证据 |
|---|---|
| 1. 重启后渠道、令牌、预算、用量记录不丢失 | 演示 D + H2 文件库 + TokenDao/BudgetDao/UsageDao 单测 |
| 2. 流式成本按增量累计，误差率有文档标注（<15%） | TokenMeterTest 流式用例 + 11.1 误差说明 + mock usage 覆盖 |
| 3. 预算扣减并发下不超扣 | BudgetManagerTest 并发用例（20 线程恰好 10 成功） |
| 4. 429 / 预算拒绝 / 降级三类行为有日志与指标 | 演示 A/B/C + 17.1 指标 + 17.2 错误码 |
| 5. 版本 3 功能回归通过 | 集成测试 V1~V3 场景 + governance.enabled=false 旁路 |

---

## 22. 任务清单（按顺序执行）

| # | 任务 | 产出 | 验收 | 归属 |
|---|---|---|---|---|
| T1 | 依赖与配置绑定 | pom 加 H2/JDBC + `GovernanceProperties` + model.yml governance 段 | 旧配置可启动；enabled=true 无令牌时自动建演示令牌 | Codex |
| T2 | 持久化基础 | schema.sql + `SchemaInitializer` + 四个 DAO | 两次启动幂等；DAO 单测全绿 | Codex |
| T3 | 令牌管理 | `TokenManager` + 单测 | 明文一次 / 哈希入库 / 三类校验用例全绿 | 🖊 H1 |
| T4 | 限流器 | `RateLimiter` + 单测 | 滑动窗口 / 维度隔离 / 占位修正用例全绿 | 🖊 H2 |
| T5 | 预算管理器 | `BudgetManager` + `TokenUsageStore` + 单测 | 并发不超扣 / 周期重置 / 预警用例全绿 | 🖊 H3（TokenUsageStore Codex） |
| T6 | Token 计量 | `TokenEstimator` 抽取 + `TokenMeter` + 单测 | usage 优先 / 近似回退 / 流式累计用例全绿 | 🖊 H4 |
| T7 | 预算降级闭环 | `GovernanceBootstrapPlugin` + `BudgetFilter` 真实化 + 单测 | 淘汰 / 信号 / 429 用例全绿；Scorer 调权零改动生效 | 🖊 H5（插件 Codex） |
| T8 | 用量落库 | `UsageLedger` + 单测 | 批量写入一次 / 失败重试用例全绿 | 🖊 H6 |
| T9 | 治理接线 | `GovernanceFilter` + `GovernanceService` + `MeteringCallback` 对齐 + ChatGatewayService 契约调整 | MockMvc 鉴权/限流用例全绿；V3 场景回归 | Codex |
| T10 | 管理端点 | `GovernanceAdminController` + DTO | 令牌/预算/用量/渠道端点可 curl 演示 | Codex |
| T11 | 渠道管理 | `ChannelStore` + ChannelDao + AvailabilityFilter 对接 | 启停即时生效；软删除保护 | Codex |
| T12 | 指标与错误码 | `GatewayMetrics` V4 增量 + 错误码表 | `/actuator/prometheus` 可见全部新指标 | Codex |
| T13 | mock 扩展 | 流式结束 chunk 补 usage | 流式计量走真实 usage 路径 | Codex |
| T14 | 集成测试与演示 | 三个场景 + 回归 + README | 按第 21 节可完整走一遍 | 你 |

**手敲顺序建议**：H1 → H2 → H3 → H4 → H5 → H6。先写令牌（无依赖），再写限流与预算两个状态机（并发是重点），然后计量（依赖估算器），接着预算降级闭环（依赖 H1/H3），最后批量落库（依赖 DAO）——每步都有单测兜底，不会带病前进。

---

## 23. 风险与注意点

1. **明文令牌泄漏**：`CreateResult.plain` 只在创建响应出现一次；管理列表 `publicView` 必须脱敏（不含哈希、不含明文）；日志不得打印明文令牌（TokenManager 只打 id）。
2. **并发扣减不超扣**：令牌额度与预算扣减都必须「锁内比较后写」或 CAS 循环；禁止“先读、再算、后写”三步分开（BudgetManagerTest 并发用例就是守门员）。
3. **失败不结算**：只有成功响应才调 `consume`；预检通过但失败的请求只留痕（result=failure，成本 0）。catch 里不要顺手扣钱。
4. **周期重置的竞态**：`lazyResetIfNeeded` 必须在 synchronized 内（与扣减同一把锁），否则“检查周期 → 重置 → 扣减”之间可能被另一线程插入一次扣减。
5. **Filter 不读 body**：模型范围 / Token 速率 / 预算预检都放服务层；GovernanceFilter 若尝试读 body 会触发 Spring 的流消费问题（body 读完 controller 拿不到），这也是分层的原因。
6. **预算信号链顺序**：`governance-bootstrap` 插件 order=100 必须最先执行（数字大先执行），否则 BudgetFilter 读到空信号、预算降级静默失效；FilterChain 按 order 降序执行，BudgetFilter order=20 在后没问题。
7. **误差口径一致性**：TokenEstimator 是唯一估算源（Scorer / CapabilityFilter / BudgetFilter / TokenMeter 统一调用），不要在别处再抄一份估算代码——口径漂移会让预算与计量对不上账。
8. **H2 文件锁**：`jdbc:h2:file:./data/gateway` 默认单进程；集成测试必须用内存库（`jdbc:h2:mem:gateway`），否则并行测试会锁库报错。
9. **批量落库失败**：`flush` 失败整批放回队尾重试；若 DB 持续不可用，队列会增长（`gateway_ledger_pending` 可观测）——本版本不做丢弃策略（文档标注取舍）。
10. **V3 契约对齐**：MeteringCallback / ChatResult 是 V4 对 V3 计划的两处契约扩展，实施 V3 时按本文档签名落代码（V3 未落地，此时对齐成本最低）；若 V3 已合并，按 14.4 备路径改造。
11. **Token 速率限流的时间窗**：`adjust` 只修正当前槽，跨槽误差在窗口尺度自动弥合——不要把“精确到毫秒”做成复杂度炸弹。
12. **手敲顺序**：不要一上来写 BudgetFilter（H5）——它依赖 TokenManager / TokenUsageStore / TokenEstimator 三个前置；按第 22 节顺序推进。
13. **管理端点免鉴权**：`/v1/admin/*` 本版本无鉴权（V5 换 Admin API），README 与演示文档要写明“仅本机演示”，避免误部署上线。

---

## 24. 版本 5 的对接预留（提前说清）

- `budget.used_value` 快照、`api_token.revoked_at`、渠道 `deleted` 标志都是 V5 管理面的地基；
- 管理端点 `GovernanceAdminController` 会被 V5 的 Admin API（带管理密钥 + 审计日志）替换，**接口路径尽量保持一致**（`/v1/admin/*`），V5 只需加鉴权层与审计表；
- 配置热更新（V5）加载时，`GovernanceProperties` 的缺省值结构就是热更新后的运行时快照形状，字段命名不要改。

