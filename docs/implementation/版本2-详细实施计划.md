# 版本 2：详细实施计划（含关键代码段）

> 配套文档：《版本2-插件流水线与决策引擎（学习版）》讲“是什么、为什么”，本文档讲“怎么做”。
> 技术基线：Java 21、Spring Boot 3.2、Spring MVC + Java 21 虚拟线程（阻塞式）、Maven 多模块（ai-gateway / mock-model-server）。
> 前置依赖：版本 1 已完成并跑通（见《版本1-详细实施计划.md》）。
> 本文档的关键代码段是**实现参考**，允许按你的编码风格调整；但接口契约（配置结构、端点、错误体）请保持一致。
>
> ⚠️ 2026-08-06：项目基于版本 1 的 Spring MVC + 虚拟线程（阻塞式）代码演进。版本 1 中已删除的旧决策引擎类（DecisionService、CandidateFilter、SimpleScorer、RoutingDecision 等）本轮按 v3.0 设计文档重建，旧类不复用。

---

## 0. 手敲清单与实施分工（先读）

版本 2 的代码分两类：**必须你手敲默写的核心（H1~H6，约 450 行）**与 **Codex 已实现的脚手架**。

### 必须手敲默写（H1~H6）

| 编号 | 手敲内容 | 位置 | 为什么必须手敲 |
|---|---|---|---|
| H1 | 插件执行引擎 PluginEngine（阶段顺序 / 作用域 / fail-closed） | 第 8 节 S6 | LiteLLM Router Plugins 的核心行为，插件化架构的“发动机” |
| H2 | Filter Chain 五道关卡编排（能力/可用性/策略/预算/插件信号） | 第 9 节 S7 | “候选收窄”框架，决策引擎第一步 |
| H3 | Scorer 多目标打分（归一化 + 加权 + 动态调权 + 明细） | 第 10 节 S8 | 多目标权衡核心算法，本项目差异化亮点 |
| H4 | Scheduler 出方案（策略分派 + effectivePolicy + ConditionalStrategy 规则求值；多目标/加权随机策略类脚手架已实现） | 第 11 节 S9 | 调度器核心，RoutingDecision 生成规则 |
| H5 | ModelStateStore 状态闭环（EWMA 延迟 + 错误率） | 第 12 节 S10 | “AI Gateway + Inference Scheduler”的核心卖点，指数移动平均算法 |
| H6 | DecisionEngine 编排 + ChatGatewayService 接入决策引擎 | 第 13、14 节 S11 | 决策与执行分离的“接线”，网关灵魂 |

手敲方法：先合上文档自己写 → 写不出来再看代码段 → 看完再自己重写一遍，直到能默写。

手敲类路径：
- H1 `ai-gateway/src/main/java/com/aigateway/plugin/engine/PluginEngine.java`
- H2 `ai-gateway/src/main/java/com/aigateway/decision/filter/FilterChain.java`
- H3 `ai-gateway/src/main/java/com/aigateway/decision/scorer/Scorer.java`
- H4 `ai-gateway/src/main/java/com/aigateway/decision/scheduler/Scheduler.java` + `ConditionalStrategy.java`（规则求值）
- H5 `ai-gateway/src/main/java/com/aigateway/decision/state/ModelStateStore.java`
- H6 `ai-gateway/src/main/java/com/aigateway/decision/engine/DecisionEngine.java` + `ai-gateway/src/main/java/com/aigateway/core/service/ChatGatewayService.java`

### Codex 已实现（脚手架，不需要手敲）

插件 SPI（GatewayPlugin / PluginScope / PluginPhase）、PluginContext、Signals、PluginRegistry、PolicyManager、五类过滤器的单个实现、策略类（MultiObjectiveStrategy / WeightedRandomStrategy 已实现；ConditionalStrategy 的规则求值留 H4）、DecisionLogStore、示例插件（复杂度识别 / 金丝雀分组 / 清空候选测试插件）、Controller metadata 提取、指标扩展、单测骨架。

代码中手敲部分留 `TODO H1~H6` 标记与说明注释，提示你该写哪一段；未完成前调用对应功能会直接报错。

---

## 1. 目标与范围

版本 2 完成后，系统支持：

- **插件框架**：全局 / 路由级 / 模型级作用域，按 order 顺序执行，共享 PluginContext，配置校验，**fail-closed**（候选被清空直接拒绝）。
- **路由信号（Signals）**：插件与决策器之间传递结构化键值，幂等写入，Scorer 可据此动态调权。
- **决策引擎**：Filter Chain（五道关卡）→ Scorer（多目标打分）→ Scheduler（出方案），输出不可变的 `RoutingDecision`（主选 + 降级链 + 打分明细 + 策略名）。
- **三类可配置策略**：`MULTI_OBJECTIVE`（多目标加权）、`WEIGHTED_RANDOM`（加权随机，复用版本 1 转盘算法）、`CONDITIONAL`（条件路由，按声明顺序求值）。
- **状态闭环雏形**：`ModelState` 记录每个候选实例的 EWMA 延迟与错误率，作为打分输入。
- **可解释路由**：打分明细随结构化日志输出，内存环形缓冲保留最近 N 条，提供调试查询端点。
- **示例插件**：复杂度识别（写 `task_complexity` 信号）、金丝雀分组（按 header 写 `allowed_instances` 信号）、清空候选测试插件（演示 fail-closed）。
- **版本 1 全部功能回归**：非流式 / 流式转发、健康过滤、失败切换、请求 ID、指标不回退。

**非目标**（版本 2 不做什么）：

- 不实现真实鉴权 / 限流插件内容（框架就绪，内容在版本 4 填充）。
- 不实现重试退避、熔断、完整降级链执行（版本 3；V2 只保留“沿降级链再试一次”的最小语义，保证 V1 行为不回退）。
- 不实现预算与成本核算（版本 4；V2 只把 priceIn/priceOut 作为打分输入，预算过滤留占位）。
- 不实现配置热更新与管理 API（版本 5；V2 的调试决策端点会被 Admin API 取代）。
- 不实现会话亲和（设计文档已说明：开启会禁用策略即时生效，默认关闭）。

---

## 2. 与现有代码的关系（先盘点，再动手）

| 现有文件 | 现状 | 处理方式 |
|---|---|---|
| `ChatGatewayService` | V1 主流程：健康过滤 + 加权随机 + 失败切换（H2 手敲产物） | 改造为“DecisionEngine 出方案 + 执行主选 / 最小降级”（🖊 H6），失败切换语义保留 |
| `ModelInstance` | record：instanceId/alias/channelId/model/weight/capability | 扩展 `priceIn/priceOut/qualityScore/latencyProfileMs`（构造器变更，测试同步更新） |
| `Capability` | 已有 contextLength/multimodal/toolCallSupport/codingAbility/reasoningLevel/qualityLevel | 增加 `streaming`（默认 true），供能力过滤使用 |
| `GatewayProperties` | channels + models + health | 增加 models[].strategy、candidates 价格/质量/延迟、policies、plugins、decision |
| `ModelRegistry` | 别名 → 候选 + 渠道表 + 启动校验 | 增加 `strategyOf(alias)`；新字段校验（价格 ≥ 0、质量分 0~1） |
| `HealthChecker` | 渠道级健康快照 | 保留不动，AvailabilityFilter 复用 `isHealthy` |
| `ChatCompletionController` | 非流式 / 流式 + requestId | 增加”提取 metadata（X-Canary-Group、X-Tenant-Id）”（已实现；传入 gatewayService 的接线留 H6） |
| `ChatRequest` | V1 record | 保留不动（输入 token 估算在 CapabilityFilter 内私有实现，见第 9.2 节） |
| `GatewayMetrics` | success/failure/streamChunk | 增加 `gateway.decisions.total` 与 `gateway.filter.drops` |
| `WeightedRandomPicker` | V1 H1 手敲 | 复用：WEIGHTED_RANDOM 策略直接调用 |
| mock-model-server | 延迟 / 故障 / 健康开关已齐 | 本轮无需改动（打分所需延迟样本来自真实调用耗时） |
| 测试 | V1 单测 + 集成测试 | 更新构造器；新增插件/过滤/打分/调度/状态/决策引擎测试；集成测试增加场景 A/B/C |

> 路径前缀：`ai-gateway/src/main/java/com/aigateway/`；新增决策引擎放在 `decision/` 包，插件框架放在 `plugin/` 包，与设计文档的模块边界（decision、config、observability）对齐。

---

## 3. 工程结构与新增文件

```
ai-gateway/src/main/java/com/aigateway/
├── GatewayApplication.java                    （保留）
├── api/
│   ├── controller/ChatCompletionController.java（改造：提取 metadata）
│   ├── controller/ModelController.java         （保留）
│   ├── controller/DecisionDebugController.java （新增：/v1/debug/decisions）
│   ├── dto/ChatRequest.java                  （保留，无改动）
│   └── error/GlobalExceptionHandler.java       （保留，错误表增加两行）
├── core/
│   ├── domain/model/
│   │   ├── Channel.java                        （保留）
│   │   ├── ModelInstance.java                  （扩展：价格/质量/延迟画像）
│   │   └── Capability.java                     （扩展：streaming）
│   ├── exception/GatewayException.java         （保留）
│   └── service/ChatGatewayService.java         （🖊 H6 改造）
├── plugin/
│   ├── spi/GatewayPlugin.java                  （新增：插件契约）
│   ├── spi/PluginScope.java                    （新增：GLOBAL / ROUTE / MODEL）
│   ├── spi/PluginPhase.java                    （新增：五类钩子）
│   ├── context/PluginContext.java              （新增：共享上下文）
│   ├── context/Signals.java                    （新增：路由信号表）
│   ├── registry/PluginRegistry.java            （新增：配置加载/去重/校验）
│   ├── engine/PluginEngine.java                （🖊 H1）
│   └── example/
│       ├── ComplexityRecognizerPlugin.java     （新增：写 task_complexity）
│       ├── CanaryGroupPlugin.java              （新增：写 allowed_instances）
│       └── ClearCandidatesTestPlugin.java      （新增：演示 fail-closed，默认关闭）
├── decision/
│   ├── model/
│   │   ├── Policy.java                         （新增：策略模板）
│   │   ├── ConditionRule.java                  （新增：条件规则）
│   │   ├── Condition.java                      （新增：条件表达式）
│   │   ├── RoutingDecision.java                （新增：调度方案）
│   │   ├── ScoringDetail.java                  （新增：打分明细）
│   │   ├── ScoreFactors.java                   （新增：四因子原始/归一化值）
│   │   └── ModelState.java                     （新增：实时状态）
│   ├── policy/PolicyManager.java               （新增：策略加载/校验/查询）
│   ├── filter/
│   │   ├── CandidateFilter.java                （新增：过滤器 SPI）
│   │   ├── FilterChain.java                    （🖊 H2）
│   │   ├── CapabilityFilter.java               （新增）
│   │   ├── AvailabilityFilter.java             （新增）
│   │   ├── PolicyFilter.java                   （新增：V2 读信号占位）
│   │   ├── BudgetFilter.java                   （新增：V4 占位）
│   │   └── PluginSignalFilter.java             （新增：消费 allowed/blocked 信号）
│   ├── scorer/Scorer.java                      （🖊 H3）
│   ├── scheduler/
│   │   ├── Scheduler.java                      （🖊 H4：策略分派 + effectivePolicy）
│   │   ├── MultiObjectiveStrategy.java         （Codex：已实现，按 finalScore 降序）
│   │   ├── WeightedRandomStrategy.java         （Codex：已实现，复用 V1 转盘）
│   │   └── ConditionalStrategy.java            （🖊 H4：规则求值 matches + 命中覆盖）
│   ├── engine/DecisionEngine.java              （🖊 H6）
│   ├── state/ModelStateStore.java              （🖊 H5）
│   └── log/DecisionLogStore.java               （新增：环形缓冲）
├── infra/
│   ├── config/GatewayProperties.java           （扩展：policies/plugins/decision）
│   ├── config/SecretResolver.java              （保留）
│   ├── http/HttpClientFactory.java             （保留）
│   └── web/RequestIdFilter.java                （保留）
├── execution/connector/OpenAIConnector.java    （保留，无改动）
└── observability/GatewayMetrics.java           （扩展）
```

---

## 4. 依赖（pom.xml）

> 路径：`ai-gateway/pom.xml`

**不需要新增任何依赖**。现有依赖已覆盖版本 2 全部需求：

- `spring-boot-starter-web`：Spring MVC + 虚拟线程；
- `jackson-databind`（starter 传递引入）：配置绑定、决策日志 JSON 序列化；
- `spring-boot-starter-test`：JUnit + Mockito + AssertJ；
- `micrometer-registry-prometheus`：新增指标。

新增的决策引擎 / 插件框架全部用 JDK 自带能力实现（`java.net.http`、`java.util.concurrent`、`java.util.regex`），不引入规则引擎等重依赖——这也符合“自研为主”的学习路线。

---

## 5. 配置设计（关键代码段 S2）

### 5.1 `model.yml`（V2 完整结构）

> 路径：`ai-gateway/src/main/resources/model.yml`

在版本 1 的 `gateway.health / channels / models` 基础上新增：`gateway.decision`（决策参数）、`models[].strategy`（策略引用）、候选的价格 / 质量 / 延迟画像、`gateway.policies`（策略模板）、`gateway.plugins`（插件挂载）。

> ⚠️ 现状：仓库中的主 model.yml 仍保持 V1 结构（满足“旧配置可直接启动”的回归要求）。以下为 V2 完整结构，**演示前需手动把主 model.yml 扩展为如下结构**（见第 21.2 节）；测试配置（src/test/resources/model.yml）同样为 V1 结构，集成测试靠 property 覆盖启用插件。

```yaml
gateway:
  health:                       # V1 保留
    intervalSeconds: 5
    timeoutMs: 2000
    consecutiveFailures: 3

  decision:                     # V2 新增：决策引擎参数
    logSize: 100                # 决策日志环形缓冲容量（最近 N 条）
    ewmaAlpha: 0.3              # 延迟 EWMA 系数（越大越跟手）
    errorRateAlpha: 0.1         # 错误率 EWMA 系数
    defaultOutputTokens: 256    # 成本预估用的默认输出 token 数

  channels:                     # V1 保留
    - id: mock-a
      provider: openai-compatible
      baseUrl: http://localhost:8001
      credentialsRef: ""
      weight: 10
    - id: mock-b
      provider: openai-compatible
      baseUrl: http://localhost:8002
      credentialsRef: ""
      weight: 5

  models:                       # V1 结构 + V2 新字段
    - alias: qwen
      strategy: simple-task     # V2：引用 gateway.policies 里的模板名
      candidates:
        - channelId: mock-a
          model: qwen-large
          weight: 8
          priceIn: 0.001        # V2：每 1K input tokens 价格（美元）
          priceOut: 0.002       # V2：每 1K output tokens 价格
          qualityScore: 0.9     # V2：0~1 静态质量分（缺省由 qualityLevel 推导）
          latencyProfileMs: 2000 # V2：EWMA 初始延迟，与实际 mock 延迟一致
          capability:
            contextLength: 128000
            multimodal: false
            toolCallSupport: true
            codingAbility: MEDIUM
            reasoningLevel: HIGH
            qualityLevel: QUALITY_HIGH
        - channelId: mock-b
          model: qwen-small
          weight: 2
          priceIn: 0.01
          priceOut: 0.02
          qualityScore: 0.4
          latencyProfileMs: 200
          capability:
            contextLength: 32000
            multimodal: false
            toolCallSupport: false
            codingAbility: LOW
            reasoningLevel: LOW
            qualityLevel: FAST
    - alias: deepseek-code
      strategy: quality-first
      candidates:
        - channelId: mock-b
          model: deepseek-code
          weight: 1
          priceIn: 0.002
          priceOut: 0.004
          qualityScore: 0.8
          latencyProfileMs: 500
          capability:
            contextLength: 64000
            multimodal: false
            toolCallSupport: true
            codingAbility: MAX
            reasoningLevel: MEDIUM
            qualityLevel: QUALITY_MEDIUM

  policies:                     # V2 新增：策略模板（打分模板管理）
    - name: balanced
      type: MULTI_OBJECTIVE
      weights: { latency: 0.25, cost: 0.25, quality: 0.25, health: 0.25 }
    - name: latency-first
      type: MULTI_OBJECTIVE
      weights: { latency: 0.6, cost: 0.1, quality: 0.15, health: 0.15 }
    - name: cost-first
      type: MULTI_OBJECTIVE
      weights: { latency: 0.1, cost: 0.6, quality: 0.2, health: 0.1 }
    - name: quality-first
      type: MULTI_OBJECTIVE
      weights: { latency: 0.15, cost: 0.1, quality: 0.6, health: 0.15 }
    - name: qwen-proportional
      type: WEIGHTED_RANDOM     # 用候选自身 weight，无需权重向量
    - name: simple-task
      type: CONDITIONAL
      defaultStrategy: balanced # 规则未命中时回退到该策略（必须是 MULTI_OBJECTIVE）
      rules:
        - when:
            signal: task_complexity
            op: EQ
            value: COMPLEX
          select:               # 命中选择的候选（必须是该别名候选的子集）
            - channelId: mock-a
              model: qwen-large

  plugins:                      # V2 新增：插件挂载（按 name 匹配 SPI 实例）
    - name: complexity-recognizer
      scope: GLOBAL
      order: 30
      config:
        keywordRegex: "代码|算法|重构|设计"
    - name: canary
      scope: ROUTE
      scopeValue: qwen          # ROUTE 作用域：只对 alias=qwen 生效
      order: 20
      config:
        stableInstances: [ "mock-a:qwen-large" ]
        canaryInstances: [ "mock-b:qwen-small" ]
        # 无 enabled 开关：演示 B 禁用金丝雀 = 注释掉本挂载项（见 21.4）
    - name: clear-candidates     # 仅演示 fail-closed 用，默认关闭
      scope: GLOBAL
      order: 10
      config:
        enabled: false
```

### 5.2 新配置字段速查

| 配置 | 说明 | 缺省 |
|---|---|---|
| `models[].strategy` | 引用 `policies` 里的策略名 | `balanced` |
| `candidates[].priceIn / priceOut` | 每 1K tokens 单价（成本打分输入） | `0` |
| `candidates[].qualityScore` | 0~1 静态质量分；缺省由 `qualityLevel` 推导（QUALITY_HIGH→0.9 / QUALITY_MEDIUM→0.6 / FAST→0.3） | 推导值 |
| `candidates[].latencyProfileMs` | EWMA 初始延迟 | `1000` |
| `policies[].name/type/weights/rules/defaultStrategy` | 策略模板 | 内置 balanced 等默认策略 |
| `plugins[].name/scope/scopeValue/order/config` | 插件挂载 | scope=GLOBAL、order=0 |
| `decision.logSize/ewmaAlpha/errorRateAlpha/defaultOutputTokens` | 决策引擎参数 | 100 / 0.3 / 0.1 / 256 |

兼容性约定：版本 1 的旧 `model.yml`（没有 strategy / policies / plugins）**必须能直接启动**——缺省走内置 `balanced` 策略、空插件链，保证回归。

---

## 6. 领域模型（关键代码段 S3）

### 6.1 `ModelInstance`（扩展）

> 路径：`ai-gateway/src/main/java/com/aigateway/core/domain/model/ModelInstance.java`

```java
// 在 V1 字段基础上增加四个决策输入字段；record 不可变，构造器变更后测试同步更新
public record ModelInstance(
        String instanceId,       // channelId + ":" + model
        String alias,            // 对外模型名
        String channelId,        // 所属渠道
        String model,            // 上游真实模型名
        int weight,              // 候选权重（WEIGHTED_RANDOM 用）
        Capability capability,   // 能力画像（能力过滤用）
        double priceIn,          // V2：每 1K input tokens 价格（成本因子）
        double priceOut,         // V2：每 1K output tokens 价格
        double qualityScore,     // V2：0~1 静态质量分（质量因子）
        long latencyProfileMs    // V2：EWMA 延迟初始值
) {
    public String describe() {
        return alias + " -> " + channelId + "/" + model;
    }
}
```

### 6.2 策略模板（`Policy` / `ConditionRule` / `Condition`）

> 路径：`ai-gateway/src/main/java/com/aigateway/decision/model/Policy.java` 等

```java
public record Policy(
        String name,                     // 策略名（models[].strategy 引用）
        PolicyType type,                 // MULTI_OBJECTIVE / WEIGHTED_RANDOM / CONDITIONAL
        Map<String, Double> weights,     // MULTI_OBJECTIVE 权重向量（latency/cost/quality/health）
        List<ConditionRule> rules,       // CONDITIONAL 规则（按声明顺序求值）
        String defaultStrategy           // CONDITIONAL 未命中时的回退策略名
) {
    public enum PolicyType { MULTI_OBJECTIVE, WEIGHTED_RANDOM, CONDITIONAL }
}

// 条件规则：命中后把候选集覆盖为 select 列出的 instanceId（只收窄、不扩大）
public record ConditionRule(Condition condition, List<String> select) {}

// 条件表达式：signal 是信号名；op 支持 EQ / NE / IN；value 是标量或列表
public record Condition(String signal, Op op, Object value) {
    public enum Op { EQ, NE, IN }
}
```

### 6.3 调度方案（`RoutingDecision`）

> 路径：`ai-gateway/src/main/java/com/aigateway/decision/model/RoutingDecision.java`

```java
// 决策输出：不可变 record，决策层与执行层的唯一契约（设计文档 3.2 原则 1）
public record RoutingDecision(
        String requestId,                // 全链路 ID
        String alias,                    // 模型别名
        String strategy,                 // 生效策略名（条件命中时 = policy.name）
        ModelInstance primary,           // 主选
        List<ModelInstance> fallbackChain, // 降级链（V3 完整执行；V2 只生成 + 最小降级）
        List<ScoringDetail> scoringDetails, // 打分明细（可解释路由）
        Map<String, Object> signals,     // 决策时信号快照（日志/复盘用）
        String conditionalRuleHit        // 命中的规则描述；未命中为 null
) {}
```

### 6.4 打分明细（`ScoringDetail` / `ScoreFactors`）

> 路径：`ai-gateway/src/main/java/com/aigateway/decision/model/ScoringDetail.java`

```java
// 一个候选的完整打分记录：原始值、归一化值、实际生效权重、加权总分
public record ScoringDetail(
        String instanceId,
        ScoreFactors raw,                // 原始因子：延迟 ms / 成本 $ / 质量分 / 健康分
        ScoreFactors normalized,         // 归一化到 0~1（延迟/成本越低越好，质量/健康越高越好）
        Map<String, Double> weights,     // 实际生效权重（含动态调权后的值）
        double finalScore                // Σ weight * normalized
) {}

// 四个评分因子的载体（原始值或归一化值复用同一 record）
public record ScoreFactors(double latency, double cost, double quality, double health) {}
```

### 6.5 实时状态（`ModelState`）

> 路径：`ai-gateway/src/main/java/com/aigateway/decision/model/ModelState.java`

```java
// 每个候选实例一份；healthy 以 HealthChecker 快照为准（不在本对象重复存储）
public record ModelState(
        String instanceId,
        double ewmaLatencyMs,    // 指数移动平均延迟（Scorer 的 latency 因子）
        double errorRate,        // 0~1，EWMA 错误率（health 因子输入）
        int inFlight,            // 当前并发数（load 雏形，V3 负载因子用）
        long lastUpdatedAt       // 最近一次更新时间戳
) {}
```

---

## 7. 配置绑定与启动校验（关键代码段 S4、S5）

> 路径：`ai-gateway/src/main/java/com/aigateway/infra/config/GatewayProperties.java`、`ai-gateway/src/main/java/com/aigateway/decision/policy/PolicyManager.java`、`ai-gateway/src/main/java/com/aigateway/plugin/registry/PluginRegistry.java`、`ai-gateway/src/main/java/com/aigateway/state/registry/ModelRegistry.java`

> 本节全部为 Codex 脚手架；**不手敲**。手敲部分从第 8 节开始。

### 7.1 `GatewayProperties` 扩展（代码段 S4a）

```java
@Configuration
@ConfigurationProperties(prefix = "gateway")
@Data
public class GatewayProperties {

    private Health health = new Health();
    private Decision decision = new Decision();   // V2 新增
    private List<ChannelDef> channels = new ArrayList<>();
    private List<ModelDef> models = new ArrayList<>();
    private List<PolicyDef> policies = new ArrayList<>(); // V2 新增
    private List<PluginDef> plugins = new ArrayList<>();  // V2 新增

    @Data
    public static class Decision {
        private int logSize = 100;
        private double ewmaAlpha = 0.3;
        private double errorRateAlpha = 0.1;
        private int defaultOutputTokens = 256;
    }

    @Data
    public static class ModelDef {
        private String alias;
        private String strategy = "balanced";     // V2：引用策略名
        private List<CandidateDef> candidates = new ArrayList<>();
    }

    @Data
    public static class CandidateDef {
        private String channelId;
        private String model;
        private int weight = 1;
        private Capability capability;
        private double priceIn;                    // V2
        private double priceOut;                   // V2
        private Double qualityScore;               // V2（null = 由 qualityLevel 推导）
        private Long latencyProfileMs;             // V2（null = 1000）
    }

    @Data
    public static class PolicyDef {
        private String name;
        private String type;
        private Map<String, Double> weights = new HashMap<>();
        private List<RuleDef> rules = new ArrayList<>();
        private String defaultStrategy;
    }

    @Data
    public static class RuleDef {
        private ConditionDef when;
        private List<CandidateRef> select = new ArrayList<>();
    }

    @Data
    public static class ConditionDef {
        private String signal;
        private String op = "EQ";
        private Object value;
    }

    @Data
    public static class CandidateRef {
        private String channelId;
        private String model;
        public String instanceId() { return channelId + ":" + model; }
    }

    @Data
    public static class PluginDef {
        private String name;
        private String scope = "GLOBAL";
        private String scopeValue = "";
        private int order = 0;
        private Map<String, Object> config = new HashMap<>();
    }
}
```

### 7.2 `PolicyManager` 启动校验（代码段 S5a）

职责：把 `policies` 定义转成 `Policy` 对象，并提供“别名 → 生效策略”查询。

```java
@Component
public class PolicyManager {

    private static final double WEIGHT_EPSILON = 1e-6;

    private final Map<String, Policy> policies = new ConcurrentHashMap<>();
    private final Map<String, String> aliasToStrategy = new ConcurrentHashMap<>();

    public PolicyManager(GatewayProperties props, ModelRegistry registry) {
        registerBuiltins();          // 内置 balanced / latency-first / cost-first / quality-first
        Set<String> userNames = new HashSet<>();
        for (GatewayProperties.PolicyDef def : props.getPolicies()) {
            if (def.getName() == null || def.getName().isBlank()) {
                throw new GatewayException(500, "invalid_config", "策略 name 不能为空");
            }
            if (!userNames.add(def.getName())) {   // 内置可被覆盖；用户之间不允许重名
                throw new GatewayException(500, "invalid_config",
                        "重复的策略名: " + def.getName());
            }
            policies.put(def.getName(), toPolicy(def));
        }

        // 两阶段校验：全部注册后再统一校验 CONDITIONAL 的 defaultStrategy 引用，
        // 避免被引用的回退策略定义在后（列表顺序）时启动误报
        for (GatewayProperties.PolicyDef def : props.getPolicies()) {
            Policy policy = policies.get(def.getName());
            if (policy != null && policy.type() == Policy.PolicyType.CONDITIONAL) {
                validateConditionalFallback(def);
            }
        }

        // 交叉校验：每个 alias 的 strategy 必须存在；CONDITIONAL 的 select 必须是该别名候选子集
        for (String alias : registry.aliases()) {
            String strategy = registry.strategyOf(alias);
            Policy policy = policies.get(strategy);
            if (policy == null) {
                throw new GatewayException(500, "invalid_config",
                        "alias[" + alias + "] 引用了不存在的策略: " + strategy);
            }
            if (policy.type() == Policy.PolicyType.CONDITIONAL) {
                validateSelectSubset(alias, policy, registry);
            }
            aliasToStrategy.put(alias, strategy);
        }
    }

    /** 内置策略：缺省配置也能启动（对应版本 1 的无策略配置） */
    private void registerBuiltins() {
        policies.put("balanced", new Policy("balanced", Policy.PolicyType.MULTI_OBJECTIVE,
                Map.of("latency", 0.25, "cost", 0.25, "quality", 0.25, "health", 0.25),
                List.of(), null));
        // latency-first / cost-first / quality-first 同构，省略
    }

    /** 配置 → Policy 对象；按 type 分派对应校验（对照学习版文档 4.6） */
    private Policy toPolicy(GatewayProperties.PolicyDef def) {
        Policy.PolicyType type = Policy.PolicyType.valueOf(def.getType().toUpperCase());
        switch (type) {
            case MULTI_OBJECTIVE -> validateWeights(def);
            case WEIGHTED_RANDOM -> { /* 用候选自身 weight，无需权重向量 */ }
            case CONDITIONAL -> validateConditional(def);
        }
        return new Policy(def.getName(), type, Map.copyOf(def.getWeights()),
                def.getRules().stream()
                        .map(r -> new ConditionRule(
                                new Condition(r.getWhen().getSignal(),
                                        Condition.Op.valueOf(r.getWhen().getOp().toUpperCase()),
                                        r.getWhen().getValue()),
                                r.getSelect().stream()
                                        .map(GatewayProperties.CandidateRef::instanceId)
                                        .toList()))
                        .toList(),
                def.getDefaultStrategy());
    }

    private void validateWeights(GatewayProperties.PolicyDef def) {
        // 四个因子必须齐全，且权重和 = 1（浮点容差 1e-6，避免 0.1+0.2 的经典精度问题）
    }

    private void validateConditional(GatewayProperties.PolicyDef def) {
        // rules 非空、when.signal 非空、op 合法、select 非空
        //（value 类型约束：IN 必须是 List、EQ/NE 必须是标量，避免运行期 ClassCastException）
    }

    private void validateConditionalFallback(GatewayProperties.PolicyDef def) {
        // defaultStrategy 必须存在且为 MULTI_OBJECTIVE（两阶段校验，见构造器注释）
    }

    private void validateSelectSubset(String alias, Policy policy, ModelRegistry registry) {
        Set<String> instanceIds = registry.findByAlias(alias).stream()
                .map(ModelInstance::instanceId).collect(Collectors.toSet());
        for (ConditionRule rule : policy.rules()) {
            for (String id : rule.select()) {
                if (!instanceIds.contains(id)) {
                    throw new GatewayException(500, "invalid_config",
                            "alias[" + alias + "] 的规则引用了不属于该别名的候选: " + id);
                }
            }
        }
    }

    /** 运行时查询：模型别名 → 生效策略 */
    public Policy resolve(String alias) {
        Policy policy = policies.get(aliasToStrategy.getOrDefault(alias, "balanced"));
        if (policy == null) {
            throw new GatewayException(500, "invalid_config",
                    "alias[" + alias + "] 没有可用策略（启动校验应已拦截）");
        }
        return policy;
    }

    /** 按策略名查询（CONDITIONAL 未命中回退用） */
    public Policy byName(String name) {
        return policies.get(name);
    }
}
```

校验要点：

- 权重和用**浮点容差**（`Math.abs(sum - 1.0) < 1e-6`）判断，避免 0.1+0.2 的经典精度问题；
- 条件规则的 `select` 只允许引用**该别名已有候选**（实例级收窄，不能”无中生有”），这是 LiteLLM “只收窄、不扩大”语义在配置层的落地；
- 条件 **value 类型**启动期校验：`IN` 必须是 List、`EQ/NE` 必须是标量，否则运行期强转会抛 `ClassCastException`（见代码段 S5a 第 4 条）；
- **两阶段校验**：defaultStrategy 的引用校验在**所有策略注册完成之后**统一进行，而不是注册循环内立即校验——否则被引用的回退策略恰好定义在列表后方时会启动误报；
- 用户定义之间**不允许重名**（内置策略可被同名覆盖，但用户之间不行）；
- 内置策略保证旧配置（没有 `policies` 段）也能启动。

### 7.3 `PluginRegistry` 启动校验（代码段 S5b）

职责：扫描 Spring 容器里的 `GatewayPlugin` bean，按 `plugins` 配置组装 `PluginRegistration`。

```java
@Component
public class PluginRegistry {

    private final List<PluginRegistration> registrations;

    public PluginRegistry(GatewayProperties props, List<GatewayPlugin> plugins) {
        Map<String, GatewayPlugin> byName = plugins.stream()
                .collect(Collectors.toMap(GatewayPlugin::name, Function.identity(),
                        (a, b) -> { throw new IllegalStateException("插件名重复: " + a.name()); }));

        Set<String> seen = new HashSet<>();
        List<PluginRegistration> list = new ArrayList<>();
        for (GatewayProperties.PluginDef def : props.getPlugins()) {
            GatewayPlugin plugin = byName.get(def.getName());
            if (plugin == null) {
                throw new GatewayException(500, "invalid_config",
                        "配置引用了不存在的插件: " + def.getName());
            }
            if (!seen.add(def.getName())) {
                throw new GatewayException(500, "invalid_config",
                        "插件重复挂载（同名去重）: " + def.getName());
            }
            plugin.validate(def.getConfig());   // 插件自检 Schema（不合法抛异常）
            plugin.configure(def.getConfig());  // 应用 YAML 里的 config
            list.add(new PluginRegistration(def.getName(),
                    PluginScope.valueOf(def.getScope().toUpperCase()),
                    def.getScopeValue(), def.getOrder(), Map.copyOf(def.getConfig()), plugin));
        }
        this.registrations = List.copyOf(list);
    }

    public List<PluginRegistration> all() {
        return registrations;
    }
}
```

### 7.4 `ModelRegistry` 增量（代码段 S5c）

`init()` 中新增：

```java
// 1. 记录 alias → strategy（供 PolicyManager 交叉校验与运行时查询）
strategyByAlias.put(def.getAlias(), def.getStrategy() == null ? "balanced" : def.getStrategy());

// 2. 候选新字段校验：
//    priceIn/priceOut < 0            → invalid_config
//    qualityScore != null && (q < 0 || q > 1) → invalid_config
//    latencyProfileMs != null && <= 0 → invalid_config

// 3. 构造 ModelInstance 时：
//    priceIn = c.getPriceIn()
//    priceOut = c.getPriceOut()
//    qualityScore = c.getQualityScore() != null ? c.getQualityScore()
//                  : deriveQuality(c.getCapability().getQualityLevel())
//    latencyProfileMs = c.getLatencyProfileMs() != null ? c.getLatencyProfileMs() : 1000L
```

新增查询方法：

```java
/** 别名 → 策略名（缺省 balanced） */
public String strategyOf(String alias) {
    return strategyByAlias.getOrDefault(alias, "balanced");
}
```

补充（与脚手架实际一致）：

- `deriveQuality` 的缺省值是 **0.5**（`capability == null` 或 qualityLevel 未知时），QUALITY_HIGH→0.9 / QUALITY_MEDIUM→0.6 / FAST→0.3；
- `init()` 末尾还有兜底校验：**未配置任何模型 → `invalid_config` 启动失败**；
- `strategyByAlias` 用 `def.getStrategy()` 为空或空白时的 "balanced" 兜底（文档 5.2 的缺省约定）。

---

## 8. 插件框架（关键代码段 S6）

> 路径：`ai-gateway/src/main/java/com/aigateway/plugin/`

> 🖊 **手敲 H1**：仅 `PluginEngine` 手敲；SPI、Context、Signals、Registry 为 Codex 脚手架。
>
> 提示：当前骨架持有 `PluginRegistry` 引用并留 TODO H1；下方代码段按“启动时快照 registrations”实现，手敲时替换骨架字段即可（两种持有方式等价）。

### 8.1 插件契约（SPI）

```java
// 路径：ai-gateway/src/main/java/com/aigateway/plugin/spi/GatewayPlugin.java
public interface GatewayPlugin {

    /** 插件唯一名（与配置 plugins[].name 对应） */
    String name();

    /** 声明式作用域：GLOBAL / ROUTE / MODEL（engine 配合 PluginRegistration.matches 精确匹配） */
    default PluginScope scope() { return PluginScope.GLOBAL; }

    /** 同阶段内的执行顺序（Kong priority 思想：数字大的先执行） */
    default int order() { return 0; }

    /** 启动校验：配置不符合 Schema 时抛 GatewayException，导致启动失败 */
    default void validate(Map<String, Object> config) {}

    /** 启动时由注册表调用一次：把 YAML 配置应用到实例 */
    default void configure(Map<String, Object> config) {}

    // 五个生命周期钩子（Kong phases 的映射：access/header_filter/body_filter/log/error）
    default void beforeDecision(PluginContext ctx) {}  // 决策前：写信号、收窄候选
    default void afterDecision(PluginContext ctx) {}   // 决策后：只读 decision
    default void beforeExecution(PluginContext ctx) {} // 执行前
    default void afterExecution(PluginContext ctx) {}  // 执行后
    default void onError(PluginContext ctx, Throwable t) {} // 异常时
}
```

```java
// 路径：ai-gateway/src/main/java/com/aigateway/plugin/spi/PluginScope.java
public enum PluginScope { GLOBAL, ROUTE, MODEL }

// 路径：ai-gateway/src/main/java/com/aigateway/plugin/spi/PluginPhase.java
public enum PluginPhase {
    BEFORE_DECISION, AFTER_DECISION, BEFORE_EXECUTION, AFTER_EXECUTION, ON_ERROR
}
```

### 8.2 共享上下文 `PluginContext`（Codex）

```java
// 路径：ai-gateway/src/main/java/com/aigateway/plugin/context/PluginContext.java
public class PluginContext {

    private final ChatRequest request;             // 原始请求
    private final String requestId;                // 全链路 ID
    private final String alias;                    // 当前模型别名
    private final Map<String, String> metadata;    // 调用方信息（tenant / headers），只读
    private final Signals signals = new Signals(); // 路由信号表（插件间共享）
    private final List<ModelInstance> candidates;  // 候选集合（可写，只允许收窄）
    private RoutingDecision decision;              // 决策结果（生成后只读）
    private Throwable error;                       // ON_ERROR 钩子用

    public PluginContext(ChatRequest request, String requestId, String alias,
                         Map<String, String> metadata, List<ModelInstance> candidates) {
        this.request = request;
        this.requestId = requestId;
        this.alias = alias;
        this.metadata = metadata == null ? Map.of() : Map.copyOf(metadata);
        this.candidates = new ArrayList<>(candidates); // 防御性拷贝：不污染注册表
    }

    public ChatRequest request() { return request; }
    public String requestId() { return requestId; }
    public String alias() { return alias; }
    public Map<String, String> metadata() { return metadata; }
    public Signals signals() { return signals; }

    /** 候选集合（可变）：插件只允许移除，不允许新增 */
    public List<ModelInstance> candidates() { return candidates; }

    /** 整体替换候选（Filter Chain 用）；只允许收窄——新集合必须是当前候选的子集 */
    public void setCandidates(List<ModelInstance> narrowed) {
        if (!candidates.containsAll(narrowed)) {
            throw new GatewayException(500, "internal_error",
                    "candidates 只允许收窄：新候选集合包含原集合之外的实例");
        }
        candidates.clear();
        candidates.addAll(narrowed);
    }

    public RoutingDecision decision() { return decision; }

    /** 决策只允许设置一次：决策不可变，后续阶段只读 */
    public void setDecision(RoutingDecision decision) {
        if (this.decision != null) {
            throw new IllegalStateException("decision 已生成，不可覆盖");
        }
        this.decision = decision;
    }

    public Throwable error() { return error; }
    public void setError(Throwable error) { this.error = error; }

    /** 当前候选的 instanceId 集合（作用域匹配用） */
    public Set<String> candidateIds() {
        return candidates.stream().map(ModelInstance::instanceId).collect(Collectors.toSet());
    }
}
```

### 8.3 路由信号 `Signals`

```java
// 路径：ai-gateway/src/main/java/com/aigateway/plugin/context/Signals.java
public final class Signals {

    private final Map<String, Object> values = new ConcurrentHashMap<>();

    /** 写入信号（可覆盖）。幂等约束：同一请求重复执行时，同一 key 写入同一值结果一致 */
    public void set(String key, Object value) { values.put(key, value); }

    /** 仅在 key 不存在时写入（“先到先得”语义） */
    public void setIfAbsent(String key, Object value) { values.putIfAbsent(key, value); }

    public Optional<Object> get(String key) { return Optional.ofNullable(values.get(key)); }

    public Optional<String> getString(String key) { return get(key).map(String::valueOf); }

    @SuppressWarnings("unchecked")
    public Optional<Set<String>> getStringSet(String key) {
        return get(key).map(v -> {
            if (v instanceof Set<?> s) {
                return s.stream().map(String::valueOf).collect(Collectors.toSet());
            }
            if (v instanceof List<?> l) {
                return l.stream().map(String::valueOf).collect(Collectors.toSet());
            }
            return Set.of(String.valueOf(v));
        });
    }

    /** 不可变快照（日志 / RoutingDecision 用） */
    public Map<String, Object> snapshot() { return Map.copyOf(values); }
}
```

写入幂等的约定（学习版文档 4.2）：同一次请求内重复执行插件链，同一信号 key 的最终值必须一致。实现时避免“先 get 再追加 list”这类非幂等写法；需要集合时整体 set 一个新集合。

### 8.4 插件注册项 `PluginRegistration`

```java
// 路径：ai-gateway/src/main/java/com/aigateway/plugin/registry/PluginRegistration.java
public record PluginRegistration(
        String name,
        PluginScope scope,
        String scopeValue,           // ROUTE=别名；MODEL=instanceId
        int order,
        Map<String, Object> config,  // 插件实例独立配置（启动时已应用）
        GatewayPlugin plugin
) {
    /** 作用域匹配：GLOBAL 全量；ROUTE 匹配当前别名；MODEL 匹配候选 instanceId */
    public boolean matches(PluginContext ctx) {
        return switch (scope) {
            case GLOBAL -> true;
            case ROUTE -> scopeValue.equals(ctx.alias());
            case MODEL -> ctx.candidateIds().contains(scopeValue);
        };
    }
}
```

### 8.5 插件执行引擎（代码段 S6，🖊 H1 全部手敲）

这是版本 2 的“发动机”：把注册表里的插件按“阶段 → 作用域 → order”组织成链，逐条执行，并实现 LiteLLM 的两个关键行为——**链式可见**（后一个插件能看到前一个写好的信号与收窄后的候选）与 **fail-closed**（候选被清空立即拒绝，不回退全量）。

```java
// 路径：ai-gateway/src/main/java/com/aigateway/plugin/engine/PluginEngine.java
package com.aigateway.plugin.engine;

@Component
public class PluginEngine {

    private final List<PluginRegistration> registrations; // 启动时快照，运行期只读

    public PluginEngine(PluginRegistry registry) {
        this.registrations = registry.all();
    }

    /**
     * 执行某个生命周期阶段。
     *
     * 顺序语义：
     * 1. 过滤出当前上下文匹配的插件（GLOBAL 全量 / ROUTE 按别名 / MODEL 按候选）；
     * 2. 按 order 降序排序（Kong priority：数字大的先执行）；
     * 3. 逐个执行；每个插件都能看到前一个插件写入的信号与收窄后的候选；
     * 4. 决策前阶段执行 fail-closed：某插件清空候选池 → 503 candidates_cleared，
     *    且立即终止（后面的插件不再执行，策略链不可被绕过）。
     */
    public void runPhase(PluginPhase phase, PluginContext ctx) {
        List<PluginRegistration> chain = registrations.stream()
                .filter(r -> r.matches(ctx))
                .sorted(Comparator.comparingInt(PluginRegistration::order).reversed())
                .toList();

        for (PluginRegistration reg : chain) {
            try {
                invoke(reg.plugin(), phase, ctx);
            } catch (GatewayException e) {
                throw e; // 业务异常原样上抛（如插件主动拒绝请求）
            } catch (Exception e) {
                throw new GatewayException(500, "plugin_error",
                        "插件[" + reg.name() + "]在" + phase + "阶段执行失败: " + e.getMessage(), e);
            }

            // fail-closed：只对“决策前”的候选收窄语义生效
            if (phase == PluginPhase.BEFORE_DECISION && ctx.candidates().isEmpty()) {
                throw new GatewayException(503, "candidates_cleared",
                        "插件[" + reg.name() + "]在" + phase + "阶段清空了候选池");
            }
        }
    }

    private void invoke(GatewayPlugin plugin, PluginPhase phase, PluginContext ctx) {
        switch (phase) {
            case BEFORE_DECISION -> plugin.beforeDecision(ctx);
            case AFTER_DECISION -> plugin.afterDecision(ctx);
            case BEFORE_EXECUTION -> plugin.beforeExecution(ctx);
            case AFTER_EXECUTION -> plugin.afterExecution(ctx);
            case ON_ERROR -> plugin.onError(ctx, ctx.error());
        }
    }
}
```

设计要点：

- **链式可见**来自同一个 `PluginContext` 实例：候选列表和 signals 都是可变对象，后执行者天然看到前面的修改；
- **fail-closed 立即终止**：在“清空候选的那个插件”之后立刻抛错，不让后续插件有机会把候选“加回来”（列表语义上只能 remove，但防御性起见仍立即终止）；
- 插件异常统一包装为 `500 plugin_error`，避免插件内部异常把 Spring 内部细节泄露给客户端；
- 执行阶段（BEFORE_EXECUTION / AFTER_EXECUTION / ON_ERROR）不检查候选是否为空——候选收窄是**决策前**的语义；决策生成后候选集合不再参与执行。

---

## 9. Filter Chain 五道关卡（关键代码段 S7）

> 路径：`ai-gateway/src/main/java/com/aigateway/decision/filter/`

> 🖊 **手敲 H2**：仅 `FilterChain` 编排手敲；五类过滤器个体为 Codex 脚手架。

### 9.1 过滤器契约（Codex）

```java
// 路径：ai-gateway/src/main/java/com/aigateway/decision/filter/CandidateFilter.java
public interface CandidateFilter {
    String name();               // 过滤器名（日志/指标维度）
    int order();                 // 执行顺序（数字大的先执行，与插件 order 同一语义）

    /** 对候选集做一次收窄：返回新列表（只允许移除，不允许新增） */
    List<ModelInstance> apply(ChatRequest request, PluginContext ctx, List<ModelInstance> candidates);
}
```

### 9.2 五道关卡（先看行为再写编排）

按顺序执行，任一道可淘汰候选（对照学习版文档 4.3）：

| order | 过滤器 | 淘汰规则 | 说明 |
|---|---|---|---|
| 50 | `CapabilityFilter` | 能力不满足：请求要流式但实例不支持 streaming、要 tools 但 `toolCallSupport=false`、要 vision 但 `multimodal=false`、上下文窗口 < 预估输入 token | 能力从 signals `required_capabilities` 读取；输入 token 数按消息字符数估算 |
| 40 | `AvailabilityFilter` | 渠道不健康（`healthChecker.isHealthy` 为 false）或权重 ≤ 0 | V3 追加熔断状态；V1 健康检查直接复用 |
| 30 | `PolicyFilter` | 令牌/租户模型范围限制（V2 读 signals `allowed_aliases`，V4 接真实令牌） | V2 为占位：信号不存在则放行 |
| 20 | `BudgetFilter` | 超预算候选淘汰或降权（V4 接入真实预算） | V2 为占位：直接放行，留 TODO V4 |
| 10 | `PluginSignalFilter` | 按 signals `allowed_instances`（白名单）与 `blocked_instances`（黑名单）收窄；**以“信号是否存在”而非“集合是否为空”判断**——插件写入空白名单（如金丝雀分组未配置实例）意味着“一个都不允许”，按 isEmpty 放行会 fail-open | 金丝雀等插件信号的统一消费点 |

`CapabilityFilter` 关键逻辑：

```java
@Component
public class CapabilityFilter implements CandidateFilter {

    public int order() { return 50; }
    public String name() { return "capability"; }

    public List<ModelInstance> apply(ChatRequest request, PluginContext ctx,
                                     List<ModelInstance> candidates) {
        // required_capabilities 由 DecisionEngine 在决策前写入（S11，V2 只写 streaming）；
        // tools/vision 的请求字段 V1 未定义，信号不写即放行，逻辑为 V3 预留
        Set<String> required = ctx.signals().getStringSet("required_capabilities").orElse(Set.of());
        int estimatedTokens = estimateInputTokens(request);  // 类内私有估算，见下方
        return candidates.stream()
                .filter(c -> capabilityOk(c, required, estimatedTokens))
                .toList();
    }

    private boolean capabilityOk(ModelInstance inst, Set<String> required, int tokens) {
        Capability cap = inst.capability();
        if (cap == null) {
            return true; // 未配置能力画像 = 不做限制
        }
        if (required.contains("streaming") && !cap.isStreaming()) return false;
        if (required.contains("tools") && !cap.isToolCallSupport()) return false;
        if (required.contains("vision") && !cap.isMultimodal()) return false;
        // contextLength <= 0 视为未配置（int 默认值 0），与 cap == null 的“不做限制”语义对齐
        return cap.getContextLength() <= 0 || cap.getContextLength() >= tokens;
    }

    /** 简单 token 估算：汉字约 3 字/token，至少 1（与 mock 保持一致）。
     *  注意：这是 CapabilityFilter 的私有方法，ChatRequest 本身没有该实例方法 */
    private static int estimateInputTokens(ChatRequest request) {
        if (request.messages() == null) return 1;
        int chars = request.messages().stream()
                .map(ChatRequest.Message::content)
                .filter(s -> s != null)
                .mapToInt(String::length)
                .sum();
        return Math.max(1, chars / 3);
    }
}
```

### 9.3 过滤链编排（代码段 S7，🖊 H2 全部手敲）

```java
// 路径：ai-gateway/src/main/java/com/aigateway/decision/filter/FilterChain.java
package com.aigateway.decision.filter;

@Component
public class FilterChain {

    private final List<CandidateFilter> filters; // 按 order 排序后的不可变链
    private final GatewayMetrics metrics;

    public FilterChain(List<CandidateFilter> filters, GatewayMetrics metrics) {
        this.filters = filters.stream()
                .sorted(Comparator.comparingInt(CandidateFilter::order).reversed())
                .toList();
        this.metrics = metrics;
    }

    /**
     * 依次执行五道关卡；每道返回收窄后的候选集。
     *
     * 规则：
     * 1. 只收窄、不扩大：过滤器的返回值不得包含入参里没有的候选；
     * 2. 淘汰计数写入指标与日志（可解释“为什么少了候选”）；
     * 3. 任一道把候选清空 → 503 no_available_model（fail-closed，
     *    不回退到过滤前的候选集——策略不可被绕过）。
     */
    public List<ModelInstance> apply(ChatRequest request, PluginContext ctx,
                                     List<ModelInstance> candidates) {
        List<ModelInstance> current = new ArrayList<>(candidates);
        for (CandidateFilter filter : filters) {
            List<ModelInstance> before = current;
            current = filter.apply(request, ctx, before);

            int dropped = before.size() - current.size();
            if (dropped > 0) {
                metrics.filterDrops(ctx.alias(), filter.name(), dropped);
                log(ctx.requestId(), "候选过滤",
                        filter.name() + " 淘汰 " + dropped + " 个，剩余 " + current.size() + " 个");
            }

            if (current.isEmpty()) {
                throw new GatewayException(503, "no_available_model",
                        "候选经[" + filter.name() + "]过滤后为空");
            }
        }
        return current;
    }

    private static void log(String requestId, String event, String detail) {
        System.out.printf("[requestId=%s] event=%s detail=%s%n", requestId, event, detail);
    }
}
```

设计要点：

- **过滤器排序在构造时完成**：新增过滤器 = 加一个 Spring bean + `order()`，核心代码不动——与插件化同一个思想；
- **fail-closed 发生在过滤链内**：清空候选就拒绝，绝不回退到“过滤前”的候选集；
- 与插件 fail-closed（`candidates_cleared`）区分：插件清空是策略冲突，过滤清空是没有合格候选，错误类型不同，日志能分清责任方。

---

## 10. Scorer 多目标打分（关键代码段 S8）

> 路径：`ai-gateway/src/main/java/com/aigateway/decision/scorer/Scorer.java`

> 🖊 **手敲 H3**：本节全部手敲（归一化 + 加权 + 动态调权 + 明细）。

四个因子：

| 因子 | 原始值来源 | 方向 | 归一化 |
|---|---|---|---|
| latency | `ModelState.ewmaLatencyMs` | 越低越好 | `1 - (v - min)/(max - min)` |
| cost | `priceIn/priceOut × 预估 token` | 越低越好 | 同上 |
| quality | `ModelInstance.qualityScore`（插件可覆盖） | 越高越好 | `(v - min)/(max - min)` |
| health | `healthy ? 1 - errorRate : 0` | 越高越好 | 同上 |

```java
// 路径：ai-gateway/src/main/java/com/aigateway/decision/scorer/Scorer.java
package com.aigateway.decision.scorer;

@Component
public class Scorer {

    private static final List<String> FACTORS = List.of("latency", "cost", "quality", "health");
    private static final Map<String, Double> DEFAULT_WEIGHTS = Map.of(
            "latency", 0.25, "cost", 0.25, "quality", 0.25, "health", 0.25);

    private final ModelStateStore stateStore;    // EWMA 延迟 / 错误率
    private final HealthChecker healthChecker;   // 健康快照
    private final GatewayProperties props;       // defaultOutputTokens 等

    public Scorer(ModelStateStore stateStore, HealthChecker healthChecker,
                  GatewayProperties props) {
        this.stateStore = stateStore;
        this.healthChecker = healthChecker;
        this.props = props;
    }

    /**
     * 对候选逐一打分，返回与入参同序的明细列表。
     *
     * 步骤：动态调权 → 收集原始因子 → min-max 归一化 → 加权求和 → 组装明细。
     */
    public List<ScoringDetail> score(List<ModelInstance> candidates,
                                     Map<String, Double> weights,
                                     Signals signals,
                                     ChatRequest request) {
        Map<String, Double> effective = normalizeWeights(
                adjustWeights(weights != null ? weights : DEFAULT_WEIGHTS, signals));

        // 1. 原始因子
        List<ScoreFactors> rawList = candidates.stream()
                .map(c -> rawFactors(c, request))
                .toList();

        // 2. 每列 min/max（同一批候选内相对比较）
        double minLat = rawList.stream().mapToDouble(ScoreFactors::latency).min().orElse(0);
        double maxLat = rawList.stream().mapToDouble(ScoreFactors::latency).max().orElse(0);
        double minCost = rawList.stream().mapToDouble(ScoreFactors::cost).min().orElse(0);
        double maxCost = rawList.stream().mapToDouble(ScoreFactors::cost).max().orElse(0);
        double minQ = rawList.stream().mapToDouble(ScoreFactors::quality).min().orElse(0);
        double maxQ = rawList.stream().mapToDouble(ScoreFactors::quality).max().orElse(0);
        double minH = rawList.stream().mapToDouble(ScoreFactors::health).min().orElse(0);
        double maxH = rawList.stream().mapToDouble(ScoreFactors::health).max().orElse(0);

        // 3. 归一化 + 加权求和 + 明细
        List<ScoringDetail> details = new ArrayList<>();
        for (int i = 0; i < candidates.size(); i++) {
            ScoreFactors raw = rawList.get(i);
            ScoreFactors norm = new ScoreFactors(
                    normalizeLowerBetter(raw.latency(), minLat, maxLat),
                    normalizeLowerBetter(raw.cost(), minCost, maxCost),
                    normalizeHigherBetter(raw.quality(), minQ, maxQ),
                    normalizeHigherBetter(raw.health(), minH, maxH));
            double score = norm.latency() * effective.getOrDefault("latency", 0.0)
                    + norm.cost() * effective.getOrDefault("cost", 0.0)
                    + norm.quality() * effective.getOrDefault("quality", 0.0)
                    + norm.health() * effective.getOrDefault("health", 0.0);
            details.add(new ScoringDetail(candidates.get(i).instanceId(),
                    raw, norm, Map.copyOf(effective), score));
        }
        return details;
    }

    /** 原始因子：延迟来自 EWMA；成本来自单价 × 预估 token；质量来自配置；健康来自快照 × 错误率 */
    private ScoreFactors rawFactors(ModelInstance inst, ChatRequest request) {
        ModelState state = stateStore.stateOf(inst.instanceId());
        double latency = state.ewmaLatencyMs();
        double cost = estimateCost(inst, request);
        double quality = inst.qualityScore();
        double health = healthChecker.isHealthy(inst.channelId())
                ? 1.0 - state.errorRate() : 0.0;
        return new ScoreFactors(latency, cost, quality, health);
    }

    /** 成本预估：input 用请求消息估算，output 用 maxTokens 或默认值（V4 换成真实计量） */
    private double estimateCost(ModelInstance inst, ChatRequest request) {
        long inputTokens = estimateInputTokens(request);  // 与 CapabilityFilter 同款私有估算（见 9.2）
        int outputTokens = request.maxTokens() != null
                ? request.maxTokens() : props.getDecision().getDefaultOutputTokens();
        return inputTokens / 1000.0 * inst.priceIn()
                + outputTokens / 1000.0 * inst.priceOut();
    }

    /** 简单 token 估算：汉字约 3 字/token，至少 1（与 CapabilityFilter 同一实现，暂不抽公共工具） */
    private static long estimateInputTokens(ChatRequest request) {
        if (request.messages() == null) return 1;
        long chars = request.messages().stream()
                .map(ChatRequest.Message::content)
                .filter(s -> s != null)
                .mapToLong(String::length)
                .sum();
        return Math.max(1, chars / 3);
    }

    /**
     * 动态调权（学习版文档 4.4）：执行前读信号，按规则调整权重。
     * 规则示例：
     * - task_complexity=COMPLEX → 质量权重 +0.2（从 latency/cost 各扣 0.1）；
     * - budget_remaining_ratio < 0.3 → 成本权重 +0.2（V4 真正写入该信号）。
     * 调整后必须重新归一化（权重和恒为 1）。
     */
    private Map<String, Double> adjustWeights(Map<String, Double> weights, Signals signals) {
        Map<String, Double> w = new HashMap<>(weights);

        if (signals.get("task_complexity").map("COMPLEX"::equals).orElse(false)) {
            w.computeIfPresent("quality", (k, v) -> v + 0.2);
            w.computeIfPresent("latency", (k, v) -> Math.max(0.0, v - 0.1));
            w.computeIfPresent("cost", (k, v) -> Math.max(0.0, v - 0.1));
        }
        if (signals.get("budget_remaining_ratio")
                .map(v -> ((Number) v).doubleValue() < 0.3).orElse(false)) {
            w.computeIfPresent("cost", (k, v) -> v + 0.2);
            w.computeIfPresent("quality", (k, v) -> Math.max(0.0, v - 0.1));
            w.computeIfPresent("health", (k, v) -> Math.max(0.0, v - 0.1));
        }
        return w;
    }

    /** 权重归一化：保证和 = 1（防御性：动态调权后和可能漂移） */
    private Map<String, Double> normalizeWeights(Map<String, Double> w) {
        double sum = w.values().stream().mapToDouble(Double::doubleValue).sum();
        if (sum <= 0) {
            return DEFAULT_WEIGHTS;
        }
        Map<String, Double> out = new HashMap<>();
        w.forEach((k, v) -> out.put(k, v / sum));
        return Map.copyOf(out);
    }

    /** 越低越好：1 - 归一化位置；max==min 时全部给满分（无区分度） */
    private static double normalizeLowerBetter(double v, double min, double max) {
        if (max == min) return 1.0;
        return 1.0 - (v - min) / (max - min);
    }

    /** 越高越好：归一化位置；max==min 时全部给满分 */
    private static double normalizeHigherBetter(double v, double min, double max) {
        if (max == min) return 1.0;
        return (v - min) / (max - min);
    }

}
```

设计要点（面试可讲）：

- **归一化是必须的**：延迟（ms）、成本（$）、质量（0~1）、健康（0~1）量纲不同，直接相加会由量级最大的因子主导，归一化到 0~1 后权重才有意义；
- **max==min 的边界**：所有候选同一因子无差异时给满分，避免除零；
- **动态调权后重新归一化**：保证权重和恒为 1，打分结果可比较；
- **可解释性 = 每个候选都留一份 raw/normalized/weights/finalScore**，这是本项目相对开源方案（多单指标或纯规则）的差异化。

---

## 11. Scheduler 调度器（关键代码段 S9）

> 路径：`ai-gateway/src/main/java/com/aigateway/decision/scheduler/`

> 🖊 **手敲 H4**：脚手架把三种策略拆成了独立类（各管一个分支，单一职责）：`MultiObjectiveStrategy` 与 `WeightedRandomStrategy` 已完整实现，`ConditionalStrategy` 留规则求值（matches）待手敲；`Scheduler` 只做**分派**（按策略类型调用对应策略类）与 **effectivePolicy**（条件策略的有效策略解析）。手敲 H4 = 分派 + 条件求值，加权随机复用版本 1 的 `WeightedRandomPicker`（已包装在 `WeightedRandomStrategy` 里）。

### 11.1 策略行为定义

| 策略 | 行为 | 输出 |
|---|---|---|
| `MULTI_OBJECTIVE` | 按 finalScore 降序排序，同分按 instanceId 字典序（确定性） | 主选 = 第一名；降级链 = 其余按得分降序 |
| `WEIGHTED_RANDOM` | 用候选自身 weight 做加权随机（V1 转盘算法） | 主选 = 随机结果；降级链 = 其余按 weight 降序 |
| `CONDITIONAL` | 规则按声明顺序求值，命中即把候选集覆盖为规则 select | 命中 → 规则目标内加权随机 + 降级链；未命中 → 回退 defaultStrategy（MULTI_OBJECTIVE） |

### 11.2 调度器与策略类（代码段 S9，🖊 H4：Scheduler 分派 + ConditionalStrategy 规则求值）

`Scheduler` 只做两件事：**effectivePolicy**（条件策略的有效策略解析）与 **decide**（按类型分派）。策略类（MultiObjectiveStrategy / WeightedRandomStrategy）已由脚手架实现，直接组合。

```java
// 路径：ai-gateway/src/main/java/com/aigateway/decision/scheduler/Scheduler.java
package com.aigateway.decision.scheduler;

@Component
public class Scheduler {

    private final PolicyManager policyManager; // CONDITIONAL 未命中时回退查询
    private final MultiObjectiveStrategy multiObjectiveStrategy;
    private final WeightedRandomStrategy weightedRandomStrategy;
    private final ConditionalStrategy conditionalStrategy;

    public Scheduler(PolicyManager policyManager,
                     MultiObjectiveStrategy multiObjectiveStrategy,
                     WeightedRandomStrategy weightedRandomStrategy,
                     ConditionalStrategy conditionalStrategy) {
        this.policyManager = policyManager;
        this.multiObjectiveStrategy = multiObjectiveStrategy;
        this.weightedRandomStrategy = weightedRandomStrategy;
        this.conditionalStrategy = conditionalStrategy;
    }

    /**
     * 决策前求"有效策略"（打分权重与调度排序一致的关键）：
     * CONDITIONAL 按信号求值——命中返回自身（调度时走规则覆盖，权重无效）；
     * 未命中返回 defaultStrategy（其权重用于打分，避免回退时按默认均衡权重误排序）；
     * 其余类型原样返回。DecisionEngine 先调它取权重，再交给 Scorer 打分。
     * ⚠️ 分派契约：H6 手敲 DecisionEngine 时，decide() 必须接收本方法的解析结果
     *（见下方设计要点第 6 条）。
     */
    public Policy effectivePolicy(String alias, Policy policy, Signals signals) {
        if (policy.type() != Policy.PolicyType.CONDITIONAL) {
            return policy;
        }
        for (ConditionRule rule : policy.rules()) {
            if (matches(rule.condition(), signals)) {
                return policy;
            }
        }
        return policyManager.byName(policy.defaultStrategy());
    }

    /** 入口：按策略类型分派到对应策略类，输出 RoutingDecision */
    public RoutingDecision decide(String requestId, String alias, Policy policy,
                                  List<ModelInstance> candidates,
                                  List<ScoringDetail> details,
                                  Signals signals) {
        return switch (policy.type()) {
            case MULTI_OBJECTIVE ->
                    multiObjectiveStrategy.decide(requestId, alias, policy, candidates, details, signals);
            case WEIGHTED_RANDOM ->
                    weightedRandomStrategy.decide(requestId, alias, policy, candidates, signals, null);
            case CONDITIONAL ->
                    conditionalStrategy.decide(requestId, alias, policy, candidates, signals);
        };
    }

    /** 条件求值：信号存在且满足 op 才命中（effectivePolicy 用；ConditionalStrategy 内同样实现一份） */
    private boolean matches(Condition condition, Signals signals) {
        Optional<Object> actual = signals.get(condition.signal());
        if (actual.isEmpty()) {
            return false;
        }
        String v = String.valueOf(actual.get());
        return switch (condition.op()) {
            case EQ -> v.equals(String.valueOf(condition.value()));
            case NE -> !v.equals(String.valueOf(condition.value()));
            case IN -> ((List<?>) condition.value()).stream()
                    .map(String::valueOf).anyMatch(v::equals);
        };
    }
}
```

```java
// 路径：ai-gateway/src/main/java/com/aigateway/decision/scheduler/MultiObjectiveStrategy.java
// 按 finalScore 降序排序，同分按 instanceId 字典序（确定性）；
// 主选 = 第一名，降级链 = 其余按得分降序。
@Component
public class MultiObjectiveStrategy {

    public RoutingDecision decide(String requestId, String alias, Policy policy,
                                  List<ModelInstance> candidates,
                                  List<ScoringDetail> details,
                                  Signals signals) {
        // 打分明细与候选必须一一对应（Scorer 契约），不一致说明上游 bug，
        // 提前抛包装异常而不是让 ordered.get(0) 裸抛 IndexOutOfBoundsException
        if (details.size() != candidates.size()) {
            throw new GatewayException(500, "internal_error",
                    "打分明细与候选数量不一致: details=" + details.size()
                            + ", candidates=" + candidates.size());
        }
        List<ScoringDetail> sorted = details.stream()
                .sorted(Comparator.comparingDouble(ScoringDetail::finalScore).reversed()
                        .thenComparing(ScoringDetail::instanceId))
                .toList();
        List<ModelInstance> ordered = sorted.stream()
                .map(d -> findById(candidates, d.instanceId()))
                .toList();
        return new RoutingDecision(requestId, alias, policy.name(),
                ordered.get(0), ordered.subList(1, ordered.size()),
                sorted, signals.snapshot(), null);
    }

    private ModelInstance findById(List<ModelInstance> candidates, String instanceId) {
        return candidates.stream()
                .filter(c -> c.instanceId().equals(instanceId))
                .findFirst()
                .orElseThrow(() -> new GatewayException(500, "internal_error",
                        "打分明细与候选不一致: " + instanceId));
    }
}
```

```java
// 路径：ai-gateway/src/main/java/com/aigateway/decision/scheduler/WeightedRandomStrategy.java
// 主选 = V1 转盘加权随机结果；降级链 = 其余候选按 weight 降序（V3 使用）。
@Component
public class WeightedRandomStrategy {

    public RoutingDecision decide(String requestId, String alias, Policy policy,
                                  List<ModelInstance> candidates,
                                  Signals signals, String ruleHit) {
        ModelInstance primary = new WeightedRandomPicker(candidates).pick()
                .orElseThrow(() -> new GatewayException(503, "no_available_model", "候选为空"));
        List<ModelInstance> rest = candidates.stream()
                .filter(c -> !c.instanceId().equals(primary.instanceId()))
                .sorted(Comparator.comparingInt(ModelInstance::weight).reversed())
                .toList();
        return new RoutingDecision(requestId, alias, policy.name(), primary, rest,
                List.of(), signals.snapshot(), ruleHit);
    }
}
```

```java
// 路径：ai-gateway/src/main/java/com/aigateway/decision/scheduler/ConditionalStrategy.java
// 🖊 H4 目标实现（当前骨架留 TODO H4：规则求值逻辑 matches）。
// 职责：只处理"命中"分支——未命中时 effectivePolicy 已把策略解析为
// defaultStrategy（MULTI_OBJECTIVE），Scheduler 分派不会走到本类。
@Component
public class ConditionalStrategy {

    private final WeightedRandomStrategy weightedRandomStrategy;

    public ConditionalStrategy(WeightedRandomStrategy weightedRandomStrategy) {
        this.weightedRandomStrategy = weightedRandomStrategy;
    }

    /** 条件路由：按声明顺序求值，命中即覆盖候选集，再在规则目标内加权随机（+ 生成降级链） */
    public RoutingDecision decide(String requestId, String alias, Policy policy,
                                  List<ModelInstance> candidates, Signals signals) {
        for (int i = 0; i < policy.rules().size(); i++) {
            ConditionRule rule = policy.rules().get(i);
            if (matches(rule.condition(), signals)) {
                List<ModelInstance> selected = candidates.stream()
                        .filter(c -> rule.select().contains(c.instanceId()))
                        .toList();
                if (!selected.isEmpty()) {
                    String hit = "rule#" + i + ":" + rule.condition().signal()
                            + "=" + rule.condition().value();
                    return weightedRandomStrategy.decide(requestId, alias, policy,
                            selected, signals, hit);
                }
            }
        }
        // 防御性兜底：effectivePolicy 已保证命中才会分派到本类，此处不应到达
        throw new GatewayException(503, "no_available_model", "条件规则未命中（effectivePolicy 应已拦截）");
    }

    /** 条件求值：信号存在且满足 op 才命中（与 Scheduler.matches 同一份语义） */
    private boolean matches(Condition condition, Signals signals) {
        Optional<Object> actual = signals.get(condition.signal());
        if (actual.isEmpty()) {
            return false;
        }
        String v = String.valueOf(actual.get());
        return switch (condition.op()) {
            case EQ -> v.equals(String.valueOf(condition.value()));
            case NE -> !v.equals(String.valueOf(condition.value()));
            case IN -> ((List<?>) condition.value()).stream()
                    .map(String::valueOf).anyMatch(v::equals);
        };
    }
}
```

设计要点：

- **策略是配置，不是代码**：加一种策略 = 加一个分支 + 一个策略模板，核心路由稳定；
- **拆类结构（脚手架已定）**：三种策略各一个 `@Component` 类（单一职责），`Scheduler` 只做分派。MultiObjectiveStrategy / WeightedRandomStrategy 已实现，手敲 H4 只需：`Scheduler.decide` 分派 + `effectivePolicy` + `ConditionalStrategy.matches`（规则求值）；
- **降级链在 V2 就生成**：`fallbackChain` 已按得分/权重排好序，V3 的完整降级链执行直接消费；
- **条件规则“命中即覆盖”**：命中后候选集被规则收窄，再在收窄集内加权随机——与插件“只收窄、不扩大”语义一致；
- **未命中不拒绝（经 effectivePolicy 解析）**：`effectivePolicy` 未命中时返回 `defaultStrategy`（MULTI_OBJECTIVE），打分与分派都基于它，避免规则写漏一条就把所有流量打挂（与 fail-closed 的”策略冲突拒绝”是两个场景）；
- **⚠️ 分派契约（H6 手敲注意）**：`Scheduler.decide` 接收的 `policy` 参数**必须是 effectivePolicy 的解析结果**——条件未命中时 effective 已是 MULTI_OBJECTIVE，分派自动落到多目标分支（`conditionalRuleHit=null`，`strategy=defaultStrategy 名`）；若传原始 policy，未命中会分派进 `ConditionalStrategy` 触发其防御性 503。条件命中时 effective == 原始 policy，`strategy=policy.name()`、`conditionalRuleHit` 有值，语义与 6.3 一致；
- **回退权重一致性**：DecisionEngine 用 `effectivePolicy` 解析有效策略再打分，未命中回退时排序用的是 `defaultStrategy` 的权重，而不是默认均衡权重（策略配置必须真实生效）；
- **规则命中但 select 为空**：信号满足但目标候选已被过滤链移除时，该规则视为未命中，继续求值下一条（”只收窄、不扩大”的运行时兜底）；
- **加权随机 / 条件命中不消费打分**：`WEIGHTED_RANDOM` 与条件命中分支的 `scoringDetails` 为空——明细只在 `MULTI_OBJECTIVE`（含条件未命中回退）下产生，debug 端点对这两种策略返回空明细属预期行为。

---

## 12. ModelStateStore 状态闭环（关键代码段 S10）

> 路径：`ai-gateway/src/main/java/com/aigateway/decision/state/ModelStateStore.java`

> 🖊 **手敲 H5**：本节全部手敲。

这是“AI Gateway + Inference Scheduler”区别于普通网关的核心：**请求产生真实运行指标 → 更新实例状态 → 驱动下一次决策**。V2 维护 EWMA 延迟与错误率；V3 再接入熔断状态。

```java
// 路径：ai-gateway/src/main/java/com/aigateway/decision/state/ModelStateStore.java
package com.aigateway.decision.state;

@Component
public class ModelStateStore {

    private final Map<String, ModelState> states = new ConcurrentHashMap<>();
    private final Map<String, AtomicInteger> inFlight = new ConcurrentHashMap<>();
    private final double latencyAlpha;  // EWMA 延迟系数（越大越跟手）
    private final double errorAlpha;    // EWMA 错误率系数
    private final ModelRegistry registry; // 读取候选的 latencyProfileMs 初始值

    public ModelStateStore(GatewayProperties props, ModelRegistry registry) {
        this.latencyAlpha = props.getDecision().getEwmaAlpha();
        this.errorAlpha = props.getDecision().getErrorRateAlpha();
        this.registry = registry;
    }

    /** 查询实例状态；首次访问用配置的 latencyProfileMs 初始化（缺省 1000） */
    public ModelState stateOf(String instanceId) {
        ModelState base = states.computeIfAbsent(instanceId, this::initialState);
        // inFlight 单独维护（高频计数），读取时合并进快照
        return new ModelState(base.instanceId(), base.ewmaLatencyMs(), base.errorRate(),
                inFlightOf(instanceId), base.lastUpdatedAt());
    }

    /**
     * 初始状态：延迟用配置的 latencyProfileMs（缺省 1000），错误率 0。
     * ⚠️ 只读 registry、不触碰 states map——CHM 明确禁止在 compute 回调里对
     * 同一 map 做任何其他更新（含 computeIfAbsent），否则运行期抛 IllegalStateException。
     */
    private ModelState initialState(String instanceId) {
        long initial = registry.findAll().stream()
                .filter(i -> i.instanceId().equals(instanceId))
                .findFirst().map(ModelInstance::latencyProfileMs).orElse(1000L);
        return new ModelState(instanceId, initial, 0.0, 0, System.currentTimeMillis());
    }

    /** 成功样本：EWMA 延迟向新样本滑动，错误率向 0 滑动 */
    public void recordSuccess(String instanceId, long latencyMs) {
        states.compute(instanceId, (id, old) -> {
            ModelState s = old != null ? old : initialState(id);
            double ewma = latencyAlpha * latencyMs + (1 - latencyAlpha) * s.ewmaLatencyMs();
            double err = (1 - errorAlpha) * s.errorRate();
            return new ModelState(id, ewma, err, s.inFlight(), System.currentTimeMillis());
        });
    }

    /** 失败样本：错误率 EWMA 向 1 滑动（延迟不更新，失败没有可用延迟样本） */
    public void recordFailure(String instanceId) {
        states.compute(instanceId, (id, old) -> {
            ModelState s = old != null ? old : initialState(id);
            double err = errorAlpha * 1.0 + (1 - errorAlpha) * s.errorRate();
            return new ModelState(id, s.ewmaLatencyMs(), err, s.inFlight(), System.currentTimeMillis());
        });
    }

    /** 请求开始：并发计数 +1（V3 负载因子输入） */
    public void beginRequest(String instanceId) {
        inFlight.computeIfAbsent(instanceId, k -> new AtomicInteger()).incrementAndGet();
    }

    /** 请求结束：并发计数 -1 */
    public void endRequest(String instanceId) {
        AtomicInteger counter = inFlight.get(instanceId);
        if (counter != null) {
            counter.decrementAndGet();
        }
    }

    private int inFlightOf(String instanceId) {
        AtomicInteger counter = inFlight.get(instanceId);
        return counter == null ? 0 : counter.get();
    }
}
```

EWMA 数学（面试可讲）：

```
new = α × sample + (1 - α) × old
```

- α 越大，越“跟手”（近期样本权重高）；α 越小，越“平滑”（抗抖动）。默认延迟 α=0.3、错误率 α=0.1；
- 相比“最近 N 次求平均”，EWMA 只需要一个状态值，O(1) 更新，天然适合高并发实时状态；
- `ConcurrentHashMap.compute` 保证并发下更新原子（两个线程同时记录样本不会丢更新）；
- ⚠️ compute 回调内**禁止**对同一 map 做任何其他更新（含 `computeIfAbsent`），初始状态必须从 map 外部构造（`initialState`）——这是 CHM 最容易踩的坑（面试可讲）。

---

## 13. DecisionEngine 决策编排（关键代码段 S11）

> 路径：`ai-gateway/src/main/java/com/aigateway/decision/engine/DecisionEngine.java`

> 🖊 **手敲 H6**：本节与第 14 节一起手敲。

决策引擎把“插件 → 过滤 → 打分 → 调度”串成一条流水线，输出不可变的 `RoutingDecision`。

```java
// 路径：ai-gateway/src/main/java/com/aigateway/decision/engine/DecisionEngine.java
package com.aigateway.decision.engine;

@Component
public class DecisionEngine {

    private final ModelRegistry registry;
    private final PluginEngine pluginEngine;
    private final FilterChain filterChain;
    private final Scorer scorer;
    private final Scheduler scheduler;
    private final PolicyManager policyManager;
    private final GatewayMetrics metrics;

    public DecisionEngine(ModelRegistry registry, PluginEngine pluginEngine,
                          FilterChain filterChain, Scorer scorer,
                          Scheduler scheduler, PolicyManager policyManager,
                          GatewayMetrics metrics) {
        this.registry = registry;
        this.pluginEngine = pluginEngine;
        this.filterChain = filterChain;
        this.scorer = scorer;
        this.scheduler = scheduler;
        this.policyManager = policyManager;
        this.metrics = metrics;
    }

    /** 决策结果 + 插件上下文（执行钩子还需要信号与元数据） */
    public record DecisionOutcome(RoutingDecision decision, PluginContext context) {}

    /**
     * 一次请求的决策流水线：
     * beforeDecision 插件（信号 + 收窄 + fail-closed）
     *   → Filter Chain 五道关卡
     *   → Scorer 多目标打分（动态调权）
     *   → Scheduler 出方案（策略分派）
     *   → afterDecision 插件（只读决策）
     *   → 指标 + 日志
     */
    public DecisionOutcome decide(ChatRequest request, String requestId,
                                  Map<String, String> metadata) {
        List<ModelInstance> initial = registry.findByAlias(request.model());
        if (initial.isEmpty()) {
            throw new GatewayException(503, "no_available_model",
                    "模型[" + request.model() + "] 不存在或未配置候选");
        }

        // 1. 决策前插件（H1）：写信号、收窄候选；清空候选 → candidates_cleared
        PluginContext ctx = new PluginContext(request, requestId, request.model(),
                metadata, initial);
        writeRequiredCapabilities(ctx);   // 请求固有属性 → 信号（能力过滤的输入，见 S7）
        pluginEngine.runPhase(PluginPhase.BEFORE_DECISION, ctx);

        // 2. Filter Chain（H2）：五道关卡收窄；清空 → no_available_model
        List<ModelInstance> filtered = filterChain.apply(request, ctx, ctx.candidates());
        ctx.setCandidates(filtered);

        // 3. Scorer（H3）：多目标打分。权重必须来自”有效策略”（H4 effectivePolicy）：
        //    CONDITIONAL 未命中时用 defaultStrategy 的权重打分，
        //    否则回退排序会按默认均衡权重算，策略配置不生效
        Policy policy = policyManager.resolve(request.model());
        Policy effective = scheduler.effectivePolicy(request.model(), policy, ctx.signals());
        List<ScoringDetail> details = scorer.score(filtered,
                effective.weights(), ctx.signals(), request);

        // 4. Scheduler（H4）：出方案。⚠️ 分派必须用 effective（见第 11.3 节分派契约）：
        //    条件未命中时 effective 已是 defaultStrategy（MULTI_OBJECTIVE），
        //    分派才会落到多目标分支；传原始 policy 会让未命中触发条件分支的 503
        RoutingDecision decision = scheduler.decide(requestId, request.model(),
                effective, filtered, details, ctx.signals());
        ctx.setDecision(decision);

        // 5. 决策后插件：只读 decision（可写审计类信号）
        pluginEngine.runPhase(PluginPhase.AFTER_DECISION, ctx);

        metrics.decision(decision);
        logDecision(decision);
        return new DecisionOutcome(decision, ctx);
    }

    /** 请求固有属性 → required_capabilities 信号（CapabilityFilter 消费，见 S7）：
     *  V2 只写 streaming（request 有 stream 字段）；tools/vision 的请求字段 V1 未定义，
     *  逻辑为 V3 预留——信号不写，CapabilityFilter 相应分支即放行 */
    private void writeRequiredCapabilities(PluginContext ctx) {
        Set<String> required = new HashSet<>();
        if (ctx.request().streaming()) {
            required.add("streaming");
        }
        if (!required.isEmpty()) {
            ctx.signals().set("required_capabilities", required);
        }
    }

    private void logDecision(RoutingDecision decision) {
        System.out.printf("[requestId=%s] event=decision alias=%s strategy=%s primary=%s "
                        + "fallback=%s ruleHit=%s%n",
                decision.requestId(), decision.alias(), decision.strategy(),
                decision.primary().instanceId(),
                decision.fallbackChain().stream().map(ModelInstance::instanceId).toList(),
                decision.conditionalRuleHit());
    }
}
```

执行层的钩子（BEFORE_EXECUTION / AFTER_EXECUTION / ON_ERROR）由第 14 节的 `ChatGatewayService` 调用，使用同一个 `PluginContext`。

---

## 14. ChatGatewayService 改造（关键代码段 S12）

> 路径：`ai-gateway/src/main/java/com/aigateway/core/service/ChatGatewayService.java`

> 🖊 **手敲 H6**：与第 13 节一起手敲。

版本 1 的主流程（健康过滤 + 加权随机 + 失败切换）拆成两半：**决策**交给 DecisionEngine，**执行**保留在 ChatGatewayService——这就是设计文档 3.2 的“决策与执行完全分离”。

### 14.1 非流式

```java
/**
 * 网关主服务（V2 改造版）：决策与执行完全分离（设计文档 3.2）。
 *
 * 分工：
 * - 决策（去哪家）：DecisionEngine 跑完 插件→过滤→打分→调度，产出 RoutingDecision（主选+降级链+明细）；
 * - 执行（怎么调）：本类只消费决策结果调上游、失败降级、记录状态、触发执行阶段钩子。
 *
 * 手敲 H6 要点：V1 的"健康过滤 + 加权随机"已整体移入 DecisionEngine，
 * 本类不再自己选候选——只读 decision.primary() / decision.fallbackChain()。
 */
@Service
public class ChatGatewayService {

    private final DecisionEngine decisionEngine;      // 决策流水线：插件→过滤→打分→调度（H1~H5）
    private final DecisionLogStore decisionLogStore;  // 决策日志环形缓冲：每次决策明细，Debug 接口可查
    private final OpenAIConnector connector;          // 上游调用 + SSE 解析（H3）
    private final ModelStateStore stateStore;         // 实例实时状态：EWMA 延迟/错误率/并发数，供下次决策打分
    private final GatewayMetrics metrics;             // Prometheus 指标：成功/失败/chunk/决策
    private final PluginEngine pluginEngine;          // 执行阶段钩子：BEFORE_EXECUTION / AFTER_EXECUTION / ON_ERROR

    public ChatGatewayService(DecisionEngine decisionEngine, DecisionLogStore decisionLogStore,
                              OpenAIConnector connector, ModelStateStore stateStore,
                              GatewayMetrics metrics, PluginEngine pluginEngine) {
        this.decisionEngine = decisionEngine;
        this.decisionLogStore = decisionLogStore;
        this.connector = connector;
        this.stateStore = stateStore;
        this.metrics = metrics;
        this.pluginEngine = pluginEngine;
    }

    /**
     * 非流式入口：三步流程——决策 → 落决策日志 → 执行主选。
     * 决策失败（无候选/条件规则锁死白名单为空）会直接在 decide() 里抛 503，不会走到这里。
     */
    public ChatCompletion complete(ChatRequest request, String requestId,
                                   Map<String, String> metadata) {
        DecisionEngine.DecisionOutcome outcome = decisionEngine.decide(request, requestId, metadata);
        decisionLogStore.record(outcome.decision());   // 决策明细先落库：执行结果与决策一一对应
        return execute(outcome, request, requestId);
    }

    /**
     * 执行主选：先跑 BEFORE_EXECUTION 钩子（插件可改写请求体），成功记成功状态，
     * 失败记失败状态并沿降级链再试一次。
     *
     * 状态记录与 EWMA 的关系（对照 4.4 实时状态）：
     * beginRequest（并发 +1）→ 调用 → recordSuccess/recordFailure（更新 EWMA 延迟/错误率）→ endRequest（并发 −1）
     * 下次决策打分读到的就是本次修正后的状态——每个请求都在自我修正。
     */
    private ChatCompletion execute(DecisionEngine.DecisionOutcome outcome,
                                   ChatRequest request, String requestId) {
        RoutingDecision decision = outcome.decision();
        outcome.context().setError(null);   // 清掉上一次的错误残留（同一 context 跨阶段复用）
        pluginEngine.runPhase(PluginPhase.BEFORE_EXECUTION, outcome.context());

        long start = System.nanoTime();     // nanoTime：单调时钟，只用于算差值，不表示墙上时间
        ModelInstance primary = decision.primary();
        stateStore.beginRequest(primary.instanceId());  // 并发计数雏形（V3 负载因子输入）
        try {
            ChatCompletion result = connector.complete(primary, request);
            stateStore.recordSuccess(primary.instanceId(), elapsedMs(start));
            metrics.success(request.model(), primary.instanceId());
            pluginEngine.runPhase(PluginPhase.AFTER_EXECUTION, outcome.context());  // 成功才触发
            return result;
        } catch (Exception e) {
            stateStore.recordFailure(primary.instanceId());  // 失败样本：错误率向 1 滑动
            metrics.failure(request.model(), primary.instanceId());
            return fallbackOnce(outcome, request, requestId, e);  // 主选失败 → 降级
        } finally {
            stateStore.endRequest(primary.instanceId());  // 无论成败都释放并发计数
        }
    }

    /**
     * V2 最小降级：主选失败时沿 fallbackChain 只再试一次（V3 做成完整降级链执行）。
     * 语义：降级目标是"决策时排好的顺序"，不是执行时重新加权随机——
     * 决策与执行完全分离的体现：去哪家是决策层定的，执行层不重新选。
     */
    private ChatCompletion fallbackOnce(DecisionEngine.DecisionOutcome outcome,
                                        ChatRequest request, String requestId, Exception cause) {
        RoutingDecision decision = outcome.decision();
        if (decision.fallbackChain().isEmpty()) {
            notifyError(outcome, cause);   // 无降级可试 → ON_ERROR 通知插件 → 502
            throw new GatewayException(502, "upstream_failed",
                    "主选候选失败且无降级候选: " + cause.getMessage(), cause);
        }
        ModelInstance backup = decision.fallbackChain().get(0);   // 只取链头（V2 最小降级）
        log(requestId, "决策降级",
                decision.primary().instanceId() + " -> " + backup.instanceId());
        long start = System.nanoTime();
        stateStore.beginRequest(backup.instanceId());
        try {
            ChatCompletion result = connector.complete(backup, request);
            stateStore.recordSuccess(backup.instanceId(), elapsedMs(start));  // 传耗时，不是时钟值
            metrics.success(request.model(), backup.instanceId());
            pluginEngine.runPhase(PluginPhase.AFTER_EXECUTION, outcome.context());
            return result;
        } catch (Exception e2) {
            stateStore.recordFailure(backup.instanceId());
            metrics.failure(request.model(), backup.instanceId());
            notifyError(outcome, e2);      // 主选+降级都失败 → ON_ERROR → 502
            throw new GatewayException(502, "upstream_failed",
                    "主选与降级候选均失败: " + e2.getMessage(), e2);
        } finally {
            stateStore.endRequest(backup.instanceId());
        }
    }

    /** ON_ERROR 钩子：最终失败上抛给客户端前通知插件（ctx.error 置为原始异常，插件可读） */
    private void notifyError(DecisionEngine.DecisionOutcome outcome, Exception cause) {
        outcome.context().setError(cause);
        pluginEngine.runPhase(PluginPhase.ON_ERROR, outcome.context());
    }

    /** 耗时换算：nanoTime 差值 → 毫秒（为什么用 nanoTime 而非 currentTimeMillis：不受系统改时间影响） */
    private static long elapsedMs(long startNanos) {
        return TimeUnit.NANOSECONDS.toMillis(System.nanoTime() - startNanos);
    }

    private static void log(String requestId, String event, String detail) {
        System.out.printf("[requestId=%s] event=%s detail=%s%n", requestId, event, detail);
    }
}
```

> 说明：执行阶段钩子（BEFORE_EXECUTION / AFTER_EXECUTION / ON_ERROR）由本类直接调用注入的 `PluginEngine`，与决策阶段共用同一个 `PluginContext`。钩子触发规则：成功 → AFTER_EXECUTION；最终失败（含降级也失败）→ ON_ERROR。

### 14.2 流式

与 V1 相同的安全边界：**首字节前失败才可切换；已经开始输出则直接中断**。区别是从 `decision.fallbackChain()` 取降级目标。

```java
/**
 * 流式入口：与 V1 相同的安全边界——首字节前失败才可切换；已经开始输出则直接中断。
 *
 * 为什么有这个边界（对照 V1 设计要点）：流式一旦发出第一个 chunk，客户端就收到了部分内容，
 * 此时重试会让客户端看到重复/错位的内容（不可安全重放），所以宁可 502 中断。
 * 区别：降级目标从 decision.fallbackChain() 取（V1 是剩余候选重新加权随机）。
 */
public void stream(ChatRequest request, String requestId, Map<String, String> metadata,
                   Consumer<ChatChunk> consumer) {
    DecisionEngine.DecisionOutcome outcome = decisionEngine.decide(request, requestId, metadata);
    decisionLogStore.record(outcome.decision());
    RoutingDecision decision = outcome.decision();

    // started 用单元素数组：lambda 里要修改外部变量，Java 要求它"有效最终"，
    // 数组元素不是变量本身，所以可以修改（学习点：lambda 捕获）
    boolean[] started = {false};
    long start = System.nanoTime();
    ModelInstance primary = decision.primary();
    stateStore.beginRequest(primary.instanceId());
    try {
        connector.stream(primary, request, chunk -> {
            if (!started[0]) {
                started[0] = true;
                // 流式样本：用首字节延迟记录成功（V3 再细化完成态）
                // 注意：流式只记"首字节"这一个样本，不是整段流——成功样本语义 = 响应可用
                stateStore.recordSuccess(primary.instanceId(), elapsedMs(start));
            }
            metrics.streamChunk(request.model(), primary.instanceId());
            consumer.accept(chunk);   // chunk 转给上层 Controller 写入 SseEmitter
        });
    } catch (Exception e) {
        if (started[0]) {
            notifyError(outcome, e);   // 已输出内容不可重放：直接 502（安全边界）
            throw new GatewayException(502, "upstream_failed",
                    "流式输出中断: " + e.getMessage(), e);
        }
        // 首字节前失败：与 V1 相同的处理——记录失败样本后尝试降级
        stateStore.recordFailure(primary.instanceId());
        metrics.failure(request.model(), primary.instanceId());
        if (!decision.fallbackChain().isEmpty()) {
            ModelInstance backup = decision.fallbackChain().get(0);
            log(requestId, "流式降级(首字节前)",
                    primary.instanceId() + " -> " + backup.instanceId());
            stateStore.beginRequest(backup.instanceId());
            try {
                connector.stream(backup, request, consumer);   // 降级成功：直接复用 consumer 转发
                return;
            } catch (Exception e2) {
                // 与非流式一致的语义：主选与降级均失败 → 502 + 计数 + ON_ERROR
                stateStore.recordFailure(backup.instanceId());
                metrics.failure(request.model(), backup.instanceId());
                notifyError(outcome, e2);
                throw new GatewayException(502, "upstream_failed",
                        "主选与降级候选均失败: " + e2.getMessage(), e2);
            } finally {
                stateStore.endRequest(backup.instanceId());
            }
        }
        notifyError(outcome, e);   // 无降级可试：ON_ERROR → 502
        throw new GatewayException(502, "upstream_failed",
                "主选候选失败且无降级候选: " + e.getMessage(), e);
    } finally {
        stateStore.endRequest(primary.instanceId());   // 主选的并发计数无论成败都释放
    }
}
```

### 14.3 Controller metadata 传递

`ChatCompletionController` 把关键 header 提取成 metadata 传入网关服务（示例插件消费）：

```java
private Map<String, String> metadata(HttpServletRequest httpRequest) {
    return Map.of(
            "tenant", header(httpRequest, "X-Tenant-Id", "default"),
            "canary_group", header(httpRequest, "X-Canary-Group", "stable"));
}

private String header(HttpServletRequest req, String name, String fallback) {
    String value = req.getHeader(name);
    return value == null || value.isBlank() ? fallback : value;
}
```

`gatewayService.complete(request, requestId, metadata)` / `gatewayService.stream(request, requestId, metadata, consumer)` 同步改签名。

> 现状：Controller 已实现 `metadata()` / `header()` 提取（含 TODO H6 注释）；`gatewayService.complete/stream` 目前仍是 V1 签名（无 metadata 参数），把 metadata 传进去是 H6 手敲内容。

---

## 15. 决策日志与环形缓冲（关键代码段 S13，Codex）

> 路径：`ai-gateway/src/main/java/com/aigateway/decision/log/DecisionLogStore.java`、`ai-gateway/src/main/java/com/aigateway/api/controller/DecisionDebugController.java`

### 15.1 环形缓冲

```java
@Component
public class DecisionLogStore {

    private final int maxEntries;                    // gateway.decision.logSize
    private final Deque<RoutingDecision> ring = new ConcurrentLinkedDeque<>();

    public DecisionLogStore(GatewayProperties props) {
        this.maxEntries = props.getDecision().getLogSize();
    }

    /** 新决策放头部；超出容量从尾部淘汰（固定内存、自动淘汰旧数据） */
    public void record(RoutingDecision decision) {
        ring.addFirst(decision);
        while (ring.size() > maxEntries) {
            ring.pollLast();
        }
    }

    /** 最近 N 条（新 → 旧） */
    public List<RoutingDecision> recent(int limit) {
        return ring.stream().limit(Math.max(0, Math.min(limit, maxEntries))).toList();
    }
}
```

为什么用环形缓冲（面试可讲）：固定内存上限、O(1) 插入与淘汰、天然保留“最近 N 条”；版本 5 会把它替换/升级为可查询的持久化存储。

### 15.2 调试查询端点

```java
@RestController
public class DecisionDebugController {

    private final DecisionLogStore decisionLogStore;

    public DecisionDebugController(DecisionLogStore decisionLogStore) {
        this.decisionLogStore = decisionLogStore;
    }

    /** GET /v1/debug/decisions?limit=10（V5 由 Admin API 取代） */
    @GetMapping("/v1/debug/decisions")
    public Map<String, Object> recent(@RequestParam(defaultValue = "10") int limit) {
        List<RoutingDecision> list = decisionLogStore.recent(limit);
        return Map.of("count", list.size(), "decisions", list);
    }
}
```

### 15.3 打分明细日志格式

决策引擎已打印摘要行；完整明细存在环形缓冲中，可通过调试端点拿到 JSON。日志建议同时输出一条完整明细（学习阶段便于肉眼核对）：

```java
// DecisionEngine.logDecision 中追加：
System.out.printf("[requestId=%s] event=scoring_details %s%n",
        decision.requestId(), objectMapper.writeValueAsString(decision.scoringDetails()));
```

示例明细（演示 B 中会看到两组不同结果；cost 按默认参数估算：请求 "你好" ≈ 1 input token + defaultOutputTokens=256，即 1/1000×0.01 + 256/1000×0.02 = 0.00513）：

```json
[
  {
    "instanceId": "mock-b:qwen-small",
    "raw": { "latency": 200.0, "cost": 0.00513, "quality": 0.4, "health": 1.0 },
    "normalized": { "latency": 1.0, "cost": 0.0, "quality": 0.0, "health": 1.0 },
    "weights": { "latency": 0.6, "cost": 0.1, "quality": 0.15, "health": 0.15 },
    "finalScore": 0.75
  }
]
```

### 15.4 错误码约定（V2 增量）

| 场景 | HTTP | type |
|---|---|---|
| 插件在决策前清空候选池（fail-closed） | 503 | `candidates_cleared` |
| 插件执行阶段抛异常 | 500 | `plugin_error` |
| 候选经 Filter Chain 过滤后为空 | 503 | `no_available_model`（沿用 V1） |

---

## 16. 示例插件（关键代码段 S14，Codex）

> 路径：`ai-gateway/src/main/java/com/aigateway/plugin/example/`

三个示例插件演示“零侵入生效”：核心代码不动，只加类 + 改配置。

### 16.1 复杂度识别 `ComplexityRecognizerPlugin`

写 `task_complexity` 信号，供 CONDITIONAL 策略与 Scorer 动态调权消费。

```java
@Component
public class ComplexityRecognizerPlugin implements GatewayPlugin {

    private Pattern keywordPattern = Pattern.compile("代码|算法|重构|设计");

    public String name() { return "complexity-recognizer"; }
    public int order() { return 30; }

    /** 配置校验：keywordRegex 必须能编译成合法正则，否则启动失败 */
    public void validate(Map<String, Object> config) {
        String regex = (String) config.get("keywordRegex");
        if (regex != null && !regex.isBlank()) {
            try {
                Pattern.compile(regex);
            } catch (Exception e) {
                throw new GatewayException(500, "invalid_config",
                        "keywordRegex 不是合法正则: " + regex);
            }
        }
    }

    public void configure(Map<String, Object> config) {
        String regex = (String) config.get("keywordRegex");
        if (regex != null && !regex.isBlank()) {
            keywordPattern = Pattern.compile(regex);
        }
    }

    public void beforeDecision(PluginContext ctx) {
        String content = ctx.request().messages() == null ? "" : ctx.request().messages().stream()
                .map(ChatRequest.Message::content)
                .filter(s -> s != null)
                .collect(Collectors.joining(" "));
        boolean complex = keywordPattern.matcher(content).find();
        ctx.signals().set("task_complexity", complex ? "COMPLEX" : "SIMPLE");
    }
}
```

### 16.2 金丝雀分组 `CanaryGroupPlugin`

按请求头 `X-Canary-Group` 写 `allowed_instances` 信号，由 Filter Chain 第 5 道统一消费。

```java
@Component
public class CanaryGroupPlugin implements GatewayPlugin {

    private Set<String> stableInstances = Set.of();
    private Set<String> canaryInstances = Set.of();
    // 注意：脚手架实现没有 enabled 开关——禁用金丝雀 = 不挂载该插件（见 21.4）

    public String name() { return "canary"; }
    public int order() { return 20; }

    public void validate(Map<String, Object> config) {
        if (config.get("stableInstances") == null || config.get("canaryInstances") == null) {
            throw new GatewayException(500, "invalid_config",
                    "canary 插件需要配置 stableInstances 与 canaryInstances");
        }
    }

    public void configure(Map<String, Object> config) {
        stableInstances = toStringSet(config.get("stableInstances"));
        canaryInstances = toStringSet(config.get("canaryInstances"));
    }

    public void beforeDecision(PluginContext ctx) {
        String group = ctx.metadata().getOrDefault("canary_group", "stable");
        ctx.signals().set("canary_group", group);
        // 金丝雀流量只进配置允许的候选集合：
        // stable 组 → 稳定实例；experimental 组 → 灰度实例；未配置的实例一律不可达
        Set<String> allowed = "stable".equals(group) ? stableInstances : canaryInstances;
        ctx.signals().set("allowed_instances", allowed);
    }

    private static Set<String> toStringSet(Object value) {
        if (value == null) {
            return Set.of(); // null = 该分组未配置任何实例（配合白名单“存在但为空 → 全淘汰”语义）
        }
        if (value instanceof List<?> list) {
            return list.stream().map(String::valueOf).collect(Collectors.toSet());
        }
        return Set.of(String.valueOf(value));
    }
}
```

### 16.3 清空候选测试插件 `ClearCandidatesTestPlugin`

只用于演示 / 测试 fail-closed，默认关闭。

```java
@Component
public class ClearCandidatesTestPlugin implements GatewayPlugin {

    private boolean enabled = false;

    public String name() { return "clear-candidates"; }
    public int order() { return 10; }

    public void configure(Map<String, Object> config) {
        this.enabled = Boolean.parseBoolean(String.valueOf(config.getOrDefault("enabled", false)));
    }

    public void beforeDecision(PluginContext ctx) {
        if (enabled) {
            ctx.candidates().clear(); // 模拟策略冲突 → 引擎抛 503 candidates_cleared
        }
    }
}
```

> 学习提示：真实项目的业务插件（鉴权 / 限流 / 护栏）会按同一 SPI 开发，核心路由代码零改动——这就是“插件化架构为什么比 if-else 好”的直观答案。

---

## 17. 指标（关键代码段 S15，Codex）

> 路径：`ai-gateway/src/main/java/com/aigateway/observability/GatewayMetrics.java`

```java
@Component
public class GatewayMetrics {

    private final MeterRegistry meterRegistry;
    /** filterDrops 的 Counter 缓存：避免每次调用重建 Builder（Micrometer 会复用同名 meter，缓存仅省去重复构建开销） */
    private final Map<String, Counter> filterDropCounters = new ConcurrentHashMap<>();

    // V1 的 success/failure/streamChunk 保留，以下为 V2 增量：

    /** 决策分布：gateway_decisions_total{model,strategy}（对应设计文档 11.1） */
    public void decision(RoutingDecision decision) {
        counter("gateway.decisions.total", decision.alias(), decision.strategy(), "")
                .increment();
    }

    /** 过滤淘汰计数：gateway_filter_drops_total{model,filter} */
    public void filterDrops(String alias, String filter, int dropped) {
        filterDropCounters.computeIfAbsent(alias + "|" + filter, k ->
                        Counter.builder("gateway.filter.drops")
                                .tag("model", alias)
                                .tag("filter", filter)
                                .register(meterRegistry))
                .increment(dropped);
    }
}
```

访问 `GET /actuator/prometheus`，可看到：

```text
gateway_decisions_total{model="qwen",strategy="simple-task"} 5.0
gateway_filter_drops_total{model="qwen",filter="capability"} 1.0
```

---

## 18. mock 服务端修改（关键代码段 S16，Codex）

**mock-model-server 本轮基本无需改动**：延迟（latencyMs）、故障（errorRate）、健康开关（health）在版本 1 已支持；打分所需的真实延迟样本由网关侧计时产生，不依赖 mock 主动上报。

唯一建议（可选）：演示时把两个实例的延迟差距拉大，让打分结果更直观——

```bash
# mock-a 用 qwen-large（基础延迟约 2000ms），mock-b 用 qwen-small（约 200ms）
mvn spring-boot:run -Dspring-boot.run.arguments=--server.port=8001,--mock.model=qwen-large
mvn spring-boot:run -Dspring-boot.run.arguments=--server.port=8002,--mock.model=qwen-small
```

`GET /mock/behavior` 可随时确认当前 latencyMs / errorRate / health，配合演示。

---

## 19. 一次请求的完整时序（非流式）

```
Client
  │ POST /v1/chat/completions {model:"qwen", messages:[...], headers:{X-Canary-Group}}
  ▼
RequestIdFilter（生成 requestId）
  ▼
ChatCompletionController（提取 metadata：tenant / canary_group）
  ▼
ChatGatewayService.complete()                          ← 🖊 H6
  └─ DecisionEngine.decide()
       ├─ PluginEngine.runPhase(BEFORE_DECISION)       ← 🖊 H1
       │    ├─ complexity-recognizer → signals.task_complexity
       │    ├─ canary → signals.allowed_instances
       │    └─ 候选被清空 → 503 candidates_cleared（fail-closed）
       ├─ FilterChain.apply() 五道关卡                  ← 🖊 H2
       │    ├─ capability → availability → policy → budget → plugin-signal
       │    └─ 清空 → 503 no_available_model
       ├─ Scorer.score()（动态调权 + 归一化 + 明细）      ← 🖊 H3
       ├─ Scheduler.decide()（策略分派 → RoutingDecision）← 🖊 H4
       └─ PluginEngine.runPhase(AFTER_DECISION)（只读）
  ├─ DecisionLogStore.record（环形缓冲，/v1/debug/decisions 可查）
  ├─ PluginEngine.runPhase(BEFORE_EXECUTION)
  ├─ ModelStateStore.beginRequest(primary) → OpenAIConnector.complete(primary) → mock
  │    ├─ 成功 → recordSuccess（EWMA 延迟/错误率）→ AFTER_EXECUTION → endRequest ← 🖊 H5
  │    ├─ 失败 → recordFailure → fallbackOnce（沿降级链再试一次，同样 begin/end + 计数）
  │    └─ 全部失败 → ON_ERROR 通知插件 → endRequest → 502 upstream_failed
  └─ GatewayMetrics（decisions / filter.drops / requests）→ /actuator/prometheus
```

流式时序相同，差异只有两点：

1. 决策后执行 `connector.stream(primary, ...)`，逐 chunk 转发；
2. **首字节前**失败才沿降级链再试；已经开始输出则直接 `502 upstream_failed`（安全边界，V3 沿用）。

---

## 20. 测试计划

### 20.1 单元测试（JUnit + Mockito）

> 测试现状（2026-08-07）：`PolicyManagerTest` / `PluginRegistryTest` / `ModelRegistryTest` / `ChatGatewayServiceTest` / `OpenAIConnectorTest` / `RequestIdFilterTest` / `WeightedRandomPickerTest` / `ChatGatewayIntegrationTest` 已随脚手架提交；`CapabilityFilterTest` / `PluginSignalFilterTest` / `MultiObjectiveStrategyTest` / `PluginContextTest` / `CanaryGroupPluginTest` 已新增（未提交）。下表中 H1~H6 相关测试（PluginEngineTest / FilterChainTest / ScorerTest / SchedulerTest / ModelStateStoreTest / DecisionEngineTest）在对应手敲完成后补写。

| 测试类 | 用例 | 断言要点 | 对应 |
|---|---|---|---|
| `plugin/engine/PluginEngineTest.java` | 按 order 顺序执行 | 执行顺序记录与 order 一致 | H1 |
| `plugin/engine/PluginEngineTest.java` | GLOBAL / ROUTE 作用域 | ROUTE 插件只对指定 alias 生效 | H1 |
| `plugin/engine/PluginEngineTest.java` | 后置插件可见前置信号与收窄结果 | 候选列表传递、signals 可见 | H1 |
| `plugin/engine/PluginEngineTest.java` | 插件清空候选 | 503 `candidates_cleared`，后续插件不再执行 | H1 |
| `plugin/engine/PluginEngineTest.java` | 插件抛异常 | 500 `plugin_error`，message 含插件名与阶段 | H1 |
| `plugin/registry/PluginRegistryTest.java` | 未知插件名 / 重复挂载 / validate 失败 | 启动抛 `invalid_config` | 脚手架 |
| `decision/filter/FilterChainTest.java` | 五道关卡按 order 执行 | 淘汰顺序正确 | H2 |
| `decision/filter/FilterChainTest.java` | 信号白名单收窄 / 过滤清空 | 503 `no_available_model`；filterDrops 计数 | H2 |
| `decision/scorer/ScorerTest.java` | min-max 归一化与边界 | max==min 给满分；低延迟/低成本得分更高 | H3 |
| `decision/scorer/ScorerTest.java` | 动态调权 | task_complexity=COMPLEX 时质量权重升高 | H3 |
| `decision/scorer/ScorerTest.java` | 明细字段 | raw/normalized/weights/finalScore 完整 | H3 |
| `decision/scheduler/SchedulerTest.java` | 多目标排序 | 主选为最高分，降级链按得分降序 | H4 |
| `decision/scheduler/SchedulerTest.java` | 加权随机 | 权重 8:2 跑 1000 次落在宽松区间（75%~85%） | H4 |
| `decision/scheduler/SchedulerTest.java` | 条件命中 / 未命中 | 命中覆盖候选集；未命中回退 defaultStrategy | H4 |
| `decision/state/ModelStateStoreTest.java` | EWMA 收敛 | 连续成功样本后延迟接近样本值；错误率下降 | H5 |
| `decision/state/ModelStateStoreTest.java` | 并发更新 | 多线程 record 不丢更新（最终状态合法） | H5 |
| `decision/policy/PolicyManagerTest.java` | 非法权重 / 非法引用 | 权重和 ≠ 1、select 非本别名候选 → 启动失败 | 脚手架 |
| `decision/engine/DecisionEngineTest.java` | 端到端编排 | 插件→过滤→打分→调度全链路；503/502 语义保持 | H6 |
| `core/service/ChatGatewayServiceTest.java` | 更新构造器 + 降级 | 主选失败 → fallbackChain[0]；全失败 → 502 | H6 |

关键测试示例（代码段 S17，H1/H3 完成后可跑）：

```java
// 路径：ai-gateway/src/test/java/com/aigateway/plugin/engine/PluginEngineTest.java
@Test
void pluginClearsCandidates_shouldRejectWithCandidatesCleared() {
    GatewayPlugin clearer = new GatewayPlugin() {
        public String name() { return "clear"; }
        public void beforeDecision(PluginContext ctx) { ctx.candidates().clear(); }
    };
    PluginEngine engine = new PluginEngine(singlePluginRegistry(clearer));
    PluginContext ctx = contextWithCandidates("mock-a:qwen-large", "mock-b:qwen-small");

    assertThatThrownBy(() -> engine.runPhase(PluginPhase.BEFORE_DECISION, ctx))
            .isInstanceOf(GatewayException.class)
            .satisfies(e -> {
                assertThat(((GatewayException) e).getStatus()).isEqualTo(503);
                assertThat(((GatewayException) e).getType()).isEqualTo("candidates_cleared");
            });
}
```

```java
// 路径：ai-gateway/src/test/java/com/aigateway/decision/scorer/ScorerTest.java
@Test
void lowerLatencyAndCost_shouldScoreHigherWhenWeightsFavorThem() {
    // a：延迟 2000、价格低、质量 0.9；b：延迟 200、价格高、质量 0.4
    // latency-first → b 第一；cost-first → a 第一（演示 B 的单元版）
    List<ScoringDetail> latencyFirst = scorer.score(List.of(a, b),
            Map.of("latency", 0.6, "cost", 0.1, "quality", 0.15, "health", 0.15), signals, req);
    assertThat(latencyFirst.get(0).instanceId()).isEqualTo("mock-b:qwen-small");

    List<ScoringDetail> costFirst = scorer.score(List.of(a, b),
            Map.of("latency", 0.1, "cost", 0.6, "quality", 0.2, "health", 0.1), signals, req);
    assertThat(costFirst.get(0).instanceId()).isEqualTo("mock-a:qwen-large");
}
```

### 20.2 集成测试

`ai-gateway/src/test/java/com/aigateway/ChatGatewayIntegrationTest.java` 目前为 V1 四场景（非流式 / 流式 / 故障切换 / 健康剔除），随脚手架提交。H1~H6 完成后在此基础新增三个场景（V2 验收）：

1. **场景 A（插件零侵入生效）**：用 property 覆盖启用 complexity + canary 插件，代码类请求（含“请写一段 Java 代码”）命中条件路由 → `qwen-large`；`X-Canary-Group: experimental` → `qwen-small`。
2. **场景 B（可解释路由）**：不重启网关（避免跨进程断言）——请求后 `GET /v1/debug/decisions` 返回的打分明细结构完整（instanceId / raw / normalized / weights / finalScore 字段齐全）；"不同权重 → 不同主选"由 ScorerTest 单测覆盖（20.1 已列），演示 B 用 curl 展示。
3. **场景 C（fail-closed）**：在测试 model.yml 的 plugins 段追加 clear-candidates 挂载（`config.enabled: true`，该插件支持 enabled 开关），请求返回 503 且 `type=candidates_cleared`。

回归要求：V1 四个场景必须继续通过（旧配置缺省策略/空插件链可启动）。

---

## 21. 演示与验收（curl 命令）

### 21.1 演示配置

使用第 5.1 节的 `model.yml`（qwen 走 `simple-task` 条件策略 + 两个示例插件），关键点：

- mock-a（qwen-large）：延迟 2000ms、价格低（0.001/0.002）、质量 0.9 → **成本/质量优先时胜出**；
- mock-b（qwen-small）：延迟 200ms、价格高（0.01/0.02）、质量 0.4 → **延迟优先时胜出**；
- 条件规则：`task_complexity=COMPLEX` → 只选 mock-a；
- 金丝雀：`X-Canary-Group: stable` → mock-a；`experimental` → mock-b。

### 21.2 启动

```bash
# 1) 启动两个 mock 实例
cd mock-model-server
mvn spring-boot:run -Dspring-boot.run.arguments=--server.port=8001,--mock.model=qwen-large &
mvn spring-boot:run -Dspring-boot.run.arguments=--server.port=8002,--mock.model=qwen-small &

# 2) 先按第 5.1 节把主 model.yml 扩展为 V2 结构（当前主配置保持 V1 结构，
#    需手动补 decision 段、strategy、候选价格/质量/延迟、policies、plugins），再启动网关
cd ../ai-gateway
mvn spring-boot:run
```

### 21.3 演示 A：插件零侵入生效

```bash
# 复杂任务 → 条件路由命中 → qwen-large
curl -s http://localhost:8080/v1/chat/completions \
  -H 'Content-Type: application/json' \
  -d '{"model":"qwen","messages":[{"role":"user","content":"请写一段 Java 代码实现快速排序"}]}'
# 网关日志：event=decision strategy=simple-task ruleHit=rule#0:task_complexity=COMPLEX primary=mock-a:qwen-large

# 金丝雀分组：stable 组只进稳定候选
curl -s http://localhost:8080/v1/chat/completions \
  -H 'Content-Type: application/json' -H 'X-Canary-Group: stable' \
  -d '{"model":"qwen","messages":[{"role":"user","content":"你好"}]}'
# → primary=mock-a:qwen-large

curl -s http://localhost:8080/v1/chat/completions \
  -H 'Content-Type: application/json' -H 'X-Canary-Group: experimental' \
  -d '{"model":"qwen","messages":[{"role":"user","content":"你好"}]}'
# → primary=mock-b:qwen-small
```

### 21.4 演示 B：可解释路由（latency-first vs cost-first）

```bash
# ⚠️ 演示 B 必须禁用金丝雀插件：否则 stable 组会把 mock-b 提前淘汰，
#   两种策略都只能选出 mock-a，打分对比无法展示。
#   注：CanaryGroupPlugin 没有 enabled 开关（见 16.2），禁用 = 编辑 model.yml
#   注释掉 canary 挂载项后重启（plugins[1] 即 model.yml 里的 canary）
# 停掉网关，注释 model.yml 中 canary 挂载项，以 latency-first 重启：
cd ai-gateway
mvn spring-boot:run -Dspring-boot.run.arguments=--gateway.models[0].strategy=latency-first

# 普通对话请求（同一份 body，不带金丝雀 header）：
curl -s http://localhost:8080/v1/chat/completions \
  -H 'Content-Type: application/json' \
  -d '{"model":"qwen","messages":[{"role":"user","content":"你好"}]}'
# → primary=mock-b:qwen-small（延迟低，latency 权重 0.6 占主导）

# 停掉，以 cost-first 重启（canary 挂载项保持注释）：
mvn spring-boot:run -Dspring-boot.run.arguments=--gateway.models[0].strategy=cost-first

# 同样的请求：
curl -s http://localhost:8080/v1/chat/completions \
  -H 'Content-Type: application/json' \
  -d '{"model":"qwen","messages":[{"role":"user","content":"你好"}]}'
# → primary=mock-a:qwen-large（价格低，cost 权重 0.6 占主导）

# 对比两份决策日志 / 决策记录：
curl -s 'http://localhost:8080/v1/debug/decisions?limit=5'
# 两份明细的 weights、normalized、finalScore 不同，日志能说清“为什么选它”
```

### 21.5 演示 C：fail-closed

```bash
# 在 model.yml 的 plugins 段追加 clear-candidates 挂载（config.enabled: true），然后启动
cd ai-gateway
mvn spring-boot:run

curl -s -i http://localhost:8080/v1/chat/completions \
  -H 'Content-Type: application/json' \
  -d '{"model":"qwen","messages":[{"role":"user","content":"你好"}]}'
# 期望：503 {"type":"candidates_cleared","message":"插件[clear-candidates]在BEFORE_DECISION阶段清空了候选池"}
```

### 21.6 决策记录与指标

```bash
# 最近决策（含打分明细，可解释路由）
curl -s 'http://localhost:8080/v1/debug/decisions?limit=10'

# 指标：决策分布 + 过滤淘汰
curl -s http://localhost:8080/actuator/prometheus | grep -E 'gateway_(decisions|filter)_'
```

### 21.7 验收标准对照（与学习版文档一致）

1. **插件框架**：全局/路由级作用域、顺序执行、上下文传递、配置校验 → 单测 + 演示 A；
2. **三类策略可配置生效**：MULTI_OBJECTIVE / WEIGHTED_RANDOM / CONDITIONAL → 演示 A/B + PolicyManagerTest；
3. **打分明细随日志输出，内存保留最近 N 条可查** → 演示 B + `/v1/debug/decisions`；
4. **fail-closed 有单元测试** → PluginEngineTest；
5. **版本 1 全部功能回归通过** → 集成测试 V1 四场景。

---

## 22. 任务清单（按顺序执行）

| # | 任务 | 产出 | 验收 | 归属 |
|---|---|---|---|---|
| T1 | 扩展领域模型与配置绑定 | ModelInstance / Capability / GatewayProperties / ModelRegistry 增量（ChatRequest 不动，token 估算在过滤器内私有实现） | 编译通过；旧配置无 policies/plugins 也能启动 | Codex |
| T2 | PolicyManager 加载与校验 | `decision/policy/PolicyManager.java` + 单测 | 权重和 ≠ 1、非法引用 → 启动失败且报错可读 | Codex |
| T3 | 插件 SPI / Context / Signals / Registry | `plugin/` 基础类 + 单测 | 注册表校验（未知名/重复/validate 失败）通过 | Codex |
| T4 | 插件执行引擎 | `plugin/engine/PluginEngine.java` + 单测 | fail-closed / 顺序 / 作用域用例全绿 | 🖊 H1 |
| T5 | Filter Chain 与五类过滤器 | `decision/filter/` + 单测 | 过滤顺序 / 淘汰 / 清空用例全绿 | 编排 🖊 H2，个体 Codex |
| T6 | ModelStateStore | `decision/state/ModelStateStore.java` + 单测 | EWMA 收敛与并发用例全绿 | 🖊 H5 |
| T7 | Scorer | `decision/scorer/Scorer.java` + 单测 | 归一化 / 动态调权 / 明细用例全绿 | 🖊 H3 |
| T8 | Scheduler 分派 + ConditionalStrategy | `decision/scheduler/Scheduler.java` + `ConditionalStrategy.java` + 单测 | 三策略用例全绿（含 effectivePolicy 回退权重） | 🖊 H4 |
| T9 | DecisionEngine + ChatGatewayService 改造 | `decision/engine/DecisionEngine.java` + `core/service/ChatGatewayService.java` | 端到端决策单测 + V1 回归通过 | 🖊 H6 |
| T10 | 示例插件 | `plugin/example/` 三个插件 | 演示 A 通过 | Codex |
| T11 | 决策日志 / 调试端点 / 指标 | DecisionLogStore / DecisionDebugController / GatewayMetrics | debug 端点与指标可查 | Codex |
| T12 | 单测补全 | 第 20.1 节全部测试 | `mvn test` 全绿 | 你（H1~H6 完成后） |
| T13 | 集成测试（场景 A/B/C + V1 回归） | `ChatGatewayIntegrationTest` 扩展 | 集成测试全绿 | 你 |
| T14 | 文档与演示 | model.yml 更新 / README / curl 演示 | 按第 21 节可完整走一遍 | 你 |

---

## 23. 风险与注意点

1. **浮点权重和**：校验用容差 `1e-6`；动态调权后必须重新归一化，避免权重和漂移。
2. **归一化除零**：max==min 时给满分，先写单测覆盖再写业务。
3. **插件顺序与作用域**：`PluginRegistration.matches` + `order()` 排序是正确性关键；单测覆盖 GLOBAL/ROUTE 与顺序。
4. **fail-closed 与“无候选”要分清**：插件清空 → `candidates_cleared`；过滤清空 → `no_available_model`；日志必须写明插件名/阶段/过滤器名。
5. **EWMA 并发更新**：必须用 `ConcurrentHashMap.compute` 原子更新，不要在 compute 内做阻塞 IO；**严禁在 compute 回调里对同一 map 做任何其他更新**（含 `computeIfAbsent`），初始状态从 map 外部构造（见 S10 `initialState`）。
6. **流式样本语义**：V2 用首字节延迟作为成功样本（简化），V3 再细化完成态；首字节后失败严禁降级（客户端会收到重复内容）。
7. **V1 回归**：`model.yml` 缺省策略 / 空插件链必须可启动；`ChatGatewayService` 构造器变更后同步更新所有测试。
8. **决策不可变**：`RoutingDecision` 是 record；`PluginContext.setDecision` 只允许一次，afterDecision 只读。
9. **测试 flaky**：加权随机用宽松断言（75%~85%）；EWMA 用“收敛区间”断言，不用精确值。
10. **条件规则引用**：select 只能引用本别名候选，启动期校验，防止配置”无中生有”；IN 的 value 必须是 List、EQ/NE 必须是标量，启动期校验避免运行期 ClassCastException。
11. **调试端点安全**：`/v1/debug/decisions` 无鉴权，仅学习阶段使用；版本 5 的 Admin API 会加管理密钥。
12. **手敲顺序建议**：H1 → H5 → H3 → H2 → H4 → H6。先写“发动机”（插件引擎），再写“状态”（EWMA），然后“打分 → 过滤 → 调度”，最后接线（决策引擎 + 主流程）——每步都有单测兜底，不会带病前进。
